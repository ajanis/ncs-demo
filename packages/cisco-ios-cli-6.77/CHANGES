cisco-ios v6.77.10 [2022-01-21]
================================================================================

Corrections:

  - Fixed problem removing second-dot1q on interfacve service encapsulation config.
    (CISCOIOS-2363 / PS-43519 / RT48407)


Enhancements:

  - Additions to the YANG model:
     route-map * / set community * gshut
    (CISCOIOS-2361 / PS-43478 / RT48364)

  - Additions to the YANG model:
     mpls traffic-eng pcc peer * password
    (CISCOIOS-2364 / PS-43530 / RT48418)

YANG model changes since 6.77.9:

  New nodes:
  /mpls/traffic-eng/pcc/peer/password/secret
  /mpls/traffic-eng/pcc/peer/password/type

  Changed nodes:
  /mpls/traffic-eng/pcc/peer
  /mpls/traffic-eng/pcc/peer/precedence
  /mpls/traffic-eng/pcc/peer/source
  /route-map/set/community/community-number


cisco-ios v6.77.9 [2022-01-07]
================================================================================

Enhancements:

  - Additions to the YANG model:
     interface * / service instance * / mac security aging
    (CISCOIOS-2351 / PS-43308 / RT48191)

  - Added new static warning exception (matches lower and upper-case):
     "the current number of routes in the routing table is equal to, or exceeds"
    (CISCOIOS-2359 / PS-43440 / RT48323)

  - Additions to the YANG model:
     cable upstream ofdma-prof-mgmt eq-poll-interval 5
     cable upstream ofdma-prof-mgmt downgrade min-cws 100
     cable upstream ofdma-prof-mgmt downgrade rxmer-exempt-mslot-pct 100
    (CISCOIOS-2360 / PS-43459 / RT48345)

YANG model changes since 6.77.8:

  New nodes:
  /cable/upstream/ofdma-prof-mgmt/downgrade/min-cws
  /cable/upstream/ofdma-prof-mgmt/downgrade/rxmer-exempt-mslot-pct
  /cable/upstream/ofdma-prof-mgmt/eq-poll-interval

  New nodes in grouping <interface-ethernet-grouping@21851>
  /interface/BDI/...
  /interface/Port-channel-subinterface/Port-channel/...
  /interface/Virtual-PPP/...
  /interface/Virtual-Template/...
  /interface/Vlan/...
  /interface/wlan-ap/...
      service/instance/mac/security/aging/static
      service/instance/mac/security/aging/sticky
      service/instance/mac/security/aging/time/inactivity
      service/instance/mac/security/aging/time/value

  New nodes in grouping <interface-ethernet-switch-grouping@24833>
  /interface/Ethernet-Internal/...
  /interface/Ethernet/...
  /interface/FastEthernet/...
  /interface/FortyGigabitEthernet/...
  /interface/GigabitEthernet/...
  /interface/HundredGigE/...
  /interface/Port-channel/...
  /interface/TenGigabitEthernet/...
  /interface/TwentyFiveGigE/...
  /interface/TwoGigabitEthernet/...
  /interface/Wlan-GigabitEthernet/...
  /interface/ucse/...
      service/instance/mac/security/aging/static
      service/instance/mac/security/aging/sticky
      service/instance/mac/security/aging/time/inactivity
      service/instance/mac/security/aging/time/value


cisco-ios v6.77.8 [2021-12-23]
================================================================================

Corrections:

  - Send initial shutdown and ignore 'Removal of physical interfaces is not
     permitted' with "no interface Wideband-Cable"
    (CISCOIOS-2352 / PS-43357 / RT48240)

  - Added leaf specific support for:
     event manager applet * / action * string
     event manager applet * / action * comment
      in order to support correct string handling.
    (CISCOIOS-2355 / PS-43391 / RT48274)

  - Send 'no' + newline and ignore waiting for prompt with write terminal nvram warning
    (CISCOIOS-2356 / PS-43387 / RT48270)

  - Fixed interface order dependency when raising mpls mtu higher than old mtu.
    (CISCOIOS-2357 / PS-43407 / RT48290)

YANG model changes since 6.77.7:

  New nodes:
  /event/manager/applet/action/comment
  /event/manager/applet/action/string

  Changed nodes:
  /interface/Wideband-Cable

  Changed nodes in grouping <interface-common-macsec-grouping@24813>
  /interface/*/...
      mpls/mtu

  Changed nodes in grouping <interface-common-ptp-grouping@20900>
  /interface/Multilink/...
  /interface/Serial-subinterface/Serial/...
  /interface/Serial/...
      mpls/mtu

  Changed nodes in grouping <interface-common-grouping@11425>
  /interface/*/...
      mpls/mtu


cisco-ios v6.77.7 [2021-12-17]
================================================================================

Corrections:

  - Do not delete key config-key password-encrypt with known old password.
    (CISCOIOS-2350 / PS-43298 / RT48181)

YANG model changes since 6.77.6:


cisco-ios v6.77.6 [2021-12-10]
================================================================================

Corrections:

  - Added new static warning exception (matches lower and upper-case):
     "removing named tag mapping will result in the ap rejoin"
    (CISCOIOS-2348 / PS-43271 / RT48154)

  - Update to nedcom 1.226 to workaround NSO prefix confusion when multiple
     packages loaded in NSO contains module with prefix "ncs".
    (CISCOIOS-2349)


Enhancements:

  - Added support for "exec " prefix in inject-command ned-setting to be able to run
     commands in exec mode without 'do' prefix. Note: Must be injected in top-mode only.
    (CISCOIOS-2344 / PS-43125 / RT48010)

  - Added check for config-line with 'where' = last|first and inject-command ned-setting
    (CISCOIOS-2344 / PS-43125 / RT48010)

YANG model changes since 6.77.5:

  Changed nodes:
  /wireless/tag


cisco-ios v6.77.5 [2021-12-03]
================================================================================

Corrections:

  - Added missing default values (0)  in:
     cable rpd * / rpd-ds * tilt-slope
     cable rpd * / rpd-ds * tilt-pivot-freq
    (CISCOIOS-2321 / PS-42712 / RT47591)

  - Corrected and improved interface delete order dependency fix in v6.77.4
     (CISCOIOS-2347 / PS-43233 / RT48118)

YANG model changes since 6.77.4:

  Changed nodes:
  /cable/rpd/rpd-ds/tilt-pivot-freq
  /cable/rpd/rpd-ds/tilt-slope
  /interface/FortyGigabitEthernet
  /interface/GigabitEthernet
  /interface/HundredGigE
  /interface/TenGigabitEthernet
  /interface/TwentyFiveGigE
  /interface/TwoGigabitEthernet

  Changed nodes in grouping <interface-switch-grouping@26260>
  /interface/Ethernet-Internal/...
  /interface/Ethernet/...
  /interface/FastEthernet/...
  /interface/FortyGigabitEthernet/...
  /interface/GigabitEthernet/...
  /interface/HundredGigE/...
  /interface/Port-channel-subinterface/Port-channel/...
  /interface/Port-channel/...
  /interface/TenGigabitEthernet/...
  /interface/TwentyFiveGigE/...
  /interface/TwoGigabitEthernet/...
  /interface/Wlan-GigabitEthernet/...
  /interface/ucse/...
      switchport


cisco-ios v6.77.4 [2021-11-26]
================================================================================

Corrections:

  - Added support for temporarily unlocking 'interface * / service-policy input|output *'
     when 'platform qos marker-statistics|match-statistics' is modified.
    (CISCOIOS-2345 / PS-43129 / RT48014)

  - Fixed order dependency problem, correct order is:
     no interface <sub-if>
     interface <main-if> / switchport
    (CISCOIOS-2346 / PS-43171 / RT48056)

YANG model changes since 6.77.3:

  Changed nodes:

  Changed nodes in grouping <interface-common-macsec-grouping@24810>
  /interface/*/...
      service-policy

  Changed nodes in grouping <interface-switch-grouping@26260>
  /interface/*/...
      switchport

  Changed nodes in grouping <interface-common-ptp-grouping@20897>
  /interface/*/...
      service-policy

  Changed nodes in grouping <service-policy-grouping@4410>
  /interface/*/service-policy/...
      input
      output

  Changed nodes in grouping <interface-common-grouping@11425>
  /interface/*/...
      service-policy


cisco-ios v6.77.3 [2021-11-19]
================================================================================

Corrections:

  - Removed 'tailf:cli-recursive-delete' annotation from Port-channel sub-interfaces.
    (CISCOIOS-2339 / PS-43029 / RT47917)

  - Added ordered-by user and tailf:cli-show-long-obu-diffs on:
     aaa group server radius * / server name *
    (CISCOIOS-2341 / PS-43060 / RT47948)


Enhancements:

  - Additions to the YANG model:
     cable rf-change-up-multiplier
    (CISCOIOS-2340 / PS-43051 / RT47939)

  - Expanded 'cisco-ios write inject-command *' ned-setting with 4 new 'where' values:
     last, first, before-topmode and after-topmode. Note: for the topmode values, set
     the config-line regex to the start of the topmode block line only.
     (CISCOIOS-2342 / PS-43069 / RT47955)

  - Additions to the YANG model:
     radius-server attribute 32 include-in-accounting-req
    (CISCOIOS-2343 / PS-43122 / RT48007)

YANG model changes since 6.77.2:

  New nodes:
  /cable/rf-change-up-multiplier
  /radius-server/attribute/attribute-list/include-in-accounting-req

  Changed nodes:
  /aaa/group/server/radius/server/name
  /interface/Port-channel-subinterface/Port-channel


cisco-ios v6.77.2 [2021-11-12]
================================================================================

Enhancements:

  - Added new static warning exception (matches lower and upper-case):
     "failed to generate persistent self-signed certificate"
    (CISCOIOS-2336 / PS-42984 / RT47872)

  - Additions to the YANG model:
     login quiet-mode access-class * vrf-also
     performance-measurement / delay-profile interfaces / advertisement / accelerated / threshold
     performance-measurement / delay-profile interfaces / probe / computation-interval
    (CISCOIOS-2338 / PS-43025 / RT47913)

YANG model changes since 6.77.1:

  New nodes:
  /login/quiet-mode/vrf-also
  /performance-measurement/delay-profile/interfaces/advertisement/accelerated/threshold
  /performance-measurement/delay-profile/interfaces/probe/computation-interval

  Changed nodes:
  /login/quiet-mode
  /performance-measurement/delay-profile/interfaces/advertisement/periodic/threshold


cisco-ios v6.77.1 [2021-11-05]
================================================================================

Corrections:

  - Pre-inject interface address delete when moving ip address between interfaces.
    (CISCOIOS-2335 / PS-42966 / RT47852)


Enhancements:

  - Updated NEDCOM to version 1.222 for standardized progress report messages.
    Added progress report on live-status exec action and input transformation.
    (CISCOIOS-2327)

  - Additions to the YANG model:
     interface * / [no ]enable
    (CISCOIOS-2334 / PS-42962 / RT47848)

YANG model changes since 6.77:

  New nodes:

  New nodes in grouping <interface-ethernet-switch-grouping@24783>
  /interface/Ethernet-Internal/...
  /interface/Ethernet/...
  /interface/FastEthernet/...
  /interface/FortyGigabitEthernet/...
  /interface/GigabitEthernet/...
  /interface/HundredGigE/...
  /interface/Port-channel/...
  /interface/TenGigabitEthernet/...
  /interface/TwentyFiveGigE/...
  /interface/TwoGigabitEthernet/...
  /interface/Wlan-GigabitEthernet/...
  /interface/ucse/...
      enable

  New nodes in grouping <interface-ethernet-grouping@21838>
  /interface/BDI/...
  /interface/Port-channel-subinterface/Port-channel/...
  /interface/Virtual-PPP/...
  /interface/Virtual-Template/...
  /interface/Vlan/...
  /interface/wlan-ap/...
      enable


cisco-ios v6.77 [2021-10-29]
================================================================================
Changes that might affect a package upgrade from version 6.76.2:
  NOTE: The below YANG model changes are not backwards compatible:

  Removed nodes:

  Removed nodes in grouping <router-isis-grouping@33942>
  /router/isis-container/isis/...
  /router/isis/...
      hello/padding-conf/padding/value


Corrections:

  - API CHANGE:
     router isis * / no hello padding multi-point
     router isis * / no hello padding point-to-point
      Corrected from enum leaf to two separate type boolean leaves in order to be
      able to send no-values. Set 'no hello padding' to disable both.
      Also unified IOS API by transforming 'no hello padding multi-point' and
       'no hello padding point-to-point' from device to 'no hello padding'.
    (CISCOIOS-2330 / PS-42902 / RT47779)

  - Fixed order dependency problem, correct order is:
     router isis * / no vrf
     no ip vrf
    (CISCOIOS-2330 / PS-42902 / RT47779)


Enhancements:

  - Additions to the YANG model:
     vrf definition * / address-family ipv4 / export ipv4 unicast map
    (CISCOIOS-2328 / PS-42887 / RT47764)

  - Additions to the YANG model:
     crypto isakmp profile * / isakmp authorization list * password
     crypto isakmp profile * / accounting
    (CISCOIOS-2329 / PS-42901 / RT47778)

  - Additions to the YANG model:
     no ip igmp snooping vlan * check
     crypto ikev2 profile * / aaa accounting anyconnect-eap
    (CISCOIOS-2331 / PS-42905 / RT47782)

YANG model changes since 6.76.2:

  New nodes:
  /crypto/ikev2/profile/aaa/accounting/anyconnect-eap
  /crypto/isakmp/profile/accounting
  /crypto/isakmp/profile/isakmp/authorization/password/encryption
  /crypto/isakmp/profile/isakmp/authorization/password/secret
  /ip/igmp/snooping-vlan/snooping/vlan/no-list/check/rtr-alert-option
  /ip/igmp/snooping-vlan/snooping/vlan/no-list/check/ttl

  New nodes in grouping <router-isis-grouping@33942>
  /router/isis-container/isis/...
  /router/isis/...
      hello/padding-multi-point/padding/multi-point
      hello/padding-point-to-point/padding/point-to-point

  New nodes in grouping <vrf-definition-af-grouping@38511>
  /vrf/definition/address-family/ipv4-multicast/ipv4/multicast/...
  /vrf/definition/address-family/ipv4/...
  /vrf/definition/address-family/ipv6/...
      export/ipv4/unicast/map

  Changed nodes:
  /crypto/isakmp/profile/isakmp/authorization

  Changed nodes in grouping <router-isis-grouping@33942>
  /router/isis-container/isis/...
  /router/isis/...
      vrf


cisco-ios v6.76.2 [2021-10-15]
================================================================================

Enhancements:

  - Additions to the YANG model:
     wireless tag site * / flex-profile
     wireless tag site * / no local-site
    (CISCOIOS-2322 / PS-42709 / RT47588)

  - Additions to the YANG model:
     interface * / plim ethernet vlan filter disable
     monitor session * type erspan-destination / destination interface *
     monitor session * type erspan-destination / source / vrf
     monitor session * type erspan-source / destination / vrf
    (CISCOIOS-2325 / PS-42769 / RT47647)

YANG model changes since 6.76.1:

  New nodes:
  /monitor/session-erspan-destination/session/destination/interface
  /monitor/session-erspan-destination/session/destination/interface/id
  /monitor/session-erspan-destination/session/source/vrf
  /monitor/session-erspan-source/session/destination/vrf
  /wireless/tag/site/flex-profile
  /wireless/tag/site/local-site

  New nodes in grouping <interface-ethernet-grouping@21838>
  /interface/BDI/...
  /interface/Port-channel-subinterface/Port-channel/...
  /interface/Virtual-PPP/...
  /interface/Virtual-Template/...
  /interface/Vlan/...
  /interface/wlan-ap/...
      plim/ethernet/vlan/filter/disable

  New nodes in grouping <interface-ethernet-switch-grouping@24775>
  /interface/Ethernet-Internal/...
  /interface/Ethernet/...
  /interface/FastEthernet/...
  /interface/FortyGigabitEthernet/...
  /interface/GigabitEthernet/...
  /interface/HundredGigE/...
  /interface/Port-channel/...
  /interface/TenGigabitEthernet/...
  /interface/TwentyFiveGigE/...
  /interface/TwoGigabitEthernet/...
  /interface/Wlan-GigabitEthernet/...
  /interface/ucse/...
      plim/ethernet/vlan/filter/disable


cisco-ios v6.76.1 [2021-10-07]
================================================================================

Corrections:

  - Improved privilege modification(s) by reseting all previous entries
     before sending all existing ones, sorted.
    (CISCOIOS-2294 / PS-42250 / RT47132)

  - Ignore 'Removal of physical interfaces is not permitted' with "no interface
     Integrated-Cable" since interface deleted by injecting 'shutdown' (on some devices?)
     (CISCOIOS-2320 / PS-42701 / RT47580)


Enhancements:

  - Added tailf:cli-case-insensitive annotation on controller SDH * /
     au-4 * / tug-3 * / mode and tug-2 * payload config.
    (CISCOIOS-2304 / PS-42518 / RT47397)

  - Additions to the YANG model:
     interface * / eapol eth-type
     interface * / macsec dot1q-in-clear
     interface * / macsec access-control
     mka policy * / key-server priority
    (CISCOIOS-2315 / PS-42639 / RT47518)

  - Added new static warning exception (matches lower and upper-case):
     "bringing \\S+ the port due to config change"
    (CISCOIOS-2319 / PS-42671 / RT47550)

  - Additions to the YANG model:
     aaa group server tacacs+ * / timeout
    (CISCOIOS-2323 / PS-42720 / RT47599)

YANG model changes since 6.76:

  New nodes:
  /aaa/group/server/tacacs-plus/timeout
  /mka/policy/key-server/priority

  New nodes in grouping <interface-common-ptp-grouping@20887>
  /interface/Multilink/...
  /interface/Serial-subinterface/Serial/...
  /interface/Serial/...
      eapol/eth-type
      macsec/access-control
      macsec/dot1q-in-clear

  New nodes in grouping <interface-common-grouping@11421>
  /interface/*/...
      eapol/eth-type
      macsec/access-control
      macsec/dot1q-in-clear

  New nodes in grouping <interface-common-macsec-grouping@24772>
  /interface/*/...
      eapol/eth-type
      macsec/access-control
      macsec/dot1q-in-clear

  Changed nodes:
  /controller/SDH/au-4/tug-3/mode
  /controller/SDH/au-4/tug-3/tug-2/payload
  /privilege
  /privilege/exec/all

  Changed nodes from embedded grouping <privilege-level-grouping@108312>
  /privilege/configure/...
  /privilege/exec/...
  /privilege/list/...
      /...
      all/...
          level


cisco-ios v6.76 [2021-09-30]
================================================================================
Changes that might affect a package upgrade from version 6.75.1:
  NOTE: The below YANG model changes are not backwards compatible:

  Incompatible nodes:
  /ip/nat/outside/source/static/nat-static-address-list-vrf


Corrections:

  - Strip delete of previous logging source-interface when changing it.
     (CISCOIOS-2313 / PS-42613 / RT47492)

  - API CHANGE:
     ip nat outside source static * * vrf *
      added vrf to 'nat-static-address-list-vrf' list key
    (CISCOIOS-2314 / PS-42470 / RT47349)

  - Fixed order dependency, delete control-plane service-policy entries before
     modifying their class-maps or policy-maps.
    (CISCOIOS-2316 / PS-42638 / RT47517)


Enhancements:

  - Additions to the YANG model:
    controller SDH *
    controller SDH * / no snmp trap link-status
    controller SDH * / rate
    controller SDH * / no ais-shut
    controller SDH * / alarm-report
    controller SDH * / shutdown
    controller SDH * / clock source
    controller SDH * / overhead
    controller SDH * / aug mapping
    controller SDH * / au-4 *
    controller SDH * / au-4 * / clock source
    controller SDH * / au-4 * / mode
    controller SDH * / au-4 * / tug-3 *
    controller SDH * / au-4 * / tug-3 * / mode
    controller SDH * / au-4 * / tug-3 * / tug-2 * payload
    controller SDH * / au-4 * / tug-3 * / tug-2 * payload / e1 *
    controller SDH * / au-4 * / tug-3 * / tug-2 * payload / e1 * cem-group
    controller SDH * / au-4 * / tug-3 * / tug-2 * payload / e1 * description
    controller SDH * / au-4 * / tug-3 * / tug-2 * payload / e1 * framing
    controller SDH * / au-4 * / tug-3 * / tug-2 * payload / vc *
    controller SDH * / au-4 * / tug-3 * / tug-2 * payload / vc * overhead v5
    sdm prefer disable_tdm_to_ip_iw
    sdm prefer disable_l2pt_fwd_all
    sdm prefer disable_multicast_stats
    sdm prefer disable_bdi_mtu
    sdm prefer disable_egr_l3vpn_cm
    sdm prefer disable_l3vpn_cm
    sdm prefer enable_copp
    sdm prefer disable_match_inner_dscp
    sdm prefer sr_pfp_disable
    sdm prefer sr_5_label_push_disable
   (CISCOIOS-2281 / PS-42006 / RT46889)

  - Added new ned-setting 'api aaa-accounting-dot1x-mode-format' to override
     the general aaa accounting mode format ned-setting for dot1x only.
    (CISCOIOS-2300 / PS-42461 / RT47340)

  - Additions to the YANG model:
     controller SDH * / au-4 * / tug-3 * / t3
     controller SDH * / au-4 * / tug-3 * / t3 framing
     controller SDH * / au-4 * / tug-3 * / t3 clock source
     controller SDH * / au-4 * / tug-3 * / e1 *
     controller SDH * / au-4 * / tug-3 * / e1 * cem-group
     controller SDH * / au-4 * / tug-3 * / e1 * framing unframed
    (CISCOIOS-2304 / PS-42518 / RT47397)

  - Added ned-secrets documentation in README file.
    (CISCOIOS-2312)

YANG model changes since 6.75.1:

  New nodes:
  /controller/SDH
  /controller/SDH/ais-shut
  /controller/SDH/alarm-report
  /controller/SDH/au-4
  /controller/SDH/au-4/clock/source
  /controller/SDH/au-4/id
  /controller/SDH/au-4/mode
  /controller/SDH/au-4/tug-3
  /controller/SDH/au-4/tug-3/e1
  /controller/SDH/au-4/tug-3/e1/cem-group/id
  /controller/SDH/au-4/tug-3/e1/cem-group/unframed
  /controller/SDH/au-4/tug-3/e1/framing
  /controller/SDH/au-4/tug-3/e1/id
  /controller/SDH/au-4/tug-3/id
  /controller/SDH/au-4/tug-3/mode
  /controller/SDH/au-4/tug-3/t3/clock/source
  /controller/SDH/au-4/tug-3/t3/framing
  /controller/SDH/au-4/tug-3/tug-2
  /controller/SDH/au-4/tug-3/tug-2/e1
  /controller/SDH/au-4/tug-3/tug-2/e1/cem-group/id
  /controller/SDH/au-4/tug-3/tug-2/e1/cem-group/unframed
  /controller/SDH/au-4/tug-3/tug-2/e1/description
  /controller/SDH/au-4/tug-3/tug-2/e1/framing
  /controller/SDH/au-4/tug-3/tug-2/e1/id
  /controller/SDH/au-4/tug-3/tug-2/id
  /controller/SDH/au-4/tug-3/tug-2/payload
  /controller/SDH/au-4/tug-3/tug-2/vc
  /controller/SDH/au-4/tug-3/tug-2/vc/id
  /controller/SDH/au-4/tug-3/tug-2/vc/overhead/v5
  /controller/SDH/aug/mapping
  /controller/SDH/clock/source
  /controller/SDH/name
  /controller/SDH/overhead/s1s0
  /controller/SDH/rate
  /controller/SDH/shutdown
  /controller/SDH/snmp/trap/link-status
  /sdm/prefer/disable_bdi_mtu
  /sdm/prefer/disable_copp
  /sdm/prefer/disable_egr_l3vpn_cm
  /sdm/prefer/disable_l2pt_fwd_all
  /sdm/prefer/disable_l3vpn_cm
  /sdm/prefer/disable_match_inner_dscp
  /sdm/prefer/disable_multicast_stats
  /sdm/prefer/disable_tdm_to_ip_iw
  /sdm/prefer/enable_copp
  /sdm/prefer/enable_egr_l3vpn_cm
  /sdm/prefer/sr_5_label_push_disable
  /sdm/prefer/sr_pfp_disable
  /ztp/disable

  New nodes in grouping <router-bgp-bgp-grouping@29395>
  /router/bgp/...
  /router/bgp/address-family/ipv6-with-vrf/ipv6/vrf/...
  /router/bgp/address-family/with-vrf/ipv4/vrf/...
      bgp/ha-mode/sso/prefer

  Changed nodes:
  /controller/MediaType/mode
  /ip/nat/outside/source/static/nat-static-address-list-vrf
  /platform/enable/controller

  Changed nodes in grouping <class-map-match-grouping@8752>
  /class-map/match/...
  /class-map/match/not/...
      access-group


cisco-ios v6.75.1 [2021-09-23]
================================================================================

Corrections:

  - Fixed support for multiple 'cable fiber-node * / upstream sg-channel * peer-node-us'
     entries broken with YANG redesign in v6.71.
    (CISCOIOS-2309 / PS-42556 / RT47435)

YANG model changes since 6.75:

  Changed nodes:
  /cable/fiber-node/upstream/sg-channel


cisco-ios v6.75 [2021-09-22]
================================================================================

Changes that might affect a package upgrade from version 6.74.11:
  NOTE: The below YANG model changes are not backwards compatible:

  Incompatible nodes:

  Incompatible nodes in grouping <router-isis-grouping@33849>
  /router/isis-container/isis/...
  /router/isis/...
      authentication/key-chain
      authentication/key-chain/level

  Incompatible nodes in grouping <router-isis-interface-grouping@33509>
  /interface/*/isis/...
      authentication/key-chain
      authentication/key-chain/level

Corrections:

  - Fixed interface order dependency, correct order is:
     no mka
     no macsec
     no switchport mode trunk
    Removed "ip route" interface dependencies since ordered by java.
    This solved a problem with macsec vs interface sub-config ordering.
    (CISCOIOS-2306 / PS-42524 / RT47403)

  - API CHANGE:
     router isis * / authentication key-chain
     interface * / isis authentication key-chain
      Redesigned from container to list to support two different level names.
    (CISCOIOS-2308 / PS-42554 / RT47433)


Enhancements:

  - Additions to the YANG model:
     router ospf * / microloop avoidance
    (CISCOIOS-2305 / PS-42519 / RT47398)

  - Additions to the YANG model:
     mpls feature php mpls
     mpls ldp tcp pak-priority
    (CISCOIOS-2307 / PS-42544 / RT47423)

YANG model changes since 6.74.11:

  New nodes:
  /mpls/feature/php/mpls
  /mpls/ldp/tcp/pak-priority

  New nodes in grouping <router-isis-grouping@33849>
  /router/isis-container/isis/...
  /router/isis/...
      microloop/avoidance/segment-routing

  New nodes in grouping <router-ospf-grouping@31007>
  /ipv6/router/ospf/...
  /router/ospf/...
      microloop/avoidance/disable
      microloop/avoidance/protected
      microloop/avoidance/segment-routing

  Changed nodes:
  /ip/route/static/bfd/ip-route-bfd-forwarding-list/interface
  /ip/route/vrf

  Changed nodes in grouping <interface-switch-grouping@26164>
  /interface/Ethernet-Internal/...
  /interface/Ethernet/...
  /interface/FastEthernet/...
  /interface/FortyGigabitEthernet/...
  /interface/GigabitEthernet/...
  /interface/HundredGigE/...
  /interface/Port-channel-subinterface/Port-channel/...
  /interface/Port-channel/...
  /interface/TenGigabitEthernet/...
  /interface/TwentyFiveGigE/...
  /interface/TwoGigabitEthernet/...
  /interface/Wlan-GigabitEthernet/...
  /interface/ucse/...
      switchport/mode/trunk

  Changed nodes in grouping <router-isis-grouping@33849>
  /router/isis-container/isis/...
  /router/isis/...
      authentication/key-chain
      authentication/key-chain/level
      authentication/key-chain/name
      ignore-attached-bit
      purge-transmit

  Changed nodes in grouping <router-isis-interface-grouping@33509>
  /interface/*/isis/...
      authentication/key-chain
      authentication/key-chain/level
      authentication/key-chain/name


cisco-ios v6.74.11 [2021-09-17]
================================================================================

Corrections:

  - Fixed order dependency, correct order is:
     no radius-server *
     no aaa new-model
    (CISCOIOS-2302 / PS-42484 / RT47363)

Enhancements:

  - Additions to the YANG model:
     interface * / ospfv3 hello-interval
     interface * / ospfv3 dead-interval
    (CISCOIOS-2301 / PS-42474 / RT47353)

  - Additions to the YANG model:
     router isis * / advertise link attributes
    (CISCOIOS-2303 / PS-42504 / RT47383)

YANG model changes since 6.74.10:

  New nodes:

  New nodes from embedded grouping <interface-ospfv3-grouping@10822>
  /interface/*/ospfv3/...
      /...
      ospfv3-list/...
      ospfv3-list/ipv4/...
      ospfv3-list/ipv6/...
          dead-interval
          hello-interval

  New nodes in grouping <router-isis-grouping@33802>
  /router/isis-container/isis/...
  /router/isis/...
      advertise/link/attributes

  Changed nodes:
  /radius-server


cisco-ios v6.74.10 [2021-09-10]
================================================================================

Enhancements:

  - Redeploy bgp neighbor config with remote-as change.
     (CISCOIOS-2293 / PS-42190 / RT47071)

  - Additions to the YANG model:
     router rip / neighbor * bfd
     router eigrp * / address-family * / bfd interface *
    (CISCOIOS-2298 / PS-42427 / RT47306)

YANG model changes since 6.74.9:

  New nodes:
  /router/eigrp/address-family/ipv4/vrf/bfd/all-interfaces
  /router/eigrp/address-family/ipv4/vrf/bfd/interface
  /router/eigrp/address-family/ipv4/vrf/bfd/interface/name
  /router/rip/address-family/ipv4/vrf/neighbor/bfd

  New nodes in grouping <router-rip-grouping@32326>
  /router/rip/...
  /router/rip/address-family/ipv4/unicast/...
      neighbor/bfd

  New nodes in grouping <router-eigrp-af-virtual-instance-grouping@33058>
  /router/eigrp-virtual-instance/eigrp/address-family/ipv4-vrf/ipv4/unicast/vrf/...
  /router/eigrp-virtual-instance/eigrp/address-family/ipv4/...
  /router/eigrp-virtual-instance/eigrp/address-family/ipv6-vrf/ipv6/unicast/vrf/...
  /router/eigrp-virtual-instance/eigrp/address-family/ipv6/...
      bfd/all-interfaces
      bfd/interface
      bfd/interface/name

  Changed nodes:
  /router/bgp
  /router/bgp/neighbor/remote-as


cisco-ios v6.74.9 [2021-09-06]
================================================================================

Corrections:

  - Added new static warning exception (matches lower and upper-case):
     "service policy .+ not attached"
    (CISCOIOS-2297 / PS-42387 / RT47266)


cisco-ios v6.74.8 [2021-08-28]
================================================================================

Corrections:

  - Fixed deletion order of privilege exec commands.
     (CISCOIOS-2294 / PS-42250 / RT47132)

Enhancements:

  - Redeploy 'ip sla' when modifying 'ethernet y1731' content.
     (CISCOIOS-2285 / PS-42035 / RT46918)

  - Additions to YANG model:
    router isis * / router-id
    ip http client username
    ip http client password
    license smart url smart
    platform hardware throughput crypto 2.5G
     (CISCOIOS-2290 / PS-42176 / RT47057)

  - Additions to YANG model:
    logging trap syslog-format
     (CISCOIOS-2295 / PS-42261 / RT47142)

YANG model changes since 6.74.7:

  New nodes:
  /ip/http/client/password/secret
  /ip/http/client/password/type
  /ip/http/client/username
  /license/smart/url-smart/url/smart
  /logging/trap-syslog-format/trap/level
  /logging/trap-syslog-format/trap/syslog-format

  New nodes in grouping <interface-name-grouping@2726>
  /router/isis-container/isis/router-id/...
  /router/isis/router-id/...
      ATM
      Analysis-Module
      AppNav-Compress
      AppNav-UnCompress
      Asi
      Async
      BDI
      BRI
      BVI
      Bundle
      Bundle-subinterface/Bundle
      CEM
      CEM-ACR
      CMON-Tunnel
      Cable
      Cellular
      Dialer
      Dot11Radio
      Embedded-Service-Engine
      Ethernet
      Ethernet-Internal
      FastEthernet
      FortyGigabitEthernet
      GMPLS
      GMPLS-subinterface/GMPLS
      GigabitEthernet
      Group-Async
      HundredGigE
      ISM
      Integrated-Cable
      Integrated-Service-Engine
      LISP
      LISP-subinterface/LISP
      Loopback
      MFR
      MFR-subinterface/MFR
      Modular-Cable
      Multilink
      Null
      POS
      Port-channel
      Port-channel-subinterface/Port-channel
      Qam-red
      SM
      Serial
      Serial-subinterface/Serial
      Service-Engine
      TenGigabitEthernet
      ToP
      Tunnel
      TwentyFiveGigE
      TwoGigabitEthernet
      Video
      Virtual-PPP
      Virtual-Template
      VirtualPortGroup
      Vlan
      Wideband-Cable
      Wlan-GigabitEthernet
      mgmt
      nve
      pseudowire
      ucse
      vasileft
      vasiright
      wlan-ap

  Changed nodes:
  /license/smart/url
  /platform/hardware/throughput/crypto
  /privilege/exec/all

  Changed nodes from embedded grouping <privilege-level-grouping@107614>
  /privilege/configure/...
  /privilege/exec/...
  /privilege/list/...
      /...
      all/...
          level


cisco-ios v6.74.7 [2021-08-19]
================================================================================

Corrections:

  - Ignored the warning related to the second attempt of interface service-policy removal.
     (CISCOIOS-2291 / PS-42182 / RT47063)

Enhancements:

  - Updated to nedcom 1.211.
     (CISCOIOS-2292)


cisco-ios v6.74.6 [2021-08-13]
================================================================================

Corrections:

  - Insert shutdown before delete in 'interface Integrated-Cable' and
    ignore delete of unknown interface to support use case where
    interface is deleted with shutdown command.
     (CISCOIOS-2274 / PS-41847 / RT46730)

Enhancements:

  - Additions to YANG model:
    interface * / tunnel mpls traffic-eng load-share
    mpls ldp session protection for
     (CISCOIOS-2286 / PS-42089 / RT46970)

  - Additions to YANG model:
    x29 profile *
     (CISCOIOS-2287 / PS-42143 / RT47024)

  - Infrastructure changes affecting only non-delivered files.
     (CISCOIOS-2288)

YANG model changes since 6.74.5:

  New nodes:
  /mpls/ldp/session/protection/for
  /x29/profile
  /x29/profile/name
  /x29/profile/values

  New nodes in grouping <interface-tunnel-grouping@20824>
  /interface/CMON-Tunnel/...
  /interface/Tunnel/...
  /interface/Virtual-PPP/...
  /interface/Virtual-Template/...
      tunnel/mpls/traffic-eng/load-share

  Changed nodes:
  /interface/Integrated-Cable


cisco-ios v6.74.5 [2021-08-05]
================================================================================

Corrections:

  - Fixed explicit exit for 'performance-measurement' mode.
     (CISCOIOS-2280 / PS-41955 / RT46837)

Enhancements:

  - Additions to YANG model:
    license smart url cslu
    license smart transport cslu
     (CISCOIOS-2276 / PS-41875 / RT46757)

  - Insert missing 'show vtp password' configuration.
     (CISCOIOS-2282 / PS-42017 / RT46900)

  - Additions to YANG model:
    aaa common-criteria policy * / numeric-count
    aaa common-criteria policy * / special-case
     (CISCOIOS-2284 / PS-42029 / RT46912)

  - Redeploy 'ip sla' when modifying 'ethernet y1731 delay receive' content.
     (CISCOIOS-2285 / PS-42035 / RT46918)

YANG model changes since 6.74.4:

  New nodes:
  /aaa/common-criteria/policy/numeric-count
  /aaa/common-criteria/policy/special-case
  /license/smart/url-cslu/url/cslu

  Changed nodes:
  /license/smart/transport
  /license/smart/url
  /performance-measurement
  /performance-measurement/interface
  /performance-measurement/interface/delay-measurement


cisco-ios v6.74.4 [2021-07-22]
================================================================================

Corrections:

  - Fixed explicit exit from sub-mode in 'l2vpn vfi context * / autodiscovery bgp signaling'.
     (CISCOIOS-2277 / PS-41893 / RT46775)

  - Added tailf:cli-reset-container annotation on 'crypto ipsec transform-set *' list.
     (CISCOIOS-2279 / PS-41928 / RT46810)

Enhancements:

  - Additions to YANG model:
    radius-server attribute 11 default direction
    interface * / authentication event fail retry
     (CISCOIOS-2278 / PS-41925 / RT46807)

YANG model changes since 6.74.3:

  New nodes:
  /radius-server/attribute-11/attribute/default/direction
  /radius-server/attribute-11/attribute/id

  New nodes in grouping <interface-ethernet-switch-grouping@24662>
  /interface/Ethernet-Internal/...
  /interface/Ethernet/...
  /interface/FastEthernet/...
  /interface/FortyGigabitEthernet/...
  /interface/GigabitEthernet/...
  /interface/HundredGigE/...
  /interface/Port-channel/...
  /interface/TenGigabitEthernet/...
  /interface/TwentyFiveGigE/...
  /interface/TwoGigabitEthernet/...
  /interface/Wlan-GigabitEthernet/...
  /interface/ucse/...
      authentication/event/fail/retry

  New nodes in grouping <interface-ethernet-grouping@21745>
  /interface/BDI/...
  /interface/Port-channel-subinterface/Port-channel/...
  /interface/Virtual-PPP/...
  /interface/Virtual-Template/...
  /interface/Vlan/...
  /interface/wlan-ap/...
      authentication/event/fail/retry

  Changed nodes:
  /crypto/ipsec/transform-set
  /l2vpn-vfi/l2vpn/vfi/context/autodiscovery/bgp/signaling

  Changed nodes in grouping <interface-ethernet-switch-grouping@24662>
  /interface/Ethernet-Internal/...
  /interface/Ethernet/...
  /interface/FastEthernet/...
  /interface/FortyGigabitEthernet/...
  /interface/GigabitEthernet/...
  /interface/HundredGigE/...
  /interface/Port-channel/...
  /interface/TenGigabitEthernet/...
  /interface/TwentyFiveGigE/...
  /interface/TwoGigabitEthernet/...
  /interface/Wlan-GigabitEthernet/...
  /interface/ucse/...
      authentication/event/fail
      authentication/event/fail/action
      authentication/event/fail/action/authorize

  Changed nodes in grouping <interface-ethernet-grouping@21745>
  /interface/BDI/...
  /interface/Port-channel-subinterface/Port-channel/...
  /interface/Virtual-PPP/...
  /interface/Virtual-Template/...
  /interface/Vlan/...
  /interface/wlan-ap/...
      authentication/event/fail
      authentication/event/fail/action
      authentication/event/fail/action/authorize


cisco-ios v6.74.3 [2021-07-09]
================================================================================

Corrections:

  - New diff-deps added in order to avoid unnecesary vlan-range mode entry and exit operations
    (CISCOIOS-2264 / PS-41656 / RT46542)

  - Unbundle/bundle the interface when deleting macsec configuration
    (CISCOIOS-2270 / PS-41752)

  - Fixed controller Integrated-Cable order dependency, delete rf-chan entries first.
    (CISCOIOS-2275 / PS-41849 / RT46732)

Enhancements:

  - Additions to the YANG model:
     interface Tunnel * / tunnel route-via
    (CISCOIOS-2266 / PS-41727 / RT46613)

  - Additions to the YANG model:
     cable virtual-service-group * / downstream-video
    (CISCOIOS-2268 / PS-41755 / RT46639)

  - Additions to the YANG model:
     platform hardware throughput crypto
    (CISCOIOS-2269 / PS-41759 / RT46643)

  - Additions to the YANG model:
     ip ddns update method * / DDNS
     ip ddns update method * / interval maximum
     ip ddns update method * / interval minimum
     ip host-list *
     ip host-list * / host
     interface * / ip ddns
     interface * / ip ddns update *
     interface * / ip ddns update * host-group
     interface * / ip dhcp client update
     interface * / ip dhcp client update dns
     interface * / ip dhcp client update dns server
    (CISCOIOS-2271 / PS-41778 / RT46662)

  - Additions to the YANG model:
     l2vpn vfi context * / l2protocol forward
    (CISCOIOS-2273 / PS-41814 / RT46697)

  - Updated NEDCOM to version 1.210.

YANG model changes since 6.74.2:

  New nodes:
  /cable/virtual-service-group/downstream-video
  /ip/ddns/update/method
  /ip/ddns/update/method/DDNS/both
  /ip/ddns/update/method/interval/maximum/days
  /ip/ddns/update/method/interval/maximum/hours
  /ip/ddns/update/method/interval/maximum/minutes
  /ip/ddns/update/method/interval/maximum/seconds
  /ip/ddns/update/method/interval/minimum/days
  /ip/ddns/update/method/interval/minimum/hours
  /ip/ddns/update/method/interval/minimum/minutes
  /ip/ddns/update/method/interval/minimum/seconds
  /ip/ddns/update/method/name
  /ip/host-list
  /ip/host-list/host
  /ip/host-list/name
  /platform/hardware/throughput/crypto

  New nodes in grouping <interface-name-grouping@2726>
  /interface/*/tunnel/route-via/...

  New nodes in grouping <interface-common-macsec-grouping@24660>
  /interface/BDI/...
  /interface/Ethernet-Internal/...
  /interface/Ethernet/...
  /interface/FastEthernet/...
  /interface/FortyGigabitEthernet/...
  /interface/GigabitEthernet/...
  /interface/HundredGigE/...
  /interface/Port-channel-subinterface/Port-channel/...
  /interface/Port-channel/...
  /interface/TenGigabitEthernet/...
  /interface/TwentyFiveGigE/...
  /interface/TwoGigabitEthernet/...
  /interface/Virtual-PPP/...
  /interface/Virtual-Template/...
  /interface/Vlan/...
  /interface/Wlan-GigabitEthernet/...
  /interface/ucse/...
  /interface/wlan-ap/...
      ip/ddns/update/hostname
      ip/ddns/update/list
      ip/ddns/update/list/host-group
      ip/ddns/update/list/name
      ip/dhcp/client/update/dns/server

  New nodes in grouping <interface-tunnel-grouping@20824>
  /interface/CMON-Tunnel/...
  /interface/Tunnel/...
  /interface/Virtual-PPP/...
  /interface/Virtual-Template/...
      tunnel/route-via/mandatory-preferred

  New nodes in grouping <interface-common-grouping@11374>
  /interface/*/...
      ip/ddns/update/hostname
      ip/ddns/update/list
      ip/ddns/update/list/host-group
      ip/ddns/update/list/name
      ip/dhcp/client/update/dns/server

  New nodes in grouping <interface-common-ptp-grouping@20804>
  /interface/Multilink/...
  /interface/Serial-subinterface/Serial/...
  /interface/Serial/...
      ip/ddns/update/hostname
      ip/ddns/update/list
      ip/ddns/update/list/host-group
      ip/ddns/update/list/name
      ip/dhcp/client/update/dns/server


cisco-ios v6.74.2 [2021-06-24]
================================================================================

Enhancements:

  - Added new ned-setting 'api new-aaa-list-syntax' to switch YANG model for
     ordered by-user support in aaa authentication|authorization lists.
    (CISCOIOS-2263 / PS-41627 / RT46513)

YANG model changes since 6.74.1:

  New nodes:
  /tailfned/api/new-aaa-list-syntax

  New nodes in grouping <aaa-authorization-method-grouping@38818>
  /aaa/authorization/auth-proxy/...
  /aaa/authorization/commands/...
  /aaa/authorization/credential-download/...
  /aaa/authorization/eventmanager/...
  /aaa/authorization/exec/...
  /aaa/authorization/network/...
  /aaa/authorization/reverse-access/...
  /aaa/authorization/subscriber-service/...
      method

  New nodes in grouping <aaa-authentication-method-grouping@38719>
  /aaa/authentication/dot1x/...
  /aaa/authentication/enable/default/...
  /aaa/authentication/login/...
  /aaa/authentication/ppp/...
      method

  Changed nodes:
  /aaa/group/server/tacacs-plus/ipv6

  Changed nodes in grouping <aaa-authorization-method-grouping@38818>
  /aaa/authorization/auth-proxy/...
  /aaa/authorization/commands/...
  /aaa/authorization/credential-download/...
  /aaa/authorization/eventmanager/...
  /aaa/authorization/exec/...
  /aaa/authorization/network/...
  /aaa/authorization/reverse-access/...
  /aaa/authorization/subscriber-service/...
      cache
      group
      group2
      group3
      if-authenticated
      krb5-instance
      local
      none
      tacacsplus

  Changed nodes in grouping <aaa-authentication-method-grouping@38719>
  /aaa/authentication/dot1x/...
  /aaa/authentication/enable/default/...
  /aaa/authentication/login/...
  /aaa/authentication/ppp/...
      cache
      enable
      group
      group2
      group3
      krb5
      line
      local
      local-case
      none
      tacacsplus


cisco-ios v6.74.1 [2021-06-22]
================================================================================

Corrections:

  - Raised 'system mtu' max value from 9180 to 9216.
    (CISCOIOS-2260 / PS-41584)

  - Added tailf:cli-optional-in-sequence annotation on
     ethernet cfm domain * level / service * efc ? vlan
    (CISCOIOS-2261 / PS-41600 / RT46484)

  - Added dependecy between 'interface * / service-policy'
     and 'interface * / vlan-range * / service-policy'
    (CISCOIOS-2264 / PS-41656 / RT46542)

Enhancements:

  - Additions to Yang model:
     interface * / ip pim dense-mode proxy-register list
    (CISCOIOS-2259 / PS-41582 / RT46467)

  - Additions to Yang model:
     interface * / ip pim redundancy *
    (CISCOIOS-2265 / PS-41686 / RT46572)

YANG model changes since 6.74:

  New nodes:

  New nodes in grouping <interface-common-grouping@11365>
  /interface/*/...
      ip/pim/dense-mode/proxy-register/list
      ip/pim/redundancy
      ip/pim/redundancy/dr-priority
      ip/pim/redundancy/name
      ip/pim/redundancy/protocol

  New nodes in grouping <interface-common-ptp-grouping@20717>
  /interface/*/...
      ip/pim/dense-mode/proxy-register/list
      ip/pim/redundancy
      ip/pim/redundancy/dr-priority
      ip/pim/redundancy/name
      ip/pim/redundancy/protocol

  Changed nodes:
  /ethernet/cfm/domain/service/vlan
  /interface/Bundle-subinterface/Bundle
  /interface/GMPLS-subinterface/GMPLS
  /interface/LISP-subinterface/LISP
  /interface/MFR-subinterface/MFR
  /interface/Port-channel-subinterface/Port-channel
  /interface/Serial-subinterface/Serial
  /l2vpn-vfi/l2vpn/vfi/context/mtu
  /monitor/session-erspan-source/session/destination/mtu
  /system/mtu/jumbo
  /system/mtu/size


cisco-ios v6.74 [2021-06-08]
================================================================================
Changes that might affect a package upgrade from version 6.73:
  NOTE: The below YANG model changes are not backwards compatible:

  Incompatible nodes:

  Incompatible nodes in grouping <cable-video-logical-edge-device-protocol-grouping@40834>
  /cable/video/logical-edge-device/protocol/gqi/...
  /cable/video/logical-edge-device/protocol/table-based/...
      vcg


Corrections:

  - Solved dependency problem configuring macsec on interfaces.
    Solved config diff with macsec key chain key-string.
    (CISCOIOS-2254 / PS-41378)

  - Fixed interface order dependency, correct order is:
     no ip access-group *
     no switchport mode
    (CISCOIOS-2255 / PS-41483 / RT46364)

  - Shutdown Tunnel interface before deleting it.
    (CISCOIOS-2256 / PS-41499 / RT46380)

  - Added remove-before-change to:
     ptp clock ordinary domain * / clock-port * / clock source
    (CISCOIOS-2257 / PS-41496 / RT46377)


Enhancements:

  - Additions to the YANG model:
     ip sla * / distribution delay one-way
     ip sla * / distribution delay two-way
     ip sla * / distribution delay-variation one-way
     ip sla * / distribution delay-variation two-way
     router bgp * / address-family link-state link-state
     router isis * / flex-algo * / priority
     segment-routing mpls / local-block
    (CISCOIOS-2231 / PS-41222 / RT46105)

  - Additions to the YANG model:
     ip ssh server algorithm encryption aes256-gcm aes256-cbc
    (CISCOIOS-2253 / PS-41379)

  - Additions to the YANG model:
     mka policy * / send-secure-announcements
     mka policy * / ssci-based-on-sci
    (CISCOIOS-2254 / PS-41378)

  - Expanded 'cable video / table-based / vcg * / rf-channel * / session *' config
    (CISCOIOS-2256 / PS-41499 / RT46380)

  - Additions to the YANG model:
     controller VDSL * / shutdown
     interface ATM* / atm oversubscribe
     interface * / dialer persistent
    (CISCOIOS-2258 / PS-41514 / RT46395)

YANG model changes since 6.73:

  New nodes:
  /cable/video/table-based/vcg/rf-channel/session/eas
  /cable/video/table-based/vcg/rf-channel/session/increment-by
  /cable/video/table-based/vcg/rf-channel/session/input-program-num
  /cable/video/table-based/vcg/rf-channel/session/master-slave
  /cable/video/table-based/vcg/rf-channel/session/output-program-num
  /cable/video/table-based/vcg/rf-channel/session/pcr-pid
  /cable/video/table-based/vcg/rf-channel/session/psip
  /controller/VDSL/shutdown
  /interface/ATM/atm/oversubscribe/factor
  /mka/policy/send-secure-announcements
  /mka/policy/ssci-based-on-sci

  New nodes in grouping <ptp-clock-domain-grouping@45031>
  /ptp/clock/boundary/domain/...
  /ptp/clock/ordinary/domain/...
      clock-port/clock/source-conf/source/ipv6

  Changed nodes:
  /cable/video/table-based/vcg/rf-channel/session
  /cable/video/table-based/vcg/rf-channel/session/vbr
  /call-home/profile/active
  /call-home/profile/reporting/smart-licensing-data
  /interface/Serial-subinterface/Serial/ip/access-group
  /interface/Serial-subinterface/Serial/shutdown
  /interface/Serial-subinterface/Serial/snmp/trap/link-status
  /interface/Tunnel
  /ip/sla/ip-sla-list/ethernet/y1731
  /key/chain/key/key-string
  /key/macsec/chain/key/key-string

  Changed nodes in grouping <ptp-clock-domain-grouping@45031>
  /ptp/clock/boundary/domain/...
  /ptp/clock/ordinary/domain/...
      clock-port/clock/source

  Changed nodes in grouping <interface-common-ptp-grouping@20667>
  /interface/Multilink/...
  /interface/Serial/...
      ip/access-group
      shutdown
      snmp/trap/link-status

  Changed nodes in grouping <password-grouping@2657>
  /key/chain/key/key-string/...
  /key/macsec/chain/key/key-string/...
      secret

  Changed nodes in grouping <cable-video-logical-edge-device-protocol-grouping@40834>
  /cable/video/logical-edge-device/protocol/gqi/...
  /cable/video/logical-edge-device/protocol/table-based/...
      vcg

  Changed nodes in grouping <ip-ssh-algorithm-grouping@41440>
  /ip/ssh/client/...
  /ip/ssh/server/...
      algorithm/encryption


cisco-ios v6.73 [2021-05-28]
============================

Changes that might affect a package upgrade from version 6.72.5:
  NOTE: The below YANG model changes are not backwards compatible:

  Removed nodes:
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4/strict-spf


Corrections:

  - API CHANGE:
     segment-routing mpls / connected-prefix-sid-map / address-family ipv4
      moved strict-spf from a leaf to a mode container to allow both modes.
     (CISCOIOS-2243 / PS-41383 / RT46263)

  - Restored SSH connection error with key mismatch.
     (CISCOIOS-2248 / PS-41212 / RT46095)

Enhancements:

  - Additions to the YANG model:
     interface * / no ip dhcp client request domain-name
     interface * / ip dhcp client hostname
     interface * / ip dhcp client lease
    (CISCOIOS-2242 / PS-41362 / RT46244)

  - Raised max values in "interface * / ip rsvp bandwidth percent" config
     (CISCOIOS-2244 / PS-41402 / RT46282)

  - Additions to the YANG model:
     fips authorization-key
     (CISCOIOS-2245 / PS-41397 / RT46277)

  - Additions to the YANG model:
     crypto map * ipsec-isakmp / set session-key inbound esp
     crypto map * ipsec-isakmp / set session-key inbound ah
     crypto map * ipsec-isakmp / set session-key outbound esp
     crypto map * ipsec-isakmp / set session-key outbound ah
     track * / carrier-delay
     (CISCOIOS-2247 / PS-41411 / RT46291)

   - Updated NEDCOM to version 1.207.
     (CISCOIOS-2248 / PS-41212 / RT46095)

YANG model changes since 6.72.5:

  New nodes:
  /crypto/map/seq-list/set/session-key/inbound/ah/key
  /crypto/map/seq-list/set/session-key/inbound/ah/spi
  /crypto/map/seq-list/set/session-key/inbound/esp/authenticator
  /crypto/map/seq-list/set/session-key/inbound/esp/cipher
  /crypto/map/seq-list/set/session-key/inbound/esp/spi
  /crypto/map/seq-list/set/session-key/outbound/ah/key
  /crypto/map/seq-list/set/session-key/outbound/ah/spi
  /crypto/map/seq-list/set/session-key/outbound/esp/authenticator
  /crypto/map/seq-list/set/session-key/outbound/esp/cipher
  /crypto/map/seq-list/set/session-key/outbound/esp/spi
  /fips/authorization-key
  /interface/*/ip/dhcp/client/hostname
  /interface/*/ip/dhcp/client/lease/days
  /interface/*/ip/dhcp/client/lease/hours
  /interface/*/ip/dhcp/client/lease/minutes
  /interface/*/ip/dhcp/client/request/domain-name
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/strict-spf/address-list
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/strict-spf/address-list/absolute
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/strict-spf/address-list/index
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/strict-spf/address-list/prefix
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/strict-spf/address-list/range
  /track/track-object/carrier-delay

  New nodes in interface groupings
  /interface/*/...
      ip/dhcp/client/hostname
      ip/dhcp/client/lease/days
      ip/dhcp/client/lease/hours
      ip/dhcp/client/lease/minutes
      ip/dhcp/client/request/domain-name

  Changed nodes:
  /interface/*/carrier-delay
  /interface/*/ip/dhcp/client/client-id
  /interface/*/ip/rsvp/bandwidth-percent/bandwidth/percent/max-flow-bw
  /interface/*/ip/rsvp/bandwidth/interface-bandwidth
  /interface/*/ip/rsvp/bandwidth/single-flow-bandwidth
  /interface/*/ntp/disable
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/algorithm


cisco-ios v6.72.5 [2021-05-20]
==============================

Corrections:

  - Removed 0-7 range limitation on
     interface Cable* / upstream *
    (CISCOIOS-2240 / PS-41332 / RT46214)

Enhancements:

  - Additions to the YANG model:
     interface * / backup delay
    (CISCOIOS-2239 / PS-41312 / RT46194)

  - Additions to the YANG model:
     controller Upstream-Cable * / us-channel * hop-priority modulation frequency channel-width
     interface Cable* / cable udc-capability
     interface Cable* / no cable d31-mode
     interface Cable* / no cable upstream ext-power
     interface Integrated-Cable* / cable downstream qos wfq weights
    (CISCOIOS-2240 / PS-41332 / RT46214)

YANG model changes since 6.72.4:

  New nodes:
  /interface/Cable/cable/d31-mode
  /interface/Cable/cable/udc-capability
  /interface/Cable/cable/upstream/ext-power
  /interface/Integrated-Cable/cable/downstream/qos/wfq/weights/pri0
  /interface/Integrated-Cable/cable/downstream/qos/wfq/weights/pri1
  /interface/Integrated-Cable/cable/downstream/qos/wfq/weights/pri2
  /interface/Integrated-Cable/cable/downstream/qos/wfq/weights/pri3
  /interface/Integrated-Cable/cable/downstream/qos/wfq/weights/pri4
  /interface/Integrated-Cable/cable/downstream/qos/wfq/weights/pri5
  /interface/Integrated-Cable/cable/downstream/qos/wfq/weights/pri6
  /interface/Integrated-Cable/cable/downstream/qos/wfq/weights/pri7

  New nodes in grouping <us-channel-grouping@42471>
  /cable/upstream/controller-profile/...
  /controller/Upstream-Cable/...
      us-channel/hop-priority/modulation/frequency/channel-width

  New nodes in grouping <interface-common-pre-grouping@10567>
  /interface/*/...
      backup/delay/disable-delay-period
      backup/delay/enable-delay-period

  Changed nodes:
  /interface/Cable/upstream/id
  /interface/Serial-subinterface/Serial/dampening

  Changed nodes in grouping <interface-common-ptp-grouping@20608>
  /interface/Multilink/...
  /interface/Serial/...
      dampening

  Changed nodes in grouping <sub-interface-common-grouping@20591>
  /interface/Bundle-subinterface/Bundle/...
  /interface/GMPLS-subinterface/GMPLS/...
  /interface/LISP-subinterface/LISP/...
  /interface/MFR-subinterface/MFR/...
  /interface/Port-channel-subinterface/Port-channel/...
      dampening

  Changed nodes in grouping <interface-common-grouping@20576>
  /interface/*/...
      dampening


cisco-ios v6.72.4 [2021-05-12]
==============================

Corrections:

  - Fixed order dependency, correct order is:
     ap country
     ap dot11
    Added automatic toggling of ap dot11 5ghz|24ghz shutdown to allow config
     changes when config is active.
    Added support for answering yes to '? (y/n)[' device prompt when configuring.
    Restored support for auto-exit of config mode when logging into device.
    (CISCOIOS-2236 / PS-41252 / RT46134)

Enhancements:

  - Additions to the YANG model:
     ip sla * / distribution delay one-way
     ip sla * / distribution delay two-way
     ip sla * / distribution delay-variation one-way
     ip sla * / distribution delay-variation two-way
     router bgp * / address-family link-state link-state
     router isis * / flex-algo * / priority
     segment-routing mpls / local-block
    (CISCOIOS-2231 / PS-41222 / RT46105)

  - Additions to the YANG model:
     router bgp * / neighbor * ao
    (CISCOIOS-2232 / PS-41228 / RT46111)

  - Additions to the YANG model:
     cable video / multicast-uplink * access-list-global
     cable video / default-onid
     cable video / default-psi-interval
     cable video / use-pcr-fo
     cable video / pcr-based-source-switch
     cable video / default-nit-reference
     cable video / service-distribution-group * / onid
     cable video / service-distribution-group * / psi-interval
     cable video / service-distribution-group * / rf-port integrated-cable *
     cable video / service-distribution-group * / serving-area
     cable video / announce-event-profile *
     cable video / announce-event-profile * / ack-timeout
     cable video / announce-event-profile * / filter
    (CISCOIOS-2233 / PS-41231 / RT46114)

  - Additions to the YANG model:
     cable video / logical-edge-device * / protocol gqi / mgmt-ip
     cable video / logical-edge-device * / protocol gqi / reset interval
     cable video / logical-edge-device * / protocol gqi / reset on-active
     cable video / logical-edge-device * / protocol gqi / event-profile
     cable video / logical-edge-device * / protocol gqi / create-session-response-max-delay
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6 / vendor-string
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6 / streaming-zone
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6 / component-name
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6 / d6-server
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6 / timeout
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6 / holdtime
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6 / report-output-port-number
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6 / input-group *
     cable video / logical-edge-device * / protocol gqi / discovery-protocol d6 / enable
    (CISCOIOS-2234 / PS-41255 / RT46137)

  - Additions to the YANG model:
     cable video / virtual-carrier-group * / encrypt
     cable video / logical-edge-device * / protocol gqi / udp-port-mapping-method
     cable video / virtual-carrier-group * / override-tsid
     cable video / virtual-carrier-group * / low-latency
     cable video / virtual-carrier-group * / virtual-edge-input-ip *
     cable video / logical-edge-device * / protocol gqi / pmv
     cable video / table-based / vcg * / rf-channel * / session * vbr
    (CISCOIOS-2235 / PS-41256 / RT46138)

YANG model changes since 6.72.3:

  New nodes:
  /cable/video/announce-event-profile
  /cable/video/announce-event-profile/ack-timeout
  /cable/video/announce-event-profile/filter
  /cable/video/announce-event-profile/id
  /cable/video/announce-event-profile/name
  /cable/video/default-nit-reference
  /cable/video/default-onid
  /cable/video/default-psi-interval
  /cable/video/multicast-uplink/access-list-global
  /cable/video/pcr-based-source-switch
  /cable/video/service-distribution-group/onid
  /cable/video/service-distribution-group/psi-interval
  /cable/video/service-distribution-group/rf-port/integrated-cable
  /cable/video/service-distribution-group/rf-port/integrated-cable/name
  /cable/video/service-distribution-group/serving-area
  /cable/video/table-based/vcg/rf-channel/session/vbr
  /cable/video/use-pcr-fo
  /cable/video/virtual-carrier-group/encrypt
  /cable/video/virtual-carrier-group/low-latency
  /cable/video/virtual-carrier-group/override-tsid
  /cable/video/virtual-carrier-group/virtual-edge-input-ip
  /cable/video/virtual-carrier-group/virtual-edge-input-ip/address
  /cable/video/virtual-carrier-group/virtual-edge-input-ip/input-port-number
  /cable/video/virtual-carrier-group/virtual-edge-input-ip/vrf
  /router/bgp/address-family/link-state
  /router/bgp/template/peer-session/ao/keychain-name
  /segment-routing/mpls/local-block/end
  /segment-routing/mpls/local-block/start

  New nodes in grouping <ip-sla-distribution-delay-grouping@47385>
  /ip/sla/ip-sla-list/distribution/delay-variation/...
  /ip/sla/ip-sla-list/distribution/delay/...
      one-way/boundary
      one-way/number-of-bins
      two-way/boundary
      two-way/number-of-bins

  New nodes in grouping <cable-video-logical-edge-device-protocol-grouping@40717>
  /cable/video/logical-edge-device/protocol/gqi/...
  /cable/video/logical-edge-device/protocol/table-based/...
      create-session-response-max-delay
      discovery-protocol/d6/component-name
      discovery-protocol/d6/d6-server/address/ip
      discovery-protocol/d6/d6-server/address/port
      discovery-protocol/d6/d6-server/fqdn/domain-name
      discovery-protocol/d6/d6-server/fqdn/port
      discovery-protocol/d6/enable
      discovery-protocol/d6/holdtime
      discovery-protocol/d6/input-group/led
      discovery-protocol/d6/input-group/led/bandwidth
      discovery-protocol/d6/input-group/led/group-name
      discovery-protocol/d6/input-group/led/vei-ip
      discovery-protocol/d6/mgmt-ip
      discovery-protocol/d6/report-output-port-number
      discovery-protocol/d6/streaming-zone
      discovery-protocol/d6/timeout
      discovery-protocol/d6/vendor-string
      event-profile
      pmv
      reset/interval
      reset/on-active
      udp-port-mapping-method

  New nodes in grouping <router-isis-grouping@33458>
  /router/isis-container/isis/...
  /router/isis/...
      flex-algo/priority

  Changed nodes:
  /cable/downstream/controller-profile/rf-chan/rf-chan
  /cable/video/mgmt-intf/VirtualPortGroup
  /cable/video/service-distribution-group/id
  /cable/video/table-based/vcg/rf-channel/session/cbr
  /cable/video/table-based/vcg/rf-channel/session/processing-type
  /ip/sla/ip-sla-list/ethernet/y1731

  Changed nodes in grouping <cable-video-logical-edge-device-protocol-grouping@40717>
  /cable/video/logical-edge-device/protocol/gqi/...
  /cable/video/logical-edge-device/protocol/table-based/...
      active
      mgmt-ip

  Changed nodes in grouping <router-isis-grouping@33458>
  /router/isis-container/isis/...
  /router/isis/...
      flex-algo


cisco-ios v6.72.3 [2021-05-06]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     controller SONET * / sts-1 * / vtg * t1 * Loopback network
    (CISCOIOS-2229 / PS-41137 / RT46020)

  - Additions to the YANG model:
     segment-routing mpls / connected-prefix-sid-map / address-family ipv4 strict-spf
    (CISCOIOS-2230 / PS-41161 / RT46044)

YANG model changes since 6.72.2:

  New nodes:
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4/strict-spf

  New nodes in grouping <controller-SONET-grouping@44976>
  /controller/SONET-ACR/...
  /controller/SONET/...
      sts-1/loopback/network/line
      sts-1/vtg/Loopback/Network/line

  Changed nodes in grouping <controller-SONET-grouping@44976>
  /controller/SONET-ACR/...
  /controller/SONET/...
      sts-1/loopback/network
      sts-1/vtg/Loopback/Local


cisco-ios v6.72.2 [2021-04-29]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / isis fast-reroute protection
     interface * / isis fast-reroute candidate
     interface * / isis fast-reroute ti-lfa protection
     interface * / ip dhcp client client-id
     interface * / ip rsvp tunnel overhead-percent
    (CISCOIOS-2225 / PS-41099 / RT45982)

Corrections:

  - Fixed typo in ned-connector-proxy.json resuling in connect to wrong end device
    (CISCOIOS-2226 / PS-41108 / RT45991)

  - Fixed order dependency, correct order is:
     network-clock synchronization
     network-clock revertive
    (CISCOIOS-2228 / PS-41117 / RT46000)

YANG model changes since 6.72.1:

  New nodes:
  /interface/Serial-subinterface/Serial/ip/dhcp/client/client-id/ascii
  /interface/Serial-subinterface/Serial/ip/rsvp/tunnel/overhead-percent
  /interface/Serial-subinterface/Serial/isis/fast-reroute/candidate/level-1
  /interface/Serial-subinterface/Serial/isis/fast-reroute/candidate/level-2
  /interface/Serial-subinterface/Serial/isis/fast-reroute/protection/level-1
  /interface/Serial-subinterface/Serial/isis/fast-reroute/protection/level-2
  /interface/Serial-subinterface/Serial/isis/fast-reroute/ti-lfa/protection/level-1
  /interface/Serial-subinterface/Serial/isis/fast-reroute/ti-lfa/protection/level-2

  New nodes in grouping <interface-common-ptp-grouping@20564>
  /interface/Multilink/...
  /interface/Serial/...
      ip/dhcp/client/client-id/ascii
      ip/rsvp/tunnel/overhead-percent
      isis/fast-reroute/candidate/level-1
      isis/fast-reroute/candidate/level-2
      isis/fast-reroute/protection/level-1
      isis/fast-reroute/protection/level-2
      isis/fast-reroute/ti-lfa/protection/level-1
      isis/fast-reroute/ti-lfa/protection/level-2

  New nodes in grouping <sub-interface-common-grouping@20547>
  /interface/Bundle-subinterface/Bundle/...
  /interface/GMPLS-subinterface/GMPLS/...
  /interface/LISP-subinterface/LISP/...
  /interface/MFR-subinterface/MFR/...
  /interface/Port-channel-subinterface/Port-channel/...
      ip/dhcp/client/client-id/ascii
      ip/rsvp/tunnel/overhead-percent
      isis/fast-reroute/candidate/level-1
      isis/fast-reroute/candidate/level-2
      isis/fast-reroute/protection/level-1
      isis/fast-reroute/protection/level-2
      isis/fast-reroute/ti-lfa/protection/level-1
      isis/fast-reroute/ti-lfa/protection/level-2

  New nodes in grouping <interface-common-grouping@20532>
  /interface/*/...
      ip/dhcp/client/client-id/ascii
      ip/rsvp/tunnel/overhead-percent
      isis/fast-reroute/candidate/level-1
      isis/fast-reroute/candidate/level-2
      isis/fast-reroute/protection/level-1
      isis/fast-reroute/protection/level-2
      isis/fast-reroute/ti-lfa/protection/level-1
      isis/fast-reroute/ti-lfa/protection/level-2

  Changed nodes:
  /network-clock/revertive


cisco-ios v6.72.1 [2021-04-22]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Injsert 'no service pad' on IOS version 17.3 to support changed hidden default.
     (CISCOIOS-2220 / PS-41022 / RT45905)

Corrections:

  - Fixed null Exception in setupInstance caused by nedcom initialization error.
    Restored missing device id in java log.
    Updated NEDCOM to version 1.203.
    (CISCOIOS-2223 / PS-40162 / RT45945)


cisco-ios v6.72 [2021-04-15]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added support for 'proxy2 authgroup' ned-setting
    Updated NEDCOM to version 1.197.
    (CISCOIOS-2215 / PS-40931 / RT45816)

Corrections:

  - Delete 'interface ATM* / pvc * / service-policy' before interface Dialer
    (CISCOIOS-2214 / PS-40929 / RT45814)

  - Support alternate name 'max-flap' in errdisable flap-setting cause link-flap config
    (CISCOIOS-2216 / PS-40945 / RT45830)

  - Fixed order dependency, correct order is:
     snmp-server ifindex persist
     snmp mib persist cbqos
    (CISCOIOS-2217 / PS-40946 / RT45831)

  - Fixed problem dequoting strings with 2 passwords, e.g. snmp-server user
    (CISCOIOS-2218 / PS-40987 / RT45870)

  - API CHANGE:
     router bgp * / address-family * / network * *
    Added mask as list key and split single address entry in a separate list
    in order to support multiple entries with the same address but different mask.
    (CISCOIOS-2219 / PS-41011 / RT45894)

YANG model changes since 6.71.5:

  Changed nodes:
  /cts/credentials/password
  /ephone/username/password
  /errdisable/flap-setting/cause/dtp-flap/max-flaps
  /errdisable/flap-setting/cause/link-flap/max-flaps
  /errdisable/flap-setting/cause/pagp-flap/max-flaps
  /interface/ATM/pvc/service-policy
  /interface/Serial-subinterface/Serial/isis/password-level-1-2/password/password
  /key/config-key/password-encrypt
  /snmp/mib/persist
  /telephony-service/authentication/credential/password
  /telephony-service/web/admin/system/password
  /vtp/password

  Changed nodes in grouping <policy-map-type-control-class-grouping@7540>
  /policy-map-control/policy-map/class-always/class/...
  /policy-map-control/policy-map/class-name/class/...
      action/authorize/aaa/password

  Changed nodes in grouping <sub-interface-common-grouping@20458>
  /interface/Bundle-subinterface/Bundle/...
  /interface/GMPLS-subinterface/GMPLS/...
  /interface/LISP-subinterface/LISP/...
  /interface/MFR-subinterface/MFR/...
  /interface/Port-channel-subinterface/Port-channel/...
      isis/password-level-1-2/password/password

  Changed nodes in grouping <interface-common-grouping@20443>
  /interface/*/...
      isis/password-level-1-2/password/password

  Changed nodes in grouping <interface-common-ptp-grouping@20475>
  /interface/Multilink/...
  /interface/Serial/...
      isis/password-level-1-2/password/password

  Changed nodes in grouping <snmp-server-user-grouping@38213>
  /snmp-server/user-remote/user/...
  /snmp-server/user/...
      auth-password
      priv-password

  Changed nodes in grouping <router-isis-grouping@33319>
  /router/isis-container/isis/...
  /router/isis/...
      area-password/name
      domain-password/name


cisco-ios v6.71.5 [2021-04-01]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     bfd-template single-hop * / echo
     interface * / isis affinity flex-algo / name *
     interface * / isis delay normalize
     key chain * tcp
     key chain * tcp / key * / send-id
     key chain * tcp / key * / receive-id
     license smart transport off
     performance-measurement / protocol twamp-light
     performance-measurement / protocol twamp-light / measurement delay / unauthenticated / querier-dst-port
     performance-measurement / protocol twamp-light / measurement delay / unauthenticated / querier-src-port
     performance-measurement / delay-profile interfaces / advertisement / periodic / threshold
     performance-measurement / delay-profile sr-policy / probe / sweep / destination
     router isis * / affinity-map * bit-position *
     router isis * / flex-algo *
     router isis * / flex-algo * / advertise-definition
     router isis * / flex-algo * / affinity / *
     router isis * / flex-algo * / affinity / * / name *
     router isis * / flex-algo * / metric-type delay
     router isis * / ucmp local
     segment-routing mpls / connected-prefix-sid-map / address-family ipv4 algorithm *
     segment-routing mpls / connected-prefix-sid-map / address-family ipv4 algorithm * / *
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / per-flow
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / per-flow / forward-class *
     segment-routing traffic-eng / on-demand color * / description
    (CISCOIOS-2197 / PS-40435)

  - Added 'write config-revert-timer' ned-setting to allow use of the IOS exec
     command 'config revert' to automatically revert to last archived config if
     device error or loss of connection.
    (CISCOIOS-2209)

Corrections:

  - Fixed cable order dependency problem between:
     cable rpd * / core-interface
     cable rpd * / rpd-ds * base-power
    (CISCOIOS-2210 / PS-40759 / RT45641)

  - Fixed dependency issue with redeploy-with-change annotation causing
     interface to be deleted after change with router bgp in commit.
    (CISCOIOS-2213 / PS-40849 / RT45734)

YANG model changes since 6.71.4:

  New nodes:
  /interface/Serial-subinterface/Serial/isis/affinity/flex-algo/name
  /interface/Serial-subinterface/Serial/isis/affinity/flex-algo/name/name
  /interface/Serial-subinterface/Serial/isis/delay/normalize/interval
  /interface/Serial-subinterface/Serial/isis/delay/normalize/offset
  /key/tcp/chain
  /key/tcp/chain/key
  /key/tcp/chain/key/cryptographic-algorithm
  /key/tcp/chain/key/id
  /key/tcp/chain/key/key-string/secret
  /key/tcp/chain/key/key-string/type
  /key/tcp/chain/key/recv-id
  /key/tcp/chain/key/send-id
  /key/tcp/chain/name
  /key/tcp/chain/tcp
  /performance-measurement/delay-profile/interfaces/advertisement/periodic/threshold
  /performance-measurement/delay-profile/sr-policy/probe/sweep/destination/ipv4
  /performance-measurement/delay-profile/sr-policy/probe/sweep/destination/range
  /performance-measurement/protocol/twamp-light/measurement/delay/unauthenticated/querier-dst-port
  /performance-measurement/protocol/twamp-light/measurement/delay/unauthenticated/querier-src-port
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/algorithm
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/algorithm/address-list
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/algorithm/address-list/absolute
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/algorithm/address-list/index
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/algorithm/address-list/prefix
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/algorithm/address-list/range
  /segment-routing/mpls/connected-prefix-sid-map/address-family/ipv4-conf/ipv4/algorithm/id

  New nodes in grouping <description-grouping@2622>
  /key/tcp/chain/...
  /segment-routing/traffic-eng/on-demand/color/...
      description

  New nodes in grouping <segment-routing-preference-grouping@46712>
  /segment-routing/traffic-eng/on-demand/color/candidate-paths/preference/...
  /segment-routing/traffic-eng/policy/candidate-paths/preference/...
      per-flow/forward-class
      per-flow/forward-class/color
      per-flow/forward-class/id

  New nodes in grouping <lifetime-grouping@41969>
  /key/tcp/chain/key/accept-lifetime/...
  /key/tcp/chain/key/accept-lifetime/local/...
  /key/tcp/chain/key/send-lifetime/...
  /key/tcp/chain/key/send-lifetime/local/...
      duration
      infinite
      start-date
      start-month
      start-time
      start-year
      stop-date
      stop-month
      stop-time
      stop-year

  New nodes in grouping <sub-interface-common-grouping@20458>
  /interface/Bundle-subinterface/Bundle/...
  /interface/GMPLS-subinterface/GMPLS/...
  /interface/LISP-subinterface/LISP/...
  /interface/MFR-subinterface/MFR/...
  /interface/Port-channel-subinterface/Port-channel/...
      isis/affinity/flex-algo/name
      isis/affinity/flex-algo/name/name
      isis/delay/normalize/interval
      isis/delay/normalize/offset

  New nodes in grouping <interface-common-grouping@20443>
  /interface/*/...
      isis/affinity/flex-algo/name
      isis/affinity/flex-algo/name/name
      isis/delay/normalize/interval
      isis/delay/normalize/offset

  New nodes in grouping <bfd-template-grouping@46282>
  /bfd-template/multi-hop/...
  /bfd-template/single-hop/...
      echo

  New nodes in grouping <interface-common-ptp-grouping@20475>
  /interface/Multilink/...
  /interface/Serial/...
      isis/affinity/flex-algo/name
      isis/affinity/flex-algo/name/name
      isis/delay/normalize/interval
      isis/delay/normalize/offset

  New nodes in grouping <router-isis-grouping@33319>
  /router/isis-container/isis/...
  /router/isis/...
      affinity-map
      affinity-map/bit-position
      affinity-map/name
      flex-algo
      flex-algo/advertise-definition
      flex-algo/affinity/attribute-list
      flex-algo/affinity/attribute-list/id
      flex-algo/affinity/attribute-list/name
      flex-algo/affinity/attribute-list/name/name
      flex-algo/id
      flex-algo/metric-type/delay
      ucmp/local/prefix-list

  Changed nodes:
  /cable/rpd
  /cable/rpd/rpd-ds/base-power
  /key/chain
  /license/smart/transport
  /segment-routing/traffic-eng/on-demand/color


cisco-ios v6.71.4 [2021-03-25]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     vlan group * vlan-list *
    (CISCOIOS-2207 / PS-40737 / RT45619)

  - Additions to the YANG model:
     interface * / authentication event server dead action authorize
     radius-server host * ignore-auth-port
     radius-server host * ignore-acct-port
     radius-server host * idle-time
    (CISCOIOS-2212 / PS-40802 / RT45687)

Corrections:

  - Added retry on inteface speed|duplex to support delayed availability.
    (CISCOIOS-2178 / PS-40327 / RT45214)

  - Added 'cli-delete-container-on-delete' annotation on 'interface pseudowire* / neighbor' address leaf
    (CISCOIOS-2194 / PS-40504 / RT45390)

  - Use default command to delete line transport config to avoid ending with 'none'
    (CISCOIOS-2208 / PS-40752 / RT45634)

  - Remove backup configuration before changing xconnect address in interface service instance
    (CISCOIOS-2211 / PS-40769 / RT45651)

YANG model changes since 6.71.3:

  New nodes:
  /vlan/group
  /vlan/group/name
  /vlan/group/vlan-list

  New nodes in grouping <interface-ethernet-grouping@21354>
  /interface/*/...
      authentication/event/server/dead/action/authorize-enable/authorize

  New nodes in grouping <interface-ethernet-switch-grouping@24245>
  /interface/*/...
      authentication/event/server/dead/action/authorize-enable/authorize

  New nodes in grouping <radius-server-grouping@42232>
  /aaa/group/server/radius/server-private/...
  /radius-server/host/...
      idle-time
      ignore-acct-port
      ignore-auth-port

  Changed nodes:
  /ethernet/ring/g8032/g8032-list/port1-none
  /ethernet/ring/g8032/g8032-list/port1-none/port1
  /interface/pseudowire/neighbor/address

  Changed nodes in grouping <line-grouping@9467>
  /line/aux/...
  /line/console/...
  /line/number-single-list/...
  /line/vty-single-conf/vty/...
  /line/vty/...
      transport/input
      transport/output
      transport/preferred

  Changed nodes in grouping <interface-ethernet-grouping@21354>
  /interface/BDI/...
  /interface/Port-channel-subinterface/Port-channel/...
  /interface/Virtual-PPP/...
  /interface/Virtual-Template/...
  /interface/Vlan/...
  /interface/wlan-ap/...
      authentication/event/server/dead/action/authorize
      service/instance/xconnect/backup/peer/peer-router-ip-addr

  Changed nodes in grouping <interface-ethernet-switch-grouping@24245>
  /interface/*/...
      authentication/event/server/dead/action/authorize
      service/instance/xconnect/backup/peer/peer-router-ip-addr


cisco-ios v6.71.3 [2021-03-18]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     crypto ipsec profile * / reverse-route
    (CISCOIOS-2206 / PS-40692 / RT45575)

  - Updated NEDCOM to version 1.193 fixing issue with NSO Maapi encrypt and \n
    (CISCOIOS-2205)

  - Additions to the YANG model:
     aaa group server radius * / attribute nas-port
     ip ssh server algorithm kex
    (CISCOIOS-2203 / PS-40682 / RT45565)

Corrections:

  - Fixed order dependency in l2vpn xconnect, delete members first.
    (CISCOIOS-2202 / PS-40666 / RT45549)

YANG model changes since 6.71.2:

  New nodes:
  /aaa/group/server/radius/attribute/nas-port/format
  /aaa/group/server/radius/attribute/nas-port/none
  /aaa/group/server/radius/attribute/nas-port/string
  /crypto/ipsec/profile/reverse-route/gateway
  /crypto/ipsec/profile/reverse-route/remote-peer
  /crypto/ipsec/profile/reverse-route/static

  New nodes in grouping <ip-ssh-algorithm-grouping@40762>
  /ip/ssh/client/...
  /ip/ssh/server/...
      algorithm/kex


cisco-ios v6.71.2 [2021-03-11]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     crypto gdoi group * / client fail-close revert
    (CISCOIOS-2196 / PS-40553 / RT45439)

  - Additions to the YANG model:
     router isis * / distribute link-state instance-id
     mka policy * / macsec-cipher-suite * gcm-aes-xpn-256
    (CISCOIOS-2198 / PS-40554)

Corrections:

  - Revert to old NSO diff-dep API to restore order dependency between:
     l2vpn xconnect context * / member <member-list{service-instance}
     interface * / service instance *
    (CISCOIOS-2201 / PS-40624 / RT45508)

YANG model changes since 6.71.1:

  New nodes:
  /crypto/gdoi/group/client/fail-close/revert

  New nodes in grouping <router-isis-grouping@33245>
  /router/isis-container/isis/...
  /router/isis/...
      distribute/link-state/instance-id

  Changed nodes:
  /mka/policy/macsec-cipher-suite


cisco-ios v6.71.1 [2021-03-10]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     app-hosting appid *
     app-hosting appid * / app-resource profile *
     app-hosting appid * / app-resource profile * / cpu
     app-hosting appid * / app-resource profile * / memory
     app-hosting appid * / app-vnic gateway*
    (CISCOIOS-2192 / PS-40489 / RT45375)


Corrections:

  - Fixed order dependency problem between
     l2vpn xconnect context * / member <member-list{service-instance}
     interface * / service instance *
    (CISCOIOS-2199 / PS-40595 / RT45479)


YANG model changes since 6.71:

  New nodes:
  /app-hosting/appid
  /app-hosting/appid/app-resource/profile
  /app-hosting/appid/app-resource/profile/cpu
  /app-hosting/appid/app-resource/profile/memory
  /app-hosting/appid/app-resource/profile/name
  /app-hosting/appid/app-vnic/gateway
  /app-hosting/appid/app-vnic/gateway/guest-interface
  /app-hosting/appid/app-vnic/gateway/name
  /app-hosting/appid/app-vnic/gateway/virtualportgroup
  /app-hosting/appid/name


cisco-ios v6.71 [2021-03-04]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - API CHANGE:
     cable fiber-node * / upstream sg-channel *
      Redesigned YANG model to support delete|add of individual entries.
    (CISCOIOS-2191 / RT45364)

  - Changed cable fiber-node dependencies to inject the service-profile delete
     first and the remaining changes after controller changes.
    (CISCOIOS-2191 / RT45364)


cisco-ios v6.70.1 [2021-03-03]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Remove and redeploy 'service-group profile' when modifying 'cable fiber-node *'
    Note: the service-group profile is added back last in the transaction.
    (CISCOIOS-2191 / RT45364)

  - Added remove-before-change to:
     cable fiber-node * / upstream sg-channel * us-channel
    (CISCOIOS-2191 / RT45364)

  - Remove first in controller before modifying:
     controller Upstream-Cable * / us-channel * frequency
     controller Upstream-Cable * / us-channel * channel-width
    (CISCOIOS-2191 / RT45364)


cisco-ios v6.70 [2021-03-02]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     aaa accounting identity *
     aaa authorization credential-download *
     aaa common-criteria policy * / upper-case
     aaa common-criteria policy * / lower-case
     access-session mac-move deny
     ap dot11 * beamforming
     ap dot11 * cac voice sip
     ap dot11 * cac voice acm
     ap dot11 * channelswitch quiet
     ap dot11 * rrm channel dca chan-width
     ap dot11 * rrm channel dca chan-width width-max
     ap dot11 * rrm txpower max
     ap dot11 * rrm txpower min
     ap dot11 * edca-parameters
     ap dot11 * exp-bwreq
     ap dot11 * rate *
     ap dot11 * rf-profile * / channel add *
     ap dot11 * rf-profile * / channel remove *
     ap dot11 * rf-profile * / channel chan-width
     ap dot11 * rf-profile * / client-network-preference
     ap dot11 * rf-profile * / coverage exception
     ap dot11 * rrm channel cleanair-event
     ap dot11 * rrm channel cleanair-event sensitivity
     ap dot11 * rrm channel dca * add
     ap dot11 * rrm channel dca interval
     ap dot11 * rrm monitor channel-list
     ap dot11 * rrm tpc threshold
     ap dot11 * tsm
     ap fra interval
     ap profile * / packet-capture
     ap profile * / preferred-mode
     ap profile * / rogue detection min-rssi
     ap profile * / tcp-adjust-mss size
     crypto pki certificate pool / cabundle
     no device-tracking logging theft
     location expiry calibrating-client
     location expiry client
     location expiry tags
     mdns-sd service-definition *
     mdns-sd service-definition * / description
     mdns-sd service-definition * / service-type
     mdns-sd service-list *
     mdns-sd service-list * / match *
     mdns-sd service-policy *
     mdns-sd service-policy * / service-list * IN|OUT
     telemetry ietf subscription * / filter tdl-transform
     telemetry ietf subscription * / filter tdl-uri
     telemetry ietf subscription * / update-policy on-change
     telemetry transform *
     telemetry transform * / input table *
     telemetry transform * / input table * / field *
     telemetry transform * / input table * / join-key
     telemetry transform * / input table * / logical-op
     telemetry transform * / input table * / type
     telemetry transform * / input table * / uri
     telemetry transform * / operation *
     telemetry transform * / operation * / filter *
     telemetry transform * / operation * / filter * / event on-change
     telemetry transform * / operation * / filter * / field
     telemetry transform * / operation * / filter * / logical-op
     telemetry transform * / operation * / output-field *
     telemetry transform * / operation * / output-field * / field
     telemetry transform * / specified
     wireless client onboarding-event
     no wireless ipv6 ra wired
     wireless mobility group name
     wireless multicast
     wireless profile ap packet-capture *
     wireless profile ap packet-capture * / description
     wireless profile policy * / aaa-override
     wireless profile policy * / accounting-list
     wireless profile policy * / no exclusionlist
     wireless profile policy * / exclusionlist timeout
     wireless profile policy * / mdns-sd service-policy
     wireless profile policy * / nac
     wireless profile policy * / service-policy
     wireless profile policy * / vlan
     wireless profile policy * / session-timeout
     wireless profile policy * / no shutdown
     wireless rf-network
     wireless rfid timeout
     wireless security dot1x eapol-key retries
     wireless security dot1x eapol-key timeout
     wireless security dot1x identity-request retries
     wireless security dot1x identity-request timeout
     wireless security dot1x request retries
     wireless security dot1x request timeout
     wireless tag rf * / 5ghz-rf-policy
     wireless wps rogue ap rldp alarm-only
     wireless wps rogue rule * priority
     wireless wps rogue rule * / classify
     wireless wps rogue rule * / condition duration
     wireless wps rogue rule * / condition rssi
     wireless wps rogue rule * / condition infrastructure ssid
     wireless wps rogue rule * / match
     wireless wps rogue rule * / no shutdown
     wlan * / band-select
     wlan * / no security wpa akm dot1x
     wlan * / security dot1x authentication-list
     wlan * / dtim dot11 5ghz
     wlan * / radio
     wlan * / mac-filtering
     wlan * / channel-scan defer-priority
     wlan * / no shutdown
    (CISCOIOS-2188 / RT45318)

  - API CHANGE
     ap dot11 * rrm channel dca *
      changed remove leaf to action leaf with enum add|remove to support add
    (CISCOIOS-2188 / RT45318)

  - Additions to the YANG model:
     interface * / ospfv3 bfd
    (CISCOIOS-2187 / RT45303)

  - Additions to the YANG model:
     router isis * / microloop avoidance segment-routing
    (CISCOIOS-2186 / RT45279)

  - Additions to the YANG model:
     interface * / fhrp delay minimum
     interface * / fhrp delay reload
    (CISCOIOS-2185 / RT45278)

  - Expanded cause key from type uint8 to union with uint8 and string
     platform punt-policer *
    (CISCOIOS-2184 / RT45270)

  - Changed ned-setting 'read snmp-server-user-defaults passwords from type
     string to tailf:aes-cfb-128-encrypted-string.
    (CISCOIOS-2180 / RT45242)

  - Additions to the YANG model:
     controller Upstream-Cable * / us-channel * initial-rng-frequency-start
    (CISCOIOS-2174 / RT45137)

  - Added support for NSO 3rd gen progress report API.
    Updated NEDCOM to version 1.190.
    (CISCOIOS-2146)

Corrections:

  - Changed snmp-server user priv-password type from  type string to type
     NEDCOM_SECRET_TYPE, allowing support for encrypted strings.
     Rebuild src with NEDCOM_SECRET_TYPE="tailf:aes-cfb-128-encrypted-string".
    (CISCOIOS-2190 / RT45327)

  - Improved unlocking trigger for interface * / service instance * ethernet /
     cfm mep' to avoid unlock and relock other evc entries.
    (CISCOIOS-2183 / RT40433)


cisco-ios v6.69.2 [2021-02-19]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / service instance * ethernet / xconnect * circuit-type ethernet
    (CISCOIOS-2181 / RT45244)

  - Added ned-setting 'read snmp-server-user-defaults' for replacing default
     snmp-server user auth- and priv-password(s) NOT-SET-IN-NCS (when not readable)
     Configurable per username by use of regexp or leave unset for all users, e.g.
     read snmp-server-user-defaults SSUP auth-password cisco123 priv-password cisco456
    (CISCOIOS-2180 / RT45242)

  - Additions to the YANG model:
     telemetry ietf subscription *
     telemetry ietf subscription * / encoding
     telemetry ietf subscription * / filter xpath
     telemetry ietf subscription * / stream
     telemetry ietf subscription * / update-policy periodic
     telemetry ietf subscription * / receiver ip address *
    (CISCOIOS-2176 / RT45150)

  - Additions to the YANG model:
     aaa group server radius * / throttle
    (CISCOIOS-2175 / RT45145)

  - Redeploy 'interface pseudowire' config if encapsulation is deleted.
    (CISCOIOS-2172 / RT45074)

Corrections:

  - Fixed interface order dependency, correct order is:
     no ppp multilink
     encapsulation *
    (CISCOIOS-2179 / RT45232)

  - Fixed interface order dependency, correct order is:
     no shutdown
     duplex
    (CISCOIOS-2178 / RT45214)


cisco-ios v6.69.1 [2021-02-11]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / isis ipv6 tag
     interface * / ppp timeout ncp
     interface * / ip load-sharing per-longest-match-prefix
     interface Port-channel* / port-channel bfd destination
     router isis * / address-family ipv6 / bfd all-interfaces
     interface * / no peer default ip address
    (CISCOIOS-2176 / RT45150)

  - Additions to the YANG model:
     aaa policy interface-config allow-subinterface
     call admission new-model
     call admission limit
     call admission cpu-limit
     call admission vpdn
     license smart reservation
     logging host fqdn ipv4 *
     radius-server configure-nas
     radius-server throttle
     router bgp * / no bgp default ipv6-nexthop
     route-map * / set ipv6 next-hop recursive
    (CISCOIOS-2175 / RT45145)

  - Improved the bgp neighbor recreate code by also redeploying address-family
     * / neighbor * config for ipv6 unicast, vpnv4 unicast and vpnv6 unicast.
    (CISCOIOS-2173 / RT45134)

Corrections:

  - Raised max range on 'bfd-template single-hop * / interval' from 999 to 9999
    (CISCOIOS-2176 / RT45150)


cisco-ios v6.69 [2021-02-05]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     cable mod-profile-ofdma * / initial-rng-preamble
    (CISCOIOS-2170 / RT45062)

  - Additions to the YANG model:
     cable profile wideband-interface * / description
     cable profile service-group * / mac-domain * / cable downstream dsg timer
     cable profile service-group * / wideband-interface * / cable ds-resiliency
    (CISCOIOS-2169 / RT45053)

  - Additions to the YANG model:
     cable upstream ofdma-prof-mgmt downgrade enable
     cable upstream ofdma-prof-mgmt downgrade interval
     cable upstream ofdma-prof-mgmt downgrade partial-threshold
     cable upstream ofdma-prof-mgmt downgrade rxmer-enable
     cable upstream ofdma-prof-mgmt downgrade partial-hold-down
     cable upstream ofdma-prof-mgmt rxmer-poll-interval
    (CISCOIOS-2168 / RT45052)

  - Additions to the YANG model:
     track * / object * weight
     track * / threshold weight
    (CISCOIOS-2165 / RT45042)

  - Additions to the YANG model:
     ip igmp snooping vlan * mrouter interface *
     ip igmp snooping vlan * immediate-leave
    (CISCOIOS-2164 / RT45041)

  - Additions to the YANG model:
     pppoe intermediate-agent
     pppoe intermediate-agent format-type circuit-id
    (CISCOIOS-2157 / RT44939)

Corrections:

  - API CHANGE:
     kron occurrence * / policy-list *
      Changed from leaf to leaf-list to support multiple entries.
    (CISCOIOS-2166 / RT45044)

  - API CHANGE:
     Added choice statement in pointtopoint interface * / encapsulation config.
    (CISCOIOS-2163 / RT45015)

  - Fixed support for deleting ethernet-evc-name in interface * / service instance
    (CISCOIOS-2156 / RT44925)


cisco-ios v6.68.2 [2021-02-01]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     policy-map * / class * / random-detect cos|precedence|dscp|discard-class * percent
    (CISCOIOS-2162 / RT45010)

Corrections:


cisco-ios v6.68.1 [2021-01-22]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     cable resiliency free-interval
    (CISCOIOS-2161 / RT44978)

  - Extract and append system mtu config from 'show system mtu' command on some
     devices. Added default 1500 on all system mtu config.
     NOTE: When changing the system mtu the device still need to be rebooted for
           change to affect due to IOS OS limitation.
    (CISCOIOS-2160 / RT44969)

  - Do not trace maapi decrypt failures at info level when sending lines.
    (CISCOIOS-2158 / RT44840)

Corrections:


cisco-ios v6.68 [2021-01-13]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     flow record * / collect routing source|destination as peer 4-octet
     netconf-yang ssh port
    (CISCOIOS-2154 / RT44882)

  - Additions to the YANG model:
     interface * / platform qos vlan-based
    (CISCOIOS-2150 / RT44791)

  - Additions to the YANG model:
     interface Serial* / xconnect
    (CISCOIOS-2148 / RT44741)

  - Additions to the YANG model:
     event manager applet * / event syslog severity-warning
    (CISCOIOS-2143 / RT44634)

Corrections:

  - Fixed order dependency, correct order is:
     no interface Port-channel*
     no platform qos port-channel-aggregate *
    (CISCOIOS-2155 / RT44912)

  - Fixed order dependency, correct order is:
     ethernet cfm domain *
     interface * / service instance * / cfm mep domain *
    (CISCOIOS-2153 / RT44878)

  - Fixed order dependencies, correct order is:
     router bgp * / no address-family ipv6 unicast vrf *
     interface * / no ipv6 enable
     vrf definition * / no address-family ipv6
    (CISCOIOS-2152 / RT44850)


cisco-ios v6.67.12 [2020-12-23]
===============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     flow record * / match routing destination as
     flow record * / match routing source as
    (CISCOIOS-2149 / RT44791)

Corrections:


cisco-ios v6.67.11 [2020-12-18]
===============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     boot network flash
     boot network flash *
    (CISCOIOS-2144 / RT44691)

Corrections:

  - Added missing support for proxy/menu/answer ned-setting with SSH in serial
     connector template ned-connector-serial.json.
    (CISCOIOS-2138 / RT44520)

  - Updated NEDCOM to version 1.182.


cisco-ios v6.67.10 [2020-12-11]
===============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     event manager applet * / event syslog <severity> facility
    (CISCOIOS-2143 / RT44634)

  - Additions to the YANG model:
     cns config initial
     cns image
    (CISCOIOS-2142 / RT44620)

  - Switch to use new NEDCOM NedSSHClient API in order to be ready for NSO 5.5.
    (CISCOIOS-2141)

  - Updated NEDCOM to version 1.181.

Corrections:


cisco-ios v6.67.9 [2020-12-04]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     call-home / site-id
     license solution level securityk9
    (CISCOIOS-2140 / RT44562

  - Added 'exec any-hidden' action to be able to execute device commands
     without writing in audit|trace files.
    (CISCOIOS-2139 / RT44554)

Corrections:


cisco-ios v6.67.8 [2020-11-27]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / tunnel mpls traffic-eng autoroute destination
    (CISCOIOS-2137 / RT44482)

Corrections:


cisco-ios v6.67.7 [2020-11-20]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     trapflags *
    (CISCOIOS-2136 / RT44358)

  - Additions to the YANG model:
     ap country
     ap tag-source-priority * source
     crypto pki trustpoint * / eku request *
    (CISCOIOS-2135 / RT44359)

  - Do not show interface '!ip address' in dry-run, trim it silently.
    (CISCOIOS-2134 / RT44414)

  - Additions to the YANG model:
     crypto keyring * / rsa-pubkey address *
     crypto keyring * / rsa-pubkey address * / key-string
    (CISCOIOS-2133 / RT44412)

  - Additions to the YANG model:
     wireless aaa policy *
     wireless aaa policy * / aaa-realm enable
     wireless cts-sxp profile *
     wireless management trustpoint
     wireless management interface
     wireless profile airtime-fairness *
     wireless profile flex *
     wireless profile flex * / description
     wireless profile mesh *
     wireless profile mesh * / description
     wireless tag rf *
     wireless tag rf * / description
     wireless fabric control-plane *
     wireless country *
    (CISCOIOS-2132 / RT44361)

  - Additions to the YANG model:
     ap dot11 * rf-profile *
     ap dot11 * rf-profile * / coverage data rssi threshold
     ap dot11 * rf-profile * / coverage level
     ap dot11 * rf-profile * / coverage voice rssi threshold
     ap dot11 * rf-profile * / description
     ap dot11 * rf-profile * / high-density rx-sop threshold
     ap dot11 * rf-profile * / tx-power max
     ap dot11 * rf-profile * / tx-power min
     ap dot11 * rf-profile * / tx-power v1 threshold
     ap dot11 * rf-profile * / rate *
     ap dot11 * rf-profile * / no shutdown
    (CISCOIOS-2131 / RT44362)

  - Additions to the YANG model:
     ap * where key is <mac address>
     ap * / policy-tag
     ap * / rf-tag
     ap * / site-tag
    (CISCOIOS-2129 / RT44362)

  - Additions to the YANG model:
     wireless tag site *
     wireless tag site * / description
     wireless tag site * / ap-profile
    (CISCOIOS-2128 / RT44361)

  - Additions to the YANG model:
     ap profile *
     ap profile * / description
     ap profile * / capwap backup primary
     ap profile * / capwap backup secondary
     ap profile * / no capwap fallback
     ap profile * / mgmtuser
     ap profile * / ssh
    (CISCOIOS-2127 / RT44359)

  - Additions to the YANG model:
     wireless profile policy *
     wireless profile policy * / description
     wireless tag policy *
     wireless tag policy * / description
     wireless tag policy * / wlan * policy
    (CISCOIOS-2126 / RT44358)

  - Additions to the YANG model:
     wlan *
     wlan * / description
    (CISCOIOS-2125 / RT44357)

Corrections:


cisco-ios v6.67.6 [2020-11-13]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added ned-setting 'proxy remote-ssh-args' for use with proxy connector
     to set optional SSH options, e.g. "-o StrictHostKeyChecking=no"
    (CISCOIOS-2124 / RT44334)

  - Replaced remaining meta-data with turbo-parser cli extensions.
    (CISCOIOS-2123)

  - Updated NEDCOM to version 1.176.

Corrections:


cisco-ios v6.67.5 [2020-11-06]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ipv6 hop-limit
    (CISCOIOS-2122 / RT44272)

Corrections:


cisco-ios v6.67.4 [2020-11-04]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Redeploy 'ip sla ethernet-monitor reaction-configuration' with
     'ip sla ethernet-monitor' redeploy.
    (CISCOIOS-2121 / RT44251)

  - Additions to the YANG model:
     router bgp * / address-family * / bgp additional-paths select all
     router bgp * / address-family * / bgp additional-paths select group-best
     router bgp * / neighbor * additional-paths
    (CISCOIOS-2120 / RT44248

  - Additions to the YANG model:
     crypto pki certificate pool / certificate *
     ipv6 nd reachable-time
    (CISCOIOS-2120 / RT44248

  - Added new replace-line option to cli:secret extension in username in order
     to preserve algorithm-type leaf config, avoiding issue with service deep
     sync and NSO tailf:ned-ignore-compare-config extension.
     Updated NEDCOM to version 1.174.
    (CISCOIOS-2119 / RT44230)

Corrections:

  - Fixed null Exception in getTransId() method with read timeout.

  - Fixed redeploy order dependency, correct order is:
     no ip sla ethernet-monitor schedule *
     no ip sla ethernet-monitor *
    (CISCOIOS-2121 / RT44251)


cisco-ios v6.67.3 [2020-11-03]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added tailf:cli-reset-container on username * password|secret 'secret' leaf
    (CISCOIOS-2119 / RT44230)

  - Additions to the YANG model:
     aaa authentication password-prompt
     interface * / vrrp *  vrrpv2
    (CISCOIOS-2117 / RT44217)

Corrections:


cisco-ios v6.67.2 [2020-11-02]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Close SSH connection if fail to connect in order to avoid stale connections
    Updated NEDCOM to version 1.171.
    (CISCOIOS-2118 / RT44228)


cisco-ios v6.67.1 [2020-10-30]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     netconf-yang ssh ipv4 access-list name
    (CISCOIOS-2117 / RT44217)

  - Additions to the YANG model:
     policy-map type control * / class type control * event / * authorize aaa * plus
    (CISCOIOS-2113 / RT44193)

  - Additions to the YANG model:
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / dynamic / metric / sid-limit
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / dynamic / metric / margin / relative
     segment-routing traffic-eng / policy * / autoroute / include all
     segment-routing traffic-eng / policy * / autoroute / metric
     segment-routing traffic-eng / policy * / candidate-paths / preference *
     segment-routing traffic-eng / policy * / candidate-paths / preference * / dynamic / metric / sid-limit
     segment-routing traffic-eng / policy * / candidate-paths / preference * / dynamic / metric / margin / relative
    (CISCOIOS-2112 / RT44190)

  - Added support for populating 'devices device platform model' with Linux.
    (CISCOIOS-2111 / RT44175)

  - Added section in README on how to configure ip access-list standard|extended
    (CISCOIOS-2108)

Corrections:

  - Do not reorder policy-map * / class * / shape average when deleted.
    Ignore "queueing must be removed from child classes" warning.
    (CISCOIOS-2116 / RT44210)

  - Fixed order dependency, correct order is:
     interface * / no bfd template wan-access
     no bfd fast-timers-on-slow-interface
    (CISCOIOS-2114 / RT44195)

  - Added missing optional-in-sequence annotation on
     policy-map type control * / class type control * event / * authorize aaa * password
    (CISCOIOS-2113 / RT44193)

  - Fixed order dependency, correct order is:
     l2vpn xconnect context * / no member <interface-list{name}>
     no interface *
    (CISCOIOS-2111 / RT44175)

  - Added missing default value 0 in 'ip sla ethernet-monitor * / type echo * / cos'
    (CISCOIOS-2109 / RT44174)

  - Do not reinject locks if modifying remove-before-change config.
    (CISCOIOS-2109 / RT44174)


cisco-ios v6.67 [2020-10-23]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added support for multiple show commands with 'read show-running-method'
     ned-setting. Separate each show command with " ; " as usual.
    (CISCOIOS-2107)

  - Additions to the YANG model:
     interface * / ospfv3 flood-reduction
     interface * / ospfv3 network
    (CISCOIOS-2106 / RT44148)

  - Replaced most meta-data with turbo-parser cli extensions.
    (CISCOIOS-2105)

  - Additions to the YANG model:
     cable rpd * / rpd-ds * shutdown
     cable rpd * / rpd-ds * tilt-slope
     cable rpd * / rpd-ds * tilt-pivot-freq
    (CISCOIOS-2103 / RT44100)

  - Additions to the YANG model:
     crypto key pubkey-chain rsa / addressed-key *
     crypto key pubkey-chain rsa / addressed-key * / key-string
    (CISCOIOS-2102 / RT44101)

  - Additions to the YANG model:
     flow exporter * / mtu
     flow record * / collect ipv4 tos
     flow record * / collect ipv4 ttl minimum
     flow record * / collect ipv4 ttl maximum
     flow record * / total-length
     flow record * / total-length minimum
     flow record * / total-length maximum
    (CISCOIOS-2101 / RT44092)

  - Updated NEDCOM to version 1.170.
    (CISCOIOS-2096)

Corrections:

  - Fixed order dependency problems between:
     ip sla *
     ip sla reaction-configuration *
    (CISCOIOS-2104 / RT44119)

  - API CHANGE:
     cable rpd * / rpd-ds * base-power
      type changed from uint8 to decimal64
    (CISCOIOS-2103 / RT44100)

  - Fixed order dependency, correct order is:
     segment-routing traffic-eng / policy * / candidate-paths / preference * / no explicit segment-list *
     segment-routing traffic-eng / no segment-list name *
    (CISCOIOS-2100 / RT44078)


cisco-ios v6.66.6 [2020-10-17]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Fixed order dependency, correct order is:
     no aaa auth*
     no aaa group server tacacs+ *
    (CISCOIOS-2099 / RT44074)


cisco-ios v6.66.5 [2020-10-16]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Improved 'auto if-address-delete-patch' ned-setting;
     inject interface address delete with interface delete
     create dynamic ned-diff rule with interface vrf delete
    (CISCOIOS-2097 / RT44037)


cisco-ios v6.66.4 [2020-10-15]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Fixed null Exception with new ned-setting 'api pretty-line-vty-format' when
     mixing delete and modify of vty lines.
    (CISCOIOS-2098 / RT44060)


cisco-ios v6.66.3 [2020-10-14]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added new ned-setting 'api pretty-line-vty-format' to format commit for
     pretty print in line vty 'show run' on device. More specifically, it will
     extract identical line vty passwords and commit in a single range to avoid
     the device encrypting them with different IV's. Default false.
    (CISCOIOS-2095 / RT44017)

  - Switch method to retrieve interface ipv4 address from 'show run interface'
     to 'show interface' in order to support DHCP addresses in live-status
     if:interfaces-state.
    (CISCOIOS-2093 / RT44007)

Corrections:

  - Added missing default value 5 in 'ip sla reaction-configuration * consecutive <number>'
    (CISCOIOS-2094 / RT44010)


cisco-ios v6.66.2 [2020-10-09]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / tunnel mpls traffic-eng forwarding-adjacency
    (CISCOIOS-2092 / RT43962)

  - Trim meta-data maapi-encrypted path to enhance compression with passwords
    (CISCOIOS-2090 / RT43945)

  - Additions to the YANG model:
     aaa common-criteria policy *
     aaa common-criteria policy * / min-length
     aaa common-criteria policy * / max-length
     aaa common-criteria policy * / char-changes
     username * common-criteria-policy
    (CISCOIOS-2089 / RT43948)

  - Additions to the YANG model:
     template * / preferred-path segment-routing traffic-eng policy
    (CISCOIOS-2083 / RT43902)

Corrections:

  - Fixed order dependencies when deleting ip sla with source-interface
     sub-mode config.
    (CISCOIOS-2087 / RT43938)

  - Fixed order dependency, correct order is:
     interface * / service instance * / no xconnect *
     l2 vfi * / neighbor *
    (CISCOIOS-2086 / RT43932)


cisco-ios v6.66.1 [2020-10-02]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     authentication logging verbose
     cisp enable
     mab request format attribute 32 vlan access-vlan
    (CISCOIOS-2082 / RT43887)

  - Fixed erroneous limitation in ipv6-address type in ietf-inet-types.yang by
     copying the typedef and editing it locally in the NED in order to allow
     ipv6 address with %<interface name> in router bgp ipv6 neighbor lists.
     Required new NEDCOM 1.168.
    (CISCOIOS-2081 / RT43879)

  - Rewrote range-list-syntax extension to standalone code in order to support
     compressing of lists not accepting resulting range as list key.
    (CISCOIOS-2080)

  - Compress line vty config with 'api expanded-line-vty-format' ned-setting
     when sending data to device.
    (CISCOIOS-2079 / RT43863)

  - Additions to the YANG model:
     segment-routing traffic-eng / on-demand color * / authorize restrict
     segment-routing traffic-eng / on-demand color * / authorize restrict / ipv4 prefix-list
    (CISCOIOS-2078 / RT43860)

  - Updated NEDCOM to version 1.168.

Corrections:


cisco-ios v6.66 [2020-09-28]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added no-name-on-delete on ntp server * <prefer> leaf to support delete
     on IOS 3845 [version 12.4(15)T7] device.
    (CISCOIOS-2076 / RT43825)

  - Additions to the YANG model:
     interface * / authentication event server dead action reinitialize vlan
    (CISCOIOS-2075 / RT43828)

  - Additions to the YANG model:
     aaa authentication dot1x * <group2/group> <group3/group>
     aaa authorization auth-proxy * <group3/group>
     aaa accounting system * <group3/group>
     license smart transport callhome
     no power supply autoLC shutdown
     power supply autoLC priority *
    (CISCOIOS-2074 / RT43827)

  - Additions to the YANG model:
     controller MediaType * / mode e1|t1
     controller * / no snmp trap link-status
     interface * / ip ospf shutdown
    (CISCOIOS-2072 / RT43786)

  - Added support for 'cisco-ios developer simulate-show' with live-status exec any
     and config exec actions. Report error with missing|bad file.
    (CISCOIOS-2071 / RT43773)

  - Added support for 'remove' keyword in
     interface * / ipv6 nd inspection vlan *
      required cli-replace-all keyword to be added
    (CISCOIOS-2070 / RT43773)

  - Added support for treating all 'snmp-server enable traps' as one 'all-traps'
    leaf i.e. 'snmp-server enable all-traps' instead of individual ones in a list.
    Enable by setting 'api snmp-server-enable-all-traps' ned-setting to a non-zero
    value, representing two different variants:
     (1) set to a positive value for minimum amount of traps on device
     (2) set to a negative value for maximum missing, i.e. -1 = no missing traps
    Hence, if the above condition is met, the all-traps leaf is set in show().
    (CISCOIOS-1983 / RT42702)

Corrections:

  - API CHANGE:
     power redundancy-mode
      redesigned to support N+N|1 mode and ordered slot list
    (CISCOIOS-2074 / RT43827)

  - Fixed interface / service instance order dependency, correct order is:
     no xconnect *
     bridge-domain
    (CISCOIOS-2072 / RT43786)


cisco-ios v6.65 [2020-09-18]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     netconf-yang feature candidate-datastore
    (CISCOIOS-2067)

  - Additions to the YANG model:
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints / segments / algorithm
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints / segments / type
    (CISCOIOS-2065 / RT43699)

  - Additions to the YANG model:
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / description
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / explicit
    (CISCOIOS-2063 / RT43687)

  - Updated NEDCOM to version 1.165.

Corrections:

  - Fixed order dependency problem, correct order is:
     interface TenGigabitEthernet * / no service instance *
     interface Port-channel * / service-instance
    (CISCOIOS-2069 / RT43729)

  - API CHANGE:
     l2vpn xconnect context * / member *
      service-instance leaf added to member-list key to support multiple entries
      non service-instance member moved to new interface-list
      removed interface specific 'pseudowire' list, use the new interface-list
    (CISCOIOS-2068 / RT43731)

  - API CHANGE:
     ip domain list vrf *
      added 'domain-name' to list keys in order to support multiple domain names.
    (CISCOIOS-2064 / RT43700)


cisco-ios v6.64.4 [2020-09-13]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Do not redeploy locking config if also deleted in the same transaction.
    (CISCOIOS-2060 / RT43656)

Corrections:

  - Replace line * / activation-character with line * / activation-character 13
    (CISCOIOS-2061 / RT43653)


cisco-ios v6.64.3 [2020-09-07]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     route-map * / set extcommunity color
    (CISCOIOS-2058 / RT43596)

  - Updated NEDCOM to version 1.162.
    (CISCOIOS-2059)

Corrections:

  - Fixed problem reading config with when-statements in showPartial causing
     abort to fail in restoring previous device config. Required new nedcom.
    (CISCOIOS-2057 / RT43599)

  - Fixed broken 'event manager applet * / action * regexp' string handling
    (CISCOIOS-2037 / RT43368)


cisco-ios v6.64.2 [2020-09-04]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Trim empty lines before calculating hash transaction-id.
    Inject "! sort begin|end" even if log-verbose is false.
    (CISCOIOS-2055)

  - Updated NEDCOM to version 1.161 for traceDev(). Raised access-list tracing level.

Corrections:

  - Fixed problem updating locks if value was modified resulting in bad delete.
    (CISCOIOS-2043 / RT43156)


cisco-ios v6.64.1 [2020-08-27]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     segment-routing traffic-eng / policy * / candidate-paths / preference * / dynamic / metric / margin / absolute
    (CISCOIOS-2052 / RT43492)

  - Additions to the YANG model:
     snmp-server host * vrf * with 'api new-snmp-server-host true' ned-setting
    (CISCOIOS-2050 / RT43483)

  - Additions to the YANG model:
     segment-routing traffic-eng / segment-list name *
     segment-routing traffic-eng / segment-list name * / index * mpls
    (CISCOIOS-2049 / RT43449)

  - Additions to the YANG model:
     ephone-dn * / number * secondary
     ephone-dn * / number * no-req primary|both
    (CISCOIOS-2048 / RT43456)

  - Reimplementation of range-list-syntax annotation to use cli:extension.
    (CISCOIOS-2047)

  - Added section 14 in README on how to report problems or feature requests.

  - Updated NEDCOM to version 1.160 for ned-diff when-lower|when-higher support

Corrections:


cisco-ios v6.64 [2020-08-20]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - API CHANGE:
     Deprecated 'config-only' option in 'read replace-config' ned-setting due
      to performance optimization CISCOIOS-2038, see below.
    (CISCOIOS-2044)

  - Reviewed and cleaned log and trace commands.
    (CISCOIOS-2041)

  - Additions to the YANG model:
     ephone-dn2   * / call-forward busy primary|secondary dialplan-pattern
    (CISCOIOS-2040 / RT43404)

  - Performance optimization:
     Deprecated 'cisco-ios read transaction-id-method config-hash-cached'.
     Both config-hash and config-hash-cached methods now reuse transaction-id
     from last show (sync-from or compare-config) if available.
    (CISCOIOS-2038)

  - Set police format to cirmode-bpsflat for models containing "WS-C49", e.g.
     Catalyst 4500 L3 Switch.
    (CISCOIOS-2036 / RT43362)

  - Additions to the YANG model:
     interface * / macsec network-link
    (CISCOIOS-2035 / RT43343)

  - Updated NEDCOM to version 1.159 to include below fix:
     Optimized NedDiff by only calling resetReadTimeout() every 10000 searched entry


Corrections:

  - Ignore 'write apply-reboot-timer' with NETSIM.
    (CISCOIOS-2041)

  - Fixed order dependency problem, correct order is:
     no cable modem remote-query src-ip
     no cable modem remote-query <gather>
    (CISCOIOS-2039 / RT43389)

  - Fixed problems (re)configuring 'event manager applet * / action * regexp'
    (CISCOIOS-2037 / RT43368)


cisco-ios v6.63 [2020-08-13]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip msdp timer
    (CISCOIOS-2032 / RT43329)

  - Additions to the YANG model:
     cable rphy ccap-core-event *
    (CISCOIOS-2028 / RT43276)

  - Additions to the YANG model:
     ephone-dn * / caller-id block
     ephone-dn * / conference
    (CISCOIOS-2026 / RT43221)

  - Additions to the YANG model:
     ephone * / device-security-mode
     ephone * / description
     ephone * / button *
     ephone * / pin
     ephone * / conference drop-mode local
    (CISCOIOS-2025 / RT43220)

  - Additions to the YANG model:
     sip-ua / no remote-party-id
     sip-ua / retry invite
     sip-ua / timers connect
     sip-ua / mwi-server
     sip-ua / sip-server
     sip-ua / presence enable
    (CISCOIOS-2024 / RT43205)

  - Additions to the YANG model:
     card type t3 *
     dial-peer voice * / call-block translation-profile incoming
     dial-peer voice * / call-block disconnect-cause incoming
     dial-peer voice * / session server-group
     dial-peer voice * / destination e164-pattern-map
     dial-peer voice * / voice-class sip options-keepalive
     dial-peer voice * / dtmf-relay *
     dial-peer voice * / no sip-register
     dial-peer voice * voip / incoming uri via
     dial-peer voice * voip / incoming uri from
    (CISCOIOS-2023 / RT43201)

  - Additions to the YANG model:
     segment-routing traffic-eng / interface * / metric
     segment-routing traffic-eng / interface * / affinity / name *
     segment-routing traffic-eng / logging policy status
    (CISCOIOS-2022 / RT43200)

  - Additions to the YANG model:
     ephone-dn * / ephone-dn-template
     ephone-dn * / corlist outgoing
     ephone-dn * / translation-profile
     ephone-dn-template *
     ephone-dn-template * / description
     ephone-dn-template * / allow
     ephone-dn-template * / caller-id
     ephone-dn-template * / call-forward
     ephone-dn-template * / conference
     ephone-dn-template * / corlist
     ephone-dn-template * / hold-alert
     ephone-dn-template * / translation-profile
     presence
     presence / presence call-list
     presence / max-subscription
     presence / watcher all
     presence / allow subscribe
     vxml logging-tag
    (CISCOIOS-2021 / RT43198)

- Additions to the YANG model:
     dspfarm profile *
     dspfarm profile * / description
     dspfarm profile * / codec *
     dspfarm profile * / maximum conference-participants
     dspfarm profile * / maximum sessions
     dspfarm profile * / maximum sessions hardware
     dspfarm profile * / maximum sessions software
     dspfarm profile * / conference-join custom-cptone
     dspfarm profile * / conference-leave custom-cptone
     dspfarm profile * / associate application
     dspfarm profile * / shutdown
    (CISCOIOS-2020 / RT43197)

  - Additions to the YANG model:
     call-manager-fallback / max-conferences
     call-manager-fallback / transfer-system
     call-manager-fallback / limit-dn *
     call-manager-fallback / timeouts interdigit
     call-manager-fallback / timeouts ringing
     call-manager-fallback / max-ephones
     call-manager-fallback / max-dn
     call-manager-fallback / transfer-pattern *
     call-manager-fallback / keepalive
     call-manager-fallback / translate calling
     call-manager-fallback / translate called
     call-manager-fallback / call-forward pattern *
     call-manager-fallback / call-forward system redirecting-expanded
     call-manager-fallback / call-forward busy
     call-manager-fallback / call-forward noan
     call-manager-fallback / multicast
     call-manager-fallback / time-zone
    (CISCOIOS-2019 / RT43196)

  - Additions to the YANG model:
     voice class uri * sip|tel
     voice class uri * / host *
     voice class e164-pattern-map *
     voice class e164-pattern-map * / description
     voice class e164-pattern-map * / e164 *
     voice class server-group *
     voice class server-group * / ipv4 *
     voice class server-group * / description
     voice register global
     voice register global / mode
     voice register global / source-address
     voice register global / allow-hash-in-dn
     voice register global / timeouts interdigit
     voice register global / system message
     voice register global / max-dn
     voice register global / max-pool
     voice register global / tftp-path
     voice register pool *
     voice register pool * / call-forward b2bua
     voice register pool * / id network
     voice register pool * / no vad
    (CISCOIOS-2018 / RT43195)

  - Fixed indentation and support for $9$ in maapi-encrypt meta-data inject

Corrections:

  - Added remove-before-change to:
     cable modem remote-query
    (CISCOIOS-2034 / RT43333)

  - API CHANGE:
     telephony-service / max-ephones
      changed from type uint8 to type uint16 to support higher values
    (CISCOIOS-2025 / RT43220)

  - API CHANGE:
     ephone-dn * / conference
      changed from container/container to leaf to support new value + change
    (CISCOIOS-2026 / RT43221)

  - API CHANGE:
     dial-peer voice * / dtmf-relay
      changed from container/leaf to ordered-by user flat leaf-list to support
       options ordering.
    (CISCOIOS-2023 / RT43201)


cisco-ios v6.62 [2020-08-10]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added new ned-setting 'developer trace-timestamp' to add timestamps from
     NED instance in trace messages for debug purpose.
    (CISCOIOS-2031)

  - Added support for new NSO method setProvisionalTransId() to set transaction-id
     when retrieving full device configuration, i.e. in show(). Disable by setting
     new ned-setting read/transaction-id-provisional to false. The new default
     behaviour will optimize performance for config-hash* transaction-id methods.
    (CISCOIOS-2029)

  - Additions to the YANG model:
     ephone-template * / busy-trigger-per-button
     ephone-template * / max-calls-per-button
     flow record * / match ipv6 precedence
     telephony-service / mode esrst
     telephony-service / srst mode auto-provision
     telephony-service / srst ephone template
     telephony-service / srst dn template
     telephony-service / srst dn line-mode
     telephony-service / ip source-address
     voice service voip / ip address trusted list / ipv4 *
     voice service voip / mode border-element
     voice-port * / translation-profile outgoing
    (CISCOIOS-2017 / RT43194)

  - Additions to the YANG model:
     segment-routing traffic-eng / policy * / binding-sid mpls
     segment-routing traffic-eng / policy * / candidate-paths
     segment-routing traffic-eng / policy * / candidate-paths / preference *
    (CISCOIOS-2016)

  - Additions to the YANG model:
     segment-routing traffic-eng / on-demand color *
     segment-routing traffic-eng / on-demand color * / authorize
     segment-routing traffic-eng / on-demand color * / candidate-paths
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference *
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints / bandwidth
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints / segments / dataplane mpls
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints / affinity / *
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints / affinity / * / name *
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints / association-group / identifier
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints / association-group / disjointness type
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / constraints / association-group / source
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / dynamic
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / dynamic / pcep
     segment-routing traffic-eng / on-demand color * / candidate-paths / preference * / dynamic / metric / type
    (CISCOIOS-2015)

  - Additions to the YANG model:
     router bgp * / address-family * / snmp context
    (CISCOIOS-2014 / RT43174)

  - Added support for rebuilding NED with device password type changed from
     type string to type tailf:aes-cfb-128-encrypted-string.
     Rebuild src with NEDCOM_SECRET_TYPE="tailf:aes-cfb-128-encrypted-string".
    (CISCOIOS-1275 / RT36144, CISCOIOS-1932 / RT42093)

  - Updated NEDCOM to version 1.158.

Corrections:

  - API CHANGE:
     voice service voip / ip address trusted list
      corrected from leaf to mode presence container
    (CISCOIOS-2017 / RT43194)


cisco-ios v6.61.2 [2020-07-31]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / tunnel bandwidth receive
     interface * / tunnel bandwidth transmit
    (CISCOIOS-2011 / RT43165)

  - Additions to the YANG model:
     interface * / dialer load-threshold
    (CISCOIOS-2010 / RT43145)

Corrections:

  - Added explicit exit in:
     service-template *
    (CISCOIOS-2013 / RT43175)


cisco-ios v6.61.1 [2020-07-24]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Updated NEDCOM to version 1.149 fixing maapi-encrypted meta-data indentation
    (CISCOIOS-2008)

Corrections:

  - Fixed problem with NETSIM and 'ip msdp peer * description' config
    (CISCOIOS-2009 / RT43104)


cisco-ios v6.61 [2020-07-17]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip dhcp snooping database write-delay
    (CISCOIOS-2003 / RT42987)

  - Additions to the YANG model:
     interface * / no mpls ldp igp autoconfig
    (CISCOIOS-2001 / RT42959)

  - Added 'auto interface-range-write' ned-setting to enable use of
     'interface range' config command when modifying multiple existing
     interfaces with the same sub-mode config.
    Notice: for some obscure reason IOS does not allow service instance
     to be modified with interface range command, hence interfaces with
     such config modifications are excluded from this feature.
    (CISCOIOS-1996 / RT42849)

Corrections:

  - Only read police format (tailfned police) from cdb for NETSIM devices.
    (CISCOIOS-2006 / RT43005)

  - API CHANGE:
     Changed the auto police format for CAT65xx devices:
      regex "(C65(?:0[3469]|13))"  -  "cirflat" => "cirmode"
    (CISCOIOS-1988 / RT42762)

  - Lowered min range on 'policy-map * / class * / police * bc <value>'
     from 1000 to 1.
    (CISCOIOS-1988 / RT42762)

  - Fixed order dependency problem, correct order is:
     policy-map * / class * / police * / no exceed-action *
     policy-map * / class * / police * / exceed-action *
    (CISCOIOS-1988 / RT42762)

  - Fixed order dependency problem, correct order is:
     policy-map * / class * / police * / no violate-action *
     policy-map * / class * / police * / violate-action *
    (CISCOIOS-1988 / RT42762)

  - Fixed problem reading 'police cir [percent]' sub-mode config (e.g. priority)
    (CISCOIOS-1988 / RT42762)


cisco-ios v6.60.1 [2020-07-14]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     aaa authentication banner
    (CISCOIOS-1999 / RT42867)

  - Updated NEDCOM to version 1.147 improving TELNET connect to use the
     configured device connect-timeout value (as with SSH).
    (CISCOIOS-1998 / RT42677)

  - Additions to the YANG model:
     ip nbar custom * udp *
     ip nbar custom * tcp *
    (CISCOIOS-1997 / RT42851)

  - Started using the new cli:ned-diff-xxx extensions in YANG.
    (CISCOIOS-1936)

Corrections:

  - Added reset of read timeout in live-status show handler, solving a potential
     timeout problem with retrieving large lists.


cisco-ios v6.60 [2020-07-03]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     class-map * / match vlan inner *
     flow record * / collect transport icmp ipv4 code
     flow record * / collect transport icmp ipv4 type
     flow record * / collect routing source|destination as peer|4-octet
     interface * / logging event spanning-tree
     ip http timeout-policy linger
    (CISCOIOS-1994 / RT42824)

  - Additions to the YANG model:
     track * / threshold metric
    (CISCOIOS-1991 / RT42805)

  - Additions to the YANG model:
     aaa group server radius * / server-private * probe-on
    (CISCOIOS-1989 / RT42769)

  - Additions to the YANG model:
     platform ipsec reassemble transit
    (CISCOIOS-1987 / RT42755)

Corrections:

  - Fixed order dependency problem, correct order is:
     ethernet cfm domain * / service * vlan
     ethernet evc * / oam protocol cfm svlan
    (CISCOIOS-1990 / RT42787)


cisco-ios v6.59.1 [2020-06-29]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip route static bfd * log
     router bgp * / address-family * / table-map <name> filter
    (CISCOIOS-1985 / RT42718)

  - Additions to the YANG model:
     vlan configuration * / device-tracking
    (CISCOIOS-1982 / RT42678)

  - Additions to the YANG model:
     ip sla * / control timeout
     ip sla * / control retry
    (CISCOIOS-1981)

Corrections:


cisco-ios v6.59 [2020-06-18]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added new static warning exception (matches lower and upper-case):
     "police .+ is adjusted to \\S+ to fit the interface supported range"
    (CISCOIOS-1980 / RT42555)

  - Additions to the YANG model:
     aaa attribute list * / attribute type *
    (CISCOIOS-1979 / RT42550)

  - Improved policy-map * / class * pre-inject fix in v6.55 by moving
     create before delete in some cases to avoid empty policy-map.
    (CISCOIOS-1978 / RT42537)

Corrections:


cisco-ios v6.58 [2020-06-11]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     router isis * / microloop avoidance rib-update-delay
    (CISCOIOS-1976 / RT42531)

  - Added local isAlive() method making it possible to set a developer ned-setting
     (developer fail is-alive <value>) to trigger isAlive returning false, used
     for debugging NSO.
    (CISCOIOS-1974)

  - Added C3560 to api police-format auto-detect code, defaulting to 'numflat'.
    Improved bpsflat mode (with e.g. C4500) 'police <bps> bps <byte> byte ACTIONS' to
     also support kbps|mbps|gbps and kbyte|mbyte|gbyte
    (CISCOIOS-1972)

  - Additions to the YANG model:
     aaa group server tacacs+ * / ipv6 vrf forwarding
     aaa group server tacacs+ * / ipv6 tacacs source-interface
     line * / ipv6 access-class * vrfname *
    (CISCOIOS-1971 / RT42432)

  - Additions to the YANG model:
     device-tracking tracking auto-source
    (CISCOIOS-1970 / RT42434)

  - Additions to the YANG model:
     crypto map * ipsec-isakmp / set peer * default (note: use 'set peer-default', NED will transform)
     interface * / authentication event fail action authorize vlan
     interface * / frame-relay container route *
     interface * / l2proto-forward tagged
     interface * / ip summary-address eigrp * leak-map
     interface * / pagp rate
     interface * / qos dscp
     interface * / tag-switching ip
     interface * / tag-switching mtu
     interface * / tx-ring-limit
     interface * / tx-queue-limit
     interface ATM* / pvc <handle> *
     ip msdp rpf rfc3618
     route-map * / set ip address prefix-list
     router bgp * / neighbor * advertise additional-paths best
     router eigrp * / redistribute ospf * [WARNING DEPRECATED]
     router eigrp * / bfd interface *
     router eigrp * / nsf
     router eigrp * / timers nsf converge
    (CISCOIOS-1969)

  - Detect device reboot when sending 'controller-mode' live-status command.
    (CISCOIOS-1953 / RT42291)

  - Remove and redeploy 'ip sla ethernet-monitor *' if type is replaced.
    Added 'max-elements 1' on type list.
    (CISCOIOS-1938 / RT42174)

Corrections:

  - API CHANGE:
     policy-map * / class * / queue-limit percent <value>
     policy-map * / class * / queue-limit dscp * <value>
      Changed YANG model by removing choice and embedding <value> in container
      in order to be able to remove tailf:cli-remove-before-change annotation
      which stopped value modification while policy used by interface.
    (CISCOIOS-1973 / RT42501)

  - Exclude 'ip prefix-list * description' from compressed delete since one
     has to delete description entry explicitly.


cisco-ios v6.57 [2020-06-04]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / ipv6 mode host unicast [WARNING DEPRECATED]
     router bgp * / address-family * / bgp additional-paths select best
     policy-map * / class * / queue-limit dscp * <value>
     policy-map * / class * / set mpls experimental topmost qos-group
    (CISCOIOS-1968)

  - Replace 'set mpls exp topmost' with 'set mpls experimental topmost' when
     reading policy-map * / class config from device.
    (CISCOIOS-1968)

  - Flatten out policy-map * / class * / police cir actions with cirflat mode
    (CISCOIOS-1968)

  - Additions to the YANG model:
     card type e1 *
     controller * / forward-alarm ais
     controller * / forward-alarm rai
     controller * / service-type
     controller SONET * / aps authentication
     controller SONET * / aps group
     controller SONET * / aps hspw-icrm-grp
     controller SONET * / aps timers
     crypto isakmp client configuration group * / dns
     crypto isakmp client configuration group * / max-users
     crypto isakmp client configuration group * / save-password
     crypto dynamic-map * / reverse-route
     crypto dynamic-map * / set pfs
     crypto dynamic-map * / set security-association
     firewall autostate
     interface * / auto qos trust
     interface * / glbp * authentication text
     interface * / glbp * load-balancing
     interface * / glbp * timers
     interface * / glbp * weighting track *
     interface * / ip load-sharing
     interface * / ip multicast rate-limit in [WARNING DEPRECATED]
     interface * / ip sticky-arp
     interface * / switchport port-security mac-address sticky
     interface CEM* / cem * / idle-pattern
     map-class frame-relay * / frame-relay bc
     map-class frame-relay * / frame-relay be
     map-class frame-relay * / fair-queue
     policy-map * / class * / shape peak
     privilege * level *
     privilege * all level *
     pseudowire-class * / ip tos value
    (CISCOIOS-1967)

  - Replace 'router bgp * / neighbor *' remove-private-AS with remove-private-as
     when reading config from device to support older IOS versions.
    (CISCOIOS-1967)

  - Additions to the YANG model:
     router ospf * / redistribute * vrf
    (CISCOIOS-1965)

  - Added 'api acl-resequence-range" ned-setting to allow a user to change the
     default resequence start and increment interval from its default 1000.
     Used with 'api access-list-resequence' ned-setting only.
    (CISCOIOS-1963 / RT42347)

Corrections:

  - Corrected the YANG model:
     policy-map * / class * / queue-limit
      moved queue-limit dscp into queue-limit-value to have both at same time
    (CISCOIOS-1968)

  - API CHANGE:
     interface * / switchport port-security mac-address
      changed from container to list in order to support multiple entries
    (CISCOIOS-1967)

  - Fixed order dependency problem, correct order is:
     ethernet cfm domain * / service * vlan
     interface * / ethernet cfm mep * vlan
    (CISCOIOS-1966 / RT42371)


cisco-ios v6.56.1 [2020-06-01]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     route-map * / set ip default vrf * next-hop *
    (CISCOIOS-1961)

  - Updated NEDCOM to version 1.143.
     Added support for Maapi decryption of $9$ passwords in some secrets.

Corrections:

  - Fixed order dependency problems, added the following NedDiff rule:
     "controller .+", "^rf-chan \\d+<LF> ofdm channel-profile :: last"
    (CISCOIOS-1939 / RT42202)

  - Fixed NSO crash due to negative device timeout values.
    (CISCOIOS-1764)


cisco-ios v6.56 [2020-05-28]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip dhcp pool * / network * secondary
     ip dhcp pool * / network * secondary / override default-router
    (CISCOIOS-1960 / RT42320)

  - Additions to the YANG model:
     class-map * / match security-group source tag *
     class-map * / match security-group destination tag *
    (CISCOIOS-1959 / RT42305)

  - Additions to the YANG model:
     time-range *
     time-range * / absolute
    (CISCOIOS-1958)

  - Improved 'interface * / service instance * ethernet / encapsulation dot1q'
     modifications by using add|remove keywords instead of absolute change.
    (CISCOIOS-1955 / RT42294)

  - Additions to the YANG model:
     ip dhcp pool * / client-name
     interface * / tunnel mode ipip
    (CISCOIOS-1956)

  - Additions to the YANG model:
     object-group ip address *
     object-group ip address * / host-info *
     object-group ip address * / * *
     object-group ip port *
     object-group ip port * / *
    (CISCOIOS-1954)

  - Switched to use cli extension instead of meta-data when updating config in:
     cable video / logical-edge-device * / protocol gqi [/active]
     pm-agent [/shutdown]
     virtual-service * [/activate]
    (CISCOIOS-1952)

  - Improved 'crypto tls-tunnel' modifications
    Send initial 'no shutdown' since entry created with shutdown set
    (CISCOIOS-1951)

  - Improved 'crypto pki server' modifications
    (CISCOIOS-1950)

  - Additions to the YANG model:
     interface * / ip igmp limit
    (CISCOIOS-1948 / RT42260)

  - Additions to the YANG model:
     ip pim accept-rp *
     ip pim accept-rp auto-rp
     ip msdp password peer *
     ip msdp sa-limit *
    (CISCOIOS-1947 / RT42259)

  - Additions to the YANG model:
     router isis * / vrf
    (CISCOIOS-1946 / RT42250)

  - Additions to the YANG model:
     router eigrp * / address-family ipv4 / maximum-prefix
    (CISCOIOS-1945 / RT42249)

  - Additions to the YANG model:
     ip rsvp signalling rate-limit
    (CISCOIOS-1944 / RT42248)

  - Switched to use cli extension instead of meta-data in route-target redeploy:
     ip vrf * / rd
    (CISCOIOS-1941)

Corrections:

  - Only call worker.setTracer if trace is enabled in archive code.
    (CISCOIOS-1962)

  - API change:
     interface * / service instance * ethernet / encapsulation dot1q
      removed 'any' enumeration config option since a macro
    (CISCOIOS-1955 / RT42294)

  - Added cli-no-value-on-delete on 'cable service class * tos-overwrite'
    (CISCOIOS-1949 / RT42261)


cisco-ios v6.55.2 [2020-05-22]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Encapsulation dot1q improvements:
     interface * / service instance * / encapsulation dot1q
      Split long line (>250 characters) into 16 entries per line when sending.
      Append 'encapsulation dot1q add' entries when reading.
    (CISCOIOS-1943 / RT42235)


cisco-ios v6.55.1 [2020-05-20]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Switched to use cli extension instead of meta-data:
     service-insertion appnav-controller-group * / appnav-controller *
    (CISCOIOS-1940)

  - Added support for temporarily unlocking 'ip sla ethernet-monitor *'
     referenced by 'ip sla ethernet-monitor schedule'
    Also ignore 'ip sla ethernet-monitor * / type' command to re-enter mode
    (CISCOIOS-1938 / RT42174)

Corrections:


cisco-ios v6.55 [2020-05-14]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     boot network ftp|mop|rcp|tftp *
    (CISCOIOS-1934 / RT42119)

  - Additions to the YANG model:
     platform cos-mark protocol * cos-value
    (CISCOIOS-1933 / RT42122)

  - Improved policy-map * / class * / priority|bandwidth|police cir changes.
     Old value is no longer removed if modified in order to be able to update
     value even if 'locked' by queue-buffers ratio class config.
    (CISCOIOS-1930 / RT42054)

  - Reorder instead of inject policy-map * / class * / priority|bandwidth values
    To avoid class map order diff, a shallow policy-map containing only class
    create and delete is pre-injected.
    (CISCOIOS-1930 / RT42054)

  - Updated NEDCOM to version 1.140.

Corrections:

  - API CHANGE:
     ip sla ethernet-monitor reaction-configuration * react
      Remodelled after 'ip sla reaction-configuration' to support all options
    (CISCOIOS-1931)

  - Fixed order dependency problem, correct order is:
     policy-map * / class * / no queue-buffers ratio
     policy-map * / class * / no priority
     policy-map * / class * / no shape
    (CISCOIOS-1929 / RT42051)


cisco-ios v6.54 [2020-05-07]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     mpls traffic-eng auto-tunnel p2p
     mpls traffic-eng auto-tunnel p2p config unnumbered-interface
     mpls traffic-eng auto-tunnel p2p tunnel-num
     mpls traffic-eng lsp attributes * / path-selection metric
     mpls traffic-eng lsp attributes * / pce
     mpls traffic-eng lsp attributes * / priority
     mpls traffic-eng pcc peer *
     mpls traffic-eng pcc report-all
     router isis * / distribute link-state
     router isis * / fast-reroute tie-break * srlg-disjoint
     router isis * / segment-routing prefix-sid-map advertise-local
     template * / preferred-path segment-routing traffic-eng attribute-set
    (CISCOIOS-1928 / RT41975)

  - Ignore command echos in
     mpls traffic-eng lsp attributes *
    (CISCOIOS-1928 / RT41975)

  - Additions to the YANG model:
     cable flap-list miss-threshold
     cable multicast mdf-disable DSG
     cable profile mac-domain * / cable cm-status enable *
     cable upstream ofdma-prof-mgmt exempt-mslot-pct
     controller Integrated-Cable * / power-tilt linear
     interface * / no cable proxy-arp
     interface Cable* / cable cm-status enable *
     interface Cable* / cable upstream max-channel-power-offset
     interface Port-channel* / load-balancing
     ipdr exporter keepalive
     login delay
    (CISCOIOS-1927 / RTRT41997)

  - Additions to the YANG model:
     cable load-balance fixed-primary-channel
     cable us-freq-use-cm-cap
     netconf max-message
    (CISCOIOS-1926 / RT41960)

  - Additions to the YANG model:
     interface Cable* / downstream Downstream-Cable * rf-channel *
    (CISCOIOS-1924 / RT41997)

  - Additions to the YANG model:
     l2 vfi * / neighbor * / mpls control-word
     l2 vfi * / neighbor * / mpls label
    (CISCOIOS-1922 / RT41975)

  - Pre-inject temporary shutdown in 'controller Integrated-Cable * / rf-chan *'
     when modifying contents.
    (CISCOIOS-1920 / RT41960)

  - Updated NEDCOM to version 1.139.

Corrections:

  - Added missing 'tailf:cli-optional-in-sequence' annotation to support
     interface * / ipv6 dhcp relay destination * link-address
    (CISCOIOS-1927 / RTRT41997)

   - Removed 1-7 range on:
      interface Cable* / upstream * us-channel
    (CISCOIOS-1927 / RTRT41997)

  - API CHANGE:
     cable profile mac-domain * / cable dynamic-secret
      remodelled to support mark and lock options
    (CISCOIOS-1926 / RT41960)

  - API CHANGE:
     interface Cable* / downstream Integrated-Cable * rf-channel *
     interface Cable* / downstream Modular-Cable * rf-channel *
     interface Cable* / downstream Cable * rf-channel *
      complete YANG redesign to support changes, multiple lines and upstream
    (CISCOIOS-1924 / RT41997)

  - Fixed order dependency problems between:
     cable downstream ofdm-chan-profile *
     cable downstream ofdm-modulation-profile *
     controller Integrated-Cable * / rf-chan
    (CISCOIOS-1923 / RT41960)

  - API CHANGE:
     controller Integrated-Cable * / rf-chan * / ofdm channel-profile *
      changed from list to container since only 1 entry supported
    (CISCOIOS-1920 / RT41960)

  - Restored output of config context with prepare Exception.
    (added in CISCOIOS-1389)


cisco-ios v6.53.1 [2020-04-30]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     controller SONET * / loopback
     controller SONET * / sts-1 * / loopback
    (CISCOIOS-1918 / RT41946)

  - Additions to the YANG model:
     bridge-domain * / shutdown
    (CISCOIOS-1917 / RT41940)

  - Additions to the YANG model:
     logging userinfo
    (CISCOIOS-1915 / RT41930)

  - Additions to the YANG model:
     monitor session * type erspan-source / source vlan *
    (CISCOIOS-1914 / RT41929)

  - Additions to the YANG model:
     ip http authentication aaa login-authorization
     ip http secure-ciphersuite *
    (CISCOIOS-1913 / RT41928)

  - Additions to the YANG model:
     aaa password restriction
    (CISCOIOS-1912 / RT41927)

  - Additions to the YANG model:
     exception crashinfo maximum-files
     ip flow-capture icmp
     ip igmp snooping querier version
     ip nbar protocol-pack
     mls ip multicast threshold
     mls rp ip route-map
    (CISCOIOS-1911)

  - Additions to the YANG model:
     access-session acl default passthrough
    (CISCOIOS-1910)

  - Additions to the YANG model:
     audit interval
    (CISCOIOS-1909)

  - Exit config mode at login if directly logged into it (e.g. terminal server)
    (CISCOIOS-1908)

  - Strip 'primary' from VTP Operating Mode in 'show vtp status' in order to be
     able to read 'vtp mode' config because 'vtp primary' is not config.
    (CISCOIOS-1897 / RT41743)

  - Additions to the YANG model:
     radius-server authorization permit missing service-Type [WARNING DEPRECATED]
    (CISCOIOS-1896 / RT41741)

Corrections:

  - Added missing cli-compact-syntax annotation on:
     ip msdp peer *
    (CISCOIOS-1916 / RT41937)


cisco-ios v6.53 [2020-04-24]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     line * / ip netmask-format
    (CISCOIOS-1907)

  - Additions to the YANG model:
     audit
     boot bootldr
     event manager applet * / event none
    (CISCOIOS-1904)

  - Additions to the YANG model:
     crypto ikev2 policy-default-disabled
      used to set 'no crypto ikev2 policy default' on device
    (CISCOIOS-1903 / RT41796)

  - Additions to the YANG model:
     no service single-slot-reload-enable
    (CISCOIOS-1902 / RT41746)

  - Additions to the YANG model:
     switch virtual domain * / dual-active detection pagp channel-group *
    (CISCOIOS-1897 / RT41743)

  - Additions to the YANG model:
     aaa accounting jitter maximum
     aaa accounting update * jitter maximum (deprecated)
    (CISCOIOS-1866 / RT41633)

  - Additions to the YANG model:
     interface * / no ipv6 mld snooping
     vlan configuration * / no ipv6 mld snooping
    (CISCOIOS-1817 / RT40999)

  - Updated NEDCOM to version 1.136.

Corrections:


cisco-ios v6.52 [2020-04-17]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     rtr * /
     rtr * / type echo
     rtr * / owner
     rtr * / tag
     rtr * / threshold
     rtr * / frequency
     rtr schedule * start now
     scheduler process-watchdog
     no service image-version efsu
    (CISCOIOS-1902 / RT41746)

  - Additions to the YANG model:
     location admin-tag
     macro global description
    (CISCOIOS-1901 / RT41753)

  - Additions to the YANG model:
     ip slb vserver * / replicate casa
    (CISCOIOS-1900 / RT41750)

  - Additions to the YANG model:
     ip slb vserver * / no advertise
    (CISCOIOS-1899 / RT41333)

  - Additions to the YANG model:
     module provision *
    (CISCOIOS-1898 / RT41744)

  - Additions to the YANG model:
     svclc vlan-group *
     vlan ifdescr detail
     warm-reboot
    (CISCOIOS-1897 / RT41743)

  - Additions to the YANG model:
     port-channel load-balance <value> exclude vlan
     privilege configure level *
     profile *
    (CISCOIOS-1896 / RT41741)

  - Additions to the YANG model:
     ethernet evc * / oam protocol cfm svlan
     ethernet evc * / uni count
    (CISCOIOS-1895 / RT41737)

  - Additions to the YANG model:
     no mac address-table learning vlan *
      Note: use 'mac address-table learning vlan no-list' in NSO
    (CISCOIOS-1894 / RT41730)

  - Additions to the YANG model:
     mac address-table aging-time ? vlan *
    (CISCOIOS-1893 / RT41729)

  - Additions to the YANG model:
     ip tacacs source-interface <name> vrf *
     line * / access-class * vrfname
    (CISCOIOS-1888 / RT41708)

  - Additions to the YANG model:
     platform punt-keepalive settings fatal-count
     platform punt-keepalive settings transmit-interval
     platform punt-keepalive settings warning-count
    (CISCOIOS-1887 / RT41706)

  - Additions to the YANG model:
     mls ip multicast bidir gm-scan-interval
    (CISCOIOS-1883 / RT41701)

  - Additions to the YANG model:
     ip scp remote-username
     issu set rollback-timer
     platform l2vpn hdlc-pass-through
    (CISCOIOS-1882 / RT41676)

  - Additions to the YANG model:
     default-value special-character-bits
     diagnostic monitor interval Module *
    (CISCOIOS-1881 / RT41669)

  - Additions to the YANG model:
     fabric required
     fabric timer
    (CISCOIOS-1878 / RT41664)

  - Transform VLAN to Vlan when reading config line from device:
     interface VLAN
     source VLAN
     trap-source VLAN
    Note: when setting Vlan on device the device will transform to VLAN
    (CISCOIOS-1879 / RT41665, CISCOIOS-1886 / RT41705, CISCOIOS-1892 / RT41726)

  - Additions to the YANG model:
     no ip pim v1-rp-reachability
    (CISCOIOS-1877 / RT41653)

  - Additions to the YANG model:
     ip nat inside source static <local-ip> interface *
    (CISCOIOS-1876 / RT41652)

  - Additions to the YANG model:
     ip msdp redistribute list
    (CISCOIOS-1875 / RT41651)

  - Additions to the YANG model:
     ip mroute * <interface>
    (CISCOIOS-1874 / RT41649)

  - Additions to the YANG model:
     hw-module system max-queue-limit
    (CISCOIOS-1873 / RT41648)

  - Additions to the YANG model:
     ftp-server enable
    (CISCOIOS-1872 / RT41646)

  - Additions to the YANG model:
     ip slb probe * / interval
     ip slb probe * / lookup
     ip slb probe * / port
    (CISCOIOS-1849 / RT41333)

Corrections:

  - API CHANGE:
     port-channel load-balance
      changed from leaf to container to add support for 'exclude'
    (CISCOIOS-1896 / RT41741)

  - API CHANGE:
     monitor session * destination interface * ingress
      changed 'ingress' from container to leaf to support non-vlan ingress.
    (CISCOIOS-1890 / RT41720)

  - API CHANGE:
     mls ip multicast
      removed presence true and 'tailf:cli-display-separated' annotation.
    (CISCOIOS-1883 / RT41701)


cisco-ios v6.51 [2020-04-10]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip flow-export version <version> peer-as|origin-as|bgp-nexthop
    (CISCOIOS-1871 / RT41643)

  - Additions to the YANG model:
     ip flow-egress input-interface
    (CISCOIOS-1870 / RT41642)

  - Additions to the YANG model:
     ip dhcp relay override giaddr link-selection
    (CISCOIOS-1869 / RT41640)

  - Additions to the YANG model:
     ip dhcp-server query lease timeout
    (CISCOIOS-1867 / RT41638)

  - Additions to the YANG model:
     ip igmp profile *
     no ip igmp snooping report-suppression
     ip igmp snooping querier timer expiry
     no ip igmp filter
     no ip igmp snooping vlan * querier
     ip igmp snooping vlan * static * interface *
    (CISCOIOS-1865 / RT41637)

  - Additions to the YANG model:
     credentials
    (CISCOIOS-1864 / RT41636)

  - Additions to the YANG model:
     cluster commander-address
    (CISCOIOS-1863 / RT41635)

  - Additions to the YANG model:
     interface * / authentication event server avlive action reinitialize
    (CISCOIOS-1862 / RT41607)

  - Replaced NedSecrets.java class using meta-data "secret" annotations
     with the new nedcom class NedSecretCliExt.java class using cli
     extensions.
    (CISCOIOS-1473)

  - Updated NEDCOM to version 1.134.

Corrections:

  - API CHANGE:
     ip flow-export version
      changed from enumeration leaf to container due to YANG expansions
    (CISCOIOS-1871 / RT41643)

  - API CHANGE:
     no ip igmp snooping vlan *
      moved 'ip igmp snooping' when-statement to vlan no-list.
    (CISCOIOS-1865 / RT41637)

  - API CHANGE:
     ip igmp ssm-map static *
      Added leaf 'source-address ' to list key to support multiple addresses.
    (CISCOIOS-1861 / RT41595)


cisco-ios v6.50 [2020-04-03]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added support for temporarily unlocking 'policy-map *' referenced by
     'interface * / service-policy *'
    (CISCOIOS-1860 / RT41551)

  - Do not restore newly created ip sla reaction-configuration entry
    (CISCOIOS-1858 / RT41508)

  - Additions to the YANG model:
     policy-map * / class * / account
     policy-map * / class * / police cir * account user-defined
    (CISCOIOS-1857 / RT41503)

  - Additions to the YANG model:
     interface Cable* / cable upstream ranging-poll
     interface Cable* / cable upstream resiliency sf-move *
     interface Cable* / no cable diplexer-band-edge
    (CISCOIOS-1856 / RT41495)

Corrections:

  - API CHANGE:
     policy-map * / class * / shape average
      removed 'target-bit-rate' leaf and moved account config to support
      bit-per-interval even for account and avoid duplicate config.
    (CISCOIOS-1857 / RT41503)

  - Bugfixed delete|modify order dependency problem between:
     line * / ipv6 access-class *
     ipv6 access-list *
    (CISCOIOS-1767 / RT40727)


cisco-ios v6.49 [2020-03-27]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     router bgp * / address-family * / bgp router-id auto-assign
    (CISCOIOS-1852 / RT41420)

  - Added new developer ned-setting 'developer extended-parser-fallback'
     to be able to disable fallback to NSO parser in show().
    (CISCOIOS-1851)

  - Additions to the YANG model:
     line * / refuse-message
     line * / vacant-message
     line * / flow-control NONE
    (CISCOIOS-1850 / RT41353)

  - Additions to the YANG model:
     monitor session * type capture / buffer-size
     monitor session * type capture / filter length
     monitor session * type capture / filter vlan *
     monitor session * type capture / rate-limit
     monitor session * type capture / source vlan
    (CISCOIOS-1848 / RT41306)

  - Additions to the YANG model:
     switch virtual domain * / switch * priority
    (CISCOIOS-1847 / RT41304)

  - Additions to the YANG model:
     interface * / switchport vlan mapping * drop
    (CISCOIOS-1846 / RT41303)

  - Additions to the YANG model:
     interface * / peer ip address forced
     interface * / ppp encrypt mppe
    (CISCOIOS-1845 / RT41301)

  - Additions to the YANG model:
     interface Async* / ignore dcd|cts|dsr
    (CISCOIOS-1802/ RT40918)

  - Updated NEDCOM to version 1.128.

Corrections:

  - Pre-inject delete of interface service-policy if set in the same transaction.
    (CISCOIOS-1855 / RT41476)

  - API CHANGE:
     interface * / ppp authentication
      Remodelled to support three protocols
    (CISCOIOS-1845 / RT41301)


cisco-ios v6.48 [2020-03-20]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added support for " in ip access-list standard remarks
    (CISCOIOS-1843 / RT41385)

  - Additions to the YANG model:
     router bgp * / bgp router-id vrf auto-assign
    (CISCOIOS-1842 / RT41376)

  - Additions to the YANG model:
     controller * / loopback
    (CISCOIOS-1841 / RT41364)

  - Added retry on 'cable rpd * / rpd-ds *', message:
     "Controller \\S+ failed to bind profile"
    (CISCOIOS-1840 / RT41357)

  - Additions to the YANG model:
     controller SONET * / sts-1 * / vtg * t1 * cem-group
     controller SONET * / sts-1 * / vtg * t1 * channel-group * timeslots
     controller SONET * / sts-1 * / vtg * t1 * description
     controller SONET * / sts-1 * / vtg * t1 * framing
     controller SONET * / sts-1 * / vtg * t1 * Loopback Local
    (CISCOIOS-1839 / RT41352)

  - Added 'api ios-common' ned-setting to auto-correct some IOS API discrepancies.
     See README for more details.
    (CISCOIOS-1838 / RT41355)

  - Added support for reading config from trace file in 'live-status exec any
     sync-from-file' action. Config from first 'show running-config' is used.
    (CISCOIOS-1837)

Corrections:

  - Fixed order dependency problems between:
     tacacs-server host *
     tacacs server * / address ipv4
    (CISCOIOS-1844 / RT41391)

  - Fixed order dependency problem, correct order is:
     cable tag *
     cable profile load-balance * / tag *
    (CISCOIOS-1840 / RT41357)

  - API CHANGE:
     class-map * / match input-interface *
      changed from leaf-list to list to support multiple entries and ranges per line
    (CISCOIOS-1836 / RT41335)

  - Fixed interface order dependency problem, correct order is:
     no switchport *
     no ipv6 redirects
    (CISCOIOS-1834 / RT41310)


cisco-ios v6.47 [2020-03-12]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     aaa local authentication default authorization
     crypto ikev2 limit queue sa-init
     crypto ikev2 authorization policy * / ipv6 prefix
     crypto ikev2 authorization policy * / ipv6 pool
     crypto ikev2 authorization policy * / ipv6 dns
     crypto ikev2 authorization policy * / route set remote ipv4 *
     crypto ikev2 authorization policy * / route set remote ipv6 *
     platform ipsec flexvpn-bypass-tcam
    (CISCOIOS-1830 / RT41149)

  - API CHANGE:
     Replaced 'auto cdp-run' ned-setting added in v6.45 with 'auto cdp-read-inject'.
     The new ned-setting will use output of running config and optionally 'show cdp'
     to auto-detect if the device need injection of 'no cdp run' and 'no cdp enable'
     first in interfaces or top-mode, respectively. This will make it possible to
     enable CDP on these type of devices and specified interfaces.
     Enable by specifying regex to match those interfaces which will be affected.
    (CISCOIOS-1829 / RT41119)

Corrections:

  - Fixed interface order dependency problem, correct order is:
     no cdp *
     no cdp enable
    (CISCOIOS-1829 / RT41119)

  - Bugfixed delete|modify order dependency problem between:
     line * / no access-class *
     ip access-list * / no *
    (CISCOIOS-1767 / RT40727)


cisco-ios v6.46 [2020-02-28]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     class-map type multicast-flows * / group * source *
     class-map type multicast-flows * / group * to * source *
     cpu traffic qos cos
     environment temperature-controlled
     exception core-file
     exception dump
     diagnostic monitor Module *
     fax interface-type fax-mail
     firewall module * vlan-group *
     ip audit po max-events
     ip audit notify log
     ip flow-capture ip-id
     ip http port
     ip pim accept-register list
     ip slb probe *
     ip slb serverfarm * / real * / weight
     ip tcp chunk-size
     ip tcp queuemax
     line * / flowcontrol hardware out
     line * / disconnect-character
     line * / modem DTR-active
     line * / no exec-banner
     mls ip multicast egress local
     mls ip multicast met-optimization enable
     mls qos protocol * precedence
     no mls ip slb purge global
     mls rate-limit unicast ip icmp redirect
     mls rate-limit unicast cef glean
     mls rate-limit unicast cef receive
     module clear-config
     monitor session * type capture
     monitor session * type capture / description
     monitor session * type capture / filter access-group
     monitor session * type capture / source interface *
     mpls oam / echo revision
     mta receive maximum-recipients
     rtr key-chain
     tacacs-server attempts
    (CISCOIOS-1828)

  - Additions to the YANG model:
     aaa authorization reverse-access *
     cluster run
     identity policy *
     identity policy * / inactivity-timer
     interface * / unidirectional
     line * / modem Host
     mls rp ip
     qos wireless-default-untrust
     switch virtual domain *
     switch virtual domain * / switch mode virtual
     switch virtual domain * / mac-address
    (CISCOIOS-1826)

  - Improved order dependency between host lists, correct order is:
     no logging host *
     logging *
    (CISCOIOS-1823 / RT41037)

  - Additions to the YANG model:
     exception coredump
     no exception crashinfo
     ip telnet quiet
     snmp-server host * rtr|vlan-membership|vtp|MAC-Notification|cluster|..
    (CISCOIOS-1822)

  - Strip unnecessary config with sync-from:
     no scheduler allocate
    (CISCOIOS-1822)

  - Additions to the YANG model:
     interface GMPLS*
    (CISCOIOS-1821)

  - Additions to the YANG model:
     menu * prompt
     menu * text
     menu * options
    (CISCOIOS-1820)

  - Additions to the YANG model:
     interface * / ip irdp maxadvertinterval
     interface * / ip irdp minadvertinterval
     interface * / ip irdp holdtime
     interface * / ip irdp preference
     interface * / ip pim neighbor-filter
    (CISCOIOS-1819 / RT41016)

  - Additions to the YANG model:
     define interface-range *
     interface * / clns enable
     interface * / mls rp vtp-domain
     interface * / mls rp ip
     interface * / tcam priority
    (CISCOIOS-1818 / RT41012)

  - Additions to the YANG model:
     interface * / ip igmp v3lite
     interface * / ip igmp last-member-query-count
     interface * / ip igmp last-member-query-interval
     interface * / ip igmp snooping fast-leave
     interface * / ip igmp snooping qurier
     interface * / ip igmp snooping mrouter interface
    (CISCOIOS-1816 / RT41000)

  - Optimized sync-from terminal line splitting code by only calling 'show line'
     once per 'show running-config'. Added support for simulating lines.
    (CISCOIOS-1815)

  - Additions to the YANG model:
     class-map type multicast-flows *
     class-map type multicast-flows * / group * to * source *
     ip slb serverfarm *
     ip slb vserver *
     module provision switch *
     module provision switch * / chassis-type
     module provision switch * / slot *
    (CISCOIOS-1814)

  - Added live-status exec any help|usage to show internal commands
    (CISCOIOS-1813)

  - Additions to the YANG model:
     line * / no motd-banner
     line * / terminal-type
     line * / autoselect durin-login
     line * / special-character-bits
     mac-address-table static * vlan *
     no power enable module *
    (CISCOIOS-1812)

  - Split 'line X/A X/C' in multiple individual entries.
    Do not halt line splitting after one line syntax.
    (CISCOIOS-1811)

  - Added new live-status exec any commands for development:
     check-config-trace <trace file>
     check-config-dir <directory>
     check-unique-dir <directory>
    (CISCOIOS-1810)

  - Updated NEDCOM to version 1.116:
     Improved disallow pattern in router bgp neighbor-tag list in order to
     be able to correctly separate between address and peer lists.
    (CISCOIOS-1809)

  - Additions to the YANG model:
     interface * / switch virtual link
    (CISCOIOS-1782 / RT40843)

  - Additions to the YANG model:
     interface * / service-module t1 framing
     interface * / service-module t1 clock source
    (CISCOIOS-1776 / RT40839)

  - Additions to the YANG model:
     interface * / mls l2tpv3 reserve interface *
    (CISCOIOS-1773/ RT40835)

  - Updated NEDCOM to version 1.118.

Corrections:

  - API CHANGE:
     line * / flowcontrol
      changed to container to allow for config expansion
    (CISCOIOS-1828)

  - API CHANGE:
     switch *
      moved provision list into container to allow for switch config expansion
    (CISCOIOS-1826)

  - API CHANGE:
     interface * / switchport port-security mac-address
      corrected type to support <H.H.H> format.
      added remove-before-change to support changing value.
    (CISCOIOS-1825 / RT41041)

  - API CHANGE:
     interface * / ip nhrp registration no-unique
     interface * / ip nhrp map multicast dynamic
      Changed from type empty to type boolean due to IOS inconsistency
    (CISCOIOS-1824)

  - API CHANGE:
     interface * / service-module t1 timeslots
      Modified YANG path due to expansion in top t1 container
    (CISCOIOS-1776 / RT40839)


cisco-ios v6.45 [2020-02-21]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ethernet cfm ais link-status global
     ethernet cfm ais link-status global / disable
     ethernet cfm domain * level / service icc <string> *
     ethernet cfm domain * level / service * / mip autocreate
    (CISCOIOS-1808 / RT40974)

  - Additions to the YANG model:
     router bgp * / neighbor * fall-over bfd multi-hop|single-hop [check-control-plane-failure] [strict-mode]
    (CISCOIOS-1807 / RT40967)

  - Additions to the YANG model:
     bfd map ipv4 *
     bfd-template multi-hop *
    (CISCOIOS-1806 / RT40963)

  - Additions to the YANG model:
     fabric switching-mode allow dcef-only
     modemcap entry *
    (CISCOIOS-1805 / RT40951)

  - Additions to the YANG model:
     policy-map * / class * / set cos cos
     policy-map * / class * / set cos qos-group
    (CISCOIOS-1804 / RT40949

  - Additions to the YANG model:
     interface * / half-duplex
    (CISCOIOS-1803 / RT40941)

  - Additions to the YANG model:
     hw-module slot * ipv6-hbh-r
     hw-module slot * memory test full
    (CISCOIOS-1801 / RT40914)

  - Additions to the YANG model:
     vlan * / tb-vlan1
     vlan * / tb-vlan2
     vlan * / parent
     vlan * / backupcrf
     vlan * / bridge
     vlan * / stp type
     vlan * / said
     vlan * / media
     vlan configuration * / no ip igmp snooping
    (CISCOIOS-1800 / RT40913)

  - Additions to the YANG model:
     monitor session * type local / source vlan *
    (CISCOIOS-1799 / RT40912)

  - Additions to the YANG model:
     ipv6 ospf name-lookup
    (CISCOIOS-1798 / RT40888)

  - Verify address and mask consistency in object-group network.
    Throw NedException if inconsistent, i.e. non-zero address bits.
    (CISCOIOS-1797 / RT40891)

  - Additions to the YANG model:
     ip sla * / dns
     ip sla * / ethernet * num-frames
     ip sla * / ethernet * interval
    (CISCOIOS-1795 / RT40875)

  - Additions to the YANG model:
     bfd fast-timers-on-slow-interface
     redundancy / notification-timer
    (CISCOIOS-1794 / RT40874)

  - Additions to the YANG model:
     l2tp-class * / cookie size
     l2tp-class * / timeout setup
    (CISCOIOS-1793 / RT40871)

  - Additions to the YANG model:
     event manager applet * / event snmp * exit-time
     event manager applet * / event snmp * maxrun
    (CISCOIOS-1792 / RT40870)

  - Additions to the YANG model:
     router eigrp * / variance
    (CISCOIOS-1791 / RT40858)

  - Quote interface name in 'no passive-interface Control Plane Interface'
    (CISCOIOS-1791 / RT40858)

  - Additions to the YANG model:
     router bgp * / address-family * / bgp slow-peer detection
     router bgp * / neighbor * slow-peer split-update-group
    (CISCOIOS-1790 / RT40857)

  - Additions to the YANG model:
     interface * / isis lsp-interval
     interface * / switchport vlan mapping enable
    (CISCOIOS-1789 / RT40855)

  - Additions to the YANG model:
     vrf definition * / address-family ipv4 / import ipv4 unicast map
    (CISCOIOS-1788 / RT40854)

  - Additions to the YANG model:
     route-map * / set level
     route-map * / set ip default next-hop *
    (CISCOIOS-1787 / RT40853)

  - Additions to the YANG model:
     interface * / ip igmp helper-address
     interface * / location civic-location-id
     interface * / switchport vlan mapping dot1q-tunnel *
    (CISCOIOS-1786 / RT40852)

  - Additions to the YANG model:
     interface * / history
     interface * / no ip igmp snooping tcn flood
     interface * / ip igmp explicit-tracking
     interface * / switchport capture
     interface * / switchport capture allowed vlan *
    (CISCOIOS-1785 / RT40851)

  - Additions to the YANG model:
     interface * / no vtp
     interface * / no qos
     interface * / qos vlan-based
    (CISCOIOS-1784 / RT40850)

  - Additions to the YANG model:
     interface * / ip pim bsr-border
     interface * / ip igmp query-interval
     interface * / ospfv3 cost
    (CISCOIOS-1781 / RT40842

  - Additions to the YANG model:
     ap dot11 * rrm channel dca *
     ap dot11 * rrm channel dca *
    (CISCOIOS-1780 / RT40841)

  - Additions to the YANG model:
     pseudowire-class * / ip protocol
    (CISCOIOS-1779 / RT40844)

  - Additions to the YANG model:
     router ospf * / timers spf
    (CISCOIOS-1778 / RT40845)

  - Additions to the YANG model:
     interface * / access-session host-mode
     interface * / access-session control-direction
    (CISCOIOS-1777 / RT40846)

  - Additions to the YANG model:
     interface * / tunnel checksum
     interface * / no isis hello padding always
     interface Serial* / dce-terminal-timing-enable
     line * / modem Dialin
    (CISCOIOS-1776 / RT40839)

  - Additions to the YANG model:
     interface * / ip broadcast-address
     interface * / rmon promiscuous
     interface * / rmon collection stats * owner
     interface * / spanning-tree link-type shared
    (CISCOIOS-1775 / RT40838)

  - Additions to the YANG model:
     interface * / mls l2tpv3 reserve slot
    (CISCOIOS-1774 / RT40836)

  - Added 'api cdp-run' ned-setting to insert '[no] cdp run' from 'show cdp'
    (CISCOIOS-1765 / RT40652)

Corrections:

  - API CHANGE:
     vlan access-map * / action
      remodeled from a leaf to a container with choice in order to support:
     vlan access-map * / action forward
    (CISCOIOS-1800 / RT40913)

  - Removed cli-incomplete-command annotation on:
     username * view
    (CISCOIOS-1798 / RT40888)

  - Do not disallow ipv6-prefix in router bgp neighour tag list.
    (CISCOIOS-1790 / RT40857)

  - Strip interface * / switchport trunk allowed vlan add none when
     reading config from device.
    (CISCOIOS-1775 / RT40838)


cisco-ios v6.44 [2020-02-14]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added support for configuring two groups in:
     aaa accounting *
    (CISCOIOS-1772 / RT40616)

  - Additions to the YANG model:
     crypto ipsec profile * / set security-policy limit
     crypto isakmp profile * / no keepalive
     flow exporter * / ttl
     flow monitor * / statistics packet protocol
     flow monitor * / statistics packet size
    (CISCOIOS-1770 / RT39224)

  - Additions to the YANG model:
     line * / telnet break-on-ip
    (CISCOIOS-1768 / RT40770)

  - Additions to the YANG model:
     track * list threshold / object *
    (CISCOIOS-1760 / RT40612)

  - Additions to the YANG model:
     crypto ipsec profile * / redundancy
    (CISCOIOS-1766 / RT40662)

  - Updated NEDCOM to version 1.114.

Corrections:

  - Fixed delete|modify order dependency problems between:
     line * / [ipv6] access-class *
     ip[v6] access-list *
    (CISCOIOS-1767 / RT40727)

  - Fixed modify order dependency problem between:
     interface * / ip access-group *
     ip access-list *
    (CISCOIOS-1767 / RT40727)

  - API CHANGE:
     interface * / vrrp * ip
      removed secondary leaf from ip container and added a secondary-address
      list in order to support multiple entries.
    (CISCOIOS-1763 / RT40635)


cisco-ios v6.43.4 [2020-02-07]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Fixed typo in 'no network-clock-participate slot *' conversion when reading
     from device, 'slot' was misspelled as 'slow'
    (CISCOIOS-1761 / RT40614)


cisco-ios v6.43.3 [2020-01-31]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     performance-measurement
     performance-measurement / interface *
     performance-measurement / interface * / delay-measurement
     performance-measurement / interface * / delay-measurement / advertise-delay
     router isis * / display-route-detail
    (CISCOIOS-1758 / RT40233)

  - Throw Exception in prepare phase if username config contains both
     algorithm-type and secret type.
    (CISCOIOS-1756 / RT40464)

  - Additions to the YANG model:
     segment-routing traffic-eng
     segment-routing traffic-eng / interface *
     segment-routing traffic-eng / policy *
     segment-routing traffic-eng / policy * / color
     segment-routing traffic-eng / pcc / pce address *
     segment-routing traffic-eng / pcc / report-all
     segment-routing traffic-eng / affinity-map / name * bit-position
    (CISCOIOS-1720 / RT40233)

Corrections:

  - Fixed order dependency problem, correct order is:
     no interface *
     no bridge-domain *
    (CISCOIOS-1757 / RT40483)


cisco-ios v6.43.2 [2020-01-24]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added support for unlocking 'ethernet cfm domain * / service *' referenced
     by 'interface * / service instance * ethernet / cfm mep'
    (CISCOIOS-1753 / RT40433)

  - Additions to the YANG model:
     ip ips po max-events
    (CISCOIOS-1744 / RT40351)

Corrections:

  - Added missing default value 100 in 'interface * / standby * priority'
    (CISCOIOS-1754 / RT40439)


cisco-ios v6.43.1 [2020-01-21]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Improved CISCOIOS-1723 order dependency fix by adding support for all
     interfaces in:
      * / no disable passive-interface *
      interface * / no ip vrf forwarding
    (CISCOIOS-1751 / RT40403)

Corrections:

  - Added when-statement in 'router * / passsive-interface *' list to make sure
     not used when "passive-interface default' is set.
    (CISCOIOS-1751 / RT40403)

  - Fixed order dependency problem, correct order is:
     * / no disable passive-interface *
     interface * / switchport
    (CISCOIOS-1751 / RT40403)

  - Improved order dependency problem, correct order is:
     interface * / no tunnel protection ipsec profile
     no crypto ipsec profile


cisco-ios v6.43 [2020-01-17]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface Port-channel* / counter
    (CISCOIOS-1749 / RT40358)

  - Additions to the YANG model:
     service log backtrace
    (CISCOIOS-1748 / RT40357)

  - Split 'line \\d+ \\d+' into multiple single lines when sync-from device
    (CISCOIOS-1747 / RT40354)

  - Additions to the YANG model:
     track * list threshold
     track * list threshold / threshold percentage
    (CISCOIOS-1746 / RT40353)

  - Additions to the YANG model:
     router bgp * / neighbor * allow-policy
    (CISCOIOS-1745 / RT40352)

  - Additions to the YANG model:
     class-map * / match input vlan *
    (CISCOIOS-1741 / RT40317)

  - Additions to the YANG model:
     router isis * / ignore-attached-bit
    (CISCOIOS-1740 / RT40318)

  - Additions to the YANG model:
     class-map * / match non-client-nrt
    (CISCOIOS-1739 / RT40316)

  - Additions to the YANG model:
     no ftp-server write-enable
    (CISCOIOS-1738 / RT40315)

  - Additions to the YANG model:
     crypto ca certificate chain * / certificate *
    (CISCOIOS-1737 / RT40299)

  - Additions to the YANG model:
     interface * / full-duplex
    (CISCOIOS-1736 / RT40303)

  - Added support for 0 metric on:
     interface * / isis metric
     interface * / isis ipv6 metric
    (CISCOIOS-1735 / RT40301)

  - Additions to the YANG model:
     interface * / full-duplex
    (CISCOIOS-1736 / RT40303)

  - Transform 'ip domain-lookup' to 'ip domain lookup' when reading
     config from device in order to support old IOS versions.
    (CISCOIOS-1734 / RT40312)

  - Additions to the YANG model:
     ip reflexive-list timeout
    (CISCOIOS-1734 / RT40312)

  - Additions to the YANG model:
     ip pim register-rate-limit
     mls rate-limit unicast ip rpf-failure
     mls rate-limit unicast ip icmp unreachable no-route
     mls rate-limit unicast ip errors
     platform vfi dot1q-transparency
     spd extended-headroom
    (CISCOIOS-1731 / RT40308)

  - Additions to the YANG model:
     service unsupported-adapter
    (CISCOIOS-1730 / RT40307)

  - Additions to the YANG model:
     no network-clock-participate slot *
    (CISCOIOS-1729 / RT40304)

  - Additions to the YANG model:
     mac-address-table aging-time ? vlan *
    (CISCOIOS-1728 / RT40298)

  - Additions to the YANG model:
     ptp clock ordinary domain * / clock-port * / announce interval
     ptp clock ordinary domain * / clock-port * / announce timeout
    (CISCOIOS-1727 / RT40304)

  - Added new static warning exception (matches lower and upper-case):
     "the profile is in use, the update will overwrite the legacy config in"
    At the same time, ignore retrying when receiving this line.
    (CISCOIOS-1726 / RT40287)

  - Additions to the YANG model:
     epm access-control
     interface * / dot1x max-reauth-req
    (CISCOIOS-1725 / RT40275)

Corrections:

  - Fixed order dependency problem, correct order is:
     policy-map * / class * / police * / no conform-action *
     policy-map * / class * / police * / conform-action *
    (CISCOIOS-1750 / RT40371)

  - API CHANGE:
     router eigrp|rip * / offset-list * <offset>
      changed from uint8 range 1-6 to uint32
    (CISCOIOS-1742 / RT40327)


cisco-ios v6.42.2 [2020-01-09]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added support for unlocking 'ip access-list extended' referenced by
     interface * / tunnel protection ipsec policy ipv4
    (CISCOIOS-1722 / RT40248)

Corrections:

  - Fixed order dependency problem, correct order is:
     interface * / no tunnel protection ipsec policy ipv4
     no ip access-list *
    (CISCOIOS-1724 / RT40266)

  - Fixed order dependency problem, correct order is:
     * / no disable passive-interface *
     interface GigabitEthernet* / no ip vrf forwarding
    (CISCOIOS-1723 / RT40260)

  - Fixed interface order dependency problem, correct order is:
     no service instance *
     service instance *
    (CISCOIOS-1721 / RT40247)


cisco-ios v6.42.1 [2020-01-03]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     l2vpn evpn instance * vlan-based
    (CISCOIOS-1719 / RT40238)

  - Additions to the YANG model:
     bridge-domain * / member * evpn-instance
    (CISCOIOS-1718 / RT40237)

  - Additions to the YANG model:
     l2vpn evpn / mpls label mode
     l2vpn evpn / replication-type ingress
     l2vpn evpn / router-id
    (CISCOIOS-1717 / RT40233)

  - Additions to the YANG model:
     ethernet oam mib log size
    (CISCOIOS-1715 / RT38870)

  - Additions to the YANG model:
     aaa group server radius * / retransmit
     aaa group server radius * / timeout
     interface * / macro description
    (CISCOIOS-1714)

  - Additions to the YANG model:
     sdm prefer video

Corrections:


cisco-ios v6.42 [2019-12-27]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / no ip mfib forwarding input
     interface * / no ip mfib forwarding output
     no isis optimize inline
     no isis optimize lspdb-walk
    (CISCOIOS-1712)

  - Additions to the YANG model:
     interface * / service instance * ethernet / mac security sticky address *
    (CISCOIOS-1711 / RT40186)

  - Additions to the YANG model:
     interface * / l2protocol drop *
    (CISCOIOS-1710 / RT40186)

  - Additions to the YANG model:
     ip mroute * <rpf-address> [distance]
    (CISCOIOS-1709 / RT40185)

  - Added falling threshold in:
     interface * / storm-control broadcast level bps
     interface * / storm-control broadcast level pps
    (CISCOIOS-1708 / RT40180)

Corrections:

  - Fixed interface order dependency problem, correct order is:
     no switchport port-security *
     no switchport mode dot1q-tunnel
    (CISCOIOS-1713 / RT40207)

  - API CHANGE:
     segment-routing mpls / global-block
      start och end type changed from uint16 to uint32
    (CISCOIOS-1712)

  - API CHANGE:
     interface * / l2protocol peer *
      Remodelled to support only one protocol per option (peer|drop) or
      just 'l2protocol peer|drop' for all protocols.
    (CISCOIOS-1710 / RT40186)

  - API CHANGE:
     interface * / service instance * ethernet / l2protocol *
      Remodelled to support only one protocol per option (peer|discard|forward|tunnel)
      or just 'l2protocol peer|forward|tunnel' for all protocols.
    (CISCOIOS-1710 / RT40186)

  - API CHANGE:
     interface * / l2protocol forward *
      Removed due to lack of support on device
    (CISCOIOS-1710 / RT40186)


cisco-ios v6.41 [2019-12-20]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / no ipv6 mld router
     template * / vc type
    (CISCOIOS-1707)

  - Additions to the YANG model:
     exception crashinfo maximum files
    (CISCOIOS-1705)

  - Additions to the YANG model:
      username * view
      parser view *
      parser view * / secret
      parser view * / commands *
    (CISCOIOS-1703)

  - Additions to the YANG model:
     mls cef maximum-routes ip
     mls cef maximum-routes ip-multicast
     mls cef maximum-routes ipv6
     mls cef maximum-routes mpls
     mls rate-limit unicast acl output
     mls rate-limit unicast ip options
     mls sampling packet-based
     mls qos map exp-dscp
    (CISCOIOS-1704)

  - Additions to the YANG model:
     ipv6 dhcp pool * / address prefix *
     ipv6 router rip * / distribute-list prefix-list * in|out <interface>
     router bgp * / address-family * / redistribute rip *
     track * interface <name> ipv6 routing
    (CISCOIOS-1702)

  - Additions to the YANG model:
     ip flow-export destination * * vrf *
     logging exception
     route-map * / set ip vrf * next-hop *
     spanning-tree backbonefast
     no standby redirect
    (CISCOIOS-1701 / RT39904)

  - Additions to the YANG model:
     udld recovery
     udld recovery interval
    (CISCOIOS-1700)

  - Additions to the YANG model:
     mls rate-limit unicast acl input
     mls rate-limit unicast acl vacl-log
    (CISCOIOS-1697)

  - Additions to the YANG model:
     exception flash
    (CISCOIOS-1696)

  - Additions to the YANG model:
     coap http enable
    (CISCOIOS-1695)

Corrections:

  - Removed default value from 'ip flow-export version' and added
     tailf:ned-ignore-compare-config annotation due to IOS having different
     default value depending on (assumably) IOS version.
    (CISCOIOS-1706 / RT40161)

  - API CHANGE:
     mls ip cef load-sharing
      Changed from enum leaf to container with leaves in order to support
      both full and simple + exclude-port config
    (CISCOIOS-1704)

  - API CHANGE:
     router bgp * / bgp bestpath med
      Corrected model and typo in 'missing-as-worst' (was missing-at-worst)
    (CISCOIOS-1701 / RT39904)

  - Fixed problem reading 'police cir percent <value> pir percent <value>'
     config in 'policy-map * / class *' mode.
    Required new nedcom release-1.105.
    (CISCOIOS-1691)


cisco-ios v6.40.1 [2019-12-13]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Split multiple 'route-map * / set ipv6 next-hop' address entries into
     separate lines with max 6 addresses per line.
    (CISCOIOS-1693 / RT40098)

  - Additions to the YANG model:
     call-home / rate-limit
     call-home / sender reply-to
     call-home / sender from
     configuration mode exclusive
     netconf max-sessions
    (CISCOIOS-1692 / RT40058)

  - Additions to the YANG model:
     ip sla * / frequency iteration
     ip sla * / loopback direction internal
    (CISCOIOS-1690 / RT40058)

  - Added support for reconnecting in persist, revert and getTransId methods.
    (CISCOIOS-1688 / RT40054)

  - Additions to the YANG model:
     segment-routing mpls / set-attributes / address-family ipv4 / explicit-null
    (CISCOIOS-1686 / RT40058)

  - Additions to the YANG model:
     event manager applet * / event none sync
     router bgp * / address-family * / bgp additional-paths select backup
    (CISCOIOS-1684 / RT40052)

Corrections:

  - Added 'api new-mls-qos' ned-setting to support duplicates values in
     'mls qos map cos-dscp' config.
    (CISCOIOS-1688 / RT40054)


cisco-ios v6.40 [2019-12-06]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     environment router altitude
     sdm prefer vpls_stats_disable
     router bgp * / address-family * / segment-routing mpls
     router isis * / fast-reroute ti-lfa *
    (CISCOIOS-1683 / RT39994)

  - Additions to the YANG model:
     crypto pki trustpoint * / enrollment url <url> pem
     interface TwoGigabitEthernet*
     interface TwentyFiveGigE*
    (CISCOIOS-1682 / RT39992)

  - Split multiple 'route-map * / set ip next-hop' address entries into
     separate lines with max 12 addresses per line.
    (CISCOIOS-1681 / RT39987)

  - Inject 'ip redirects' when Loopback interface is created to avoid
     creating interface with old uncleared value 'no ip redirect'
    (CISCOIOS-1680)

  - Additions to the YANG model:
     banner incoming
     ip ssh server algorithm authentication *
    (CISCOIOS-1677)

  - Support for multiple non-range vlan ranges in
     class-map * / match vlan *
    (CISCOIOS-1675 / RT39926)

  - Additions to the YANG model:
     ip tcp timestamp
     logging discriminator * severity
     logging discriminator * facility
     router ospf * / event-log size
     scheduler interval
    (CISCOIOS-1674 / RT39925)

  - Delete bgp address-family after delete of neighbor remote-as entry.
    (CISCOIOS-1673)

  - Added support for sending CTRL-C with live-status exec any action.
    Send "CTRL-C" or "CTRL-C async" to send \u0003 and optionally ignore
    the device reply (async option). Also note that you most likely will
    have to extend timeouts to avoid closing the current connection and
    send CTRL-C to a new connection, i.e. CTRL-C being ignored.
    (CISCOIOS-1670 / RT39914)

  - Delete 'tacacs server * / address' before deleting another tacacs
     server or deleting another tacacs server's address.
    (CISCOIOS-1666 / RT39894)

Corrections:

  - Remove policy-map * / class * / priority before changing it.
    Delete policy-map * / class * priority before modifying police config.
    (CISCOIOS-1678 / RT39964)

  - API CHANGE:
     ip flow-export destination
      changed from container to list in order to support two entries
     ip flow-export version
      added missing default value '5'
    (CISCOIOS-1676 / RT39932)

  - Fixed problem switching between ip prefix-list with sequence numbers and
     no sequence numbers using the 'ip prefix-list sequence-number' throwing
     an ERROR in the trace when writing to device. Required new NEDCOM.
    (CISCOIOS-1672)


cisco-ios v6.39 [2019-11-29]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / Bundle#.#   (Bundle sub-interface)
    (CISCOIOS-1668)

  - Additions to the YANG model:
     interface * / mls ip directed-broadcast
     interface * / no standby redirect
    (CISCOIOS-1667 / RT39904)

  - Additions to the YANG model:
     ipv6 mfib hardware-switching replication-mode ingres
    (CISCOIOS-1664 / RT39859)

  - Additions to the YANG model:
     call rsvp-sync
     errdisable recovery cause oam-remote-failure-enable
     interface * / random-detect
     route-map * / match length
    (CISCOIOS-1663 / RT39858)

  - Additions to the YANG model:
     line * / autohangup
     line * / autoselect ppp
     line * / session-limit
    (CISCOIOS-1662 / RT39857)

  - Additions to the YANG model:
     interface * / spanning-tree vlan * port-priority
     interface * / spanning-tree vlan * cost
    (CISCOIOS-1661 / RT39856)

  - Additions to the YANG model:
     ip nbar pdlm *
     ip nbar port-map * udp *
     ip nbar port-map * tcp *
    (CISCOIOS-1660 / RT39855)

  - Raised max queue id range from 4 and 3 to 7 on:
     interface * / wrr-queue cos-map *
     interface * / rcv-queue cos-map *
     interface * / wrr-queue threshold *
     interface * / rcv-queue threshold *
    (CISCOIOS-1659 / RT39854)

  - Added support for 'show vtp status' which outputs VTP version in
     'VTP Version : running VTP\\d+ (VTP\\d+ capable)' format.
    (CISCOIOS-1658 / RT39853)

  - Additions to the YANG model:
     interface * / no ip igmp snooping report-suppression
     router isis * / nsf interval
     router isis * / nsf interface wait
    (CISCOIOS-1657 / RT39852)

  - Additions to the YANG model:
     hw-module slot * sp process-max-time
     hw-module slot * rp process-max-time
    (CISCOIOS-1656 / RT39851)

  - Ignore delete of 'vtp domain'

Corrections:

  - API CHANGE:
     router bgp * / bgp confederation identifier
      changed from type uint16 to bgp-as-no-type to support AS numbers.
    (CISCOIOS-1668 / RT39909)

  - API CHANGE:
     Moved out 'interface * / standby * redirect' from standby-list, i.e.
      standby redirect config is not in 'standby <id> redirect' list but
      in 'standby redirect' container under interface.
     'interface * / standby redirect unknown' changed from type empty to
       cli-boolean-no type boolean with default true in order to support
       show running config only showing if unset.
    (CISCOIOS-1667 / RT39904)

  - API CHANGE:
     ip multicast route-limit <limit> <threshold>
      Changed from leaf to container with <limit> to support <threshold>
    (CISCOIOS-1664 / RT39859)

  - Added spanning-tree config interface dependency, correct order:
     interface * / ethernet dot1ad nni
     interface * / spanning-tree X
    Revised the 'ethernet dot1ad nni' vs 'port-type' dependencies.
    (CISCOIOS-1642 / RT39744)


cisco-ios v6.38.1 [2019-11-25]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     flow record * / match ipv4 precedence
     flow record * / match ipv4 dscp
    (CISCOIOS-1653 / RT39816)

  - Additions to the YANG model:
     class-map * / match flow record *
    (CISCOIOS-1651 / RT39799)

Corrections:

  - Restored output of which show command was executed in live-status exec any
    (CISCOIOS-1652 / RT39804)


cisco-ios v6.38 [2019-11-21]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / qos trust extend
    (CISCOIOS-1650 / RT39790)

  - Additions to the YANG model:
     interface * / service instance * ethernet / storm-control unicast cir
    (CISCOIOS-1649 / RT39764)

  - Additions to the YANG model:
     interface * / mac-addr-filter
     interface CMON-Tunnel* / mode
     process-restart / no lcha-preferred
     redundancy / linecard-group * / standby console
     service log-hidden
     snmp-server host * <port>
    (CISCOIOS-1648)

  - Additions to the YANG model:
     cable monitor / sniff card * *
    (CISCOIOS-1647)

  - Additions to the YANG model:
     redundancy / linecard-group * / main-cpu
     redundancy / linecard-group * / standby
    (CISCOIOS-1646)

  - Additions to the YANG model:
     interface CMON-Tunnel*
     snmp-server trap link switchover
    (CISCOIOS-1645)

  - Additions to the YANG model:
     cable video / report-stream-error cc-error report-interval
     cable video / report-stream-error cc-error threshold
    (CISCOIOS-1644 / RT39750)

  - Additions to the YANG model:
     lldp holdtime
     lldp reinit
     lldp timer
    (CISCOIOS-1643)

  - Redeploy some config when deleting interface spanning-tree config:
     interface * / spanning-tree bpduguard enable
     interface * / spanning-tree guard
    (CISCOIOS-1642 / RT39744)

  - Modify existing 'ip sla group schedule *' entry by use of add|delete keyword
    (CISCOIOS-1641 / RT39741)

Corrections:

  - API CHANGE:
     Moved 'udp-port' from allowed-traps leaf-list to its own leaf with port
    (CISCOIOS-1648)

  - Fixed platform smart-sfp interface interface create and delete dependencies
    (CISCOIOS-1640 / RT39720)


cisco-ios v6.37.2 [2019-11-15]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     restconf
    (CISCOIOS-1638)

  - Additions to the YANG model:
     cable dynamic-punt enable
    (CISCOIOS-1637)

  - Additions to the YANG model:
     netconf-yang cisco-ia logging nes-log-level
     netconf-yang cisco-ia logging sync-log-level
    (CISCOIOS-1636)

  - Additions to the YANG model:
     interface Downstream-Cable* / cable bundle
     interface Downstream-Cable* / cable managed fiber-node
     interface Downstream-Cable* / cable rf-bandwidth-percent
     ptp r-dti * / clock-port * / ethernet
    (CISCOIOS-1635)

  - Additions to the YANG model:
     service-template *
     service-template * / inactivity-timer
     service-template * / linksec policy
     service-template * / voice vlan
    (CISCOIOS-1634)

  - Additions to the YANG model:
     cable video / report-stream-error cc-error
    (CISCOIOS-1630 / RT39631)

  - Added <reload> tag to client_pnp.erl.
    (CISCOIOS-1594 / RT39179)

  - Convert clock source to lowercase to support line->Line in:
     controller SONET * / au-3 * / tug-2 * t1 * clock source

Corrections:

  - Fixed double quoting when reading monitor session * / description

  - Delete old list entries before adding new ones:
     ethernet cfm domain * level / service *
    (CISCOIOS-1633 / RT39654)

  - Fixed crypto order dependency problem, correct order is:
     no crypto ipsec profile *
     no crypto ikev2 profile *
    (CISCOIOS-1626 / RT39570)


cisco-ios v6.37.1 [2019-11-08]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip flow-export template options timeout-rate
     ip flow-export template options refresh-rate
    (CISCOIOS-1628 / RT39612)

  - Additions to the YANG model:
     interface * / stackwise-virtual dual-active-detection
     interface * / stackwise-virtual link
     stackwise-virtual / domain
    (CISCOIOS-1621)

  - Added 'auto stackwise-virtual-if-indent-patch' ned-setting to fix IOS bug
     where interfaces with stackwise-virtual config are badly indented causing
     sync-from to ignore those interfaces.
    (CISCOIOS-1621)

Corrections:

  - Fixed interface order dependency problem, correct order is:
     [no] ethernet dot1ad
     no port-type
    (CISCOIOS-1627 / RT39594)


cisco-ios v6.37 [2019-11-05]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     avc sd-service / controller / source-interface
     ip ssh window-size
     ipv6 router eigrp * / passive-interface
     netconf-yang
     netconf-yang cisco-odm polling-enable
     platform hardware crypto-throughput level
     router eigrp * / bfd all-interfaces
    (CISCOIOS-1624)

  - Additions to the YANG model:
     logging ip access-list cache
     interface * / logging ip access-list cache
    (CISCOIOS-1623)

  - Additions to the YANG model:
     platform feature-manager capture rate-limit
    (CISCOIOS-1622)

Corrections:

  - API CHANGE:
     netconf-yang / cisco-ia snmp-trap-control trap-list *
      changed from leaf to mode list
     netconf-yang / cisco-odm actions *
      changed from tailf:cli-list-syntax leaf-list to mode list
    (CISCOIOS-1624)

  - Improved order dependency fix in v6.36.2, correct order is:
     ethernet cfm domain *
     interface * / ethernet cfm mep domain
    (CISCOIOS-1603 / RT39334)


cisco-ios v6.36.5 [2019-11-01]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip ssh server algorithm mac hmac-sha2-256
     ip ssh server algorithm mac hmac-sha2-512
    (CISCOIOS-1619 / RT39530)

Corrections:

  - Added Pattern.quote around exec command help prompt (ending with ?)
    (CISCOIOS-1620 / RT39534)

  - Added ordered-by user on:
     ip ssh server algorithm mac *
    (CISCOIOS-1619 / RT39530)

  - Moved global before metric leaf in ip route options.
    (CISCOIOS-1618 / RT39525)

  - Fixed interface order dependency problem, correct order is:
     interface * / tunnel mode
     interface * / ip subscriber
    (CISCOIOS-1617)


cisco-ios v6.36.4 [2019-10-31]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     aaa authorization subscriber-service *
     aaa server radius dynamic-author / server-key
     class-map type control *
     class-map type control * / match authen-status
     class-map type control * / match service-name
     class-map type control * / match timer
     interface * / ip subscriber
     interface * / ip subscriber / initiator dhcp
     interface * / service-policy type control
     interface * / tunnel mode ethernet gre
     interface * / tunnel vlan *
     policy-map type control *
     policy-map type control * / class type control *
     policy-map type control * / class type control * event / *
     policy-map type control * / class type control * event / * service
     policy-map type control * / class type control * event / * service-policy
     policy-map type control * / class type control * event / * authenticate aaa list
     policy-map type control * / class type control * event / * authorize aaa
     policy-map type control * / class type control * event / * set-timer
     policy-map type service *
     policy-map type service * / keepalive
     policy-map type service * / * class type traffic <name>
     policy-map type service * / * class type traffic / redirect to
     policy-map type service * / class type traffic *
     policy-map type service * / class type traffic * / timeout
     policy-map type service * / class type traffic * / accounting aaa list
     policy-map type service * / class type traffic default
     policy-map type service * / class type traffic default / drop
     radius-server attribute wireless|nas-port|nas-port-id
     radius-server attribute * include
     radius-server attribute * remote-id
     radius-server attribute * authentication call-station-id
     redirect server-group *
     redirect server-group * / server ip * port *
    (CISCOIOS-1617)

  - When syncing from device, strip duplicate values in:
     class-map * / match dscp *
    (CISCOIOS-1614 / RT39484)

  - Added new static warning exceptions (matches lower and upper-case):
     "the combination of icmp redirects and hsrp on the same"
     "interface may result in lost packets"
    (CISCOIOS-1612 / RT39460)

  - When adding and deleting object-group network entries at the same time
     swap the delete and the create in order to avoid empty object-group.
    (CISCOIOS-1611 / RT39438)

  - Removed the inject of internal 'xxyyzztop' variable upon sync-from.

Corrections:

  - Fixed problem deleting snmp-server contact and alias with quotes around
     its value.
    (CISCOIOS-1615 / RT39436)

  - Fixed interface order dependency problem, correct order is:
     ethernet dot1ad nni
     spanning-tree bpduguard enable
    (CISCOIOS-1613 / RT39479)

  - Added remove-before-change to:
     router bgp * / neighbor * inherit peer-policy
    (CISCOIOS-1601 / RT3924)


cisco-ios v6.36.3 [2019-10-25]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Removed runtime track remove before change patch by use of nedcom extension.
    (CISCOIOS-1608)

  - Removed runtime interface speed&duplex order patch by improving the logic.
    (CISCOIOS-1446)

Corrections:

  - Added remove-before-change to:
     ip sla group schedule *
    (CISCOIOS-1607 / RT39374)

  - Fixed order dependency, correct order is:
     interface *
     no bridge-domain *
    (CISCOIOS-1606 / RT39354)


cisco-ios v6.36.2 [2019-10-18]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     mac-address-table limit
     mac-address-table limit maximum
     vlan * / shutdown
    (CISCOIOS-1605)

  - Support use of proxy remote-name and remote-password with remote-connection
     serial and SSH.
    (CISCOIOS-1604)

  - Additions to the YANG model:
     interface * / ip ospf prefix-suppression
     mpls ldp discovery quick-start
     platform expect-single-psu
     router ospf * / fast-reroute per-prefix ti-lfa
     router ospf * / fast-reroute per-prefix ti-lfa area *
     router ospf * / segment-routing mpls
    (CISCOIOS-1602 / RT39308)

  - Added new ned-setting 'connection terminal println-mode' in order support
     terminal servers not accepting the default \r\n as ENTER. Set to ocrnl for
     \n and to onlret for \r (needed for the Opengear terminal server)
    (CISCOIOS-1598 / RT39212)

  - ned-setting 'proxy remote-connection serial' enhancement:
     Added an initial 100 ms sleep after connect to SSH terminal server before
     sending an initial ENTER to trigger the device login state.
    (CISCOIOS-1598 / RT39212)

Corrections:

  - Fixed order dependency, correct order is:
     ethernet cfm domain *
     interface * / ethernet cfm mep domain
    (CISCOIOS-1603 / RT39334)

  - Added remove-before-change to:
     router bgp * / neighbor * inherit peer-session
    (CISCOIOS-1601 / RT3924)


cisco-ios v6.36.1 [2019-10-09]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Improved 'auto if-address-delete-patch' ned-setting, do not trim shutdown
    Inject service-policy delete instead of defaulting interface.
    (CISCOIOS-1599 / RT39216)

Corrections:


cisco-ios v6.36 [2019-10-04]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / ip unnumbered poll point-to-point
    (CISCOIOS-1597 / RT39204)

  - Additions to the YANG model:
     router ospf * / bfd all-interfaces strict-mode
     router ospf * / priority
     router ospf * / segment-routing area *
    (CISCOIOS-1596 / RT39203)

  - API CHANGE:
     Changed 'live-status exec-strict-prompt' ned-setting from a boolean to a
     string in order to allow customer to custom set the strict prompt. Use
     %p for the device prompt, e.g. set it to "%p$" to support show commands
     not ending with a newline.
    (CISCOIOS-1595)

  - Additions to the YANG model:
     shell processing full
    (CISCOIOS-1593 / RT39169)

  - Additions to the YANG model:
     aaa local authentication attempts max-fail
     interface * / pseudowire
     ppp packet throttle
     pseudowire-class * / protocol l2tpv2
    (CISCOIOS-1590 / RT39148)

  - Added new static warning exception (matches lower and upper-case):
     "deleting ac member of xconnect"
    (CISCOIOS-1588 / RT39135)

  - Additions to the YANG model:
     sdm prefer advanced
    (CISCOIOS-1586 / RT39120)

  - Additions to the YANG model:
     interface Virtual-PPP*
     route-map * / match track
    (CISCOIOS-1585 / RT39110)

  - Updated NEDCOM to version 1.93.

Corrections:

  - Fixed order dependencies between:
     crypto gkm group *
     crypto map *
     interface * / crypto map *
    (CISCOIOS-1585 / RT39110)


cisco-ios v6.35.1 [2019-09-25]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     crypto ikev2 authorization policy * / dns
    (CISCOIOS-1582 / RT39076)

  - Additions to the YANG model:
     crypto ikev2 name-mangler * / eap all
    (CISCOIOS-1581 / RT39073)

  - Additions to the YANG model:
     interface * / tunnel protection ipsec policy ipv4
    (CISCOIOS-1580 / RT39064)

  - Fixed bad regex causing NedDiff mode filter ,, being slow.
    (CISCOIOS-1578 / RT39060)

  - Additions to the YANG model:
     ntp allow mode control
    (CISCOIOS-1577 / RT39054)

  - Changed NED default behaviour in abort to throw Exception on first device
     error instead of ignoring them as previously. If the old behaviour is
     still desired, it can be enabled with the new ned-setting 'write ignore-
     abort-errors'.
    (CISCOIOS-1575 / RT39012)

Corrections:

  - Fixed interface * / tunnel mode vs tunnel protection ipsec dependency.
    Replace crypto isakmp policy 'encryption' leaf with 'encr' at input.
    (CISCOIOS-1580 / RT39064)

  - Fixed interface service order dependency problem, correct order is:
     interface * / service instance * ethernet
      no rewrite *
      encapsulation dot1q
    (CISCOIOS-1579 / RT39059)


cisco-ios v6.35 [2019-09-19]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added support for unlocking flow monitor referenced by
     interface * / ipv6 flow monitor * input|output
    (CISCOIOS-1574 / RT38989)

  - Additions to the YANG model:
     flow monitor * / record netflow * original-output
    (CISCOIOS-1573 / RT38981)

  - Added 'cli-delete-container-on-delete' annotation on 'encapsulation' leaf:
     connect * / xconnect
     interface CEM* / cem * / xconnect
     interface ATM* / pvc * / xconnect

Corrections:

  - API CHANGE:
     interface * / ipv6 flow monitor * input|output
      Added leaf 'name' to list key in order to support multiple entries.
    (CISCOIOS-1574 / RT38989)


cisco-ios v6.34.2 [2019-09-17]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Reordered interface * / service-policy, send output before input or IOS XE
     may in some circumstances silently ignore the output entry.
    (CISCOIOS-1571 / RT38952)

  - Include 'ip address negotiated [previous]' in interface address restoration
     upon modifying vrf on an interface.
    (CISCOIOS-1571 / RT38952)

Corrections:

  - Upgraded nedcom.jar to release-1.87, solving a bug which erronously enabled
     trace on the wrong NedWorker, potentially leading to NSO restart problems.
    (CISCOIOS-1569)


cisco-ios v6.34.1 [2019-09-13]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Send line * / password before line * / login to avoid warning.
    (CISCOIOS-1568 / RT38938)

  - Additions to the YANG model:
     aaa group server radius * / load-balance method least-outstanding
    (CISCOIOS-1567 / RT38934)

  - Added new ned-setting 'live-status exec-strict-prompt' to enable stricter
     prompt matching for 'live-status exec any' commands. Setting it to true
     will make the NED send an initial newline to determine the device prompt,
     there after use that exact prompt in the following command(s).
    (CISCOIOS-1566 / RT38917)

  - Additions to the YANG model:
     platform mpls mtu-enable
    (CISCOIOS-1565 / RT38919)

  - Additions to the YANG model:
     ip sla * / icmp-jitter *
     track * / default-state
    (CISCOIOS-1564 / RT38912)

  - Additions to the YANG model:
     interface * / member vfi
    (CISCOIOS-1561 / RT38870)

  - Additions to the YANG model:
     bridge-domain * / member * service-instance * split-horizon group
    (CISCOIOS-1560 / RT38863)

  - Additions to the YANG model:
     crypto gkm group * / client recovery-check interval
     crypto gkm group * / client status active-sa track
    (CISCOIOS-1554 / RT38805)

Corrections:

  - API CHANGE:
     ip igmp ssm-map static *
      changed from container to list in order to support multiple entries
    (CISCOIOS-1563 / RT38910)

  - Split interface * / vlan-range * in single entries when reading config.
    (CISCOIOS-1562 / RT38895)


cisco-ios v6.34 [2019-09-06]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip sla profile packet type ip *
     ip sla profile packet type ethernet *
     ip sla endpoint-list type ip *
     monitor session * type rspan-source
     monitor session * type rspan-destination
    (CISCOIOS-1557 / RT38828)

  - Additions to the YANG model:
     router bgp * / bgp rpki server tcp * * refresh
    (CISCOIOS-1555 / RT38802)

  - Additions to the YANG model:
     buffers fastswitching *
     crypto gdoi group * / client recovery-check interval
     crypto gdoi group * / client status active-sa track
     track * stub-object
    (CISCOIOS-1554 / RT38805)

  - Additions to the YANG model:
     ptp clock ordinary domain * hybrid
     ptp clock ordinary domain * / local-priority
     ptp clock ordinary domain * / clock-port * profile g8275.1 local-priority
     ptp clock ordinary domain * / clock-port * / transport ethernet multicast
     ptp clock ordinary domain * / clock-port * / clock destination
    (CISCOIOS-1553 / RT38799)

  - Additions to the YANG model:
     spanning-tree logging
    (CISCOIOS-1551 / RT38761)

   - Switched NedDiff implementation from private to new NEDCOM NedDiff.
     (CISCOIOS-1549)

Corrections:

  - API CHANGE:
     l2vpn xconnect context * / member * *
      Added vcid to list key to support same address entries with diff vcid.
    (CISCOIOS-1552 / RT38789)

  - Fixed problem reading user name for using umap config with 'proxy authgroup'
    (CISCOIOS-1543 / RT38647)


cisco-ios v6.33.7 [2019-08-28]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     cable profile load-balance *
     cable profile load-balance * / restricted
     cable profile load-balance * / method
     cable profile load-balance * / threshold
     cable profile load-balance * / policy
     cable profile load-balance * / init-tech-list
     cable profile load-balance * / docsis-policy
     cable profile load-balance * / interval
     cable profile load-balance * / tag *
     cable profile service-group * / mac-domain * / cable downstream dsg chan-list
     cable profile service-group * / mac-domain * / cable downstream dsg tg *
    (CISCOIOS-1548 / RT38694)

  - Redeploy router ospf * vrf config from CDB with vrf change.
    (CISCOIOS-1545 / RT38666)

  - Added new ned-setting 'proxy authgroup' and updated the proxy connector
     template 'ned-connector-proxy.json' to use new methods using remote-name,
     remote-password and remote-secondary-password from authgroup.
    (CISCOIOS-1543 / RT38647)

  - Additions to the YANG model:
     interface * / mace enable
    (CISCOIOS-1542 / RT38643)

  - Additions to the YANG model:
     interface Analysis-Module*
     interface Embedded-Service-Engine*
     interface ISM*
    (CISCOIOS-1511 / RT38147)

Corrections:

  - Fixed order dependency, correct order is:
     [no] router ospf * vrf
     router bgp * / address-family * vrf * / redistribute ospf
    (CISCOIOS-1545 / RT38666)

  - Removed the 'recursive-delete' under interface/cem list added in v6.33.2.
    (CISCOIOS-1526 / RT38326)


cisco-ios v6.33.6 [2019-08-23]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added new ned-setting 'auto if-switchport-sp-redeploy' to be able to disable
     redeploy of service-policy input|output when toggling interface switchport.
    (CISCOIOS-1539 / RT38581)

  - Added live-status:
     lldp neighbors *
    (CISCOIOS-1476 / RT37958)

Corrections:

  - Do not redeploy interface config with switchport content changes.
    (CISCOIOS-1539 / RT38581)


cisco-ios v6.33.5 [2019-08-16]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added 'discovery-created' to client_pnp.erl.
    (CISCOIOS-1536 / RT38518)

  - Additions to the YANG model:
     controller SONET * / sts-1 * / description
     controller SONET * / sts-1 * / cem-group * cep
    (CISCOIOS-1535 / RT38473)

Corrections:

  - Restored broken fix from (CISCOIOS-1386 / RT37193):
     Pre-inject delete of ethernet ring g8032 exclusion-list|inclusion-list
     in order to honor both delete and add dependencies in one transaction.
    (CISCOIOS-1532 / RT38457)


cisco-ios v6.33.4 [2019-08-09]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Rewrote poor list iteration, drastically improving NedDiff performance.
    (CISCOIOS-1534)

  - Additions to the YANG model:
     crypto gdoi group * / server local / sa receive-only
     crypto gdoi group * / server local / sa ipsec * / no tag
     crypto gkm group ipv6 *
     crypto identity *
     crypto identity * / description
     crypto identity * / dn *
    (CISCOIOS-1533 / RT38454)

  - Additions to the YANG model:
     authentication critical recovery delay
    (CISCOIOS-1531 / RT38448)

  - Append \r on each line if missing with NSO config CLI command
     'load-native-config' in order to look like real device config.

Corrections:


cisco-ios v6.33.3 [2019-08-01]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Fixed diffset corruption when pre-injecting delete of bandwidth|priority
     percent in policy-map classes.
    Fixed dependency issue when modifying policy-map classes and adding
     service-policy entries in interfaces.
    (CISCOIOS-1528 / RT38358)


cisco-ios v6.33.2 [2019-07-31]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip vrf * / mdt mtu
     interface * / ip igmp static-group * source *
    (CISCOIOS-1527 / RT38339)

  - Ignore 'Please use no cem-group command under controller to remove' reply
     when trying to delete cem list under interface. Also added recursive-delete
     under cem list in order to explicitly delete the xconnect group.
    (CISCOIOS-1526 / RT38326)

  - Deprecated 'disabled' setting in cisco-ios extended-parser ned-setting.
     Setting it to disabled will hereafter mean the same as robust-mode.

Corrections:

  - Add|remove quotes around <value> to support '|' character in
     event manager environment * <value>
    (CISCOIOS-1525 / RT38318)


cisco-ios v6.33.1 [2019-07-29]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     l2vpn vfi context * / autodiscovery bgp signaling bgp / ve range
     l2vpn vfi context * / autodiscovery bgp signaling bgp / rd
     l2vpn vfi context * / autodiscovery bgp signaling bgp / no auto-route-target
    (CISCOIOS-1523 / RT38305)

Corrections:

  - Fixed interface order dependency problem, correct order is:
     interface * / switchport
     interface * / no lldp receive
    (CISCOIOS-1522 / RT38288)

  - Fixed interface order dependency problem, correct order is:
     interface * / no ethernet dot1ad nni
     interface * / no port-type nni
    (CISCOIOS-1522 / RT38288)

  - Fixed interface order dependency problem, correct order is:
     interface * / cdp enable | lldp receive
     interface * / no port-type nni|eni
    (CISCOIOS-1522 / RT38288)


cisco-ios v6.33 [2019-07-26]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Ignore error when deleting non-existant interface * cem entry to support
     delete of controller cem-group.
    (CISCOIOS-1518 / RT38247)

  - Additions to the YANG model:
     packetcable gate send-subscriberID
    (CISCOIOS-1514 / RT38219)

  - Additions to the YANG model:
     controller * / cem-group * cep|framed
    (CISCOIOS-1513 / RT38183)

  - Throw an Exception in commit if config mode is blocked by another session.
    (CISCOIOS-1513 / RT38183)

  - API CHANGE:
     interface * / switchport trunk allowed vlan
      removed presence and delete-when-empty annotation from vlan container.
    (CISCOIOS-1468 / RT37889)

  - Changed 'developer progress-verbosity' ned-setting default to debug.


Corrections:

  - Strip outer quotes around regex when sending ip community config
    (CISCOIOS-1519 / RT38259)

  - Added tailfned virtual leaves in input setupParserContext in order to
     allow correct population of data model with partial show, affecting
     rollback in abort and more.
    (CISCOIOS-1516 / RT38242)

  - API CHANGE:
     flow monitor * / cache timeout
      Added missing defaults + changed update + synchronized types
    (CISCOIOS-1515 / RT38236)


cisco-ios v6.32 [2019-07-19]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - API CHANGE:
     interface * / ethernet cfm mep
      split mep-list into 3 lists in order to support duplicate vlan and service entries.
      changed key from mpid to domain + vlan and service for the 2nd and 3rd list.
    (CISCOIOS-1512 / RT38174)

  - Fixed interface order dependency problem with locks, correct order is:
     flow monitor *
     interface * / ip[v6] flow monitor
    (CISCOIOS-1436 / RT37710)


cisco-ios v6.31 [2019-07-12]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     mpls traffic-eng nsr
    (CISCOIOS-1510 / RT38111)

  - Additions to the YANG model:
     router bgp * / bgp mpls-local-label
     router bgp * / address-family * / bgp bgp mpls-local-label
    (CISCOIOS-1508 / RT38109)

  - Additions to the YANG model:
     route-map * / set aigp-metric
    (CISCOIOS-1507 / RT38108)

  - Additions to the YANG model:
     bfd snmp traps more
    (CISCOIOS-1506 / RT38107)

  - Additions to the YANG model:
     mpls traffic-eng lsp attributes * / lockdown
    (CISCOIOS-1504 / RT38105, CISCOIOS-1505 / RT38106)

  - Additions to the YANG model:
     l2vpn / logging redundancy
    (CISCOIOS-1503 / RT38102)

  - Additions to the YANG model:
     interface * / tunnel mpls traffic-eng auto-bw adjustment-threshold
    (CISCOIOS-1502 / RT38101)

  - Additions to the YANG model:
     interface * / secondary-admin-state in-service
    (CISCOIOS-1501 / RT38099)

  - Removed max range on interface * / bandwidth/kilobits
    (CISCOIOS-1499 / RT38097)

  - Additions to the YANG model:
     controller dwdm * / g709 fec
     controller dwdm * / transport-mode
    (CISCOIOS-1498 / RT38096)

  - Additions to the YANG model:
     qos-overhead-accounting positive|negative
     qos-overhead-accounting enable GigabitEthernet *
     qos-overhead-accounting enable TenGigabitEthernet *
    (CISCOIOS-1497 / RT38095)

  - Additions to the YANG model:
     network-clock input-source ? External *
    (CISCOIOS-1496 / RT38094)

  - Additions to the YANG model:
     no ip http ctc authentication
    (CISCOIOS-1495 / RT38093)

  - Additions to the YANG model:
     sdm prefer disable_8k_efp
     sdm prefer disable_portchannel_qos_multiple_active
     sdm prefer no_efp_feat_ext
    (CISCOIOS-1494 / RT38090)

  - Additions to the YANG model:
     mpls traffic-eng auto-tunnel backup config affinity
     mpls traffic-eng auto-tunnel backup config unnumbered-interface
     mpls traffic-eng auto-tunnel backup srlg exclude
     mpls traffic-eng auto-tunnel backup timers removal unused
     mpls traffic-eng auto-tunnel backup tunnel-num
     mpls traffic-eng signalling advertise explicit-null
    (CISCOIOS-1493 / RT38089)

  - Additions to the YANG model:
     mpls ldp password option * for
     mpls ldp password required for
    (CISCOIOS-1492 / RT38088)

  - Additions to the YANG model:
     alarm-profile ? attach chassis
     platform hw-module configuration / hw-module *
    (CISCOIOS-1491 / RT38086)

  - Additions to the YANG model:
     platform qos-adjust-policer enable
     platform issu reload interface-module sequence *
     platform hw-module configuration / hw-module *
    (CISCOIOS-1490 / RT38084)

  - Additions to the YANG model:
     interface * / ethernet loopback
    (CISCOIOS-1488 / RT38077)

  - Additions to the YANG model:
     call-home / no http secure server-identity-check
     platform punt-sbrl subscriber punt-cause *
    (CISCOIOS-1487 / RT38046)

  - Additions to the YANG model:
     interface * / eapol destination-address
     interface * / macsec
     interface * / macsec replay-protection window-size
     interface * / mka policy
     interface * / mka pre-shared-key key-chain
     key chain * / description
     key chain * macsec
     mka policy *
     mka policy * / macsec-cipher-suite
     mka policy * / confidentiality-offset
     mka policy * / sak-rekey interval
     mka policy * / sak-rekey on-live-peer-loss
    (CISCOIOS-1483 / RT38036)

  - Additions to the YANG model:
     interface * / switchport vlan mapping *
    (CISCOIOS-1482 / RT38026)

Corrections:

  - API CHANGE
     transceiver type all / monitoring interval
      type changed from uint16 to uint32 and removed max range
    (CISCOIOS-1510 / RT38111)

  - API CHANGE:
     router bgp * / neighbor * translate-update
      removed ipv4 under translate-update to access multicast directly
    (CISCOIOS-1509 / RT38110)

  - API CHANGE:
     Removed choice around sdm prefer config contents
    (CISCOIOS-1494 / RT38090)

  - API CHANGE:
     mpls traffic-eng reoptimize timers delay cleanup
      type changed from uint8 to uint16 and max raised to 300.
    (CISCOIOS-1493 / RT38089)

  - API CHANGE:
     platform hw-module configuration
      changed from leaf to presence mode container
    (CISCOIOS-1490 / RT38084)

  - API CHANGE:
     platform punt-sbrl wan punt-cause *
      changed key leaf name + renamed rate to rate-per-1-sec
    (CISCOIOS-1487 / RT38046)

  - Fixed interface order dependency problem, correct order is:
     interface * / mtu
     interface * / ip mtu
    (CISCOIOS-1483 / RT38036)

  - Do not delete locks in output to avoid erronously deleting active locks
    (happened due to pre-injected unlock config triggering delete in extension)
      (CISCOIOS-1436 / RT37710)


cisco-ios v6.30 [2019-07-05]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     banner config-save
    (CISCOIOS-1481 / RT38009)

  - Added new ned-setting 'proxy send-login-newline true|false(default)' in order
     to be able to wake up the device for a login prompt after proxy connect.
    (CISCOIOS-1480)

  - Additions to the YANG model:
     router bgp * / neighbor * advertise-map * internal-vpn-client
    (CISCOIOS-1478 / RT37975)

  - Added WORD type in union to accept vlan ranges in:
     interface * / service instance * ethernet / encapsulation dot1q * second-dot1q
    (CISCOIOS-1477 / RT37973)

  - API CHANGE
     cable downstream qam-profile * / annex
      changed from leaf to container+leaf due to freq spacing for annex A.
     ptp r-dti * / clock-port * / clock source
      changed model design due to addition of ipv6 support
    (CISCOIOS-1475 / RT37949)

  - Added non-strict leafref on:
     router bgp * / address-family * / neighbor * peer-group
    (CISCOIOS-1474 / RT37945)

  - Additions to the YANG model:
     cable depi multicast pool * / ip address
     cable depi multicast pool * / ipv6 address
     cable profile mac-domain * / cable privacy mandatory
     ip mroute *
     ip mroute vrf *
     ptp r-dti * / clock-port * / transport ipv4|ipv6|other
    (CISCOIOS-1475 / RT37949)

  - Additions to the YANG model:
     cable profile mac-domain * / cable enable-trap
     cable profile mac-domain * / cable map-advance
     cable profile mac-domain * / cable privacy skip-validity-period
     cable profile mac-domain * / cable upstream *
     cable profile mac-domain * / cable upstream balance-scheduling
     cable profile mac-domain * / load-interval
     cable profile wideband-interface * / load-interval
    (CISCOIOS-1472 / RT37937)

Corrections:

  - Fixed order dependency, correct order is:
     no ptp clock ordinary domain *
     no interface *


cisco-ios v6.29 [2019-06-28]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Ignore error when deleting non-existant CEM interface to solve delete
     from controller entry.
    (CISCOIOS-1466 / RT37890)

Corrections:

  - API CHANGE
     crypto gdoi group * / server local / redundancy / peer address ipv4 *
      changed from leaf to ordered-by user leaf-list to allow for multiple
      entries.
    (CISCOIOS-1471 / RT37927)


cisco-ios v6.28 [2019-06-27]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - API CHANGE:
    Removed ordered-by user and cli-show-long-obu-diffs on:
     router/bgp/address-family/neighbor-tag/neighbor list
    (CISCOIOS-1470 / RT37914)

  - Additions to the YANG model:
     platform smart-sfp interface *
    (CISCOIOS-1469 / RT37895)

  - Additions to the YANG model:
     interface * / service instance * ethernet / mac security sticky
    (CISCOIOS-1467 / RT37893)

  - Additions to the YANG model:
     cable rpd * / ssd
    (CISCOIOS-1443 / RT37776)

Corrections:

  - Fixed E1 controller order dependency, correct order is:
     no cem-group *
     no framing
    (CISCOIOS-1466 / RT37890)


cisco-ios v6.27 [2019-06-26]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     bridge-domain * / ip igmp snooping immediate-leave
     bridge-domain * / no ip igmp snooping
    (CISCOIOS-1464 / RT37884)

  - Additions to the YANG model:
     iox
    (CISCOIOS-1463 / RT37872)

  - Additions to the YANG model:
     cable video / table-based / vcg * / rf-channel * / session * group ? source ? bit-rate
    (CISCOIOS-1461 / RT37856)

  - Additions to the YANG model:
     cable video / logical-edge-device * / protocol gqi
     cable video / logical-edge-device * / protocol gqi / keepalive
     cable video / logical-edge-device * / protocol gqi / mac-address
     cable video / logical-edge-device * / protocol gqi / mgmt-ip
     cable video / logical-edge-device * / protocol gqi / server *
    (CISCOIOS-1460 / RT37853)

  - Additions to the YANG model:
     cable video / mgmt-intf VirtualPortGroup
     cable video / multicast-uplink ? rp
    (CISCOIOS-1459 / RT37852)

  - Additions to the YANG model:
     cable rphy link redundancy
    (CISCOIOS-1458 / RT37850)

  - Additions to the YANG model:
     interface * / ip verify unicast reverse-path allow-self-ping
    (CISCOIOS-1457 / RT37849)

  - Additions to the YANG model:
     interface * / no isis hello padding
    (CISCOIOS-1456 / RT37848)

  - Additions to the YANG model:
     interface Downstream-Cable*
    (CISCOIOS-1455 / RT37847)

  - Additions to the YANG model:
     ip multicast vrf * rpf select global group-list *
    (CISCOIOS-1454 / RT37846)

  - Additions to the YANG model:
     ptp clock ordinary domain * / clock-port * g8275.2
     ptp clock ordinary domain * / clock-port * / sync one-step
    (CISCOIOS-1453 / RT37844)

  - Additions to the YANG model:
     ptp r-dti *
     ptp r-dti * / clock-port *
     ptp r-dti * / clock-port * / clock source ip
     ptp r-dti * / clock-port * / clock source ip ? alternate
     ptp r-dti * / profile
     ptp r-dti * / ptp-domain
    (CISCOIOS-1452 / RT37843)

  - Additions to the YANG model:
     cable depi fragment
     cable downstream ofdm-prof-mgmt rxmer-poll-interval
     cable profile rpd-event * / enable-notify
     cable profile rpd-event * / priority *
     cable reconcilation enable
     cable reconcilation time
     cable upstream ofdma-prof-mgmt prof-upgrade-auto
     cable virtual-service-group * upstream-cable
     cable wideband rphy-auto-reset
    (CISCOIOS-1451 / RT37841)

  - Additions to the YANG model:
     interface * / srlg gid *
    (CISCOIOS-1450 / RT37840)

  - Additions to the YANG model:
     flow record * / collect ipv6 destination address
     flow record * / collect ipv6 destination prefix
     flow record * / collect ipv6 source address
     flow record * / collect ipv6 source prefix
    (CISCOIOS-1448 / RT37830)

  - Improved the YANG model:
     cable profile ssd * / ssd
    (CISCOIOS-1443 / RT37776)

Corrections:

  - Fixed problems with NedLocks and interface * / ip flow monitor
    (CISCOIOS-1436 / RT37710)


cisco-ios v6.26.2 [2019-06-21]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     isdn leased-line BRI* 128
    (CISCOIOS-1447 / RT37808)

  - Added new static warning exception (matches any lower/upper-case):
     "policy attached to main[-]interface may not function as expected"
    (CISCOIOS-1445 / RT37774)

  - Additions to the YANG model:
     cable profile ssd *
     cable profile ssd * / description
     cable profile ssd * / ssd
    (CISCOIOS-1443 / RT37776)

  - Implemented NedLocks to solve problem with flow monitor being locked by
     interface * / ip flow monitor and flow record being locked by flow monitor * /
     record configuration. NED will temporarily detete the locking config, and
     restore once the desired changes are complete.
    (CISCOIOS-1436 / RT37710)

Corrections:

  - Fixed order dependency, correct order is:
     isdn leased-line
     interface *
    (CISCOIOS-1444 / RT37773)


cisco-ios v6.26.1 [2019-06-16]
===============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

    WARNING: This version has temporarily raised NSO min versions.
                      Upgrade to v6.26.2 if this is an issue.

Enhancements:

  - Additions to the YANG model:
     l2vpn xconnect context * / description
    (CISCOIOS-1442 / RT37759)

  - Fixed problem with interface * / speed & duplex configuration with upcoming
     NSO release on 6.7 branch, which no longer randomly includes multiple spaces
     on output diffset between leaves and their value.
    (CISCOIOS-1441)

Corrections:

  - Put back missing tailf:cli-when-target-delete annotation on interface * /
     speed tailf:cli-diff-set-after "../speed-auto/speed/auto".
    (CISCOIOS-1441)

  - NSO minimum version temporarily raised due to CISCOIOS-1425:
      NSO-4.4 branch from 4.4.5 to 4.4.7
      NSO-4.5 branch from 4.5.1.1 to 4.5.7
      NSO-4.6 branch from 4.6.1 to 4.6.2
      NSO-4.7 branch from 4.7 to 4.7.1


cisco-ios v6.26 [2019-06-14]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Complete redesign of router bgp * / neighbor dependency calculation.
     Removed over 200+ internal tailf:cli-diff-xxx annotations and switched
     to use a handful of new diff-deps in combination with Java to compress
     and sort the NSO neighbor deletes. The result is a more robust, smaller
     and consequently faster commit.
    (CISCOIOS-1439)

  - Work around for IOS not echoing characters in lines longer than 253 chars.
    (CISCOIOS-1435 / RT37706)

  - Introduced the first turbo-parser extension in output transformations.
     This will require the NSO min version in the NSO branches to be raised.
    (CISCOIOS-1425)

Corrections:

  - Updated NEDCOM to version 1.81.
     Contains a fix for a turbo parser filtering issue (with list keys
     containing both '/' and ':') which could cause out of sync issues
     when doing commit no-overwrite.
    (CISCOIOS-1437)

  - Throw apply Exception in prepare phase on device reply:
     "password/key will be truncated to \\d+ characters"
    (CISCOIOS-1435 / RT37706)


cisco-ios v6.25.1 [2019-06-07]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Improved the bgp neighbor recreate code by redeploying all address-family
     ipv4 unicast / neighbor * configuration with neighbor peer-group change.
    (CISCOIOS-1433 / RT37689)

  - Additions to the YANG model:
     ethernet cfm domain * level / service * vlan-id
     interface * / ethernet backup interface
     interface * / loopback mac
     no process cpu autoprofile hog
    (CISCOIOS-1432 / RT37659)

  - Improved and future-proofed progress reports for the upcoming NSO
     Maapi.reportDeviceProgress method.
    (CISCOIOS-1431)

  - Additions to the YANG model:
     interface * / qos-config scheduling-mode min-bw-guarantee
    (CISCOIOS-1429 / RT37652)

Corrections:


cisco-ios v6.25 [2019-06-04]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Removed range annotation on rpd-ds list port key in:
     cable rpd * / core-interface Te* / rpd-ds * downstream* *
    (CISCOIOS-1430 / RT37656)

  - Added new ned-setting 'auto bgp-nbr-password-patch' (default true) to
     send a dummy password reset when setting router bgp neighbor password
     or sometimes IOS will not be able to set the password.
    (CISCOIOS-1428)

  - Improved the bgp neighbor recreate code by also redeploying address-family
     ipv4 unicast / neighbor * activate entry if existing.
    (CISCOIOS-1427 / RT37651)

  - Additions to the YANG model:
     ethernet cfm domain * level / id
    (CISCOIOS-1426 / RT37650)

  - Removed all ned-data annotations in the YANG model.
    They are deprecated and not used by any NSO version supported by this NED.
    Consequently, support for NSO 4.2 and 4.3 is now technically discontinued.
    (CISCOIOS-1424)

Corrections:

  - Bug-fixed the 'router bgp neighbor recreate code if peer-group is modified'
     code added in previous version 6.24.1.
    (CISCOIOS-1418 / RT37562)


cisco-ios v6.24.1 [2019-06-03]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Temporarily remove all flow monitor * / exporter * referencing
     a flow exporter if its export-protocol is modified in order to
     avoid throw error on 'Failed to change export protocol'.
    (CISCOIOS-1422 / RT37628)

  - Send newline and retry login authentication if device output
     'Authorization failed' in ned default connector.
    (CISCOIOS-1421 / RT37623)

  - Do not ignore device error on interface switchport command,
     only ignore 'Incomplete command' in order to support ios891.
    (CISCOIOS-1420 / RT37585)

  - Additions to the YANG model:
     flow record * / collect datalink mac
     flow record * / collect ipv4 fragmentation flags
     flow record * / collect ipv6 fragmentation flags
     flow record * / collect routing is-multicast
    (CISCOIOS-1419 / RT37594)

  - Delete and recreate 'router bgp * / neighbor *' top list if peer-group
     is reconfigured.
    (CISCOIOS-1418 / RT37562)

  - Additions to the YANG model:
     cts server test * no-enable (no cts server test * enable)
     cts server test * deadtime
     cts sxp retry period
    (CISCOIOS-1417 / RT37547)

  - Additions to the YANG model:
     controller E1|T1|T3 * / ais-shut
    (CISCOIOS-1415 / RT37518)

  - Additions to the YANG model:
     pseudowire-class * / ip pmtu
    (CISCOIOS-1414 / RT37486)

  - Additions to the YANG model:
     cable rpd * / core-interface Hu*
    (CISCOIOS-1413 / RT37475)

Corrections:

  - Fixed order dependency, correct order is:
     line * / no authorization commands *
     no aaa new-model
    (CISCOIOS-1423 / RT37637)

  - Restored order dependency, correct order is:
     router bgp * / address-family * / no neighbor *
     router bgp * / address-family * / no neighbor * remote-as
     router bgp * / address-family * / no neighbor * peer-group
    (CISCOIOS-1416 / RT37543)


cisco-ios v6.24 [2019-05-17]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     crypto gdoi group * / server local / authorization
     crypto gdoi group * / server local / no split-resilency
     crypto gdoi group * / server local / no gdoi
     crypto gdoi group * / server local / gikev2
     crypto gdoi group * / server local / redundancy / protocol version
     crypto gdoi group * / server local / rekey algorithm
     crypto gdoi group * / server local / rekey sig-hash algorithm
     crypto gdoi group * / server local / sa ipsec * / replay counter time
     crypto gkm group * / server local /
     crypto gkm group * / server local / address ipv4
     crypto gkm group * / server local / authorization
     crypto gkm group * / server local / gikev2
     crypto gkm group * / server local / no gdoi
     crypto gkm group * / server local / no split-resilency
     crypto gkm group * / server local / redundancy
     crypto gkm group * / server local / rekey
     crypto gkm group * / server local / replay
     crypto gkm group * / server local / sa ipsec *
    (CISCOIOS-1410 / RT37438)

  - Additions to the YANG model:
     cable rpd * / core-interface Te* / network-delay dlm
     cable rpd * / core-interface Te* / network-delay static
     cable upstream controller-profile * / cable ofdma-frequency-exclusion-band
    (CISCOIOS-1408 / RT37422)

  - Added new static warning exception (matches any lower/upper-case):
     "remove wrr queue cos map on all.* ports"
    (CISCOIOS-1406 / RT37258)

  - Complete redesign of interface * / switchport dependency calculation.
     The new method will move all deletes before layer change (sending
     of 'switchport' or 'no switchport') and send all creates after.
     Interface commands showing 'no xxx' but nothing when set, will use
     the reverse method, i.e. set before change and delete after.
     Also discontinued use of old cli-diff-dependency annotations in
      switchport config. Known NSO dependency bugs are corrected in Java.
    (CISCOIOS-1405)

  - Pre-inject delete of ethernet ring g8032 exclusion-list|inclusion-list
     in order to honor both delete and add dependencies in one transaction.
    (CISCOIOS-1386 / RT37193)

  - Added new ned-setting 'live-status always-show-exec-command'
     Normally sent command is only shown if multiple commands are sent
     using ';' delimiter in 'live-status exec any' action. But if this
     setting is set to true, single commands are also shown. Default false.
    (CISCOIOS-1385 / RT37169)

Corrections:

  - API CHANGE:
     class-map * / match vlan *
      replaced cli-range-list-syntax annotation with cli-list-syntax in order
      to allow policy-map using the class-map to be accepted by interface
      service-policy config. In order to still support vlan ranges, a word
      type is added in the leaf-list using the union type.
      WARNING: This API change may cause a great number of match vlan entries
               to be sent unless the CDB is updated with a range instead of
               single entries.
    (CISCOIOS-1412 / RT37453)

  - Fixed order dependency between switchport interface and backup interface.
    Fixed delete of interface / switchport backup list entry.
    (CISCOIOS-1411)


cisco-ios v6.23.2 [2019-05-14]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Delete old default oper data in CDB instead of throwing null Exception.
    (CISCOIOS-1390 / RT37258)


cisco-ios v6.23.1 [2019-05-13]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Trim "no shutdown" in Vlan interface if interface is deleted in same
     transaction in order to avoid interface status being temporarily up.
    (CISCOIOS-1404 / RT37343)

Corrections:


cisco-ios v6.23 [2019-05-10]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip spd queue max-threshold
     ip spd queue min-threshold
     no ipv6 mld snooping
     ipv6 mld state-limit
     mls rate-limit unicast ip icmp unreachable acl-drop
    (CISCOIOS-1403 / RT37339)

  - Added "auto" enum option to extended-parser ned-setting and changed
     default from "disabled" to "auto" to make input parsing more robust.
    (CISCOIOS-1401)

  - Do not write login passwords in the NED trace with log-verbose true.
    (CISCOIOS-1399)

  - Replaced NedSecrets NAVU code with CdbSession.getObjects for improved
     performance and replace a deprecated API.
    (CISCOIOS-1397)

Corrections:

  - API CHANGE:
     connect *
      Changed list key from "interface1 interface2" to "name".
    (CISCOIOS-1402 / RT37339)

  - Fixed create interface order dependency problem, correct order is:
     no switchport
     ip ospf .*
     ip pim .*
    (CISCOIOS-1398 / RT37288)

  - Do not modify output data if data is empty (e.g. in abort with no diff)


cisco-ios v6.22.2 [2019-05-03]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Statically disabled upgrade code of ned-settings (from v5.x -> v6.x) due
     to performance reasons. Set ENABLED to true in UpgradeNedSettings.java
     and recompile if needed.
    (CISCOIOS-1396)

  - Replaced NedDefaults NAVU code with Maapi for increased performance.
    (CISCOIOS-1395)

  - Additions to the YANG model:
     cable downstream controller-profile * / broadcast
    (CISCOIOS-1394 / RT37273)

  - Additions to the YANG model:
     radius server * / non-standard
     radius server * / retransmit
     radius server * / timeout
    (CISCOIOS-1391 / RT37262)

  - Show config mode in ApplyException when a config command fails on device.
    (CISCOIOS-1389)

  - Strip superfluous 'no logging console' from show running when reading
     config to avoid unmodeled info with extended-parser.

Corrections:

  - Fixed key pattern in interface_list.gili template, restoring live-status
     /ios-stats:crypto/session/interface
    (CISCOIOS-1393 / RT37272)

  - Fixed double detach from to-transaction when prepare-dry got Exception.
    (CISCOIOS-1392)

  - Delete default oper data if interface is missing in show running-config
    Delete default oper data if config is missing in the CDB
    (CISCOIOS-1390 / RT37258)


cisco-ios v6.22.1 [2019-04-26]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added config-hash-modeled option to read transaction-id-method ned-setting.
     The setting is the same as config-hash except that it strips all unmodeled
     config before hash calculation. This may be useful if unmodeled config is
     being altered dynamically or out of band.
    (CISCOIOS-1384)

  - Updated and restructured README file with new ned-settings
     + a new section on internal live-status commands.
    (CISCOIOS-1383)

  - Improved output (input buffer + patterns) for all read timeout Exceptions.
    (CISCOIOS-1381)

Corrections:

  - Removed default value from:
     interface * / carrier-delay up|down seconds
    (CISCOIOS-1386 / RT37193)


cisco-ios v6.22 [2019-04-19]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     crypto ikev2 name-mangler * / eap suffix delimiter
     utd engine standard multi-tenancy / policy * / all-interfaces
    (CISCOIOS-1377 / RT37099)

  - Always treat password in below config as encryped, to avoid uncaching NedSecret:
     utd engine standard / threat-inspection / signature update server cisco
    (CISCOIOS-1376)

Corrections:

  - API CHANGE:
     netconf-yang / cisco-odm actions *
      Changed from leaf to leaf-list to support multiple entries.
    (CISCOIOS-1378)

  - Fixed order dependency problem, must delete vrf first when moving it:
     utd engine standard multi-tenancy / policy * / vrf *
    (CISCOIOS-1373 / RT37080)

  - Fixed delete problem with:
     no mpls ip propagate-ttl forwarded
    (CISCOIOS-1372 / RT37082)


cisco-ios v6.21.1 [2019-04-15]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     cable oob / static-pseudowire *
     cable oob / static-pseudowire * / dest-ip
     cable oob / static-pseudowire * / server-ip
     cable oob / static-pseudowire * / session-id
     cable rpd * / rpd-55d1-us-event
     controller downstream-oob * id * /
     controller upstream-oob * id * /
     cable rpd * / core-interface Te* / rpd-ds * static-pseudowire
     cable rpd * / core-interface Te* / rpd-us * static-pseudowire
    (CISCOIOS-1371 / RT37047)

  - Additions to the YANG model:
     crypto ipsec profile * / description
     login quiet-mode access-class
     redundancy / application redundancy / group * / timers
    (CISCOIOS-1368 / RT37029)

  - Additions to the YANG model:
     acr *
     controller SONET-ACR *
     controller SONET-ACR * / aps group acr
     controller SONET * / aps protect *
     controller SONET-ACR * / aps working
     controller SONET[-ACR] * / au-4 * tug-3 * / tug-2 * e1 * description
     interface CEM-ACR*
     platform acl egress-disable
    (CISCOIOS-1365 / RT36991)

  - API CHANGE:
     controller SONET * / au-4 * tug-3 * / tug-2 * e1 * cem-group *
     interface CEM* / cem *
      list key leaf 'id' type changed from uint8 to uint16.
    (CISCOIOS-1365 / RT36991)

Corrections:

  - Improved 'ip explicit-path index input transformation to work with blank
     lines and only be called for the top line.

  - Restored full sync-from-file device transformations broken in 6.20.3.


cisco-ios v6.21 [2019-04-10]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     aaa server radius dynamic-author / port
     aaa server radius dynamic-author / auth-type
     aaa server radius dynamic-author / ignore
    (CISCOIOS-1364 / RT36978)

  - Added ordered-by user and tailf:cli-show-long-obu-diffs on:
     aaa server radius dynamic-author / client *
    (CISCOIOS-1364 / RT36978)

  - Upgraded nedcom.jar to release-1.73.

Corrections:

  - API CHANGE:
     ipv6 cef load-sharing algorithm
      Remodelled to support all configuration variations + include-ports
    (CISCOIOS-1367 / RT37014)

  - API CHANGE:
     Fixed problems modifying|deleting router ospf[v3] * redistribute config.
    (CISCOIOS-1366)


cisco-ios v6.20.4 [2019-04-08]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     wan mode
     no bridge * bridge clns
     no bridge * bridge ip
    (CISCOIOS-1363 / RT36969)

Corrections:

  - Fixed interface order dependency problems, correct order is:
     bfd echo
     no bfd interval
       and
     no dot1x pae
     no switchport mode acces
       and
     no ip pim
     switchport
       and
     no mab
     no switchport mode acces
       and
     no dot1x timeout
     no dot1x pae
    (CISCOIOS-1362 / RT36964)


cisco-ios v6.20.3 [2019-04-05]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added support for NSO 5.1 config CLI command 'load-native-config' which
     can be used to load and convert device config into ncs_cli config mode:
     admin@ncs(config)# devices device <device> load-native-config data|file
    Note: Even with netsim the data is parsed as it was in native IOS format.
    (CISCOIOS-1360)

  - Added support for new NSO 5.1 'verbose' flag to sync-from CLI command,
     printing additional info like unknown commands etc. in the CLI.
    (CISCOIOS-1360)

  - Additions to the YANG model:
     platform ipsla classify cpu packets
    (CISCOIOS-1357 / RT36937)

  - Added a second group leaf (YANG: <group2>/<group>) in:
     aaa authorization exec *
     aaa authorization commands *
     aaa authorization eventmanager *
     aaa authorization network *
    (CISCOIOS-1356 / RT36934)

  - Additions to the YANG model:
     device-tracking policy *
     device-tracking policy * / device-role
     device-tracking policy * / limit address-count
     device-tracking policy * / no protocol udp|arp|ndp
     device-tracking policy * / tracking
     device-tracking policy * / trusted-port
     interface * / device-tracking attach-policy
     interface * / dot1x timeout supp-timeout
     interface * / ipv6 nd inspection
     template * / access-session closed
     template * / access-session control-direction
     source template
    (CISCOIOS-1353 / RT36930)

  - Added new ned-setting 'write config-output-retry-interval'. Set to change
     the default retry sleep interval from 1000 milliseconds to user defined.
    (CISCOIOS-1352 / RT36929)

  - Additions to the YANG model:
     mls cef adjacency-mcast
     mls mpls recir-agg
     mls qos protocol * police
     mpls traffic-eng fast-reroute timers promotion
     route-map * / set ip tos
     router bgp * / bgp advertise-best-external
     router isis * / mpls ldp auto-config
    (CISCOIOS-1350 / RT36893)

  - Additions to the YANG model:
     ip http max-connections
     snmp mib community-map *
    (CISCOIOS-1349 / RT36895)

  - Additions to the YANG model:
     policy-map * / class * / random-detect dscp values *
    (CISCOIOS-1348 / RT36893)

  - Additions to the YANG model:
     radius-server attribute * mac format
    (CISCOIOS-1347 / RT36895)

  - Additions to the YANG model:
     interface SM*
     interface * / service-module fail-open
     interface * / service-module ip address
     interface * / service-module ip default-gateway
    (CISCOIOS-1346 / RT36877)

Corrections:

  - Added ordered-by user and tailf:cli-show-long-obu-diffs on:
     aaa group server tacacs+ * / server name *
    (CISCOIOS-1361 / RT36962)

  - Fixed order dependency, correct order is:
     router ospf * / no redistribute route [* route-map]
     no route-map
    (CISCOIOS-1358 / RT36938)

  - Fixed interface order dependency problems, correct order is:
     interface * / no snmp trap mac-notification
     interface * / no ipv6 nd raguard
     interface * / no ipv6 nd inspection
     interface * / no switchport
    (CISCOIOS-1353 / RT36930)

  - Added multi-word-key to key leaf id in order to support whitespaces in name
     ip dhcp pool *
    (CISCOIOS-1349 / RT36895)

  - API CHANGE:
     policy-map * / class * / random-detect dscp-based * mark-probability
      Added 'tailf:alt-name mark-prob' since device shows leaf abbrev.
    (CISCOIOS-1348 / RT36893)


cisco-ios v6.20.2 [2019-03-29]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip multicast hardware-switching replication-mode
     ipv6 multicast hardware-switching replication-mode
    (CISCOIOS-1344 / RT36848)

  - Added new ned-setting to enable progress reports in potentially time
     consuming code in order to get a better overview of time spent. The
     first Verbosity.NORMAL progress reports implemented in the NED are:
      - modifying output - prepare[-dry] phase
      - sending config   - prepare phase
      - reading config   - initialize|get-trans-id|show phases
      - extended parsing - show phase
      - reconnecting     - prepare|commit|keepalive phases
     Set 'cisco-ios developer progress-verbosity' ned-setting to enable.
     Notes:
      - NED progress reports are not supported in all NSO versions.
      - NED progress is not shown in 'commit | details <verbosity level>'
         yet due to NSO limitations. File a ticket if needed.
    (CISCOIOS-1343)

  - Added 'write config-dependency' ned-setting list in order to be able to
     dynamically fix diff dependency (config order) problems in prepare[-dry]
     phase. This setting should be used for urgent fixes only because the NED
     should be able to handle all dependencies correctly without this setting.
     Hence if you stumble upon a case where this setting is needed, you must
     report the dependency bug with a raw trace to postsales so we can fix it
     in the NED statically as well.
    (CISCOIOS-1342)

Corrections:

  - API CHANGE:
    Added presence true and delete-when-empty in order to fix delete bug with:
     ethernet ring g8032 * / port0 interface
     ethernet ring g8032 * / port1 interface
    (CISCOIOS-1345 / RT36868)

  - Fixed bug in out-transforming:
     cable profile service-group * / mac-domain * / downstream sg-channel * upstream *


cisco-ios v6.20.1 [2019-03-22]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     flow record * / match datalink vlan
    (CISCOIOS-1338 / RT36775)

  - Added new developer ned-setting 'trace-level' and moved some traceVerbose
     calls to traceDebug2 and traceDebug3 level to save time and trace size.
    (CISCOIOS-1337)

  - Fixed order dependency problem, correct order is:
     route-map * / no match interface *
     no interface *
    (CISCOIOS-1333 / RT36731)

  - Implemented NedDiff class in order to Java-sort config dependencies NSO
     fails to sort using diff annotations.
    (CISCOIOS-1334)

Corrections:

  - Added non-strict leafref on:
     vlan * / private-vlan association *
    (CISCOIOS-1336)

  - Replaced a vlan * / name cli-diff-dependency with cli-diff-delete-before
     due to the older one causing serious NSO performance issues. Because of
     this, also had to raise the NSO min version from 4.4.2 to 4.4.5 and 4.5
     to 4.5.2; due to the new dependencies not working properly in older NSO.
    (CISCOIOS-1335)


cisco-ios v6.20 [2019-03-17]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     no hw-module module * logging onboard
     NOTE: Use 'hw-module module no-list * logging onboard' in CDB.
           It will be transformed to proper device format.
    (CISCOIOS-1314 / RT36607)

  - Additions to the YANG model:
     platform resource
    (CISCOIOS-1327 / RT36642)

  - Additions to the YANG model:
     flow exporter * / option vrf-table
     flow record * / collect interface output snmp
     flow record * / collect interface output physical snmp
     flow record * / match routing vrf input
     flow record * / match routing vrf output
    (CISCOIOS-1326 / RT36641)

  - Improved rebuilding of NED since the pyang to yanger change (CISCOIOS-1283)
    (CISCOIOS-1310)

  - Raised 'cisco-ios write config-output-max-retries' ned-setting default
     from 90 to 180.

Corrections:

  - Fixed policy-map / class / bandwidth changes order dependencies.
     Prioritize change if class bandwidth is lowered and vice versa.
    (CISCOIOS-1331 / RT36712)

  - API CHANGE:
     policy-map * / class * / compress header ip
      Remodelled due to bad modelling.

  - Always delete main interface channel-group first.
     (or sub-interface not accessible)


cisco-ios v6.19 [2019-03-08]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Improved the EXPERIMENTAL apply-reboot-timer ned-setting:
     Removed the optional waiting for SHUTDOWN banner and changed the possible
     timeout values to avoid an immediate banner, thereby avoiding blocking if
     no initial banner was output (which happened on e.g. cat4500 and cat6500).
    (CISCOIOS-1325)

  - Optimized list modification performance for the following config:
     ip community-list standard *
     ip community-list expanded *
     access-list *
      Also fixed support for access-list modification with sequence numbers.
    (CISCOIOS-1324)

  - Additions to the YANG model:
     event manager applet * / trigger occurs|period|period-start|delay
    (CISCOIOS-1320 / RT36615)

  - Additions to the YANG model:
     interface * / ppp pap sent-username
    (CISCOIOS-1318 / RT36612)

  - Additions to the YANG model:
     interface * / dialer hold-queue
    (CISCOIOS-1317 / RT36611)

  - Additions to the YANG model:
     interface * / dialer remote-name
    (CISCOIOS-1316 / RT36610)

  - Removed string pattern "primary.+" on:
     interface * / isdn switch-type
    (CISCOIOS-1315 / RT36609)

  - Additions to the YANG model:
     interface * / tx-queue * / shape
    (CISCOIOS-1314 / RT36613)

  - Additions to the YANG model:
     interface * / dialer pool-member *
    (CISCOIOS-1312 / RT36608)

  - Improved access-list-resequence access-list insert performance in
     PREPARE[-DRY] by replacing NAVU list read code with Maapi.getObjects().
    (CISCOIOS-1311)

  - Additions to the YANG model:
     interface * / ip rsvp signalling hello graceful-restart neighbor *
    (CISCOIOS-1309 / RT36582)

  - Upgraded nedcom.jar to release-1.68.

Corrections:

  - API CHANGE:
     event manager applet * / trigger / correlate
      Changed from hard-coded model to multi-vale string in order to support
       an unlimited number of events.
    (CISCOIOS-1319 / RT36614)

  - Fixed service-fragment and interface order dependency, correct order is:
     (sub)interface * / no service-policy
     (parent)interface * / no service-policy
    (CISCOIOS-1273 / RT36172)


cisco-ios v6.18.2 [2019-03-04]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     cable profile service-group * / load-balance docsis-group * profile *
     cable profile service-group * / load-balance docsis-group * profile * / downstream sg-channel *
     cable profile service-group * / load-balance docsis-group * profile * / upstream * sg-channel
    (CISCOIOS-1307 / RT36538)

  - Improved the duplicate name check with ordered and unordered ip access-lists
     in PREPARE phase to also perform name check when commiting unordered lists.
    (CISCOIOS-1306 / RT36536)

  - Upgraded nedcom.jar to release-1.67.

Corrections:


cisco-ios v6.18.1 [2019-03-01]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added duplicate name check with ordered and unordered ip access-lists
     in PREPARE phase, i.e. when commiting config.
    (CISCOIOS-1306 / RT36536)

  - Removed static warning/error exception for (lower case):
     "can't delete last \\d+ vty lines"
      If still needed, configure with 'write config-warning' ned-setting list.
    (CISCOIOS-1305 / RT36324)

  - Added new ned-setting 'api expanded-line-vty-format' to support individual
     editing of 'line vty <start> <end>' entries in show running-config. This
     will fix all issues with overlapping line vty entries in show running.
     Note: when this ned-setting is set to true, then the dual-key line vty
           list is disabled and only the 'vty-single-conf' line is used.
    (CISCOIOS-1304 / RT36520)

  - Additions to the YANG model:
     interface ATM* / pvc * / vbr-nrt
    (CISCOIOS-1303 / RT36494)

  - Upgraded nedcom.jar to release-1.66, needed for live-status with CDM.

Corrections:

  - Restored partial source code rebuilding by allowing rebuild within same
     NSO branch.
    (CISCOIOS-1299)

  - Made it possible to inject config in sub-interfaces with additional leaf,
     e.g. 'interface ATM0/0/0.1 point-to-point'


cisco-ios v6.18 [2019-02-22]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added 'tailf:ned-default-handling report-all' and default value 10 0
     on line * / exec-timeout to avoid diff with older NED versions.
    (CISCOIOS-1297)

  - API CHANGE:
     connect * / xconnect
     interface * / xconnect
     interface CEM* / cem * / xconnect
     interface ATM* / pvc * / xconnect
     interface * / service instance * ethernet / xconnect
      Reverted the !exit mode-fix made in (CISCOIOS-1279 / RT36216),
       instead, always send exit unless the xconnect misses encapsulation.
      Removed non-mode xconnect-pw-class/xconnect config and instead added
       remove-before-change and optional-in-sequence on xconnect encapsulation.
    (CISCOIOS-1296)

  - Using the new GILIParser buf-replace-all annotation from NEDCOM.
    (CISCOIOS-1295)

  - Additions to the YANG model:
     router lisp / disable-ttl-propagate
     router lisp / site-id
     router lisp / instance-id * / disable-ttl-propagate
     router lisp / instance-id * / loc-reach-algorithm rloc-probing
     router lisp / ipv4 etr accept-map-request-mapping
     router lisp / ipv4 map-cache * drop
    (CISCOIOS-1285 / RT36288)

Corrections:

  - Trim all router bgp changes if router bgp is deleted in the same commit.
    (CISCOIOS-1300 / RT36435)

  - API CHANGE:
     connect * / xconnect
     interface CEM* / cem * / xconnect
     interface ATM* / pvc * / xconnect
      Removed unavailable 'xconnect / mpls|mtu|remote' config.
      Note: this will also get rid of the invalid 'no mpls control-word'
    (CISCOIOS-1296)

  - Removed cli-incomplete-command annotation on:
     router lisp / * / database-mapping *
    (CISCOIOS-1285 / RT36288)

  - Reverted the (CISCOIOS-933 / RT32324) fix where ! was sent instead of exit
     on interface xconnect config. Instead, throw an Exception if mtu command
     leaves the sub-mode.
    (CISCOIOS-1279 / RT36216)


cisco-ios v6.17 [2019-02-15]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added 'auto compress-spanning-tree-vlan' to enable compress of spanning-tree
     vlan when created or deleted. Default set to false to support C800 device.
    (CISCOIOS-1294 / RT36339)

  - Additions to the YANG model:
     interface * / spanning-tree port-priority
    (CISCOIOS-1292 / RT36328)

  - Trim non-config 'cpp system-default' when reading config from device.
    (CISCOIOS-1292 / RT36328)

  - Additions to the YANG model:
     radius-server load-balance method least-outstanding batch-size
    (CISCOIOS-1290 / RT36316)

  - Additions to the YANG model:
     ip nat service sip tcp *
     ip nat service sip udp *
     mpls traffic-eng fault-oam
     mpls traffic-eng logging tunnel lsp-switchover
    (CISCOIOS-1289)

  - Additions to the YANG model:
     aaa accounting multicast default
     aaa accounting onep default
    (CISCOIOS-1288)

  - Additions to the YANG model:
     ptp clock boundary domain *
     ptp clock ordinary domain * / profile
    (CISCOIOS-1287)

  - Additions to the YANG model:
     ptp clock ordinary domain * / clock-port * / clock source <ip> *
     ptp clock ordinary domain * / clock-port * / delay-req interval
     ptp clock ordinary domain * / clock-port * / sync interval
    (CISCOIOS-1284)

  - Switched from pyang to yanger with turbo mode parser.
    Removed support for ncs-3.4.x due to this change.
    Upgraded to NEDCOM 2019-02-15_10:50:41, commit c3ab0b89
    (CISCOIOS-1283)

Corrections:

  - API CHANGE:
     interface * / ip flow monitor * input|output
      Added leaf 'name' to list key in order to support multiple entries.
    (CISCOIOS-1293 / RT36337)

  - Fixed another blocking issue with the new experimental apply-reboot-timer option
     which happened when devices did not output an immediate SHUTDOWN notice.
    (CISCOIOS-1286)


cisco-ios v6.16 [2019-02-08]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / service instance * ethernet / cfm mep / alarm notification
    (CISCOIOS-1282 / RT36259)

  - Additions to the YANG model:
     crypto isakmp aggressive-mode disable
    (CISCOIOS-1281 / RT36243)

  - Added UNTESTED internal live-status command 'accept-eula <seconds to wait>'
    NOTE:
     You must also add
       {"expect": "ACCEPT \\[y/n\\]","action": "y\n", "nextState": "login"},
     to the ned-connector-default.json template "login" state in order to accept
     the agreement when connecting to the device.
    (CISCOIOS-1278 / RT36206)

  - Additions to the YANG model:
     ap dot11 airtime-fairness policy-name *
     flow record * / collect counter bytes layer2 long
     flow record * / match datalink mac
     flow record * / match transport icmp ipv4
     interface * / private-vlan mapping
     interface * / switchport private-vlan host-association
     interface * / switchport private-vlan mapping *
     ip admission watch-list expiry-time
     l2tp congestion-control
     radius server * / automate-tester username
     vlan * / private-vlan association *
     vlan * / private-vlan community
     vlan * / private-vlan isolated
     vlan * / private-vlan primary
    (CISCOIOS-1277 / RT36192)

  - Additions to the YANG model:
     license accept end user agreement force
     router isis * / purge-transmit strict
    (CISCOIOS-1276 / RT36173)

  - Additions to the YANG model:
     snmp-server user * remote
     snmp-server user * remote udp-port
    WARNING: due to snmp-server user not being shown in running-config the NED
             does a best-effort creation of the entries using show snmp user.
             If multiple identical engineID's exist, there will be a mismatch
             causing a config diff/loss when comparing/reading.
    (CISCOIOS-1269 / RT36152)

Corrections:

  - Send "exit" when leaving interface service instance xconnect mode
     interface * / service instance * ethernet / xconnect
      on devices where xconnect is a mode and not a one-liner.
    (CISCOIOS-1279 / RT36216)

  - Fixed a blocking issue with the new experimental apply-reboot-timer option
     for IOS devices which do not output a prompt after the reload message.


cisco-ios v6.15.1 [2019-02-01]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added dual key {id vrf} live-status list:
     bgp vpnv4 unicast all neighbors *
      in order to be able to allow more efficient reading/filtering.
      Note: Use '-' as vrf value for non-vrf entries.

  - Additions to the YANG model:
     policy-map * / class * / service-fragment
    (CISCOIOS-1273 / RT36172)

  - Added remove-before-change to:
     ip local pool *
      In order to be able to expand ip range.
    (CISCOIOS-1270 / RT36161)

  - Upgraded nedcom.jar to release-1.59, needed for live-status fixes.
    (CISCOIOS-1268 / RT33409)

  - Optimized live-status, reducing the number of show calls by use of a new
     NEDCOM meta-value option 'run-after-show':false.
    (CISCOIOS-1267)

  - Added dual key {id vrf} live-status list:
     bgp vpnv4 unicast all neighbors *
      in order to be able to allow more efficient reading/filtering.
      Note: Use '-' as vrf value for non-vrf entries.
    (CISCOIOS-1262 / RT33409)

Corrections:

  - Fixed live-status interfaces-state ip address extraction for NETSIM.
    (CISCOIOS-1272)

  - Fixed order dependency, correct order is:
     router bgp * / no neighbor * update-source
     no interface *
    (CISCOIOS-1271 / RT36166)

  - Fixed two problems with the new experimental apply-reboot-timer option.
    (CISCOIOS-1265)


cisco-ios v6.15 [2019-01-29]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     bridge-domain * / snmp context
    (CISCOIOS-1266 / RT36104)

  - Added experimental ned-setting 'cisco-ios write apply-reboot-timer'
     to automatically reboot the device if a command disables the link
     and the device prompt is never returned.
     The timer is set in minutes and is reset before each command sent
     to device if less than half the time is remaining.
    (CISCOIOS-1265)

  - Additions to the YANG model:
     cable depi multicast pool *
     cable depi multicast pool * / description
     cable downstream controller-profile * / multicast-pool
     cable dynamic-bonding-group
     cable load-balance d30-ggrp-default method
     cable profile rpd-event *
    (CISCOIOS-1235 / RT35714)

  - API CHANGE:
     Removed 'cisco-ios deprecated connection legacy-mode' ned-setting and
      the legacy connect code.
    (CISCOIOS-1264)

Corrections:

  - Fixed order dependencies between aaa new-model and:
     line * / login local
     line * / login authentication
     line * / authorization exec
    (CISCOIOS-1263 / RT36066)


cisco-ios v6.14 [2019-01-23]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     monitor session * filter address-type *
    (CISCOIOS-1258 / RT36022)

  - Additions to the YANG model:
     card * * r-phy
     cable rpd * / type
     cable rpd * / rpd-ds * max-carrier
     cable rpd * / rpd-ds * base-power
    (CISCOIOS-1254 / RT35986)

  - Added live-status:
     crypto session interface *
    (CISCOIOS-1247 / RT35851)

Corrections:

  - Added remove-before-change to:
     route-map * / set ipv6 next-hop *
      In order to be able to delete individual entries correctly.
    (CISCOIOS-1260)

  - Added remove-before-change to:
     route-map * / set ip next-hop *
      In order to be able to delete individual entries correctly.
    NOTE: 'replace-all' was added in 6.0.11, causing an API CHANGE.
    (CISCOIOS-1259 / RT36024)

  - API CHANGE:
     interface * / standby * ip * secondary
      Changed from container to list in order to support multiple entries.
    (CISCOIOS-1257 / RT36009)

  - API CHANGE:
     snmp-server group *
     snmp-server group * context *
      remodelled lists in order to support multiple model and/or level entries.
    (CISCOIOS-1236 / RT35710)


cisco-ios v6.13.4 [2019-01-16]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     platform xconnect load-balance-hash-algo mac-ip-instanceid
    (CISCOIOS-1250 / RT35921)

Corrections:

  - Do not use 'remove' macro with interface * / switchport trunk allowed vlan
     when deleting all remaining vlans.
    (CISCOIOS-1249 / RT35920)


cisco-ios v6.13.3 [2019-01-11]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     no ip nat service H225
     no ip nat service sip tcp 5060
     no ip nat service sip udp 5060
     ip policy-list * / match ip address prefix-list *
     router eigrp * / no eigrp log-neighbor-changes
    (CISCOIOS-1246 / RT35850)

  - Upgraded nedcom.jar to 2019-01-11_15:21:49, 6dbe04eb.
    (CISCOIOS-1244)

Corrections:

  - Added back "interfaces" container and "interfaces-ref" type in
     local copy of ietf-interfaces.yang file.
    Warning: NSO will interpret "interfaces" container as config false
             even though it is not.
    (CISCOIOS-1248 / RT35862)


cisco-ios v6.13.2 [2019-01-04]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     platform time-source ntp
    (CISCOIOS-1243 / RT35816)

Corrections:


cisco-ios v6.13.1 [2018-12-28]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ethernet ring g8032 * / open-ring
    (CISCOIOS-1240 / RT35799)

  - Upgraded nedcom.jar.

Corrections:


cisco-ios v6.13 [2018-12-23]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip cef load-sharing algorithm include-ports
    (CISCOIOS-1237 / RT35749)

  - Additions to the YANG model:
     cable depi multicast pool *
    (CISCOIOS-1235 / RT35714)

  - Raised max range on 'crypto isakmp profile * / virtual-template' from 200
     to 4095 and type from uint8 to uint16.
    (CISCOIOS-1233 / RT35666)

  - Added new ned-setting 'api aaa-accounting-mode-format' to support
     commiting of newer mode format syntax for aaa accounting settings.
    (CISCOIOS-1230 / RT35628)

  - Additions to the YANG model:
     interface * / ipv6 nd inspection vlan *
    (CISCOIOS-1221 / RT35537)

Corrections:

  - API CHANGE:
     router ospf * / mpls traffic-eng area *
      Changed from leaf to leaf-list to support multiple entries.
    (CISCOIOS-1238 / RT35780)

  - API CHANGE:
     snmp-server group *
      Removed context leaf and created a new list with context as key as well in
      order to support multiple entries.
    (CISCOIOS-1236 / RT35710)

  - API CHANGE:
     bridge-domain * / member * <vcid> encapsulation|template
      Added <vcid> to ip-list key.
    (CISCOIOS-1232)

  - Added cli-recursive-delete to bridge-domain/bridge-domain-list list and
    added "Removed below static warning introduced in CISCOIOS-522:
      "bridge-domain \\d+ cannot be deleted because it is not empty""
     since sometimes the entry is deleted and sometimes not.
    (CISCOIOS-1232)

  - Fixed order dependency, correct order is:
     bridge-domain * / no member [ip-list|interface-list]
     bridge-domain * / no member vfi
    (CISCOIOS-1232)

  - Support broadcast in aaa accounting config compacted from mode syntax.
    (CISCOIOS-1230 / RT35628)


cisco-ios v6.12 [2018-12-07]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - The NED now supports the new showStatsPath API for which is implemented in
     newer versions of NSO (currently 4.7.2). This API allows for a more efficient
     fetching of operational data compared with the old APIs. In particular it
     supports direct fetch of a single entry in a list instead of first downloading
     the whole list when reading operational data (provided the device supports
     such operations).
    (CISCOIOS-1226)

  - Removed support for legacy live-status implementation. Remove the
     'cisco-ios deprecated live-status legacy-mode' from your ned-settings.
    (CISCOIOS-1226)

  - Upgraded nedcom.jar to release-1.50.
    (CISCOIOS-1226)

Corrections:

  - Dequote enable config password and strip quotes.
    (CISCOIOS-1225 / RT35545)


cisco-ios v6.11 [2018-11-30]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Fixed interface * / service instance * ethernet / rewrite vs encapsulation
     default dependency issue, correct order is:
     interface *
      service instance * ethernet
       no rewrite ingress tag pop * symmetric
       encapsulation default
    (CISCOIOS-1220 / RT35526)

  - Added missing 'cli-optional-in-sequence', restoring engineid-string leaf
     in 'snmp-server engineID remote *' config.
    (CISCOIOS-1219 / RT35509)

  - API CHANGE:
     Removed 'traffic' enum from type leaf in class-map and created a new list
     (class-map-traffic/class-map) in order to support entries using the same
     name for both a normal class-map and a traffic type class-map.
    (CISCOIOS-1217)


cisco-ios v6.10.1 [2018-11-28]
==============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / crypto ipsec df-bit
    (CISCOIOS-1214 / RT35452)

  - Rewrote ethernet ring dependencies introduced in v6.10 to avoid triggering
     NSO bug which randomly inserted a delete of other config.
    (CISCOIOS-1209 / RT35373)

Corrections:


cisco-ios v6.10 [2018-11-23]
============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     flow record * / match ipv4 destination address
     flow record * / match routing next-hop address ipv4
     flow record * / match transport tcp flags
    (CISCOIOS-1213 / RT35437)

  - Added 3 seconds sleep after 'do clear ip nat translation ' command in
     order for command to take effect.
    (CISCOIOS-1212)

Corrections:

  - Fixed order dependencies, correct order is:
     no ip nat inside|outside
     no ip nat translation
    (CISCOIOS-1212)

  - Added \n to running-config dump before running inject/replace code in order
     to be able to strip first entry with \n in the beginning of the regex.
    (CISCOIOS-1211 / RT35428)

  - API CHANGE:
    Added presence true, delete-when-empty and incomplete-command in order to
     fix delete problem with:
      interface * / service instance * ethernet / cfm mep
    (CISCOIOS-1210 / RT35376)

  - Fixed order dependencies, correct order is:
     interface * / service instance * ethernet /
     ethernet ring * / exclusion-list vlan-ids *
     ethernet ring * / instance * / inclusion-list vlan-ids *
    (CISCOIOS-1209 / RT35373)


cisco-ios v6.9 [2018-11-15]
===========================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Fixed tailf-ned-cisco-ios-oper.yang for CDM support.
    (CISCOIOS-1208)

  - Additions to the YANG model:
     crypto keyring * / pre-shared-key hostname *
     crypto isakmp profile * / match identity user-fqdn *
     crypto isakmp profile * / match identity user-fqdn * <vrf>
    (CISCOIOS-1206 / RT35337)

  - Added a fourth method of configuring access-lists to use with access-lists
     where rule order does not matter. To use:
     1) - Configure a regex (excluding braccets) specifying which lists should
           should reside in the unordered extended list, e.g.:
           ned-settings cisco-ios api unordered-ip-access-list-regex "UNORDERED.+"
          Note: The NED will add the unordered keyword if list matches the regex
                when reading from the device.
     2) - Configure the accesss-list in ip/access-list/unordered list
          Note: when NED commits the list, it will strip the unordered keyword.
    (CISCOIOS-1204 / RT35292)

  -  Added missing default values (looked up on an asr1002, version 15.4):
      flow monitor * / cache type synchronized
      flow monitor * / history size 0
    (CISCOIOS-1103)

Corrections:

  - API CHANGE
     crypto isakmp profile * / match identity host *
     crypto isakmp profile * / match identity host domain *
      changed from leaf to leaf-list
     crypto isakmp profile * / match identity host * <vrf>
     crypto isakmp profile * / match identity host domain * <vrf>
      added vrf leaf to key
    (CISCOIOS-1206 / RT35337)


cisco-ios v6.8 [2018-11-09]
===========================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - API CHANGE:
    Added support for multiple 'l2 vfi * / neighbor *' entries with the same id
     but differnet vcid by removing vcid from original list and creating a new
     list with both id and vcid as list keys.
    (CISCOIOS-1205 / RT35293)

  - Additions to the YANG model:
     interface * / isis metric maximum
     router isis * / mpls ldp ac-igp-shortcuts
     router isis * / mpls ldp sync-igp-shortcuts
    (CISCOIOS-1202 / RT35229)

  - Do not log MAAPI decryption Exception since $8 may also be IOS encrypted.
    (CISCOIOS-1194 / RT35164)

Corrections:

  - Fixed dependency problems with deleting and adding an access-list of the
     same name but different type (standard|extended) in the same transaction.
    (CISCOIOS-1203 / RT35235)


cisco-ios v6.7 [2018-11-02]
===========================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     ip identd
    (CISCOIOS-1201 / RT35214)

  - Additions to the YANG model:
     cdp filter-tlv-list
     interface * / cdp filter-tlv-list
    (CISCOIOS-1200 / RT35206)

  - Additions to the YANG model:
     cdp tlv-list *
    (CISCOIOS-1199 / RT35205)

  - Raised mtu from 9100 to 9216 on
     interface * / xconnect encapsulation / mtu
    (CISCOIOS-1198 / RT35203)

  - Inject 'do terminal length 0' when line * / length is modified to avoid
     blocking on pagination due to new terminal length.
    (CISCOIOS-1197 / RT35197)

Corrections:

  - API CHANGE:
     mpls ldp advertise-labels for
      Changed from container to list to support multiples entries.
    (CISCOIOS-1196 / RT35188)

  - API CHANGE:
     call-home / profile * / destination address http *
     call-home / profile * / destination address email *
      Changed from leaf to leaf-list to support multiple entries.
    (CISCOIOS-1195 / RT35180)

  - cable profile service-group * / mac-domain * / downstream sg-channel * upstream
     Dequote and quote upstream value list when writing/reading to/from device.
     NOTE: A type string and not type leaf-list due to NSO YANG limitations.
    (CISCOIOS-1191 / RT35086)


cisco-ios v6.6.2 [2018-10-26]
=============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / tunnel mpls traffic-eng auto-bw frequency|max-bw|min-bw
     interface * / tunnel mpls traffic-eng autoroute metric
     interface * / tunnel mpls traffic-eng backup-bw
    (CISCOIOS-1193 / RT35093)

  - Do not show developer prepare-dry-model in dry-run output unless log-verbose
    (CISCOIOS-1177)

Corrections:


cisco-ios v6.6.1 [2018-10-23]
=============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

Corrections:

  - Moved banner input transformations in order to preserve multiple newlines.
    (CISCOIOS-1172 / RT34806)


cisco-ios v6.6 [2018-10-22]
===========================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Upgraded nedcom.jar to release-1.46, prepared for NSO 5.0 with CDM support.
    (CISCOIOS-1189)

  - Added support for reconnecting in e.g. commit/keepAlive using TELNET too.
    (CISCOIOS-1185 / RT34799)

Corrections:

  - Fixed %i substitution in config archive file name.
    (CISCOIOS-1188 / RT35052)

  - Fixed order dependency, correct order is:
     tacacs server <name>
     aaa group server tacacs+ * / server name <name>
    (CISCOIOS-1182 / RT35003)


cisco-ios v6.5 [2018-10-19]
=============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - API CHANGE:
    Switched to use device write-timeout setting for the following operations:
     #1 All actions, i.e. live-status exec any <command> and config exec <command>
     #2 Config Archiving
    (CISCOIOS-1186)

  - API CHANGE:
    show, getTransId, applyConfig, prepareDry, revert and abort methods now all
     begin with setting timeout to read-timeout to override the NSO value.
    (CISCOIOS-1186)

  - Added support for reconnecting using NEDCOM SSH2Session if remote SSH server
     closed the session due to inactivity in the following methods:
      #1 commit
      #2 keepAlive
      #3 applyConfig, commit and revert (after transform but before sending)
    Note: Although working, this solution is temporary and will be replaced
          when the NSO NED API supports a reconnect using standard methods.
    (CISCOIOS-1185 / RT34799)

Corrections:

  - Added missing 'presence true' on:
     cable video
    (CISCOIOS-1184 / RT35021)


cisco-ios v6.4.2 [2018-10-11]
=============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Removed range annotation on rpd-us list in:
     cable rpd * / core-interface Te* / rpd-us * upstream* *
    (CISCOIOS-1179 / RT34955)

  - Added a new ned-setting 'developer prepare-dry-model' which may be used
     to specify a temporary device model used in prepare-dry (dry-run) only.
    (CISCOIOS-1177)

  - Added built-in archive support, executed in commit or revert phase.
    See 'ned-settings cisco-ios write config-archive' list description
     for details.
    (CISCOIOS-1176 / RT34899)

  - Upgraded nedcom.jar to release-1.45.

Corrections:


cisco-ios v6.4.1 [2018-10-05]
=============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / ip ospf demand-circuit
     interface * / ip ospf flood-reduction
     interface * / ip ospf retransmit-interval
     interface * / ip ospf transmit-delay
    (CISCOIOS-1173 / RT34815)

  - Additions to the YANG model:
     controller Integrated-Cable * / shutdown
    (CISCOIOS-1171 / RT34803)

  - Additions to the YANG model:
     interface * / vrrp delay
     interface * / vrrp * name
    (CISCOIOS-1170 / RT34792)

Corrections:

  - Fixed interface standby ip vs vrf forwarding dependency problem,
     correct order is:
      interface * /
       vrf forwarding
       standby * ip
    (CISCOIOS-1175 / RT34848)

  - Reverted to set terminal settings and show version as 3 separate commands
     in order to support (odd?) parsing by 'Sheer simulator' getting stuck.
    (CISCOIOS-1174 / RT34821)

  - Fixed support for a single \ in banners.
    (CISCOIOS-1172 / RT34806)


cisco-ios v6.4 [2018-09-28]
===========================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     no errdisable detect rootguard
     no errdisable detect udld
    (CISCOIOS-1168 / RT34755)

Corrections:

  - API CHANGE:
     Removed the following macros, which should not have been modelled:
      errdisable detect cause all
      errdisable recovery cause all
    (CISCOIOS-1168 / RT34755)

  - API CHANGE:
     Changed all 'errdisable detect cause <name>' leaves from type empty
      to cli-boolean-no type boolean with default true in order to support
      show running config only showing if unset.
    (CISCOIOS-1168 / RT34755)

  - Strip "No time source.." line(s) from transaction id checksum material
     used in e.g. config-id transaction-id-method ned-setting.
    (CISCOIOS-1167 / RT34739)


cisco-ios v6.3.2 [2018-09-24]
=============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     policy-map * / class * / fragment
    (CISCOIOS-1165)

  - Additions to the YANG model:
     ip rsvp signalling hello graceful-restart refresh interval
     ip rsvp signalling hello graceful-restart refresh misses
    (CISCOIOS-1162 / RT34675)

  - Additions to the YANG model:
     l2tp-class * / digest secret
     pseudowire-class * / ip pmtu max
    (CISCOIOS-1161 / RT34686)

  - Additions to the YANG model:
     interface mgmt0
     interface * / ipv6 nd router-preference
     ip policy-list * / match route-type *
     line * / media-type rj45
     no platform rate-limit unicast acl vacl_log
    (CISCOIOS-1160 / RT34656)

  - The NED package now contains folder with third party licences.
    (CISCOIOS-1006)

Corrections:


cisco-ios v6.3.1 [2018-09-14]
=============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Use the new NSO feature rollback-files-octal to avoid escaping \ with NETSIM
    (CISCOIOS-1159)

  - Additions to the YANG model:
     interface * / channel-group * non-silent
    (CISCOIOS-1158 / RT34622)

Corrections:

  - Raised max range on 'ip sla * / request-data-size' from 1500 to 17512.
    (CISCOIOS-1157 / RT34604)


cisco-ios v6.3 [2018-09-11]
===========================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / ip access-group common *
     interface * / ipv6 traffic-filter common *
    (CISCOIOS-1154 / RT34589)

  - Additions to the YANG model:
     ipv6 route static bfd vrf *
    (CISCOIOS-1153 / RT34588)

  - Additions to the YANG model:
     ip rsvp signalling hello bfd
     interface * / ip rsvp signalling hello bfd
    (CISCOIOS-1150 / RT34580)

  - Explicitly delete and create the cable dsg tg * entry, i.e. show
     like 'tailf:cli-display-separated' was available for a list.
    (CISCOIOS-1146 / RT34553)

Corrections:

  - Replace ',' with ' ' when writing cable 'downstream sg-channel' config.
    (CISCOIOS-1151 / RT34583)

  - API CHANGE:
     interface * / ip rsvp signalling hello
     ip rsvp signalling hello
      removed presence from container and created separate enable leaf.
    (CISCOIOS-1150 / RT34580)

  - Fixed order dependency, correct order is:
     interface * / no mpls traffic-eng backup-path
     no interface Tunnel*
    (CISCOIOS-1150 / RT34580)

  - Bugfixed the new ned-setting 'auto if-address-delete-patch':
     Interface changes was not stripped after insert of defaulting
    (CISCOIOS-1147 / RT34550)

  - API CHANGE:
     router ospf * / nsf
      Restructured due to bad modeling.
    (CISCOIOS-1139)


cisco-ios v6.2.1 [2018-09-07]
=============================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Additions to the YANG model:
     interface * / network-clock wait-to-restore
    (CISCOIOS-1148 / RT34563)

  - Added new ned-setting 'auto if-address-delete-patch' (default true) to
     automatically pre-inject delete of interface(s) address and vrf in order
     to solve complex dependencies.
    (CISCOIOS-1147 / RT34550)

  - Additions to the YANG model:
     cable ofdm-rf-change-trigger
     routing-default-optimize
    (CISCOIOS-1145 / RT34532)

  - Adapted NED build system to support future NSO versions containing the
     new feature CDM.
    (CISCOIOS-1142)

Corrections:

  - Fixed problem with changing key in 'crypto isakmp key * address'
    (CISCOIOS-1149 / RT34574)

  - API CHANGE:
     platform qos port-channel-aggregate *
      Changed from leaf to leaf-list.
    (CISCOIOS-1145 / RT34532)

  - Quote 'vlan * / name' if name contains whitespace when writing to device
    Do not inject vlan entry from show vlan if already in show running.
    Added support for vlan names with whitespace in them in show vlan.
    (CISCOIOS-1144 / RT34501)


cisco-ios v6.2 [2018-09-03]
===========================

General:

    Look for 'API CHANGE' below to see what changes have been made that may
    not be backwards compatible.

Enhancements:

  - Added cli-recursive-delete to ip dns view [vrf <name>] lists in
     order to be able to delete entry with rollback.
    Also added back static warning exceptions to ignore the top delete warning.
    (CISCOIOS-1137 / RT34457)

  - Additions to the YANG model:
     platform qfp utilization monitor load
    (CISCOIOS-1136 / RT34453)

  - Disabled automatic use of temporary file and 'load merge' command with
     local NETSIM to transfer config. Added 'cisco-ios write transfer-via-file'
     ned-setting to be able to enable the optimization with NETSIM only.
    (CISCOIOS-1135 / RT34446)

  - Additions to the YANG model:
     controller (E1|T1|T3)* / attach profile
     controller T3* / cablelength short|long (with no value)
     controller (E1|T1|T3)* / secondary-admin-state
     controller (E1|T1|T3)* / threshold
    (CISCOIOS-1134 / RT34412)

  - Additions to the YANG model:
     crypto pki token * max-retries
     router rip / redistribute lisp
    (CISCOIOS-1133)

  - API CHANGE - major default-value review.
    Added missing default values (looked up on an asr1002, version 15.4):
     crypto ipsec optional retry 300
     line * / length 24
     line * / width 80
     router isis * / max-area-addresses 3
     router isis * / is-type level-1-2
     router isis * / max-lsp-lifetime 1200
     router isis * / lsp-refresh-interval 900
     router isis * / update-queue-depth 200
    (CISCOIOS-1138)

  - API CHANGE - major default-value review.
    Added missing default values (looked up on an isr4331, version 16.6.3):
     crypto call admission limit ike in-negotiation-sa 1000
     crypto ipsec security-association lifetime kilobytes 4608000
     crypto ipsec security-association lifetime seconds 3600
     crypto ipsec security-association replay window-size 64
     ethernet cfm mep crosscheck start-delay 30
     exception crashinfo buffersize 32
     interface * / arp timeout 14400
     interface * / carrier-delay 2
     interface * / ethernet oam max-rate 10
     interface * / ethernet oam min-rate 1
     interface * / ip igmp version 2
     interface * / ip pim dr-priority 1
     interface * / ipv6 nd dad attempts 1
     interface * / ipv6 nd ra interval 200
     interface * / ipv6 nd ra lifetime 1800
     interface * / ipv6 nd reachable-time 0
     interface * / load-interval 300
     ip admission max-login-attempts 5
     ip dhcp binding cleanup interval 120
     ip domain retry 2
     ip domain timeout 3
     ip domain recursive retry 10
     ip igmp snooping last-member-query-interval 1000
     ip multicast route-limit 2147483647
     ip route static adjust-time 60
     ip rsvp signalling refresh reduction ack-delay 250
     ip sla server twamp / timer inactivity 900
     ip tcp synwait-time 30
     ip tcp window-size 4128
     ip tcp intercept connection-timeout 86400
     ip telnet tos C0
     ip tftp blocksize 512
     ipv6 spd queue min-threshold 73
     ipv6 spd queue max-threshold 74
     lacp system-priority 32768
     line * / absolute-timeout 0
     line * / activation-character 13
     line * / escape-character DEFAULT
     line * / history size 10
     line * / privilege level 1
     line * / timeout login response 30
     logging history size 1
     logging queue-limit 1024
     mpls ldp discovery hello holdtime 15
     mpls ldp discovery hello interval 5
     mpls ldp discovery targeted-hello holdtime 90
     mpls ldp discovery targeted-hello interval 10
     mpls ldp graceful-restart timers forwarding-holding 600
     mpls ldp graceful-restart timers max-recovery 120
     mpls ldp graceful-restart timers neighbor-liveness 120
     mpls ldp holdtime 180
     mpls ldp maxhops 255
     mpls traffic-eng auto-bw timers frequency 300
     mpls traffic-eng link-management timers bandwidth-hold 15
     mpls traffic-eng link-management timers periodic-flooding 60
     mpls traffic-eng reoptimize timers delay cleanup 10
     mpls traffic-eng reoptimize timers delay installation 3
     mpls traffic-eng reoptimize timers frequency 3600
     netconf lock-time 10
     ntp max-associations 100
     ntp mindistance  1
     ntp maxdistance  8
     process-max-time 50
     router bgp * / bgp listen limit 100
     router bgp * / bgp update-delay 120
     router bgp * / bgp default local-preference 100
     router ospf * / auto-cost reference-bandwidth 100
     router ospf * / distance 110
     router ospf * / maximum-paths 4
     router ospf * / queue-depth update 200
     router ospf * / timers pacing flood 33
     router ospf * / timers pacing lsa-group 240
     router ospf * / timers pacing retransmission 66
     router rip / distance 120
     router rip / input-queue 150
     router rip / maximum-paths 4
     service counters max age 0
     snmp-server packetsize 1500
     snmp-server queue-length 10
     spd headroom 1000
     vtp version 1
    (CISCOIOS-1133)

  - Upgraded nedcom.jar to release-1.38.


Corrections:

  - Fixed order dependency, correct order is:
     interface Y * / no vrf forwarding
     interface X * / vrf forwarding
    (CISCOIOS-1143 / RT34493)

  - Fixed order dependency, correct order is:
     router bgp * / address-family ipv6 * vrf * / no neighbor *
     router bgp * / address-family ipv4 * vrf * / no neighbor * remote-as
    (CISCOIOS-1141 / RT34483)

  - API CHANGES:
     mpls ldp logging
       Changed from type empty to type boolean
     mpls ldp graceful-restart timers
       Removed presence and added graceful-restart-enable/graceful-restart leaf
     mpls traffic-eng auto-bw timers
       Removed presence true and added choice.
     router isis * / metric-style
       Added compact-syntax annotation
     router ospf * / auto-cost
       Removed presence true
     router ospf * / discard-route
       Remodelled due to previsouly bad design. Use of 'disabled' -> no keyword
     router ospf * / shutdown
       Changed from boolean leaf to type empty
    (CISCOIOS-1133)

  - Added missing delete-when-empty and reset-container annotations on:
     router isis * / authentication send-only
     router ospf * / area * authentication
     router isis * / default-information originate
     router isis * / fast-flood
     router isis * / log-adjacency-changes
     router isis * / metric-style
     router ospf * / nsf
    (CISCOIOS-1133)

  - Added missing delete-when-empty on:
     router eigrp * / redistribute isis
     router eigrp * / redistribute odr
     router eigrp * / redistribute rip
    (CISCOIOS-1133)

  - Restored internal live-status commands broken in v6.1.
    (CISCOIOS-1108 / RT34236)


cisco-ios v6.1.2 [2018-08-24]
=============================

Enhancements:

  - Implemented keepAlive method for use with newer NSO in keeping idle
     device connections alive. Set /devices/device/ned-keep-alive/interval
     or /devices/global-settings/ned-keep-alive/interval to enable.
    (CISCIOS-1130)

  - Additions to the YANG model:
     bridge-domain * / no mac learning
     bridge-domain * / mac limit action flooding disable
    (CISCOIOS-1129 / RT34385)

Corrections:

  - Fixed vrf definition order dependency problem, correct order is:
     no address-family
     no rd
    (CISCOIOS-1131 / RT34408)

  - Added tailf:cli-remove-before-change on:
     snmp-server host * <community-string>
    (CISCOIOS-1128 / RT34382)

  - Fixed interface order dependency problem, correct order is:
     encapsulation
     ip
    (CISCOIOS-1126)


cisco-ios v6.1.1 [2018-08-20]
=============================

Enhancements:

Corrections:

  - Fixed an order issue with removing and adding the same ip address in
     different interfaces for older NSO (4.5.2) versions.
    (CISCOIOS-1125 / RT34343)

  - Do not try to apply config using load merge if NETSIM is remote.
    (CISCOIOS-1122 / RT34335)

  - Fixed order dependency, correct order is:
     no interface Tunnel*
     no crypto ipsec profile *
     no crypto isakmp profile *
    (CISCOIOS-1112 / RT34270)

  - Improved interface ip vs encapsulation dependency fix in 6.0.11
     to also support NSO-4.5.2 where new dependencies do not work.
    (CISCOIOS-1088 / RT33983)

  - Reverted change made in 6.0.11 and removed ordered-by user on
     route-map * / match ip address prefix-list *
    (CISCOIOS-1087 / RT34012)


cisco-ios v6.1 [2018-08-16]
===========================

Enhancements:

  - Additions to the YANG model:
     crypto ikev2 profile * / aaa authentication anyconnect-eap
     crypto ikev2 profile * / aaa authorization group anyconnect-eap list
     crypto ikev2 profile * / aaa authorization user anyconnect-eap
     crypto ikev2 profile * / authentication remote anyconnect-eap aggregate
    (CISCOIOS-1118 / RT34297)

  - Additions to the YANG model:
     l2 vfi * / bridge-domain ? vlan
    (CISCOIOS-1114 / RT34282)

  - Additions to the YANG model:
     interface * / no ip mfib cef input|output
    (CISCOIOS-1111 / RT34264)

  - Additions to the YANG model:
     crypto keyring * / description
     crypto keyring * / local-address
    (CISCOIOS-1110 / RT34256)

  - Additions to the YANG model:
     ip vrf * / export ipv4
    (CISCOIOS-1109 / RT34255)

  - Major rewrite of live-status action handling, adding support for
     passing one-shot auto-prompts list entries and built-in answer
     keywords <ignore>, <prompt>, <exit> and <timeout> for improved
     device output parsing flexibility. See README for details.
    (CISCOIOS-1108 / RT34236)

  - Additions to the YANG model:
     redundancy / main-cpu / standby console
    (CISCOIOS-1092 / RT34067)

  - Additions to the YANG model:
     interface * / isdn bind-l3 ccm-manager
    (CISCOIOS-1049 / RT33672)

Corrections:

  - Added cli-no-value-on-delete on interface * / description
    (CISCOIOS-1119 / RT34301)

  - Added ordered-by user and tailf:cli-show-long-obu-diffs on:
     aaa attribute list * / attribute type interface-config *
    (CISCOIOS-1117 / RT34005)

  - Fixed policy-map order dependency, correct order is:
     class class-default / no bandwidth
     class * / band bandwidth
    (CISCOIOS-1113 / RT34281)

  - String (de)quote 'crypto pki trustpoint * / subject-name' to support
     white spaces.
    (CISCOIOS-1107 / RT34237)


cisco-ios v6.0.13 [2018-08-08]
==============================

Enhancements:

  - Re-inject ip sla reaction-configuration with ip sla modification(s).
    (CISCOIOS-1106 / RT34218)

  - Additions to the YANG model:
     interface * / isdn bchan-number-order
     interface * / isdn map address *
     interface * / isdn map address transparent
    (CISCOIOS-1049 / RT33672)

Corrections:

  - Fixed mixed route vs interface dependencies by always adding routes last.
    (CISCOIOS-1105 / RT34214)

  - Properly clean up NSO resources when closing NED.
    (CISCOIOS-1104 / RT33669)

  - Fixed order dependency, correct order is:
     route-map * / no match interface *
     no interface *
    (CISCOIOS-1103)

  - Convert comma and space for multiple values in both directions in:
     class-map * / match vlan *
    WARNING: Due to bad device API, there may be duplicate values
             causing a device diff and problems deleting.
    (CISCOIOS-1069 / RT33764)


cisco-ios v6.0.12 [2018-08-03]
==============================

Enhancements:

  - Added 'max-elements 1' on the following lists:
     ip sla * / tcp-connect
     ip sla * / udp-jitter *
     ip sla * / icmp-echo *
     ip sla * / udp-echo *
    (CISCOIOS-1102 / RT33886)

  - Added support for including \n in live-status action handler
    (CISCOIOS-1101 / RT34161)

  - Additions to the YANG model:
     crypto pki trustpoint * / crl cache
     crypto pki trustpoint * / hash
     crypto pki trustpoint * / query certificate
    (CISCOIOS-1096 / RT34126)

  - Additions to the YANG model:
     redundancy / main-cpu / standby initialization
    (CISCOIOS-1092 / RT34067)

  - Additions to the YANG model:
     segment-routing mpls / set-attributes / address-family ipv4 /  sr-label-preferred
    (CISCOIOS-1091 / RT34031)

  - Additions to the YANG model:
     interface * / spanning-tree
    (CISCOIOS-1086 / RT33938)

  - Additions to the YANG model:
     ipv6 local policy route-map
     route-map * / set ipv6 precedence
    (CISCOIOS-1079 / RT33856)

  - Additions to the YANG model:
     hw-module uplink select
    (CISCOIOS-1072 / RT33792)

  - Additions to the YANG model:
     interface * / isdn outgoing
    (CISCOIOS-1049 / RT33672)

Corrections:

  - Removed static error ignore on "no ip dns view( vrf \\S+)? default" command
    (CISCOIOS-1100 / RT34146)

  - Fixed probling modifying:
     ethernet ring g8032 * / instance * / inclusion-list *
    Replaced replace-all with remove-before change on:
     ethernet ring g8032 * / exclusion-list vlan-ids *
    (CISCOIOS-1099 / RT34150)

  - Removed tailf:cli-suppress-no on:
     tailfned police
    (CISCOIOS-1098 / RT34132)

  - Added missing default value '1' on:
     interface * / switchport trunk native
    (CISCOIOS-1097 / RT34110)

  - Fixed order dependency, correct order is:
     interface * / no service-policy
     policy-map * / no class *
    (CISCOIOS-1095 / RT34109)

  - API CHANGE:
     cable load-balance docsis-policy * tag
      changed from leaf to list to support multiple tag entries.
    (CISCOIOS-1092 / RT34067)

  - Fixed interface order dependency problem, correct order is:
     no spanning-tree
     no port-type
    (CISCOIOS-1086 / RT33938)

- Do not ignore 'Maximum number of interfaces reached"' on switchport change


cisco-ios v6.0.11 [2018-07-25]
==============================

Enhancements:

  - Additions to the YANG model:
     ip nat settings gatekeeper-size
     ip nat translation timeout
     ip nat translation dns-timeout
     ip nat translation syn-timeout
     ip nat translation udp-timeout
     ip nat translation routemap-entry-timeout
     no ip nat service dns-reset-ttl
     vrf definition * / ipv4 multicast multitopology
     vrf definition * / address-family ipv4 multicast
    (CISCOIOS-1093 / RT34020)

  - Additions to the YANG model:
     interface * / trust device
    (CISCOIOS-1083 / RT33919)

  - Additions to the YANG model:
     ptp clock ordinary domain * / servo tracking-type
    (CISCOIOS-1081 / RT33907)

  - Additions to the YANG model:
     kron occurrence *
     kron occurrence * / policy-list
    (CISCOIOS-1075 / RT33797)

  - Additions to the YANG model:
     dial-peer voice * / voice-class codec
     dial-peer voice * / voice-class sip outbound-proxy
     dial-peer voice * / voice-class sip bind control
     dial-peer voice * / voice-class sip bind media
    (CISCOIOS-1067 / RT33743)

  - Additions to the YANG model:
     ephone * / blf-speed-dial *
    (CISCOIOS-1066 / RT33740)

  - Additions to the YANG model:
     ephone * / speed-dial *
    (CISCOIOS-1063 / RT33739)

  - Additions to the YANG model:
     ephone * / type
    (CISCOIOS-1059 / RT33735)

  - Additions to the YANG model:
     ephone *
     ephone-dn *
     ephone-template *
     telephony-service *
    (CISCOIOS-1053 / RT33692)

  - Additions to the YANG model:
     ephone * / fastdial *
    (CISCOIOS-1051 / RT33686)

  - Additions to the YANG model:
     voice-port * / bearer-cap
     voice-port * / station-id
     voice-port * / caller-id enable
     voice-port * / timeouts
     voice-port * / trunk-group
     voice-port * / connection plar
     voice-port * / description
     voice-port * / echo-cancel coverage
     voice-port * / no vad
     voice-port * / signal groundStart
     voice-port * / translation-profile incoming
    (CISCOIOS-1045 / RT33664)

  - Additions to the YANG model:
     policy-map * / class * / police cir * exceed-action policed-dscp-transmit
      NOTE: with ned-setting 'cisco-ios api police-format' set to cirflat
    (CISCOIOS-1039 / RT33650)

Corrections:

  - Inject active and reporting small-licensing-data in call-home profile
     and delete reporting small-licensing-data before set in other profile.
    (CISCOIOS-1090 / RT34049)

  - Fixed interface order dependency problem, correct order is:
     no ip
     no encapsulation
    (CISCOIOS-1088 / RT33983)

  - Added ordered-by user on the following lists:
     route-map * / set ipv6 next-hop
     route-map * / match ip address prefix-list *
    (CISCOIOS-1087 / RT34012)

  - Improved order issue with removing and adding the same ip address
     in GigabitEthernet and TenGigabitEthernet (sub)interfaces.
    (CISCOIOS-1085 / RT33956)

  - API CHANGE:
     voice-port * / busyout action shutdown
    (CISCOIOS-1045 / RT33664)


cisco-ios v6.0.10 [2018-07-13]
==============================

Enhancements:

Corrections:

  - Fixed an order issue with removing and adding the same ip address in
     GigabitEthernet and TenGigabitEthernet (sub)interfaces.
    (CISCOIOS-1085 / RT33956)


cisco-ios v6.0.9 [2018-07-06]
=============================

Enhancements:

  - Additions to the YANG model:
     crypto ikev2 name-mangler * / email
    (CISCOIOS-1080 / RT33882)

  - Additions to the YANG model:
     interface * / authentication event fail action next-method
     interface * / authentication violation
    (CISCOIOS-1073 / RT33794)

  - Additions to the YANG model:
     interface * / trust device cisco-phone
    (CISCOIOS-1068 / RT33755)

  - Additions to the YANG model:
     device-sensor filter-list cdp list *
     device-sensor filter-list lldp list *
     device-sensor filter-list dhcp list *
     device-sensor filter-spec dhcp
     device-sensor filter-spec lldp
     device-sensor filter-spec cdp
    (CISCOIOS-1062 / RT33738)

Corrections:

  - Fixed support for long values in policy-map * / class * / bandwidth and
     shape average dependency fixes.
    (CISCOIOS-1078 / RT33849)

  - Fixed interface order dependency problem, correct order is:
     no ipv6 ospf
     no ipv6 address
    (CISCOIOS-1076 / RT33785)

  - API CHANGE:
     controller Integrated-Cable * / rf-chan * / type
      changed from leaf to container + value in order to support video sync mode
    (CISCOIOS-1074 / RT33813)

  - API CHANGE:
     crypto ikev2 policy * / proposal *
      changed from leaf to user ordered leaf-list to support multiple entries.
    (CISCOIOS-1071 / RT33784)


cisco-ios v6.0.8 [2018-06-26]
=============================

Enhancements:

  - Additions to the YANG model:
     qos map dscp policed * to dscp
    (CISCOIOS-1065 / RT33741)

  - Additions to the YANG model:
     buffers tune automatic
     buffers small|middle|big|verybig|large|huge
    (CISCOIOS-1064 / RT33742)

  - Additions to the YANG model:
     interface * / ipv6 address <general prefix name> *
    (CISCOIOS-1061 / RT33737)

  - Additions to the YANG model:
     router bgp * / neighbor * next-hop-self all
    (CISCOIOS-1057 / RT33704)

  - Additions to the YANG model:
     policy-map * / class * / police * / exceed-action set-dscp-transmit * table
    (CISCOIOS-1056 / RT33698)

  - Additions to the YANG model:
     file privilege
    (CISCOIOS-1055 / RT33697)

  - Additions to the YANG model:
     privilege exec all level *
    (CISCOIOS-1054 / RT33694)

  - Additions to the YANG model:
     voice class custom-cptone *
     voice class custom-cptone * / dualtone *
     voice class custom-cptone * / dualtone * / frequency
     voice class custom-cptone * / dualtone * / cadence
     voice class codec *
     voice class codec * / codec preference
     voice class h323 *
     voice class h323 * / h225 timeout tcp establish
     voice class h323 * / h225 timeout setup
     voice class sip-profiles *
    (CISCOIOS-1050 / RT33673)

  - Additions to the YANG model:
     voice service voip / ip address trusted list
     voice service voip / allow-connections
     voice service voip / fax protocol
     voice service voip / no supplementary-service h450.[237]
     voice service voip / supplementary-service h450.12
     voice service voip / supplementary-service sip
     voice service voip / redirect ip2ip
     voice service voip / notify redirect
     voice service voip / h323 / h225 display-ie ccm-compatible
     voice service voip / qsig
     voice service voip / modem passthrough nse codec
     voice service voip / sip / e911
     voice service voip / sip / registrar server
    (CISCOIOS-1048 / RT33670)

  - Additions to the YANG model:
     interface * / l2protocol-tunnel shutdown-threshold point-to-point
    (CISCOIOS-1047 / RT33668)

  - Additions to the YANG model:
     voice-port * / bearer-cap
     voice-port * / description
     voice-port * / echo-cancel coverage
     voice-port * / timeouts ringing
     voice-port * / translation-profile incoming
    (CISCOIOS-1045 / RT33664)

  - Additions to the YANG model:
     voice translation-profile * / translate calling
     voice translation-profile * / translate redirect-called
    (CISCOIOS-1044 / RT33661)

  - Additions to the YANG model:
     kron policy-list *
     kron policy-list * / cli *
    (CISCOIOS-1042 / RT33660)

  - Additions to the YANG model:
     mls ip cef load-sharing
     mls ip multicast replication-mode
     mls nde sender version
     mls rp ip input-acl
     mls qos map policed-dscp *
    (CISCOIOS-1041 / RT33658)

  - Additions to the YANG model:
     ip http path
    (CISCOIOS-1040 / RT33655)

  - Additions to the YANG model:
     mpls label mode vrf *
    (CISCOIOS-1036 / RT33640)

  - Additions to the YANG model:
     network-clock-select *
    (CISCOIOS-1035 / RT33632)

  - Added new ned-setting 'api new-snmp-server-host' for supporting multiple
     community strings in snmp-server host list.
    NOTE: New api requires 'traps' or 'informs' due to YANG limitations.
    (CISCOIOS-1034 / RT33624)

  - Additions to the YANG model:
     ip device tracking probe count
    (CISCOIOS-1032 / RT33620)

Corrections:

  - Also include group in aaa accounting config compacted from mode syntax.
    (CISCOIOS-1060 / RT33736)

  - Improved dynamic order dependency with policy-map * / class class-default
     shape average value change. Old code did not move block first if value was
     raised. This has now been fixed.
    (CISCOIOS-1058 / RT33718)

  - API CHANGE
     router bgp * / neighbor * next-hop-self
      Changed from leaf to presence container to allow support for 'all' option.
    (CISCOIOS-1057 / RT33704)

  - Added missing 'wic' in 'network-clock-participate wic wic-disabled *' list.
    (CISCOIOS-1052 / RT33685)

  - Fixed interface order dependency problem, correct order is:
     no ip address
     media-type
    (CISCOIOS-1046 / RT33666)

  - Use erlc from NCS_DIR instead of system erlc.
    (CISCOIOS-1038 / RT33646)

  - Improved order dependency between:
     interface * / mtu
     interface * / service instance * / xconnect / mtu
    (CISCOIOS-1037 / RT33643)


cisco-ios v6.0.7 [2018-06-15]
=============================

Enhancements:

  - Additions to the YANG model:
     sccp
     sccp local
     sccp ccm *
     sccp ccm group * / associate ccm * priority
     sccp ccm group * / associate profile * register
    (CISCOIOS-1033 / RT33623)

  - Additions to the YANG model:
     voice-card * / dsp services dspfarm
    (CISCOIOS-1032 / RT33620)

  - Additions to the YANG model:
     ipv6 general-prefix *
    (CISCOIOS-1031 / RT33618)

  - Additions to the YANG model:
     dial-peer cor list *
     dial-peer cor list * / member *
     dial-peer voice * / corlist
     dial-peer voice * / numbering-type
     dial-peer voice * / port [WARNING: did not find on device]
     dial-peer voice * / preference
     dial-peer voice * / prefix
    (CISCOIOS-1030 / RT33619)

  - Additions to the YANG model:
     hw-module sm *
    (CISCOIOS-1029 / RT33602)

  - Additions to the YANG model:
     crypto pki trustpoint * / authorization list
     dial-peer voice * / session transport
     sip-ua
     sip-ua / crypto signaling
     sip-ua / transport
    (CISCOIOS-1027 / RT33588)

  - Additions to the YANG model:
     aaa accounting session-duration ntp-adjusted
    (CISCOIOS-1026 / RT33587)

  - Additions to the YANG model:
     ip sla * / precision
     ip sla * / history statistics-distribution-interval (usec version)
    (CISCOIOS-1024 / RT33547)

  - Additions to the YANG model:
     interface * / tunnel mpls traffic-eng bfd encap-mode gal
     interface * / tunnel mpls traffic-eng bidirectional association
     interface * / tunnel mpls traffic-eng path-option * bandwidth
     interface * / tunnel mpls traffic-eng path-option * diverse
    (CISCOIOS-1022 / RT33515)

  - Additions to the YANG model:
     alarm-profile *
     alarm-profile * attach card *
     interface * / attach profile
     interface * / cablelength short
     interface * / ip rsvp bandwidth percent
     interface * / secondary-admin-state
     platform hw-module configuration
    (CISCOIOS-1010 / RT33374)

  - Raised maximum interface BDI index from 4095 to 8191
    (CISCOIOS-1010 / RT33374)

  - Added live-status:
     access-tunnel summary *
     access-tunnel detail *
     ip dhcp snooping binding *
    (CISCOIOS-876 / RT31426)

Corrections:

  - Added extra null pointer check in NedSettings.refresh()
    (CISCOIOS-1028 / RT33589)

  - Removed tailf:cli-compact-syntax from 'l2 vfi' list.
    (CISCOIOS-1023 / RT33537)

  - API CHANGE:
     Moved "path-option protect" to its own list in order to support both
      interface * / tunnel mpls traffic-eng path-option protect *
    (CISCOIOS-1022 / RT33515)

  - Added Java code to auto-order and removed ordered-by user in YANG model:
     route-map * / set community *
    (CISCOIOS-1017 / RT33473)


cisco-ios v6.0.6 [2018-06-07]
=============================

Enhancements:

  - Improved device pattern (for pre-enable mode) in connector templates.
    (CISCOIOS-1020 / RT33488)

  - Additions to the YANG model:
     no snmp-server trap authentication vrf
    (CISCOIOS-1019 / RT33484)

  - Additions to the YANG model:
     license smart utility customer_info
     license smart utility
     license smart url
     license smart url utility
     license smart transport smart (note: delete to set callhome)
    (CISCOIOS-1018 / RT33479)

  - Additions to the YANG model:
     cable load-balance docsis-group * / downstream Downstream-Cable *
    (CISCOIOS-1016 / RT33441)

  - Additions to the YANG model:
     line * / international
    (CISCOIOS-1014 / RT33433)

  - Additions to the YANG model:
     ethernet cfm alarm notification
     ethernet cfm logging alarm
     ip sla * / frame interval
     ip sla * / frame size
     ip sla group schedule *
    (CISCOIOS-1009 / RT33370)

Corrections:

  - Fixed bug when modifying bgp neighbor password causing bad exception.
    (CISCOIOS-1021 / RT33492)

  - Added remove-before-change, replace-all and no-value-on-delete to:
     route-map * / set community *
      In order to be able to modify the list correctly.
    (CISCOIOS-1017 / RT33473)

  - API CHANGE:
     qos map cos * to dscp
     qos map dscp * to cos
     qos map dscp * to tx-queue
      Changed YANG API due to lack of support for more than 8 entries.
    (CISCOIOS-1015 / RT33419)

  - API CHANGE:
     subscriber templating
      Changed from type empty to type boolean in order to show no-form too.
    (CISCOIOS-1014 / RT33433)

  - Improved delete (strip args in delete if list is empty) in:
     router ospf * / summary-address *
     router ospf * / area * range *
     router ospf * / area * sham-link *
     router ospf * / area * virtual-link *
    (CISCOIOS-1012)


cisco-ios v6.0.5 [2018-05-31]
=============================

Enhancements:

  - Added default=never on bgp-state-uptime leaf in live-status bgp neighbors.
    (CISCOIOS-990 / RT33074)

Corrections:

  - Added vlan to ip source binding key *
    (CISCOIOS-1013 / RT33404)

  - Expand abbreviated interface names in ip source binding * interface config.
    (CISCOIOS-1007 / RT33354)

  - Reverted CISCOIOS-1004 change in 6.0.4 and added support for both lower-
     case and upper case C in cable fiber-node * / downstream sg-channel *
     to support both IOS and XE syntax.
    (CISCOIOS-1005)


cisco-ios v6.0.4 [2018-05-24]
=============================

Enhancements:

  - Additions to the YANG model:
     interface * / ip ospf bfd strict-mode
    (CISCOIOS-1003 / RT33277)

  - Additions to the YANG model:
     controller Integrated-Cable * / rf-chan * / shutdown
    (CISCOIOS-1000 / RT33259)

  - Added 'cisco-ios developer simulate-show *' ned-setting list for simulating
     show command output, usable when debugging live-status problems.
    (CISCOIOS-990 / RT33074)

Corrections:

  - Make 'cable fiber-node * / downstream sg-channel *' all lowercase to
     support the new lowercase only controller names, changed in 6.0.2
    (CISCOIOS-1004)

  - Improved the config-warning NedSetting fix by also dequoting the string
     in order to not add additional \ on e.g. \S+.
    (CISCOIOS-995 / RT33181)

  - Added : in last-reset leaf regex in bgp_neighbors_include.gili.
    Fixed problem in NedLiveStats causing inner list to be empty.
    (CISCOIOS-990 / RT33074)


cisco-ios v6.0.3 [2018-05-21]
=============================

Enhancements:

Corrections:

  - Strip quotes erronously added by NedSettings on write/config-warning
     list on entries containing whitespace.
    (CISCOIOS-995 / RT33181)


cisco-ios v6.0.2 [2018-05-17]
=============================

Enhancements:

  - Added more protocols to l2protocol-type used in e.g.
     interface * / service instance * ethernet / l2protocol forward
    (CISCOIOS-998 / RT33227)

  - Additions to the YANG model:
     cable downstream-pilot-tone profile *
     cable downstream-pilot-tone profile * / rf-chan *
     cable downstream-pilot-tone profile * / rf-chan * / frequency
     cable downstream-pilot-tone profile * / rf-chan * / no mute
     cable rpd * / rpd-ds * downstream-pilot-tone profile
     cable rpd * / rpd-ds * mute
     cable rpd * / rpd-ds * rf-channel *
    (CISCOIOS-991 / RT33082)

  - Added live-status:
     bgp ipv4 unicast neighbors *
     bgp vpnv4 unicast neighbors *
     bgp vpnv4 unicast vrf * neighbors *
    (CISCOIOS-990 / RT33074)

  - NedLiveStats GILIParser improvements:
     Improved parsing of live-status performance.
     Added 'trim' option on <list> to delete list if no non-key leaves set.
     Added 'default' option on <leaf> to set default if value not found.
     Added src/gili/properties.gili properties for show error detection regex.
     Improved src/gili/README.gili for syntax help on templates
    (CISCOIOS-990 / RT33074)

  - Additions to the YANG model:
     cable downstream ofdm-chan-profile * / guardband-override
     cable downstream ofdm-modulation-profile * / assign modulation * freq-offset
     cable flap-list power-adjust threshold
     cable flap-list size
     cable l2-vpn-service xconnect extended-mtu
     cable load-balance docsis-group * / method modems
     cable load-balance us-attribute-mask force-check
     cable source-verify enable-sav-static
     cable source-verify group *
     cable sflog stop-record-time-overwrite
     call-home / profile * / reporting smart-call-home-data
     controller Upstream-Cable * / us-channel * chan-class-id
     controller Upstream-Cable * / us-channel * description
     controller Upstream-Cable * / us-channel * threshold corr-fec
     controller Upstream-Cable * / us-channel * threshold hysteresis
     controller Upstream-Cable * / us-channel * threshold snr-profiles
     event manager policy * authorization bypass
     interface Cable* / cable mac-domain-profile
     interface Cable* / cable privacy skip-validity-period
     ipv6 dhcp-relay bulk-lease disable
     packetcable element-id
     packetcable timer T0
     packetcable timer T1
     platform punt-sbrl subscriber ping-bypass
     platform qos port-channel-aggregate
    (CISCOIOS-982 / RT32954)

  - Added live-status:
     ip pim tunnel Tunnel *
     ip pim vrf * neighbor *
     ip mroute vrf *
    (CISCOIOS-890 / RT31613)

Corrections:

  - Added upgrade of cisco-ios/connection/device-output-delay ned-setting.
    (CISCOIOS-997 / RT33194)

  - Fixed pseudowire-class order dependency problem, correct order is:
     protocol
     status protocol notification static
    (CISCOIOS-993 / RT33124)

  - API CHANGE:
     cable load-balance docsis-group * / method
      due to support for us-method
     cable sflog max-entry
      due to support for stop-record-time-overwrite
     packetcable
      to enable support for disabling without removing config.
     call-home / profile * / reporting smart-licensing-data
      changed from type empty to boolean to support no-syntax.
     cable fiber-node * / downstream sg-channel *
      changed Cable to cable in controller names.
     cable downstream ofdm-modulation-profile * / start-freq
      corrected from start-frequency
    (CISCOIOS-982 / RT32954)


cisco-ios v6.0.1 [2018-05-11]
=============================

Enhancements:

  - Additions to the YANG model:
     call-home / mail-server *
    (CISCOIOS-988 / RT33054)

Corrections:

  - Fixed problem with 'enable secret algorithm-type' when secret includes
     characters which makes entire string quoted.
    (CISCOIOS-994 / RT33125)

  - Fixed problem with NSO 4.2.x and 4.3.x and 'auto vrf-forwarding-restore'
     ned-setting not restoring ipv6 addresses.
    (CISCOIOS-992)

  - Re-enabled tracing of version info by not checking log level in logInfo().


cisco-ios v6.0 [2018-05-04]
===========================

Enhancements:

  - New major release due to ned-settings API CHANGE.

  - API CHANGE:
     Due to NED standardisation, moved the YANG path to all ned-settings
     not residing under cisco-ios container. See README for new paths.
     Upgrade code in UpgradeNedSettings.java updates to new paths in CDB.
    (CISCOIOS-985)

  - Additions to the YANG model:
     event manager directory user repository
    (CISCOIOS-982 / RT32954)

  - Additions to the YANG model:
     interface * / no logging event power-inline-status
     interface * / ipv6 dhcp guard
     ipv6 dhcp guard policy *
     ipv6 dhcp guard policy * / device-role
     ipv6 dhcp guard policy * / match
     ipv6 dhcp guard policy * / trusted-port
     router bgp * / neighbor * shutdown graceful
    (CISCOIOS-981 / RT32943)

Corrections:

  - API CHANGE:
     router bgp * / neighbor * shutdown
      Changed from leaf to presence container to give room for options.
    (CISCOIOS-978 / RT32928)

  - Fixed order dependency problem, correct order is:
     ethernet cfm domain * / service *
     interface * / service instance * ethernet / cfm mep
    (CISCOIOS-976 / RT32897)


cisco-ios v5.9.3 [2018-04-27]
=============================

Enhancements:

  - Additions to the YANG model:
     pseudowire-static-oam class * / ttl
    (CISCOIOS-980 / RT32934)

  - Additions to the YANG model:
     ethernet ring g8032 * / instance * / description
     interface * / ip dhcp relay source-interface
    (CISCOIOS-977 / RT32908)

  - Ported GILI Parser to NEDCOM NedLiveStats.java.
    Replaced 'ned-settings live-status rpl-utah-support' with 'ned-settings
     cisco-ios deprecated live-status legacy-mode', defaulting to disabled.
    (CISCOIOS-936, NEDINFRA-165)

Corrections:

  - Inject default hidden default 'exec-timeout 10 0' first in line config
     to avoid diff and/or sending no exec-timeout.
    (CISCOIOS-986 / RT32971)

  - Raised max range on 'interface * / dialer pool' from 8 to 255.
    (CISCOIOS-979 / RT32944)

  - Fixed order dependency problem, correct order is:
     no l2vpn xconnect context *
     ethernet cfm domain *
    (CISCOIOS-976 / RT32897)

  - Moved interface * / ip unnumbered to no-address choice to be able to
     co-exist with interface * / no ip address config.
    (CISCOIOS-976 / RT32897)


cisco-ios v5.9.2 [2018-04-20]
=============================

Enhancements:

  - Additions to the YANG model:
     flow monitor * / record netflow ipv6 original-input
     interface * / ipv6 flow monitor * input
     interface * / ipv6 flow monitor * output
    (CISCOIOS-975 / RT32708)

Corrections:

  - Fixed router ospf order dependency problem, correct order is:
     no area * default-cost
     no area * nssa
    (CISCOIOS-974 / RT32864)

  - Removed cli-incomplete-command annotation on:
     interface * / standby * track <interface name>
    (CISCOIOS-973 / RT32862)

  - Removed cli-incomplete-command annotation on:
     ip nat inside source list * vrf *
    (CISCOIOS-970 / RT32822)


cisco-ios v5.9.1 [2018-04-18]
=============================

Enhancements:

Corrections:

  - Lowered min range (256 -> 4) on bits-per-interval-sustained leaf in:
     policy-map * / class * / shape average
    (CISCOIOS-962 / RT32687)


cisco-ios v5.9 [2018-04-12]
===========================

Enhancements:

  -  Changed default connect code to used the new NEDCOM Connector.
     The NED connection steps are configured in JSON connection templates,
     from now on called connectors. The connectors work in combination
     with connection ned-settings (such as those located in 'connection'
     or 'proxy' containers) to specify the prompts, commands and states
     required to connect, allowing for flexible connect configuration.
     If for some reason the old connect is required, enable it with the
     'deprecated connection legacy-mode' ned-setting.
     See src/metadata/README for more information.
     New ned-settings (see standard README):
      cisco-ios connection connector
    (CISCOIOS-960)

  - Two new connection ned-settings usable with the new connect code:
     ned-settings cisco-ios connection terminal width
     ned-settings cisco-ios connection terminal height
    See README for more infomation.
    (CISCOIOS-967)

  - Additions to the YANG model:
     l2vpn vfi context * / evc
    (CISCOIOS-965 / RT32735)

  - Additions to the YANG model:
     crypto ikev2 profile * / match address interface *
    (CISCOIOS-964 / RT32723)

Corrections:

  - Do not check tailfned police in CDB from getTransID() because it is not
     allowed with commit queues.
    (CISCOIOS-968)

  - API CHANGE:
     Fixed problem with using:
      interface * / service instance * ethernet / bridge-domain split-horizon
       Note: split-horizon had to be changed from container to leaf.
    (CISCOIOS-966 / RT32752)

  - Lowered min range (256 -> 4) on bits-per-interval-sustained leaf in:
     policy-map * / class * / shape average
    (CISCOIOS-962 / RT32687)

  - Added missing UTAH jars in private-jar:
     commons-lang3-3.0.jar
     utah-parser-1.0.1.jar
     utah-parser-cli.jar


cisco-ios v5.8.5 [2018-04-06]
=============================

Enhancements:

  - Added UpgradeNedSettings.java to upgrade old ned-settings to new for
     binary backwards compatibility.
    (CISCOIOS-960)

  - Additions to the YANG model:
     router bgp * / bgp cluster-id
    (CISCOIOS-957 / RT32660)

  - Additions to the YANG model:
     router bgp * / bgp router-id interface
     crypto isakmp policy * / hash sha384|sha512
     interface * / ip pim dense-mode
     interface * / mpls nhrp
     ip multicast mpls
     ip tcp intercept connection-timeout
     l2vpn logging pseudowire status
     l2vpn router-id
     object-group service * / esp
    (CISCOIOS-954 / RT32595)

Corrections:

  - API CHANGE:
     Due to standardisation and the introduction of CliConnector, common login class,
     moved the YANG path to the following ned-settings:
      cisco-ios connection-settings prompt-timeout -> cisco-ios connection prompt-timeout
      cisco-ios connection-settings send-login-newline -> cisco-ios connection send-login-newline
      cisco-ios connection-settings device-output-delay -> cisco-ios connection device-output-delay
    (CISCOIOS-960)

  - Fixed order dependency problem, correct order is:
     interface *
     ip route vrf * <interface> *
    (CISCOIOS-957 / RT32642)

  - Fixed 'ip nat inside source static' dependency problem from CISCOIOS-948
     for non tcp/udp entries as well.
    (CISCOIOS-956 / RT32622)

  - API CHANGE:
     Removed line/number-list{} due to not working if lines are split when
      content changes on one line and it leaves the group. Always use the
      number-single-list. NED will split lines grouped on show run.
    (CISCOIOS-955 / RT32619)

  - Fixed exception in IOSDp.interfaceSwitchportCreate restoring support for
     NCS/NSO versions lacking devices/device/platform data.
    Added back code to fallback to check config/cached-show/version/model.


cisco-ios v5.8.4 [2018-03-27]
=============================

Enhancements:

  - Additions to the YANG model:
     interface * / ip helper-address global *
    (CISCOIOS-951 / RT32553)

Corrections:

  - API CHANGE:
     interface * / pppoe-client dial-pool-number
      Corrected from leaf to list to support multiple entries and options.
    (CISCOIOS-953 / RT32569)

  - Fixed policy-map class order dependency problems, correct order is:
     policy-map * / class * / no random-detect exponential-weighting-constant
     policy-map * / class * / no random-detect dscp-based
     +++
     policy-map * / class * / no queue-limit
     policy-map * / class * / no bandwidth
    (CISCOIOS-952 / RT32556)

  - Fixed dynamic order dependency with policy-map * / class class-default
     shape average value change. Move the entire policy-map containing
     the value change last/first depending on if value raised/lowered.
    (CISCOIOS-950 / RT32551)

  - API CHANGE:
     class-map * / match service instance ethernet *
      Type change from uint32 to string to support ranges.
    (CISCOIOS-949 / RT32534)

  - Fixed 'ip nat inside source static' dependency problem, correct order is:
     no ip nat inside source static
     ip nat inside source static
    (CISCOIOS-948 / RT32537)


cisco-ios v5.8.3 [2018-03-23]
=============================

Enhancements:

  - Optimized access-list commit performance for !insert after comment.
    (CISCOIOS-945)

  - Additions to the YANG model:
     ip explicit-path name * / index * next-label
    (CISCOIOS-942 / RT32445)

  - SonarQube inspection and fixes to achieve 0 Bugs and Vulnerabilities.
    (CISCOIOS-940)

  - Additions to the YANG model:
     interface pseudowire* / vc type
    (CISCOIOS-937 / RT32379)

Corrections:

  - Fixed order dependency problem, correct order is:
     no connect *
     no interface GigabitEthernet * / service instance *
    (CISCOIOS-946 / RT32512)

  - API CHANGE:
     interface * / switchport port-security
      Removed presence container and added separate leaf to enable/disable
      port-security to match device behaviour. Services must now also set
      switchport/port-security-enable/port-security to enable. See YANG.
    (CISCOIOS-941 / RT32441)


cisco-ios v5.8.2 [2018-03-14]
=============================

Enhancements:

  - Added tailf:cli-exit-command "!" on interface * / xconnect to avoid
     exiting config mode for non-submode xconnect config.
    (CISCOIOS-933 / RT32324)

Corrections:

  - API CHANGE:
     Due to standardisation and the introduction of CliConnector, common login class,
     moved the YANG path to the following ned-settings:
      cisco-ios connection-settings number-of-retries -> cisco-ios connection number-of-retries
      cisco-ios connection-settings time-between-retry -> cisco-ios connection time-between-retry
    (CISCOIOS-934)


cisco-ios v5.8.1 [2018-03-09]
=============================

Enhancements:

  - Additions to the YANG model:
     platform punt-sbrl ping-bypass
    (CISCOIOS-932 / RT32252)

  - Additions to the YANG model:
     stack-mac persistent timer
    (CISCOIOS-931 / RT32246)

  - Additions to the YANG model:
     interface ATM* / pvc * / tx-limit
    (CISCOIOS-900 / RT31744)

Corrections:

  - Trim a \r in match interface join code affecting checksum.
    (CISCOIOS-929 / RT32217)


cisco-ios v5.8 [2018-03-02]
===========================

Enhancements:

  - Replaced caching of 'key config-key password-encrypt' code with code to
     restore the config-key password on device if transaction aborts.
     (CISCOIOS-927 / RT32207)

  - Support for NEDCOM live-status RPL and UTAH parsers by setting
      ned-settings live-status rpl-utah-support to true. Store templates
      under src/rpl or src/utah respectively.
     (CISCOIOS-926)

   - Adapted cisco-ios NED Java class to the latest NED base class features in
      NEDCOM. The NED and NEDCOM now built with Java source/target version 1.7.
     Due to use of new NEDCOM NedSettings class some ned-settings have moved
      location from cisco-ios-<name> to cisco-ios/<container>/<name>.
      See Corrections below for details.
     (CISCOIOS-921)

   - Switched NedSecrets implementation from private to new NEDCOM NedSecrets.
     (CISCOIOS-921)

Corrections:

  - Fixed cisco-ios-inject-interface-config bug with sub-interfaces with extra
     keyword on the interface line, e.g. interface Serial0/1/0.1 point-to-point
    (CISCOIOS-925 / RT32180)

  - Fixed order dependency problem, correct order is:
     no crypto
     no ip access-list
    (CISCOIOS-923 / RT32143

  - API CHANGE:
     Moved the YANG path to the following ned-settings:
      cisco-ios-cached-show-enable version -> cisco-ios deprecated cached-show-enable version
      cisco-ios-cached-show-enable inventory -> cisco-ios deprecated cached-show-enable inventory
      cisco-ios-use-ip-mroute-cache-distributed -> cisco-ios-auto use-ip-mroute-cache-distributed
    (CISCOIOS-921)


cisco-ios v5.7.5 [2018-02-23]
=============================

Enhancements:

  - Additions to the YANG model:
     ip options ignore
    (CISCOIOS-920 / RT32117)

  - Additions to the YANG model:
     crypto map * ipsec-isakmp / set ikev2-profile
    (CISCOIOS-919 / RT32106)

  - Optimized delete of entire ip prefix-list by replacing each individual
     delete with a single list delete.
    (CISCOIOS-908)

Corrections:

  - Join route-map * / match interface lines when reading from device to allow
     word sorting on all entries for consistent hash checksum calculation.
    Split the lines when writing to keep line length down.
    (CISCOIOS-895 / RT31679)


cisco-ios v5.7.4 [2018-02-16]
=============================

Enhancements:

  - Additions to the YANG model:
     router bgp * / neighbor * bmp-activate
    (CISCOIOS-914 / RT31983)

  - Additions to the YANG model:
     interface * / rep lsl-retries
     interface * / rep lsl-age-timer
     sdm prefer ip
    (CISCOIOS-913 / RT31972)

  - Additions to the YANG model:
     interface * / nat64 enable
     nat64 prefix stateful
     nat64 settings fragmentation header disable
     nat64 v4 pool *
     nat64 v6v4
     redundancy / application redundancy / group *
    (CISCOIOS-912 / RT31963)

  - Additions to the YANG model:
     interface * / platform qos queue-mode mode-dscp
     interface * / rcv-queue bandwidth
     interface * / rcv-queue dscp-map *
     interface * / rcv-queue min-reserve *
     interface * / rcv-queue queue-limit
     interface * / rcv-queue random-detect * *
     interface * / rcv-queue threshold *
     interface * / no rcv-queue random-detect *  (note: use no-list ..)
     platform ip cef load-sharing
     platform qos queueing-only
     vlan internal reserved met vlan
    (CISCOIOS-905 / RT31827)

Corrections:

  - Bug-fixed caching of 'key config-key password-encrypt':
     Do not cache value with prepare-dry or before successful device change.
    (CISCOIOS-910)


cisco-ios v5.7.3 [2018-02-12]
=============================

Enhancements:

  - Cache last set key config-key password-encrypt value and compare with
     new value in order to ignore reset with same value, caused by random
     seed in NSO.
    (CISCOIOS-910)

Corrections:


cisco-ios v5.7.2 [2018-02-08]
=============================

Enhancements:

  - Additions to the YANG model:
     macro auto execute *
     macro auto global processing
    (CISCOIOS-906 / RT31850)

  - Additions to the YANG model:
     vrf definition * / address-family ipv4 / snmp context
    (CISCOIOS-905 / RT31827)

  - Changed cli-diff annotation to avoid NSO bug corrupting the commit.
    (CISCOIOS-904 / RT31816)

Corrections:

  - Fixed interface order dependency problem, correct order is:
     channel-group
     no switchport
    (CISCOIOS-909 / RT31894)

  - Raised max range on police set-discard-class-transmit from 7 to 63.
    (CISCOIOS-905 / RT31827)

  - Improved 'delete ip prefix-list entries before creating new' fix
     by scanning the entire diffset for deletes instead of just the
     following line.
    (CISCOIOS-904 / RT31816)

  - Switched back to original sort method, to sort adjacent lines only inline.
    Added adjacent line sorting on the following config lines:
      ipv6 route
      ipv6 route vrf
      neighbor
    (CISCOIOS-895 / RT31679)


cisco-ios v5.7.1 [2018-02-03]
=============================

Enhancements:

Corrections:

  - Removed ordered-by user on:
     route-map * / match interface *
    (CISCOIOS-895 / RT31679)


cisco-ios v5.7 [2018-02-02]
===========================

Enhancements:

  - Additions to the YANG model:
     username * algorithm-type
     enable secret algorithm-type (NOTE: NED will reorder to device order)
    (CISCOIOS-901 / RT31747)

  - Additions to the YANG model:
     interface * / pppoe-client dial-pool-number
     interface * / pppoe-client ppp-max-payload
    (CISCOIOS-899 / RT31743)

  - Additions to the YANG model:
     interface * / ipv6 nd raguard attach-policy
     ipv6 nd raguard policy *
     ipv6 nd raguard policy * / trusted-port
    (CISCOIOS-897 / RT31714)

  - First version of new GILI-parser, used to specify how live-status data
     is populated in YANG stats data. See src/gili/*.gili for examples of
     existing live-status implementations. See README.gili for more info.
    (CISCOIOS-894)

Corrections:

  - Loosened restrictions, id now accepts {1,6}:{1,8} characters on:
     vrf definition * / vpn id
     ip vrf * / vpn id
    (CISCOIOS-903 / RT31786)

  - Added back compact-syntax to username *. Strip NSO duplicate deletes.
    (CISCOIOS-901 / RT31747)

  - Fixed access-list-resequence access-list insert code bug with when
     access-list modifications were split by dependencies.
    (CISCOIOS-898)

  - Removed ordered-by user on:
     route-map * / match as-path *
     route-map * / match community *
     route-map * / match extcommunity *
     route-map * / match interface *
     route-map * / match ip address *
     route-map * / match ip address prefix-list *
     route-map * / match ip next-hop *
     route-map * / match ip next-hop prefix-list *
     route-map * / match ip route-source *
     route-map * / match ip route-source prefix-list *
     route-map * / match local-preference *
     route-map * / match source-protocol *
     route-map * / match tag *
    Reason being order does not matter since single line match uses OR.
    (CISCOIOS-895 / RT31679)

  - Removed unnecessary line sort on:
     route-map * / match interface *
    (CISCOIOS-895 / RT31679)


cisco-ios v5.6.12 [2018-01-26]
==============================

Enhancements:

  - Do not prepend '! Generated offline' to dry-run unless verbose mode.
    (CISCOIOS-896 / RT31684)

  - Sort the following lines prior to calculating hash checksum in order
    to avoid transaction id changing when device reloads and reorders them:
     route-map * / match policy-list *  (line sort)
     route-map * / match interface *    (word and line sort)
     router bgp * / address-family * / aggregate-address *  (line sort)
    (CISCOIOS-895 / RT31679)

  - Additions to the YANG model:
     interface * / ip device tracking
    (CISCOIOS-893 / RT31661)

  - Implemented support for login on terminal servers with a menu selection.
    To enable:
     Set 'ned-settings cisco-ios-proxy-settings remote-connection serial'
     Set menu regexp and answer in 'ned-settings cisco-ios-proxy-settings menu'
    See README file section 16 for an example.
    (CISCOIOS-877 / RT31258)

  - Added live-status:
     cdp neighbors *
     lisp site *
    (CISCOIOS-876 / RT31426)

Corrections:

  - Removed hide-in-submode cli annotation on older eigrp:
     router eigrp <num> / address-family ipv4 vrf * autonomous-system
    (CISCOIOS-889 / RT31624, CISCOIOS-892 / RT31656)


cisco-ios v5.6.11 [2018-01-19]
==============================

Enhancements:

  - Additions to the YANG model:
     interface * / ip flow monitor <name> sampler <name> input|output
     sampler *
     sampler * / mode
    (CISCOIOS-886 / RT31568)

Corrections:

  - Added delete/create dependencies between username password and secret.
    Removed compact-syntax from username *. Re-inject privilege.
    (CISCOIOS-887 / RT31570)

  - Added delete/create dependencies between interface speed and speed auto.
    (CISCOIOS-885 / RT31567)

  - Split class-map * / match cos inner into lines of max 4 entries each.
    (CISCOIOS-884 / RT31554)

  - Fixed order dependency problem, correct order is:
     interface * / no ethernet cfm mep domain *
     no ethernet cfm domain *
    (CISCOIOS-883 / RT31536)

  - Fixed interface order dependency problem, correct order is:
     no ip address *
     xconnect
    (CISCOIOS-881 / RT31518)


cisco-ios v5.6.10 [2018-01-12]
==============================

Enhancements:

  - Additions to the YANG model:
     interface * / ip local-proxy-arp
    (CISCOIOS-878 / RT31457)

  - Added ned-setting 'cisco-ios-show-running-method scp-transfer-fallback'
     in order to be able to fallback to 'show run' if SCP transfer fails.
    (CISCOIOS-874)

Corrections:

  - Revisited 'crypto isakmp profile * / ca trust-point' fix from v5.6:
     replaced replace-all with remove-before-change cli annotation.
    (CISCOIOS-824 / RT30526)


cisco-ios v5.6.9 [2018-01-05]
=============================

Enhancements:

  - Additions to the YANG model:
     ethernet cfm domain * level / service * / continuity-check loss-threshold
     ethernet cfm domain * level / service * / continuity-check static rmep
     ethernet cfm domain * level / service * / mep mpid *
     interface * / service instance * ethernet / ip access-group ? in
     interface * / service instance * ethernet / ip access-group ? out
     network-clock log ql-changes
     network-clock synchronization ssm option
     pseudowire-class * / status protocol notification static
     pseudowire-static-oam class * / timeout refresh send
    (CISCOIOS-875 / RT29637)

Corrections:


cisco-ios v5.6.8 [2017-12-23]
=============================

Enhancements:

  - Additions to the YANG model:
     event manager applet * / action * puts
    (CISCOIOS-870 / RT31216)

Corrections:

  - Fixed interface order dependency problem, correct order is:
     speed | no speed
     negotiation auto
    (CISCOIOS-873 / RT31291)

  - Added 'tailf:cli-preformatted' to alias command line to avoid inserting ""
    (CISCOIOS-868)


cisco-ios v5.6.7 [2017-12-15]
=============================

Enhancements:

  - Additions to the YANG model:
     access-list rate-limit *
     archive / time-period
     controller VDSL * / firmware filename
     hw-module pvdm *
     hw-module usb disable
     interface * / arp authorized
     ip dhcp binding cleanup interval
     ip dhcp pool * / accounting
     ip ssh maxstartups
     radius-server attribute * send nas-port-detail
    (CISCOIOS-867)

  - Expanded cisco-ios connection-settings to also be used in the initial
     SSH/TELNET connection. Changed default number-of-retries from 0 to 1.
    (CISCOIOS-865)

  - API CHANGE:
     ned-settings cisco-ios-proxy-settings remote-password
     ned-settings cisco-ios-proxy-settings remote-secondary-password
      Changed from type string to type tailf:aes-cfb-128-encrypted-string
      in order to hide passwords.
    (CISCOIOS-864 / RT31152)

  - Also check cisco-ios-config-warning ned-setting list for internal oper
     commands in order to be able to ignore harmless device output.
    (CISCOIOS-861 / RT31131)

  - Additions to the YANG model:
     interface * / ip ospf resync-timeout
    (CISCOIOS-860 / RT31120)

  - Additions to the YANG model:
     router bgp * / bgp consistency-checker
    (CISCOIOS-859 / RT31116)

  - Additions to the YANG model:
     interface ATM* / pvc * / encapsulation aal5mux
     interface ATM* / pvc * / dialer pool-member
     interface ATM* / pvc * / tx-ring-limit
     interface ATM* / pvc * / vc-hold-queue
    (CISCOIOS-858 / RT31111)

  - Inject shutdown before deleting Dialer interface.

Corrections:

  - Split multiple class-map * / match precedence into lines of max 4
     entries each.
    (CISCOIOS-869 / RT31174)

  - Replace '  service-policy in|out ' with input|output on interfaces,
     i.e. will take affect on 'interface ATM* / pvc * / service-policy'
    (CISCOIOS-858 / RT31111)

  - Fixed delete problems with:
     qos map cos|dscp * to tx-queue|dscp *

  - Restored sync-from-file use with extended-parser turbo.
    (CISCOIOS-866)


cisco-ios v5.6.6 [2017-12-08]
=============================

Enhancements:

  - Additions to the YANG model:
     ip dns view * / domain resolver source-interface
    (CISCOIOS-856 / RT31100)

Corrections:

  - Fixed another 'ip nat inside source' dependency problem, correct order is:
     no ip nat inside source list *
     ip nat inside source static *
    (CISCOIOS-855 / RT31084)

  - Fixed interface order dependency problems, correct order is:
     ip proxy-arp
     ip redirects
     switchport
    (CISCOIOS-853 / RT31059)

  - Removed cached-show contents from config-hash transaction id calculation.
    (CISCOIOS-852 / RT31058)


cisco-ios v5.6.5 [2017-12-06]
=============================

Enhancements:

  - Additions to the YANG model:
     interface * / tunnel mpls traffic-eng path-selection metric
     interface * / tunnel mpls traffic-eng path-selection segment-routing adjacency
    (CISCOIOS-847 / RT30980)

  - Added support for configuring live-status exec done pattern (a regexp) using
     the'cisco-ios live-status exec-done-pattern' ned-setting. The regexp pattern
     can be used when the exec command does not end with a device prompt.
     The default setting is set to the output of the "issu runversion" command:
      "(Initiating active RP failover)|(Target RP will now reload)"
    (CISCOIOS-844 / RT30947)

Corrections:

  - Re-fixed ip nat inside source order dependency problem, correct order is:
     ip nat inside source static *
     ip nat inside source list *
    (CISCOIOS-849 / RT30996)


cisco-ios v5.6.4 [2017-12-01]
=============================

Enhancements:

  - Added cisco-ios-show-running-method scp-transfer connection settings:
      cisco-ios connection-settings number-of-retries [default 1]
      cisco-ios connection-settings time-between-retry [default 1 sec]
     The settings can be used to trim the number of SCP connection retries
     and the time in seconds between each retry.
    (CISCOIOS-846)

  - Additions to the YANG model:
     interface * / frame-relay class
     map-class frame-relay * / frame-relay cir
     map-class frame-relay * / frame-relay min-cir
    (CISCOIOS-845 / RT30961)

Corrections:

  - Fixed ip nat inside source order dependency problem, correct order is:
     ip nat inside source static *
     ip nat inside source list *
    (CISCOIOS-848 / RT30990)

  - Added presence true in interface * / frame-relay interface-dlci to
     correct delete problem.
    (CISCOIOS-845 / RT30961)

  - Fixed lost config problem when 11 or more switchport trunk allowed vlan
     entries on an interface. The following line was lost.
    (CISCOIOS-843 / RT30940)

  - Added cli-no-value-on-delete on interface * / ip igmp max-groups config.
    (CISCOIOS-842 / RT30906)

  - Fixed three interface order dependency problems, correct order is:
     no mac access-group
     no switchport
     +++
     no switchport port-security .*
     no switchport mode private-vlan trunk
     +++
     no spanning-tree
     no switchport
    (CISCOIOS-842 / RT30906)

  - Fixed proxy login problem for IOS/IOS-XR devices by removing an additional
     erronous proxy-prompt check.
    General clean up of proxy code.
    (CISCOIOS-841)

  - Fixed delete of 'privilege exec level' list entries by replacing the delete
     with 'privilege exec reset'. Warning: this will delete similar config.

  - Added local ethernet cfm dependencies.


cisco-ios v5.6.3 [2017-11-24]
=============================

Enhancements:

  - Added optional 'cisco-ios-proxy-settings proxy-prompt2' ned-setting to be
     able to synchronize the login process on the target. If configured, the
     NED will wait for this pattern after having sent the login command, but
     before proceeding with the login (sending username, password etc).
    (CISCOIOS-841)

  - Extended the  'cisco-ios-proxy-settings remote-command' to allow modification
     of the SSH/TELNET connection command. Accepts $address, $port, $name for
     inserting remote-xxx config, e.g.
      cisco-ios-proxy-settings remote-command "telnet $address $port /vrf Mgmt-intf"
    (CISCOIOS-841)

Corrections:

  - Improved PROXY connection error checking, throwing exception for device
     error output upcon connection failure.
    (CISCOIOS-841)

  - Fixed moving interface [Ten]GigabitEthernet * / service instance dependency
     order problem.
    (CISCOIOS-840 / RT30885)


cisco-ios v5.6.2 [2017-11-22]
=============================

Enhancements:

  - Additions to the YANG model:
     interface * / ipv6 nd cache expire
     interface * / ipv6 nd cache interface-limit
     interface * / ipv6 nd dad time
    (CISCOIOS-839 / RT30844)

  - Additions to the YANG model:
     policy-map * / class * / set cos-inner
    (CISCOIOS-837 / RT30832)

  - Additions to the YANG model:
     ip local pool * cache-size
     vpdn session-limit
    (CISCOIOS-834 / RT30778)

Corrections:

  - Added tailf:cli-remove-before-change on:
     interface * / xconnect encapsulation / backup peer * <vcid>
    (CISCOIOS-838 / RT30837)

  - Added missing config false in live-status arp and lisp data.
    (CISCOIOS-835 / RT30794)

  - API CHANGE:
     Included 'start' leaf in 'ip local pool *' key in order to support
      multiple address entries.
    (CISCOIOS-834 / RT30778)

  - Do not include empty lisp map-cache instance-id entries in show live-status.
    (CISCOIOS-832 / RT30666)

  - Do not include empty lisp eid-table-vrf entries in show live-status.
    Added missing eid-prefix leaf in lisp eid-table-vrf live-status.
    (CISCOIOS-828 / RT30578)

  - Do not include empty arp vrf entries in show live-status.
    (CISCOIOS-826 / RT30576)


cisco-ios v5.6.1 [2017-11-13]
=============================

Enhancements:

  - Commit performance optimization:
     Added cisco-ios behaviour number-of-lines-to-send-in-chunk ned-setting
     in order to be able to send certain config in larger chunks instead of
     line by line. A higher number normally results in better performance
     but may have a negative impact on the error handling. Default is 100
     lines per chunk. If you get an 'Internal ERROR: retry-command', set this
     setting to 1 and report the config line which triggered the exception.
    (CISCOIOS-833)

Corrections:


cisco-ios v5.6 [2017-11-10]
===========================

Enhancements:

  - Added live-status:
     lisp map-cache instance-id *
    (CISCOIOS-832 / RT30666)

  - Additions to the YANG model:
     cable fiber-node * / service-group managed md *
     cable fiber-node * / upstream sg-channel *  peer-node-us
    (CISCOIOS-830 / RT30596)

  - Additions to the YANG model:
     cable virtual-service-group *
    (CISCOIOS-829 / RT30560)

  - Added live-status:
     lisp session and lisp eid-table-vrf
    (CISCOIOS-828 / RT30578)

  - Added live-status:
     device-tracking-database
    (CISCOIOS-827 / RT30577)

  - Added live-status:
     arp
    (CISCOIOS-826 / RT30576)

Corrections:

  - API CHANGE:
     interface * / rate-limit input|output *
      Changed config parameters from modelled leaves to a multi-word-key list
      due to the device treating each value as a key leaf.
    (CISCOIOS-831 / RT30646)

  - Added cli-replace-all on:
     crypto isakmp profile * / ca trust-point
      in order to be able to properly order new entries.
    (CISCOIOS-824 / RT30526)


cisco-ios v5.5.2 [2017-11-03]
=============================

Enhancements:

  - Additions to the YANG model:
     monitor session * destination remote vlan
     monitor session * source remote vlan
     monitor session * source vlan *
    (CISCOIOS-825 / RT30541)

  - Support for device encrypted neighbor tag password in:
     router bgp * / address-family * unicast vrf * / neighbor *
    (CISCOIOS-823 / RT30503)

  - Additions to the YANG model:
     interface * / mvr vlan *
    (CISCOIOS-801 / RT30254)

Corrections:

  - Fixed bug in monitor session * source interface. Need to delete both
     option when changing to rx or tx only.
    (CISCOIOS-825 / RT30541)

  - Fixed interface order dependency problem, correct order is:
     no cts manual
     no switchport
    (CISCOIOS-822 / RT30491)

  - Fixed info string typo in
     interface HundredGigE *
    (CISCOIOS-821 / RT30485)


cisco-ios v5.5.1 [2017-10-27]
=============================

Enhancements:

  - Additions to the YANG model:
     ip dns view * / domain name-server *
    (CISCOIOS-820 / RT30446)

  - Additions to the YANG model:
     interface * / authentication timer inactivity ? dynamic
    (CISCOIOS-814 / RT30366)

Corrections:

  - Fixed interface order dependency problem, correct order is:
     no mvr type
     no switchport
    (CISCOIOS-801 / RT30254)


cisco-ios v5.5 [2017-10-25]
===========================

Enhancements:

  - Additions to the YANG model:
     interface * / no macro auto processing
    (CISCOIOS-818 / RT30422)

  - extended ip access-list modification improvement:
    Added a third method to store and modify extended ip access-lists - resequence.
    Set the new ned-setting cisco-ios-api access-list-resequence to enable.
    Make sure the device does not have 'ip access-list persistent' set.
    Do not specify access-list sequence numbers in NSO CDB. The NED will
     dynamically use the sequence numbers when inserting/moving access-list
     entries by starting of with a 'ip access-list resequence' command.
    To understand the new YANG model, set the access-list(s) on the device and
     sync-from to NSO, and/or grep the YANG file for 'resequence'.
    NOTES: - remark entries are not supported due to IOS does not include them
             in sequence numbering, i.e. they can't be (re)ordered properly.
           - ipv6 is not supported due to lack of ipv6 resequence command.
           - standard ip access-list not supported due to device not ordering
             the lists using the sequence number, entries always added last.
    (CISCOIOS-816)

  - Added ipv6 support with the 'cisco-ios-api new-ip-access-list' ned-setting.
     With this setting enabled the ipv6 access-lists is ordered on the sequence
     number instead of user ordered by the whole rule. Note the YANG changes,
     see tailf-ned-cisco-ios.yang or sync-from device and show for details.
    (CISCOIOS-815 / RT30390)

  - Additions to the YANG model:
     interface */ authentication control-direction
     interface */ authentication event server dead action authorize voice
     interface */ authentication open
     interface */ authentication timer inactivity
    (CISCOIOS-814 / RT30366)

  - Additions to the YANG model:
     interface * / mvr type
    (CISCOIOS-801 / RT30254)

Corrections:

  - API CHANGE:
     interface */ authentication
      Moved from all interfaces to ethernet interfaces only.
     interface */ authentication order *
     interface */ authentication priority *
      Changed API due to erronous model.
    (CISCOIOS-814 / RT30366)

  - Fixed order dependency problem, correct order is:
     interface * / switchport
     interface * / authentication
    (CISCOIOS-814 / RT30366)


cisco-ios v5.4.13 [2017-10-20]
==============================

Enhancements:

  - Additions to the YANG model:
     interface pseudowire* / preferred-path
     interface * / tunnel mpls traffic-eng path-option * segment-routing|pce
     mpls traffic-eng lsp attributes * / pce disjoint-path
     mpls traffic-eng lsp attributes * / record-route
    (CISCOIOS-813 / RT30377)

  - Additions to the YANG model:
     interface * / ip dhcp snooping vlan * information option format-type circuit-id override
     interface * / storm-control * level <rising> <falling>
    (CISCOIOS-812 / RT30383)

  - Additions to the YANG model:
     interface * / mls qos dscp-mutation
    (CISCOIOS-811 / RT30382)

Corrections:

  - Fixed order dependency problem between policy-map * / class * / queue-limit
     and random-detect dscp <max-threshold>
    (CISCOIOS-810 / RT30358)


cisco-ios v5.4.12 [2017-10-18]
==============================

Enhancements:

  - Additions to the YANG model:
     crypto isakmp profile * / match certificate
     crypto isakmp profile * / self-identify fqdn <id>
    (CISCOIOS-807 / RT30280)

  - Do not send empty lines or comments to the device unless in a text.

  - Added new static warning exceptions:
     "enrollment url must be configured"
     "reference bandwidth is changed"
    (CISCOIOS-807 / RT30280, Internal)

Corrections:

  - Replace device encrypted secrets with the cached cleartext value in the
     first update() parse as well in order to avoid checksum diff when using
     transaction-id method config-hash.
    (CISCOIOS-809 / RT30304)

  - API CHANGE
     crypto isakmp profile * / ca trust-point
      changed from leaf to ordered-by user leaf-list to allow for multiple
      entries.
    (CISCOIOS-807 / RT30280)


cisco-ios v5.4.11 [2017-10-12]
==============================

Enhancements:

  - Optimized show live-status by caching device show commands in the NED for
     cisco-ios live-status time-to-live seconds.
    (CISCOIOS-799)

  - Additions to the YANG model:
     interface * / tunnel mode ipsec ipv6
    (CISCOIOS-796 / RT30159)

  - Additions to the YANG model:
     interface * / xconnect encapsulation / mpls label
    (CISCOIOS-794 / RT30100)

  - Added ms timestamps for performance profiling. Grep for DONE in trace.

Corrections:

  - Fixed problem with deleting:
     policy-map * / class * / set dscp
      The dscp value was erronously left out in the delete due to cli-full-no.
    (CISCOIOS-806 / RT30269)

  - Added new static warning exception:
     Ignore output when deleting et-analytics / ip flow-export destination due
      to NSO bug with tailf:cli-recursive-delete and dependencies.
    (CISCOIOS-805)

  - Implemented a fix solving problems with interface speed, injecting speed
     auto or speed no in order to properly clear interface speed.
    (CISCOIOS-804 / RT30245)

  - Added cli-show-no to 'xconnect * / mpls control-word' leaf in order to
     be able to delete the default at creation.
    (CISCOIOS-803 / RT30248)

  - Added presence true to:
     utd engine standard multi-tenancy / utd global
     utd web-filter url profile * / reputation
    (CISCOIOS-800 / RT30240)

  - Fixed bugs in live-status implementation seriously degrading the performance.
    Fixed ietp-ip.yang compilation issue with NCS-3.4.5[.x].
    (CISCOIOS-799)

  - Fixed build problem with ncs-3.4.5 and ietf-interfaces.yang by removing
     unused config from the file.
    (CISCOIOS-796 / RT30159)

  - API CHANGE:
    Added presence true and cli-delete-when-empty on xconnect container in:
     interface * / xconnect * encapsulation
    Reason being without it it was not possible to properly delete it. NSO
    erronously tried to enter xconnect without mandatory mode leaves.


cisco-ios v5.4.10 [2017-10-06]
==============================

Enhancements:

  - Additions to the YANG model:
     crypto keyring * / pre-shared-key address ipv6 *
     crypto isakmp profile * / self-identify address ipv6
    (CISCOIOS-796 / RT30159)

  - Additions to the YANG model:
     interface * / service instance * ethernet / xconnect * / mpls label
    (CISCOIOS-794 / RT30100)

  - Additions to the YANG model:
     mpls static binding ipv4 *
    (CISCOIOS-793 / RT30073)

  - Additions to the YANG model:
     parameter-map type inspect * / session packet
     parameter-map type inspect * / per-box
    (CISCOIOS-791 / RT30065)

Corrections:

  - Fixed transaction id problem with secrets and non config-hash options.
    (CISCOIOS-798)

  - Remove unsupported UTD-MT CLI's.
    (CISCOIOS-792 / RT30060)

  - API CHANGE
     parameter-map type inspect * / max-incomplete ? aggressive-aging
      Changed YANG due to support for percent.
    (CISCOIOS-791 / RT30065)

  - Don't fail when debugging JVM inside Eclipse.
    (RT30198)


cisco-ios v5.4.9 [2017-09-22]
=============================

Enhancements:

  - Additions to the YANG model:
     snmp-server trap link ietf
     snmp-server inform
    (CISCOIOS-787 / RT29955)

  - Additions to the YANG model:
     et-analytics
     interface * / et-analytics enable
    (CISCOIOS-786 / RT29936)

Corrections:

  - Split up long 'route-map / set extcommunity rt additive' lines.
    (CISCOIOS-790 / RT29983)

  - Fixed problem with ip address restoration on interface vrf changes
     (in trace as 'maapiGetConfig ERROR: read exception Not able to parse')
    (CISCOIOS-789)


cisco-ios v5.4.8 [2017-09-15]
=============================

Enhancements:

  - Additions to the YANG model:
     interface * / tunnel mpls traffic-eng path-option *
     mpls traffic-eng lsp attributes * / affinity
     pseudowire-class * / preferred-path
    (CISCOIOS-784 / RT29918)

  - Additions to the YANG model:
     parameter-map type inspect * / aggressive-aging
     parameter-map type inspect * / application-inspect no-list *
     parameter-map type inspect * / icmp-unreachable-allow
     parameter-map type inspect * / inspect
     parameter-map type inspect * / lisp inner-packet-inspection
     parameter-map type inspect * / multi-tenancy
     parameter-map type inspect * / log flow-export
     parameter-map type inspect * / max-incomplete tcp|udp|icmp
     parameter-map type inspect * / tcp window-scale-enforcement
     parameter-map type inspect * / zone-mismatch drop
    (CISCOIOS-783 / RT29900)

  - Additions to the YANG model:
     ap group *
     hw-switch switch * logging onboard *
     qos queue-softmax-multiplier
    (CISCOIOS-782 / RT29894)

  - Additions to the YANG model:
     interface * / lacp port-priority
     interface * / speed auto
     lacp system-priority
     mac address-table static * vlan *
     policy-map * / class * / queue-buffers ratio
    (CISCOIOS-775 / RT29826)

  - Additions to the YANG model:
     router lisp / locator-set * / <addr>
     router lisp / map-server session passive-open
     router lisp / ipv4 route-import map-cache *
     router lisp / ipv4 route-export site-registrations
     router lisp / site * / eid-record instance-id *
    (CISCOIOS-773 / RT29824)

Corrections:

  - Fixed order dependency problem between ip sla * / timeout and threshold.
    (CISCOIOS-781 / RT29889)

  - API CHANGE
     router bgp * / bgp listen range *
      change to:
     router bgp * / bgp listen range * peer-group *
      due to newer IOS support identical range for different peer-groups.
    (CISCOIOS-778 / RT29835)

  - Added tailf:cli-remove-before-change on:
     ip nat inside source list *
    (CISCOIOS-777 / RT29832)

  - Raised system mtu range to 9198.
    (CISCOIOS-775 / RT29826)


cisco-ios v5.4.7 [2017-09-07]
=============================

Enhancements:

  - Additions to the YANG model:
     router lisp / encapsulation vxlan
     router lisp / instance-id * / dynamic-eid *
     router lisp / instance-id * / remote-rloc-probe
     router lisp / instance-id * / service ethernet eid-table vlan
     router lisp / ipv4 etr map-server * proxy-reply
     router lisp / ipv4 locator reachability exclude-default
     router lisp / locator default-set
     router lisp / service * / database-mapping limit dynamic
     router lisp / service ethernet
     router lisp / service ipv4 / sgt
    (CISCOIOS-771 / RT29756)

  - Added live-status interfaces-state for basic RFC7223 ietf-interfaces.yang
     and RFC7277 ietf-ip.yang operational support.
    See the YANG files for YANG model and IOSNedCli.java nedLiveStatusRegister()
     method for regular expressions scraping the info from show commands.
    (CISCOIOS-770)

  - Added a new optional method to calculate transaction id:
     cisco-ios-transaction-id-method config-hash-cached
      Same as config-hash except that transaction id method will reuse
      the last unmodified config from show (sync-from).
    (CISCOIOS-769)

  - Cleanup and optimization of sync-from transformations.
    (CISCOIOS-769)

  - Additions to the YANG model:
     l2 router-id
     l2 vfi * / vpls-id

Corrections:

  - Set iospolice to 'cirmode-bpsflat' for iosmodel 'ME-4924' devices.
    (CISCOIOS-772 / RT29802)

  - Fixed problem with scp-transfer method and character sequence "^C" being
     replace with single '^C', corrupting some menu config.


cisco-ios v5.4.6 [2017-09-01]
=============================

Enhancements:

  - Additions to the YANG model:
     interface * / no lisp mobility liveness test
     interface * / lisp mobility *
    (CISCOIOS-767 / RT29739)

  - Additions to the YANG model:
     controller * / channel-group * unframed
     interface * / ppp multilink links maximum
    (CISCOIOS-760 / RT29661)

  - Additions to the NSO YANG model:
     devices device platform serial-number
       or if 'serial-number' not yet added to NSO device platform:
     devices device platform ios-serial-number
    (CISCOIOS-746 / RT28787, CISCOIOS-759 / RT29587)

  - Fixed support for secondary address in interface standby ip config:
     interface * / standby * ip <addr> secondary
    (CISCOIOS-757 / RT29598)

  - Additions to the YANG model:
     domain * / border / advanced / smart-probes * burst * only-burst
    (CISCOIOS-751 / RT29549)

  - Support for device encrypted cipher in:
     router bgp * / template peer-session * / password

  - Added 'extended-parser turbo-xml-mode' to force XML-load if having trouble
    with Maapi.setValues which is used in turbo-mode.

Corrections:

  - API CHANGE:
     crypto ikev2 profile * / authentication remote
      Removed choice statement to support multiple config.
    (CISCOIOS-766 / RT29737)

  - Lower live-status version ttl from 2147483647 to 1048575 to avoid
     crashing some versions of NSO. NSO will be fixed as well.
    (CISCOIOS-763 / RT29697)

  - Restricted block-page-interface to Loopback interface only.
    Restricted redirect interface to BDI or VirtualPortGroup interfaces.
    Fixed order dependency problem between all-interfaces in utd global
     config and 'utd enable' on interfaces.
    (CISCOIOS-762 / RT29672)

  - API CHANGE:
     interface * / domain smart-probe
      Moved out of choice to be able to set with domain * path-id
    (CISCOIOS-761 / RT29640)

  - Fixed router bgp order dependency problem, correct order is:
     router bgp * / neighbor *
     router bgp * / address-family
    (CISCOIOS-758 / RT29614)

  - Fixed broken offline (noconnect) commit dry-run null exception.
    (CISCOIOS-710 / RT29186)


cisco-ios v5.4.5 [August 25, 2017]
==================================

Enhancements:

  - Enhanced 'extended-parser turbo-mode' to use Maapi.setValues to speed up
     data transfer (NOTE: Only works with NSO >= 4.4.2.2).
    (CISCOIOS-755 / RT29440)

Corrections:

  - Implemented workaround in 'extended-parser turbo-mode' for non-strict model
     (mode /policy-map/class/police-policy-map/police should have leaf
     'service-policy' in mode, but it is outside in model).
    (CISCOIOS-756 / RT29637)


cisco-ios v5.4.4 [August 18, 2017]
==================================

Enhancements:

  - Reset readTimeout when running live-status commands where the device prints
     multiple ! or . or C to show passed time, e.g. when copying a remote file.
    (CISCOIOS-754 / RT29576)

  - Additions to the YANG model:
     domain * / border / advanced / smart-probes * burst
     interface * / domain smart-probe
    (CISCOIOS-751 / RT29549)

  - Additions to the YANG model:
     domain * / vrf * / master hub / advanced / inter-dc
    (CISCOIOS-750 / RT29550)

  - Additions to the YANG model:
     ip sla * / traffic-class
    (CISCOIOS-749)

  - Additions to the YANG model:
     no cns aaa enable
    (CISCOIOS-748)

  - Additions to the YANG model:
     crypto pki profile enrollment * / enrollment url
    (CISCOIOS-745 / RT28787)

  - Added support for commit dry-run to be executed without a real device present.
    (CISCOIOS-710 / RT29186)

Corrections:

  - API CHANGE:
     aaa attribute list * / attribute type interface-config *
      Changed from leaf to list due to lack of support for multiple entries.
    (CISCOIOS-753 / RT29556)

  - Minor API CHANGE:
     Moved context|match|read|write|notify|access snmp-server group leaves
     out of security-model choice.
    (CISCOIOS-748)

  - Fixed bug with erronously injected backslash which broke turbo mode
     parsing of switchport trunk allowed vlan config. Also stripped \r.
    (CISCOIOS-747 / RT29512)


cisco-ios v5.4.3 [August 11, 2017]
==================================

Enhancements:

  - Additions to the YANG model:
     crypto ikev2 keyring * / peer * / identity email
     crypto ikev2 keyring * / peer * / identity address
    (CISCOIOS-744 / RT29463)

  - Additions to the YANG model:
     interface * / domain [name] dci-path
     interface * / domain [name] dynamic-path
     interface * / domain [name] saf neighboring
    (CISCOIOS-742 / RT29441)

  - Added live-status:
     inventory
    (CISCOIOS-737)

  - Added generic software support for showPartial(), enabling support for
     'commit no-overwrite' and 'partial-sync-from'.
    (CISCOIOS-724)

  - Added live-status running-config license udi to retrieve and cache license
     udi from running-config (read-only).
    (CISCOIOS-718)

  - Do not send empty router ospf area entry when all members are deleted.
     This fix will solve the problem with transaction aborting, but there
     will still be a compare-config diff due to the fact that the device
     removes the empty list.
    (CISCOIOS-705 / RT29156)

Corrections:

  - API CHANGE:
     ipv6 dhcp pool * / domain-name *
      Changed from leaf to ordered-by user list to support multiple entries.
    (CISCOIOS-741 / RT29420)

  - Moved nedcom.jar from shared-jar to private-jar directory.


cisco-ios v5.4.2 [August 4, 2017]
=================================

Enhancements:

  - Added live-status version to retrieve and cache non-config version data.
    (CISCOIOS-736)

  - Added support for configuring live-status time-to-live via ned-settings.
    See README for further info.
    (CISCOIOS-735)

  - Added admin-status leaf to live-status interfaces list.
    Cache leaves not found as well in TTL to avoid frequent re-reading.
    (CISCOIOS-734)

  - Rewrote showStats() and showStatsList() methods to use new NedShow class.
    (CISCOIOS-732)

  - Additions to the YANG model:
     crypto ikev2 proposal * / encryption aes-gcm-128|256
     crypto ikev2 proposal * / prf
    (CISCOIOS-731 / RT29352)

  - Verify that CLI protocol is ssh with cisco-ios-show-running-method scp-transfer.
    (CISCOIOS-726 / RT29318)

Corrections:

  - Do not cache worker in utility classes since worker may change.
    (CISCOIOS-730)

  - Rewrote showStatsList() fixing problem with show devices device <devname>
     live-status interfaces showing incorrect interface list. It also listed
     deleted interfaces.
    (CISCOIOS-729)

  - Fixed order dependency problem, correct order is:
     no virtual-service *
     interface * / no ip address
    (CISCOIOS-727 / RT29333)

  - Fixed order dependency problem, correct order is:
     router bgp * / address-family */ no *
     router bgp * / no neighbor *
    (CISCOIOS-704 / RT26625)

  - Fixed bug with newly introduced virtual-service re-activate feature.
    An unnecessary 'no activate' was sent when service was created inactive.

  - Removed debug cli-recursive-delete from 'utd engine standard multi-tenancy'
    to avoid issue when deleting it.


cisco-ios v5.4.1 [July 28, 2017]
================================

Enhancements:

  - Additions to the YANG model:
     ipv6 route * name
     ipv6 route * track
    (CISCOIOS-722 / RT29277)

  - Additions to the YANG model:
     utd multi-tenancy
     utd engine standard multi-tenancy
     virtual-service * / profile multi-tenancy
    (CISCOIOS-715)

  - Additions to the YANG model:
     utd engine standard / threat-inspection / signature update / server
     utd engine standard / threat-inspection / signature update / occur-at
     virtual-service * / vnic management GigabitEthernet* / guest ipv6 address
    (CISCOIOS-714)

  - Inactivate and re-activate a virtual-service if modified when activated by
     injecting a 'no activate' first and an 'activate' last.
    (CISCOIOS-713)

  - Added new feature for optimizing sync-from (and compare-config).
    Enable with ned-setting 'cisco-ios extended-parser turbo-mode'.
    Requires use of new nedcom package.
    (CISCOIOS-667)

Corrections:

  - Added cisco-ios-auto if-switchport-sp-patch ned-setting to inject delete
    of service-policy input|output when toggling switchport on an interface
    in order to avoid IOS bug when re-adding the service-policy.
    Note: this bug was found on (ME360x_t-UNIVERSALK9-M), Version 15.5(3)S5
    (CISCOIOS-723)

  - API CHANGE:
     ipv6 dhcp pool * / dns-server *
      Changed from leaf to ordered-by user list to support multiple servers.
    (CISCOIOS-721 / RT29282)

  - Fixed a problem (connection timeout) with SSH proxy connect.
    Use cisco-ios-proxy-settings remote-secondary-password for SSH proxy
     as well if available. If not, try the secondary password from the
     device settings.
    Added a check for "No route to host|Connection" to avoid block in timeout.
    (CISCOIOS-716 / RT29241)

  - Added no-name-on-delete, no-value-on-delete and remove-before-change on:
     utd threat-inspection whitelist / signature id * comment
    (CISCOIOS-714)

  - Fixed problem with live-status exec crypto pki authenticate command.
    (CISCOIOS-712 / RT29192)

  - Fixed support for utd threat-inspection signature list comment without
    quotes on device. Note: quotes are still needed in NSO due to type string.
    (CISCOIOS-706 / RT29161)

  - NETSIM: let cisco-ios-police-format override 'tailfned police' config.
    (CISCOIOS-696 / RT29006)


cisco-ios v5.4 [July 21, 2017]
==============================

Enhancements:

  - Additions to the YANG model:
     ipv6 dhcp pool * / prefix-delegation pool
     ipv6 local pool *
     interface * / ipv6 dhcp server
    (CISCOIOS-711 / RT29221)

  - Additions to the YANG model:
     crypto pki profile enrollment * / authentication url ? vrf
    (CISCOIOS-709 / RT29194)

  - Additions to the YANG model for UTD Snort IPS, UTD Web-filtering and Umbrella:
     virtual-service * / vnic management GigabitEthernet* / guest ip|ipv6 address
     interface * / umbrella
     interface * / utd enable
     parameter-map type umbrella global
     utd
     utd engine advanced
     utd engine standard
     utd threat-inspection whitelist
     utd web-filter
     threat-visibility
    (CISCOIOS-706 / RT29161)

  - Additions to the YANG model:
     domain * / vrf * / border / advanced / monitor-cache-percent
    (CISCOIOS-703 / RT29124)

  - Implemented cisco-ios behaviour config-output-reboot-timer-interval <num>
     ned-setting in order to automatically reboot the device after having sent
     a config command which broke the connection to the device for more than one
     minute. An example of this is deleting a route which is used to communicate
     with NSO. See README for details.<
    (CISCOIOS-681)

Corrections:

  - API CHANGE:
     router rip * / passive-interface default | <ifname>
     router ospf * / passive-interface default | <ifname>
     router isis * / passive-interface default | <ifname>
     router eigrp * / passive-interface default | <ifname>
      Removed choice statement around 'default' and <ifname> in order to be able
      to switch from default to specified by first sending "no passive-interface.
      NOTE: Hence, you must not forget to delete 'default' when switching.
    (CISCOIOS-708 / RT29163)

  - Support for encapsulation and ingress options in monitor session split code.
    (CISCOIOS-707 / RT29162)

  - Added workaround for NSO problem with dependencies when router bgp neighbor
     entries are reordered, i.e. removed and added back. The workaround will
     make sure remote-as and peer-group are deleted last.
    (CISCOIOS-704 / RT26625)


cisco-ios v5.3.2 [July 13, 2017]
================================

Enhancements:

  - Added new static warning exception:
     "use 'write' command to make"
    (CISCOIOS-700 / RT29053)

  - Added new options for /monitor/session/source+destination
    (CISCOIOS-699 / RT29049)

Corrections:

  - Fixed order dependency problem, correct order is:
     interface * / no channel-group *
     interface Port-channel * / no service instance *
    (CISCOIOS-701 / RT29043)

  - Fixed merge of 'set extcommunity rt ...' entries in route-map list,
     i.e. when device splits extcommunity into several lines.
    (CISCOIOS-698 / RT29039)

YANG model changes since 5.3.1:

  New leaves:
  /monitor/session/destination/interface/encapsulation
  /monitor/session/destination/interface/ingress/vlan
  /monitor/session/source/interface/mode

  Changed nodes:
  /monitor/session/destination/interface


cisco-ios v5.3.1 [July 07, 2017]
================================

Enhancements:

  - Additions to the YANG model:
     logging reload
    (CISCOIOS-696 / RT29006)

  - Recreate ip sla if 'http get' operation is modified.
    (CISCOIOS-694)

Corrections:


cisco-ios v5.3 [June 29, 2017]
==============================

Enhancements:

  - Additions to the YANG model:
     event manager applet * / event syslog maxrun
    (CISCOIOS-692)

  - Additions to the YANG model:
     router bgp * / address-family * / maximum-secondary-paths
     router eigrp * / maximum-paths
     router eigrp * / maximum-secondary-paths
    (CISCOIOS-691 / RT28898)

Corrections:

  - Fixed order dependency problems in:
     track * / object *
     crypto ikev2 client flexvpn * / source * track
    (CISCOIOS-693)

  - API CHANGE:
     monitor session * source|destination interface
     monitor session * type local / source|destination interface
     monitor session * type erspan-source / source interface *
      Changed model to a list of individual interfaces instead of the previous
      model configuring intervals in one string entry.
      NED will split up ranges from show running to individual entries.
      Note: You must still use the abbreviated form like the device shows them.
     monitor session * type local
      Made 'type' leaf a key leaf to avoid being populated for generic format.
    (CISCOIOS-688 / RT28849)

  - API CHANGE:
     cable rpd * / core-interface Te* / rpd-ds 0 downstream* *
     cable rpd * / core-interface Te* / rpd-us 0 upstream* *
      Integrated support for oob entries in old model, a 3rd key introduced.
    (CISCOIOS-686 / RT28813)

  - Strip ios: prefix from re-created ip sla config.
    (CISCOIOS-651, CISCOIOS-680 / RT28776)


cisco-ios v5.2.10 [June 23, 2017]
=================================

Enhancements:

  - Additions to the YANG model:
     crypto pki profile enrollment * / authentication url
     crypto pki profile enrollment * / authentication command
    (CISCOIOS-690 / RT28856)

  - Additions to the YANG model:
     ethernet cfm domain * level / service * efc inner-vlan
    (CISCOIOS-689 / RT28873)

  - Additions to the YANG model:
     interface * / service instance * ethernet / l2protocol forward all
    (CISCOIOS-687 / RT28851)

  - Additions to the YANG model:
     interface Ethernet-Internal*
     ip pim mpls source
     definition * / address-family ipv4 / mdt data mpls mldp *
     vrf definition * / vpn id
    (CISCOIOS-685 / RT28814)

  - Additions to the YANG model:
     track timer
     track resolution
    WARNING: There is a bug in IOS making it impossible to delete a track
             resolution entry on later IOS releases.
    (CISCOIOS-684 / RT28837)

Corrections:

  - API CHANGE:
     vrf definition * / address-family ipv4 / mdt default mpls mldp *
      Changed from leaf to list.
    (CISCOIOS-685 / RT28814)


cisco-ios v5.2.9 [June 19, 2017]
================================

Enhancements:

  - Additions to the YANG model:
     cts credentials id * password
      NOTE: This is an exec command on the device. The NED appends "do " before
            the command to set it, then re-injets if from the cdb upon show.
    (CISCOIOS-683)

  - Raised interface * / hold-queue * interval from 4096 to 240000
    (CISCOIOS-682 / RT28783)

  - Temporarily remove ip sla schedule when modifying ip sla entry entry.
    Replace the entire ip sla entry if operation modified.
    (CISCOIOS-651, CISCOIOS-680 / RT28776)

Corrections:

  - Model fixes:
     logging discriminator - Commit issue, quotes were removed.
     vpdn search-order - fixed numerous bugs


cisco-ios v5.2.8 [June 16, 2017]
================================

Enhancements:

  - Additions to the YANG model:
     cable downstream controller-profile * / max-ofdm-spectrum
     cable downstream ofdm-chan-profile * / profile-data * modulation-profile
     cable downstream ofdm-modulation-profile *
     cable downstream ofdm-power-profile *
     cable load-balance docsis30-enable dynamic downstream
     cable load-balance exclude modem *
     cable load-balance method-utilization min-threshold
     controller Upstream-Cable * / cable spectrum-group
     controller Upstream-Cable * / us-channel * spectrum-group
     interface * / cable managed fiber-node
     interface Wideband-Cable* / cable wideband-profile
    (CISCOIOS-675 / RT28733)

  - Avoid deadlocking NED for bad banners/texts:
     Strip \t from banners.
     Ignore waiting for echo if text mode includes \t.
    (CISCOIOS-668 / RT28684)

  - Optimized commit on NETSIM by writing config to a temporary file from
     NSO/NED and then load it back in NETSIM.

Corrections:

  - Raised policy-map * / class * / queue-limit range.
    (CISCOIOS-679 / RT28773)

  - Remove router ospf / max-metric router-lsa on device if deleted in NSO.
    (CISCOIOS-677 / RT28758)

  - Strip show configuration history|id of dynamic info like 'Load for'
     and 'Time source is NTP' to avoid corrupting transaction id.
    (CISCOIOS-676)

  - API CHANGE
     redundancy / linecard-group * / revertive
      Changed due to bad modeling, lacked support for value or 'no revertive'.
    (CISCOIOS-675 / RT28733)

  - API CHANGE:
     Model changes in
      voice translation-rule * / rule
     Due to bad modeling and handling of special characters in regexp.
    (CISCOIOS-672)

  - Fixed order dependency problem, correct order is:
     router eigrp * / no af-interface
     no interface *
    (CISCOIOS-669 / RT28690)


cisco-ios v5.2.7 [June 08, 2017]
================================

Enhancements:

  - Additions to the YANG model:
     policy-map * / class * / set dscp dscp table
    (CISCOIOS-662 / RT28654)

  - Additions to the YANG model:
     avc sd-service / segment
     avc sd-service / controller / address
     avc sd-service / controller / vrf
    (CISCOIOS-658 / RT28628)

  - DOCSIS 3.1 additions to the YANG model:
     cable downstream ofdm-chan-profile *
     cable downstream ofdm-prof-mgmt
     cable downstream ofdm-flow-to-profile profile-data *
     cable mod-profile-ofdma *
    (CISCOIOS-657 / RT28612)

  - DOCSIS 3.1 expansions in the YANG model:
     controller Integrated-Cable*
     controller Upstream-Cable*
    (CISCOIOS-657 / RT28612)

  - Additions to the YANG model:
     class-map * / match application *
     flow record * / match transport rtp ssrc
     flow record * / collect application media
     flow record * / collect counter bytes rate
     flow record * / collect ipv4 ttl
     flow record * / collect monitor event
     flow record * / collect routing forwarding-status
     flow record * / collect transport event packet-loss
     flow record * / collect transport packets
     flow record * / collect transport rtp jitter
     interface Vlan* / no autostate
     ip device tracking probe interval
     ip igmp snooping querier
     ip host-routing
     policy-map * / class * / monitor metric rtp / clock-rate *
     policy-map * / class * / monitor metric rtp / react * / action
     policy-map * / class * / monitor metric rtp / react * / threshold value ge
    (CISCOIOS-653 / RT28582)

  - Added cisco-ios-replace-commit list ned-setting, usable for replacing or
     filtering out config line(s) upon writing config to device. See README
     for more details.
    (CISCOIOS-651)

  - Additions to the YANG model:
     platform memory add
    (CISCOIOS-650 / RT28556)

  - Fixed problem deleting Serial interface by adding a static warning exception:
     "Removal of channelized Sonet/SDH interface configuration is not permitted"
    And adding cli-recursive-delete in order to delete 'ppp multilink group'.
    (CISCOIOS-649 / RT28552)

Corrections:

  - Dequote passwords before storing them in NedSecrets.
    (CISCOIOS-660 / RT28649, CISCOIOS-661 / RT28658)

  - Fixed order dependency problem, correct order is:
     interface Serial* ( / ppp multilink group)
     interface Multilink* / service-policy
      Note: reason being the Multilink bandwidth need to be updated first.
    (CISCOIOS-649 / RT28552)


cisco-ios v5.2.6 [June 06, 2017]
================================

Enhancements:

  - Additions to the YANG model:
     interface * / tunnel mpls traffic-eng record-route
    (CISCOIOS-656 / RT28592)

  - Added support for splitting range terminal lines with multiple subslots
     into single entries, e.g 'split 'line 0/2/15 0/3/0' into 2 lines'
    (CISCOIOS-654 / RT28588)

  - Additions to the YANG model:
     call-home / profile * / reporting smart-licensing-data
     no mpls mldp
     no mpls mldp forwarding recursive
     mpls mldp logging notifications
     vrf definition * / address-family ipv4 / bgp nexthop Loopback
    (CISCOIOS-650 / RT28556)

Corrections:

  - Fixed order dependency problem, correct order is:
     router bgp * / neighbor * remote-as <val>
     router bgp * / neighbor * peer-group <val>
    (CISCOIOS-655 / RT28601)

  - Workaround for NSO problem with dependencies, non-strict-leafref on
     service-policy did not work if other dependencies kicked in before.
    (CISCOIOS-638 / RT28399, CISCOIOS-649 / RT28552)


cisco-ios v5.2.5 [June 02, 2017]
================================

Enhancements:

  - Additions to the YANG model:
     cts authorization list
     cts role-based enforcement
     cts role-based sgt-map * sgt
     cts sgt
     cts sxp default password
     cts sxp default source-ip
     cts sxp connection peer *
     interface * / cts manual / policy static sgt
     interface * / cts manual / no propagate sgt
     radius server * / pac key
    (CISCOIOS-646 / RT28469)

Corrections:

  - Fixed order dependency problem, correct order is:
     no interface Multilink *
     controller SONET * / au-4 * tug-3 * / no tug-2 * unframed
    (CISCOIOS-652 / RT28580)

  - Fixed interface order dependency problem, correct order is:
     ethernet cfm enable
     switchport mode dot1q-tunnel
    (CISCOIOS-648 / RT28523)

  - Read device-profile from devices device <devname> device-profile.
    Default to cisco-ios.
    (CISCOIOS-647)


cisco-ios v5.2.4 [May 26, 2017]
===============================

Enhancements:

  - Added support for ned-settings cisco-ios-show-running-method scp-transfer
     to use SCP to retrieve running-config from device. Slower for small config
     files but possibly faster for large configuration files.
    (CISCOIOS-643)

  - Added new static warning exception:
     "Removing RRI will delete all routes and .."
    (CISCOIOS-642 / RT28414)

  - Split range terminal lines into single entries.
    (CISCOIOS-641 / RT28411)

  - Set police format to cirflat for C6503, C6506, C6509 and C6513.
    (CISCOIOS-640 / RT28413)

  - Additions to the YANG model:
     cable rpd * / core-interface Te* / rpd-ds 0 downstream-oob-vom
     cable rpd * / core-interface Te* / rpd-us * upstream-oob-varpd
     cable rpd * / sbfd enable
     controller downstream-oob *
     controller upstream-oob *
    (CISCOIOS-615 / RT28105)

Corrections:

  - Added retry on delete of BGP peer, message:
     "is being deleted.* Try it later"
    (CISCIOS-645 / RT28459)

  - API CHANGE:
     chat-script
      Changed 'line' from multi-value string to quoted string.
    (CISCIOS-635 / RT28360)

  - API CHANGE:
    Changed cisco-ios-police-format from leaf to leaf-list to enable support
     for multiple formats.


cisco-ios v5.2.3 [May 19, 2017]
===============================

Enhancements:

  - Workaround for NSO problem with dependencies, affecting interface Multilink
     and policy-map ordering.
    (CISCOIOS-638 / RT28399)

  - Additions to the YANG model:
     crypto pki trustpoint * / match certificate
    (CISCOIOS-637 / RT28390)

  - Workaround for NSO problem with dependencies, affecting policy-map and
     policy-map * / class * / service-policy ordering.
    (CISCOIOS-636)

  - Removed range check on msecs and min_rx in due to ISRv supporting 9000.
     interface * / bfd interval
    (CISCOIOS-634 / RT28359)

  - Additions to the YANG model:
     cable video / multicast-uplink
     cable oob / virtual-om *
     cable oob / virtual-arpd *
    (CISCOIOS-615 / RT28105)

Corrections:

  - Improved banner code, fixed problem with one liner banners.
    (CISCOIOS-639 / RT28394)

  - Split multiple class-map * / match ip precedence into lines of max 4
     entries each.
    (CISCOIOS-632 / RT28236)


cisco-ios v5.2.2 [May 12, 2017]
===============================

Enhancements:

  - Additions to the YANG model:
     bridge-domain * / member vni
     crypto ikev2 name-mangler * / dn
     crypto ikev2 profile * / aaa accounting cert
     crypto ikev2 profile * / aaa authorization group cert list
     crypto ikev2 profile * / match certificate *
     crypto ikev2 profile * / match identity remote email domain *
     crypto pki certificate map * / alt-subject-name *
     interface nve*
     interface nve* / member vni * / ingress-replication
    (CISCOIOS-631 / RT28274)

  - Support ip sla reaction-configuration * threshold-type average without N.
    (CISCOIOS-626)

  - Added cli-allow-abbrev-keys NedCapability for newer NSO where key
     abbreviations are default off.
    (CISCOIOS-622)

  - Additions to the YANG model:
     interface * / multilink min-links mandatory
    (CISCOIOS-614 / RT28104)

  - Added support for policy-map * / class * / police cir percent in flat mode
     (used with tailfned police cirflat)

  - Automatically set police mode to cirflat for C12KPRP devices.

  - Inject shutdown before deleting Multilink interface.

Corrections:

  - Added cli-recursive-delete to ipsec profile list in order to send
     no set ikev2-profile upon delete of ipsec profile.
    (CISCOIOS-630 / RT28267)

  - Fixed order dependency problems between:
     interface Serial* / encapsulation
     interface * / no peer neighbor-route
    (CISCOIOS-629 / RT28261)

  - Fixed order dependency problems between:
     interface Multilink*
     interface * / ppp multilink group
    (CISCOIOS-628 / RT28256)

  - Fixed order dependency problems between:
     interface Serial* / encapsulation
     interface Serial* / ppp
    (CISCOIOS-627 / RT28255)

  - Added ordered-by user on boot system list.
    (CISCOIOS-625 / RT28209)

  - Fixed order dependency problems between:
     interface Multilink* / ppp
     interface Multilink* / service-policy
    (CISCOIOS-624 / RT28211)

  - Fixed order dependency problems between:
     interface Serial* / encapsulation
     interface Serial* / service-policy
    (CISCOIOS-623 / RT28216)

  - Fixed order dependency problems between:
     router isis * / metric-style wide
     router isis * / mpls traffic-eng level-2
    (CISCOIOS-621 / RT28179)

  - Fixed order dependency problem between:
     interface * / encapsulation frame-relay
     interface * / frame-relay
    (CISCOIOS-620 / RT28170)

  - Fixed order dependency problem between:
     policy-map * / class * / service-policy
     policy-map * / class * / bandwidth
    (CISCOIOS-619 / RT28168)


cisco-ios v5.2.1 [May 5, 2017]
==============================

Enhancements:

  - Added new static warning exception:
     "Previously established LDP sessions may not have graceful restart protection"
    (CISCOIOS-617 / RT28134)

  - Additions to the YANG model:
     interface * / multilink min-links
     interface * / multilink max-links
     interface * / multilink max-fragments
    (CISCOIOS-614 / RT28104)

  - Additions to the YANG model:
     interface * / ppp multilink fragment-delay
    (CISCOIOS-612 / RT28103)

  - Expanded and completed 'interface * / dampening' config.
    (CISCOIOS-611 / RT28101)

  - Additions to the YANG model:
     no aaa accounting system guarantee-first
     aaa authentication fail-message
     cdp holdtime
     cdp timer
     interface * / vrrp * timers advertise (raised interval)
     ip policy-list * / match as-path *
     ip policy-list * / match community *
     mpls label mode all-vrfs
     netconf lock-time
     netconf ssh
     router bgp * / bgp asnotation dot
     route-map * / match policy-list *
    (CISCOIOS-610 / RT28092)

  - Additions to the YANG model:
     policy-map * / class * / set mpls experimental
    (CISCOIOS-605 / RT28036)

  - Additions to the YANG model:
     bfd slow-timers
     policy-map * / class * / police rate child-conform-aware
    (CISCOIOS-604 / RT28034)

Corrections:

  - API CHANGE:
     Added message-type (informs|traps) to snmp-server host key in order
      to support both entries at the same time.
    (CISCOIOS-616 / RT28073)

  - Fixed order dependency problem between:
     key config-key password-encrypt
     password encryption aes
    (CISCOIOS-613 / RT28098)

  - Fixed order dependency problems between:
     policy-map * / class * / random-detect
     policy-map * / class * / bandwidth
     policy-map * / class * / shape
    (CISCOIOS-609 / RT28074)

  - Support for device encrypted cipher in:
     line con * / password
    (CISCOIOS-608 / RT28054)

  - API CHANGE:
     interface Multilink changed from uint16 to uint32.
    (CISCOIOS-604 / RT28034)


cisco-ios v5.2.0 [April 28, 2017]
=================================

Enhancements:

  - Additions to the YANG model:
     l2vpn evpn instance * point-to-point / vpws context * / shutdown
    (CISCOIOS-607 / RT28047)

  - Additions to the YANG model:
     policy-map * / class * / random-detect * packets
    (CISCOIOS-606 / RT28037)

  - Additions to the YANG model:
     route-map * / set extcommunity soo
    (CISCOIOS-603 / RT28033)

  - API CHANGE:
     ip sla responder
      Wrapped responder container within a drop-node-name responder-enable
      container in order to avoid a bug with NSO-4.4 and commit-queues.
    (CISCOIOS-602)

  - Disable automatic caching of inventory items ("show inventory") in config.
     Introduced 'cisco-ios-cached-show-enable/inventory' ned-setting to enable.
    (CISCOIOS-601)

  - Additions to the YANG model:
     crypto ipsec profile * / set transform-set *
     crypto isakmp profile * / ca trustpoint
     crypto pki trustpoint * / chain-validation
     crypto pki certificate map *
    (CISCOIOS-599 / RT28012)

  - Removed cached-show contents from config-hash transaction id calculation.

  - Cleaned up and unified trace transform messages.

  - Added cached-show version xe-version.

Corrections:

  - API CHANGE:
    Added remove-before-change, replace-all and no-value-on-delete to:
     route-map * / set extcommunity rt *
      In order to be able to solve all range and additive behaviours.
    (CISCOIOS-603 / RT28033)

  - Fixed transaction-id bug with snmp-server user and commit queues.
    (CISCOIOS-600)

  - Added cli-replace-all to:
     ip dhcp pool * / option
     ip dhcp pool * / option * / instance *
    (CISCOIOS-596 / RT27952)


cisco-ios v5.1.5 [April 21, 2017]
=================================

Enhancements:

  - Additions to the YANG model:
     crypto pki server * / cdp-url
     crypto pki server * / database level
     crypto pki server * / database url
     crypto pki server * / issuer-name
     crypto pki server * / lifetime ca-certificate
     crypto pki server * / lifetime certificate
     crypto pki server * / no database archive
     crypto pki trustpoint * / fingerprint
     crypto pki trustpoint * / vrf
    (CISCOIOS-591 / RT27831)

  - Implemented cisco-ios-inject-answer ned-setting list, usable for injecting
     answers to device questions which show up when applying config, i.e. during
     a transaction. See README for more details.
    (CISCOIOS-591 / RT27831)

Corrections:

  - Fixed order dependency problems between:
     interface * / tunnel mpls and interface * / description
     l2vpn vfi context * / member * and template type pseudowire *
     interface Tunnel* and template * / preferred-path interface
    (CISCOIOS-593 / RT27902)

  - Added retry on delete of template type pseudowire.
    (CISCOIOS-593 / RT27902)

cisco-ios v5.1.4 [April 14, 2017]
=================================

Enhancements:

  - New ned-setting:
     cisco-ios connection-settings send-login-newline
      Set to true in order to send an initial newline in the login phase.
      Usable, for example, if the prompt is lost due to banner config.
    (CISCOIOS-589 / RT27767)

  - Throw exception for missing ned-setting leaves at connect.
    (CISCOIOS-586 / RT27811)

  - Added 'when' leaf to cisco-ios-replace-config list ned-setting. Usable to
     limit when the replace/filter entry should trigger, config-only or
     trans-id-only.
    (CISCOIOS-584)

  - Do not send "snmp trap link-status" when an interface is deleted.
    (CISCOIOS-582 / RT27772)

  - Updated and improved the README file on available ned-settings.

Corrections:

  - Fixed order dependency problem between:
     mpls traffic-eng
     interface * / mpls traffic-eng
    (CISCOIOS-590 / RT27807)

  - Restored support for "show ?" in live-status exec any command, broken
     in 5.1.2 when fixing (CISCOIOS-579 / RT27401).
    (CISCOIOS-585 / RT27794)

  - Added cli-replace-all to 'mls qos map cos-dscp|ip-prec-dscp'
    (CISCOIOS-583 / RT27780)

  - Fixed problem building with python3.4.3.
    (CISCOIOS-581 / RT27765)


cisco-ios v5.1.3 [April 8, 2017]
================================

Enhancements:

Corrections:

  - Fixed problem with setting and deleting snmp-server user, also affecting
     transaction-id calculation if set in same transaction as a secret.
    (CISCOIOS-574 / RT27673)


cisco-ios v5.1.2 [April 7, 2017]
================================

Enhancements:

  - Added support for restoring lost route-targets in ip vrf when rd is deleted
     or changed. Previously a rd change caused the device to loose all current
     route-targets, and only get newly added ones. The default behaviour is now
     for the NED to restore all route-targets on the device by pushing them
     down again. This new behaviour can be disabled by setting the ned-setting
      cisco-ios-auto ip-vrf-rd-restore to false.
    (CISCOIOS-577 / RT27687)

  - Re-enable service-insertion service-context entries when appnav-controller
     is added in a matching appnav-controller-group. Note: to avoid AppNav
     interface diff, try this new replace-config ned-setting:
      cisco-ios-replace-config if-appnav regexp "(\ninterface AppNav\\S+).*?(\n!)"
    (CISCOIOS-515 / RT26987)

  - Added AppNav static warnings:
     "ac with local ip is being removed\\. service context cannot be enabled"
     "service context must have a appnav controller group attached to it before it can be enabled"
     "last vrf removed\\. disable this service-context"
    (CISCOIOS-515 / RT26987)

  - Raised default retry counter from 66 to 90. Change with the ned-setting
     cisco-ios behaviour config-output-max-retries

Corrections:

  - Fixed problem with live-status exec any command interpreting "Device#" in
     show verison command as prompt.
    (CISCOIOS-579 / RT27401)

  - API CHANGE:
     Bug-fixed cisco-ios-replace-config list ned-setting, it can not handle
     multiple replacements in same config. Due to this fix, a "\n" is needed
     in injected before replacing a single line, e.g:
      cisco-ios-replace-config x regexp "\nno service-routing capabilities-manager"
    (CISCOIOS-574 / RT27673)

  - Fixed service-insertion order dependencies by always deleting them first
     and adding them last in a transaction, hence no longer dependant on
     interface cli-diff-dependency annotations.
    Removed suppress-no on 'service-insertion service-context * / enable'
    (CISCOIOS-515 / RT26987)

  - Restored cisco-ios-inject-config ned-setting temporarily broken in 5.1.1.


cisco-ios v5.1.1 [April 5, 2017]
================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Added cisco-ios-replace-config list ned-setting, usable for replacing or
     filtering out config line(s) upon reading config from device. See README
     for more details.
    (CISCOIOS-578)

  - Added 'where' leaf in cisco-ios-inject-config ned-setting list entry in
     order to be able to inject config last when syncing from device.
    (CISCOIOS-575)

  - Support for device encrypted cipher in:
     domain * / vrf * / border / password
    (CISCOIOS-574 / RT27673)

  - Support for running multiple commands in 'live-status exec any' and config
     mode 'exec' commands. Use the delimiter " ; " (note: must be white spaces)
     to separate each command. If need to answer prompts, supply them separatedly
     for each command, e.g.:
       exec "command 1 | prompts Y ; command 2 | prompts N"
     Warning: No error checking is done on command output, i.e. this command
              is 100% raw, you must check the errors yourself. Only exception
              (of course) is that config command may not leave config mode.
    (CISCOIOS-517 / RT26989)

Corrections:

  - Fixed order dependency problem between 'ipv6 access-list' and:
     interface * / ipv6 traffic-filter * <acl> in|out
     interface * / ipv6 verify unicast reverse-path <acl>
     snmp-server community * ipv6 <acl>
     snmp-server group * ipv6 <acl>
     snmp-server user * access ipv6 <acl>
    (CISCOIOS-576)

  - Removed order-by user from 'privilege exec level *' list.
    Strip meta-data prefix in NedSecrets if shown, to avoid bad regexp.
    (CISCOIOS-574 / RT27673)


cisco-ios v5.1.0 [March 31, 2017]
=================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Additions to the YANG model:
     vrf definition * / address-family ipv4 / import map
    (CISCOIOS-573 / RT27660)

  - Added DOTALL to inject-command pattern lookup.
    (CISCOIOS-573 / RT27660)

  - Added "| noprompts" option to live-status exec commands in order to be able
     to ignore (erronously) triggered question prompts. Also added 'IGNORE' as
     an option to '| prompts' in order to specify what prompts to ignore.
    (CISCOIOS-568)

  - Recognize "Cisco Wide Area Application Services Software" show version
     string as an IOS device.
    Improved exec proxy startup error checking.
    (CISCOIOS-567 / RT27524)

  - Added static warning exception for 'ip vrf * / no route-target' config:
     "unable to remove extended community"
    Please note that you can always set your own dynamic warning exceptions
     using the cisco-ios-config-warning ned-setting. See README for details.
    (CISCOIOS-562 / RT27476)

  - Added static warning exception for 'interface * / mdix auto' config:
     "speed auto-negotiation also needs to be set for auto-mdix to take effect"
    Please note that you can always set your own dynamic warning exceptions
     using the cisco-ios-config-warning ned-setting. See README for details.
    (CISCOIOS-552 / RT27374)

  - Log proxy ned-settings before use.

Corrections:

  - Fixed problems with 'key config-key password-encrypt' command.
    (CISCOIOS-571 / RT27552)

  - API CHANGE
     ip dns name-list *
    Changed YANG due to erronous modelling. New design is a list with key id
     with an internal list of regular expressions in it.
    (CISCOIOS-570 / RT27542)

  - Fixed order dependency problem between:
      mls qos aggregate-policer
      policy-map * / class * / police aggregate
    (CISCOIOS-565 / RT27477)

  - Do not send empty interface standby entry when all members are deleted.
    This fix will solve the problem with transaction aborting, but there
    will still be a compare-config diff due to the fact that the device
    removes the empty list.
    (CISCOIOS-536 / RT27236)


cisco-ios v5.0.15 [March 23, 2017]
==================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Additions to the YANG model:
     crypto ikev2 profile * / ivrf
    (CISCOIOS-563 / RT27485)

  - Added 'cisco-ios behaviour config-output-max-retries' ned-setting in order
     to configure maximum number of retries when sending config command to the
     device (default 66). Reason for retrying may be that a resource on device
     is not available yet.
    (CISCOIOS-562 / RT27476)

  - Additions to the YANG model:
     interface * / ipv6 mld query-timeout
     interface * / ipv6 mld query-interval
     interface * / ipv6 pim hello-interval
    (CISCOIOS-561 / RT27462)

  - Additions to the YANG model:
     l2vpn evpn instance * point-to-point / auto-route-target
     l2vpn evpn instance * point-to-point / rd
     l2vpn evpn instance * point-to-point / route-target
    (CISCOIOS-559 / RT27453)

Corrections:

  - Ignore 'shutdown can't be applied on standby interface' when sending
     'no shutdown', a result of interface being a backup interface.
    (CISCOIOS-566 / RT27504)

  - Fixed order dependency problem between:
      policer aggregate *
      policy-map * / class * / police aggregate
    (CISCOIOS-565 / RT27477)

  - Added disallow-value "blackhole|routing|next-fallback" on path-preference
     next-fallback-path[23] to avoid misconfigure.
    (CISCOIOS-564 / RT27498)

  - Fixed dependency problem with deletion of LISP interfaces by always deleting
     them last (which may mean they do not exist, but that error is ignored).
    (CISCOIOS-560 / RT27461)

  - Fixed dependency problem with crypto tls-tunnel and local-interface causing
     creation entry to be left incomplete.
    (CISCOIOS-560 / RT27461)


cisco-ios v5.0.14 [March 17, 2017]
==================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Additions to the YANG model:
     ip sla * / tcp-connect
     track * ipv6 route
     track * / ipv6 vrf
    (CISCOIOS-556 / RT27423)

  - Additions to the YANG model:
     l2vpn vfi context * / autodiscovery bgp signaling * template
    (CISCOIOS-554 / RT27346)

  - Additions to the YANG model:
     interface * / service-module / t1 timeslots
    (CISCOIOS-544 / RT27297)

Corrections:

  - Do not re-inject interface ip address after vrf change if interface
     is being deleted in same transaction.
    (CISCOIOS-558 / RT27413)

  - Added tailf:cli-remove-before-change on:
     policy-map * / class * / service-policy
    (CISCOIOS-557 / RT27426)

  - Added choice around track * ip[v6] route reachability and metric threshold.
    Solved issue deleting track * / ip[v6] vrf and vrf entry.
    (CISCOIOS-556 / RT27423)

  - Added tailf:cli-remove-before-change on:
     ip dhcp pool * network
    (CISCOIOS-555 / RT27418)

  - Fixed delete order dependency problem between:
      qos aggregate-policer
      policy-map * / class * / police aggregate
    (CISCOIOS-553 / RT27410)

  - Fixed order dependency: snmp-server engineID vs snmp-server user
    (CISCOIOS-551 / RT27377)

  - Fixed order dependency: delete existing router bgp before adding new.
    (CISCOIOS-550 / RT27369)

  - Added tailf:cli-remove-before-change on:
     router eigrp * / address-family ipv4
    (CISCOIOS-549 / RT27366)

  - Split multiple class-map * / match dscp values into lines of max 8 each.
    (CISCOIOS-547 / RT27354)

  - Fixed order dependency between:
     interface * / mtu
     interface * / service instance * / xconnect / mtu
    (CISCOIOS-545 / RT27313)


cisco-ios v5.0.13 [March 13, 2017]
==================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Additions to the YANG model:
     line * / modem autoconfigure type
    (CISCOIOS-543 / RT27298)

Corrections:

  - Added explicit-exit in ip dhcp top container to solve issue with top mode
     vrf deletion in same transaction.
    (CISCOIOS-546 / RT27336)

  - Fixed order dependency interface * / access-group vs ip access-list.
    (CISCOIOS-542 / RT27312)


cisco-ios v5.0.12 [March 08, 2017]
==================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Additions to the YANG model:
     l2vpn vfi context * / mtu
    (CISCOIOS-539 / RT27258)

  - Additions to the YANG model:
     cable fiber-node * / downstream Downstream-Cable *
     cable fiber-node * / downstream sg-channel *
     cable fiber-node * / upstream Cable *
     cable fiber-node * / service-group profile
     cable profile service-group * / mac-domain * / downstream sg-channel * upstream
     cable profile service-group * / mac-domain * / us-bonding-group * / admission-control
     cable rpd * / r-dti
     cable rpd * / rpd-event profile
    (CISCOIOS-538 / RT27251)

  - Additions to the YANG model:
     interface * / tunnel protection ipsec profile ? isakmp-profile|shared
    (CISCOIOS-535 / RT27227)

Corrections:

  - API CHANGE:
     cable rpd * / core-interface te* -> cable rpd * / core-interface Te*
    (CISCOIOS-538 / RT27251)

  - Fixed modification order problem with ip access-list's and the ned-setting
     cisco-ios-api new-ip-access-list by always deleting before adding.
    (CISCOIOS-541 / RT27269)

  - API CHANGE:
     service prompt config changed from enum leaf to boolean-no leaf in
      container, in order to show if set to 'no'. WARNING: do not set to
      'no' or the NED will get no prompt from device and will deadlock.
    (CISCOIOS-540 / RT27261)

  - Added missing default values:
     router bgp * / bgp graceful-restart restart-time 120
     router bgp * / bgp graceful-restart stalepath-time 360
    (CISCOIOS-533 / RT27216)

  - Fixed problem with deleting router ospf * / redistribute bgp
    (CISCOIOS-532 / RT26991)

  - API CHANGE:
     router ospf * / fast-reroute per-prefix enable
      Changed from container to list + leaf to support multiple entries.
    (CISCOIOS-531 / RT27215)

  - Fixed problem with bad transaction ID when commiting config with cleartext
     passwords encrypted by device in combination with ip vrf entries.
    (CISCOIOS-530 / RT27190)

  - Fixed order dependency with delete of routes by always deleting them first.
    (CISCOIOS-530 / RT27190)

  - Do not delete cached NedSecrets entry if differ vs device. This will
     make it possible to do a secrets resync even after a compare-config diff.
    (CISCOIOS-527 / RT27166)


cisco-ios v5.0.11 [March 03, 2017]
==================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Additions to the YANG model:
     interface * / carrier-delay up|down
    (CISCOIOS-529 / RT27178)

  - Additions to the YANG model:
     ip igmp snooping source-only-learning age-timer
    (CISCOIOS-528 / RT27173)

  - Added an internal live-status command to force a resync of all cached
     encrypted values used by NedSecrets. Useful to solve an issue where the
     device rehashes type 7 secrets with a different seed, causing the values
     to diff from the old ones, even using the same cleartext password.
     Issue like this:
      admin@ncs# devices device <devname> live-status exec any "secrets resync"
     Note: the command will do a "show running-config" from the device, hence
           may be quote slow for large configs.
    (CISCOIOS-527 / RT27166)

  - Additions to the YANG model:
     domain * / border / advanced / smart-probes burst
    (CISCOIOS-526 / RT27169)

  - Added support for restoring secondary ipv4 and all ipv6 addresses as well
     when vrf is changed and ned-settings cisco-ios-auto vrf-forwarding-restore
     is set to true (default = true).
    (CISCOIOS-525 / RT27164)

  - Additions to the YANG model:
     interface * / service instance * ethernet / rewrite ingress tag translate
    (CISCOIOS-524)

  - Additions to the YANG model:
     interface pseudowire* / mtu
    (CISCOIOS-523)

Corrections:

  - Support for device encrypted cipher in:
     router bgp * / address-family ipv[46] * vrf * / neighbor * password
    (CISCOIOS-530 / RT27190)


cisco-ios v5.0.10 [February 24, 2017]
=====================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Additions to the YANG model:
     crypto ikev2 client flexvpn * / peer * track
    (CISCOIOS-520)

  - Before creating
     service-insertion appnav-controller-group * / appnav-controller *
    Make sure any changes to interface Loopback * / ip address are
    shown first. Required to be able to add this command inject ned-setting
    for AppNav Controller changes support:
      devices device ext-isr4431 ned-settings cisco-ios-inject-command ISRWAAS-SCG-ENABLE
         config-line "appnav-controller \d+.*"
         command "service-insertion service-context waas/1\n enable" after-last
    (CISCOIOS-515 / RT26987)

Corrections:

  - Ignore "bridge-domain \\d+ cannot be deleted because it is not empty""
     since the bridge-domain entry is deleted regardless of the error text.
    (CISCOIOS-522)

  - API CHANGE:
    Changed fqdn from container to leaf to allow for new track option:
     crypto ikev2 client flexvpn * / peer * fqdn
    Changed source from container to list:
     crypto ikev2 client flexvpn * / source *
    (CISCOIOS-520)

  - Removed : after mpls label protocol tdp
    (CISCOIOS-519 / RT27026)

  - Added /ipv6/prefix-list/prefixes-no-seq list to support prefix lists
     without seq numbers (available with no ipipv6 prefix-list sequence-number).
    Delete ipv6 prefix-list entries before creating new.
    (CISCOIOS-518 / RT27025)

  - Added cli-recursive-delete to virtual-service in order to send no activate.
    (CISCOIOS-517 / RT26989)

  - Added tailf:cli-remove-before-change on:
     domain * / vrf * / master hub / hub
      Since the address needs to be removed before changed.
    (CISCOIOS-516)

  - API CHANGE:
    Fixed interface * / ip unnumbered dependency problem.
    Had to change unnumbered type from string to container
    with interface-name-grouping.
    (CISCOIOS-386 / RT25698)


cisco-ios v5.0.9 [February 16, 2017]
====================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Additions to the YANG model:
     route-map * / set global
    (CISCOIOS-514 / RT26945)

  - Support for device encrypted cipher in:
     router bgp * / neighbor * password
    (CISCOIOS-512 / RT26961)

Corrections:

  - Fixed problems with '?' character in passwords.
    (CISCOIOS-512 / RT26961)

  - Added ordered-by user on radius-server and tacacs-server host lists.
    CISCOIOS-511 / RT26674)

  - API CHANGE:
     Removed ip/route/ip-route-interface-vasileft-list and
      ip/route/ip-route-interface-vasiright-list.
     Use the normal ip-route-interface* lists.
    (CISCOIOS-510 / RT26934)

  - Fixed problem with ip route 'dhcp' option being discarded by NSO.
    Added cli-reset-container on ip-route-interface-list.
    (CISCOIOS-510 / RT26934)

  - Added support for rip entry without process id:
     router lisp / ipv4 route-import database rip
    (CISCOIOS-509)

  - API CHANGE:
     ip sla reaction-configuration *
      Changed key from operation-number to "operation-number react"
    (CISCOIOS-508)

  - Fixed problem with router ospf * / area stub and nssa.
    (CISCOIOS-504 / RT26874, CISCOIOS-506 / RT26899)

  - Strip console log messages from show running output to avoid corrupting
     transaction id when using config-hash method.
    (CISCOIOS-502 / RT26795)

  - Fixed problems with new inject of shutdown and delete of entire entry.
    (CISCOIOS-497 / RT26736)

  - Put back an old fix erronously removed:
    Removed tailf:cli-delete-when-empty on interface * / standby * preempt
    (RT23553)


cisco-ios v5.0.8 [February 10, 2017]
====================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Added new static warning exception:
     "Only classful networks will be redistributed"
    (CISCOIOS-503 / RT26867)

  - Additions to the YANG model:
     domain / master
     domain / border
     domain * / master hub / advanced / smart-probes burst
    (CISCOIOS-420 / RT26025)

Corrections:

  - Delete ip prefix-list entries before creating new.
    (CISCOIOS-505 / RT26900)

  - Remove router ospf * / area * nssa on device if deleted in NSO.
    (CISCOIOS-506 / RT26899)

  - Remove router ospf * / area * stub on device if deleted in NSO.
    (CISCOIOS-504 / RT26874)

  - Fixed newly introduced problem where ned-setting cisco-ios-auto
     interface-switchport-status did not remove the default inject
     of "no switchport" on Ethernet/Port-channel interfaces.

  - Fixed problem with inject of shutdown on older NCS/NSO versions.


cisco-ios v5.0.7 [February 03, 2017]
====================================

    WARNING:
    Calculating southbounds diffs using nso-4.0.4 may take "forever" due to
    a NSO bug (Issue tracker: #22205). Manifests when CDB contains many vlans.
    If you think you are experiencing this problem, upgrade to nso-4.0.5.

Enhancements:

  - Inject shutdown/no shutdown before modifying below containers/lists:
     pm-agent
     crypto tls-tunnel
    (CISCOIOS-497 / RT26736)

  - Support for NedSecrets on non-transaction id setups.
    (CISCOIOS-494)

  - New ned-setting:
     cisco-ios connection-settings device-output-delay
      Delay in milliseconds after each config command output to the device.
      Can for example be used to not overload link or device. Default 0.
    (CISCOIOS-491 / RT26671)

  - Support for encrypted authentication-key in
      router ospf * / area * virtual-link authentication-key

Corrections:

  - Fixed aaa new-model vs tacacs server dependency issue.
    (CISCOIOS-501 / RT26780)

  - Replaced "show logging" with "show logging xml" for improved performance
     showing the logging monitor|console|buffered config status.
    (CISCOIOS-500)

  - Fixed interface * / no nhrp route-watch vs ip nhrp dependency issue.
    (CISCOIOS-499 / RT26756)

  - Removed unnecessary presence container under cisco-ios-proxy-settings.
    (CISCOIOS-496)

  - API CHANGE:
     crypto gkm group * / server address
      due to support for ipv6 and ordered-by user list.
    (CISCOIOS-495)

  - Ignore error when deleting non-existant LISP interface.


cisco-ios v5.0.6 [January 27, 2017]
===================================

Enhancements:

  - Additions to the YANG model:
     crypto tls-tunnel * / overlay-address
    (CISCOIOS-493)

  - Additions to the YANG model:
     crypto ipsec profile * / responder-only
    (CISCOIOS-490 / RT26655)

  - Additions to the YANG model:
     ip extcommunity-list *
     route-map * / match extcommunity *
     route-map * / set extcomm-list
    (CISCOIOS-488 / RT26647)

  - Support for cisco-ios-proxy-settings remote-connection serial in order to
     support terminal server login, and optionally specify a second set of
     device login credentials. See README file for more details.
    (CISCOIOS-483 / RT26602)

  - Support for cisco-ios/connection-settings/prompt-timeout millisecond
     ned-setting. This ned-setting is set to device connect-timeout by default.
     If prompt-timeout triggers, a newline is sent to the device to wake it.
    (CISCOIOS-483 / RT26602)

  - Additions to the YANG model:
     key config-key password-encrypt
      Note:  password stored in NSO as tailf:aes-cfb-128-encrypted-string type.
      Note2: previous password deleted before setting new one, rendering all
             current type 6 encrypted keys useless.
    (CISCOIOS-480 / RT26580)

  - Removed old revision statements from YANG [internal node].

  - Changed ned-setting log format to match the YANG paths.

  - Suport for $8 maaapi encrypted passwords (previously only $4 was supported)

Corrections:

  - Discontinued support for NSO-4.3.0 due to NSO bug. Use NSO-4.3.1.

  - Added missing default value 'dhe-psk-aes256-cbc-sha1' on
     crypto tls-tunnel * / protection
    (CISCOIOS-492)

  - Removed bad navuExist() ERROR log.
    (CISCOIOS-455 / RT26387)


cisco-ios v5.0.5 [January 23, 2017]
===================================

Enhancements:

  - Moved new MaapiCrypto(mm) to place of use to avoid exception with erronous
     crypto initialization.
    (RT26604)

Corrections:


cisco-ios v5.0.4 [January 20, 2017]
===================================

Enhancements:

  - Additions to the YANG model:
     controller MediaType *
     controller * / fdl
     controller SONET * / rate
     controller SONET * / sts-1 *
     controller SONET * / threshold sf-ber
     controller SONET * / no TU-AIS
     interface * / esmc mode
     interface FortyGigabitEthernet0/4/1 *
    (CISCOIOS-479)

  - Additions to the YANG model:
     event manager applet * / action * snmp-trap
    (CISCOIOS-478 / RT26553)

  - Additions to the YANG model:
     cached-show inventory
      Caches all entries from "show inventory".
    (CISCOIOS-475 / RT26471)

  - Additions to the YANG model:
     interface * / power inline auto|never|static
    (CISCOIOS-473 / RT26502)

  - Additions to the YANG model:
     interface * / nhrp group
    (CISCOIOS-471 / RT26500)

  - Additions to the YANG model:
     crypto ikev2 policy * / match
    (CISCOIOS-470 / RT26499)

  - Additions to the YANG model:
     policy-map * / class * / queue-limit dscp * percent
    (CISCOIOS-469 / RT26498)

  - Additions to the YANG model:
     router bgp * / template peer-policy * remove-private-as
    (CISCOIOS-468 / RT26496)

  - Additions to the YANG model:
     router bgp * / template peer-policy * / soft-reconfiguration
    (CISCOIOS-467 / RT26495, CISCOIOS-472 / RT26501)

  - Additions to the YANG model:
     router bgp * / template peer-session * / disable-connected-check
    (CISCOIOS-466 / RT26494)

  - Additions to the YANG model:
     router bgp * / template peer-session * / transport
    (CISCOIOS-465 / RT26493)

  - Additions to the YANG model:
     router bgp * / template peer-session * / description
    (CISCOIOS-464 / RT26492)

  - Additions to the YANG model:
     bridge-domain * / member * service-instance * encapsulation
    (CISCOIOS-463)

Corrections:

  - Allow setting router isis * / set-overload-bit with no sub-leaves.
    (CISCOIOS-481 / RT26583)

  - API CHANGE:
     controller SONET * / ais-shut
      changed from type empty to cli-boolean-no type boolean.
    (CISCOIOS-479)

  - Ignore invalid command error on "no ipv[46] map-cache-persistent
    (CISCOIOS-477 / RT26552)

  - Fixed order dependency crypto map vs interface * / ipv6 crypto map.
    (CISCOIOS-477 / RT26552)


cisco-ios v5.0.3 [January 13, 2017]
===================================

Enhancements:

  - Additions to the YANG model:
     ip rsvp signalling refresh reduction
    (CISCOIOS-461 / RT26460)

  - Additions to the YANG model:
     logging snmp-authfail
    (CISCOIOS-459 / RT26449)

  - Additions to the YANG model:
     router bgp * / template peer-policy * / route-reflector-client
    (CISCOIOS-458 / RT26426)

  - Additions to the YANG model:
     router bgp * / template peer-policy * / prefix-list *
    (CISCOIOS-457 / RT26425)

  - Additions to the YANG model:
     crypto ikev2 fragmentation
    (CISCOIOS-456)

Corrections:

  - API CHANGE:
     crypto pki trustpoint * / revocation-check
      Changed from leaf to ordered-by user leaf-list.
    (CISCOIOS-462 / RT26472)

  - API CHANGE:
     Reverted bad change (CISCOIOS-434 / RT26134) done in cisco-ios 4.4.17.
     monitor session * destination|source
      is once more multi-word-key lists.
    (CISCOIOS-454 / RT26369)

  - Fixed router bgp vs route-map dependency problems: Make sure route-map
     entries are deleted before router bgp.
    (CISCOIOS-431 / RT26137)


cisco-ios v5.0.2 [January 06, 2017]
===================================

Enhancements:

  - Added type 7 password to crypto-key-grouping used in 10+ places.
    (CISCOIOS-453 / RT26356)

  - Additions to the YANG model:
     aaa group server radius * / server-private *
     interface * / ip pim passive
     ip tcp selective-ack
     monitor session * type erspan-source
     mpls discovery vpn
     route-map * / match local-preference
     router bgp * / neighbor * path-attribute discard * in
     spd headroom
    (CISCOIOS-451 / RT26310)

  - Include "ip addressd dhcp" in vrf-forwarding-restore ned-setting, i.e. if
     vrf is modified on an interface with DHCP, the DHCP setting is restored.
    (CISCOIOS-449 / RT26284)

  - Changed cisco-ios-auto / vrf-forwarding-restore ned-setting to default true.

  - Replaced internal debug command 'show fixed-config' with 'sync-from-file'
    The internal live-status exec any sync-from-file command is used to inject
    a config file in NETSIM. The next 1 sync-from will sync-from this file.
    For example:
      admin@ncs# devices device netsim-0 live-status exec any "sync-from-file
         /home/myuser/cisco-test-config.yxy"
      admin@ncs# devices device netsim-0 sync-from

Corrections:

  - API CHANGE:
     interface * / ip ospf network
      Changed from leaf to leaf-list in order to support setting both
      point-to-multipoint and non-broadcast.
     (CISCOIOS-452 / RT26319)

  - Set interface * / service instance * ethernet / bridge-domain after
     interface * / service instance * ethernet / l2protocol.
    (CISCOIOS-450 / RT26304)


cisco-ios v5.0.1 [December 28, 2016]
====================================

Enhancements:

Corrections:

  - Split monitor session * filter packet-type into good & bad, making it
     it possible to configure both simultaneously.
    (CISCOIOS-447 / RT26262)

  - Removed compact-syntax from monitor session *
    (CISCOIOS-434 / RT26134)


cisco-ios v5.0 [December 22, 2016]
==================================

Enhancements:

  - MAJOR UPGRADE:
    First version with support for commit queues. This feature only works
     with the latest versions of NSO (currently 4.3.0.1 and 4.2.2).
    (CISCOIOS-338 / RT25292, CISCOIOS-385 / RT25700)

  - Additions to the YANG model:
     monitor session * filter vlan
    (CISCOIOS-448 / RT26268)

  - Additions to the YANG model:
     monitor session * filter packet-type
    (CISCOIOS-447 / RT26262)

  - Additions to the YANG model:
     interface * / tunnel mpls traffic-eng name
     interface * / tunnel mpls traffic-eng path-option * verbatim
     ip explicit-path name * / index * next-address loose
     template * / control-word
     template * / mtu
     template * / preferred-path
    (CISCOIOS-446 / RT26255)

  - Additions to the YANG model:
     ip nat settings mode
    (CISCOIOS-443 / RT26241)

  - Added static warnings:
     "the email address configured in .* will be used as"
     "wait for .* license request to succeed"
    (CISCOIOS-442)

Corrections:

  - API CHANGE:
     vpdn-group * / l2tp tunnel password
      Due to support for encrypted vs cleartext mapping to avoid diff.

  - Properly clean up resources when closing NED.
    (CISCOIOS-445)

  - Remove usage of inets application and httpc in the the netsim PnP code
     since it is not available in the confd Erlang distribution".
    (CISCOIOS-382)


cisco-ios v4.4.17 [December 16, 2016]
=====================================

Enhancements:

  - IWAN 3.0 addditions to the YANG model:
     router lisp / service ipv4 / eid-table default
     exit-service-ipv4 and exit-service-ipv6
    (CISCOIOS-441)

  - Additions to the YANG model:
     router bgp * / neighbor * advertise-map * exist-map *
    (CISCOIOS-433 / RT26137)

  - Additions to the YANG model:
     flow exporter * / option application-table
     flow record * / collect flow sampler
     flow record * / collect ipv4 id
     flow record * / collect ipv4 source prefix
     netconf-yang / cisco-ia snmp-trap-control trap-list
     netconf-yang / cisco-odm actions
     parameter-map type regex *
    (CISCOIOS-429 / RT26098)

  - Added 'interface-switchport-status' ned-setting for reading Ethernet and
     Port-channel switchport status using "show int <interface> switchport"
     If enabled, inject "switchport". If disabled, inject "no switchport".
     If command not supported on interface, nothing is injected.
     WARNING: Enabling this option downgrades performance. For optimal
              performance, see section 8 "Fixing switchport issues" in README.
    (CISCOIOS-426 / RT26079)

  - Decrypt maaapi encrypted passwords (starting with $4$) before sending
     them to the device. Cleartext password decrypted by the NED are not
     logged in NED trace.
    (CISCOIOS-425 / RT26075)

  - Added static warning:
     "virtual service .* install has not completed"
    (CISCOIOS-386 / RT25698)

Corrections:

  - IWAN 3.0 changes to the YANG model:
     Added presence true to router lisp / service ip[v4|v6] containers.
    (CISCOIOS-441)

  - Fixed ip[v6] wccp vs interface ip[v6] wccp dependency problems.
    (CISCOIOS-440)

  - API CHANGE:
     router isis * / metric
     Adeds support for wide metric and level-1 & level-2 metric config.
    (CISCOIOS-439 / RT26182)

  - Fixed ethernet vs ip sla dependency problem.
    (CISCOIOS-436 / RT26153)

  - API CHANGE:
     monitor session * destination|source
      corrected from multi-word-key list to multi-value string.
    (CISCOIOS-434 / RT26134)

  - API CHANGE:
     monitor session * filter
      corrected from multi-word-key to specific model due to duplicate entries.
    (CISCOIOS-433 / RT26131)


cisco-ios v4.4.16 [December 09, 2016]
=====================================

Enhancements:

  - Added static warning for graceful-restart timers with active BGP sessions.
    (CISCOIOS-428 / RT26097)

  - Additions to the YANG model:
     mpls ldp nsr
    (CISCOIOS-423 / RT26042)

  - Include device_id in ncs-java-vm.log log entries.
    (CISCOIOS-421 / RT26028)

  - Additions to the YANG model:
     mpls traffic-eng auto-tunnel backup
     router isis * / distribute-list
     router isis * / microloop avoidance disable
    (CISCOIOS-418 / RT26022)

  - Additions to the YANG model:
     interface * / mls qos trust dscp pass-through cos
    (CISCOIOS-416 / RT26000)

  - Additions to the YANG model:
     interface * / group
    (CISCOIOS-415 / RT259921)

  - IWAN 3.0 addditions to the YANG model:
     control-plane host / management-interface
     crypto ikev2 authorization policy * / session-lifetime
     pm-agent / device type
     router lisp / etr map-server source-address
    (CISCOIOS-410 / RT25965)

  - API CHANGE:
    Support for caching cleartext password to avoid compare-config diff
     for the following config: ntp authentication-key *. Unfortunately
     the YANG had to be changed in order to enable the feature.
    (CISCOIOS-409 / RT25964)

Corrections:

  - Added tailf:cli-remove-before-change on new ip access-list in order to
     be able to update existing entries.
    (CISCOIOS-427 / RT26088)

  - Removed tailf:cli-diff-dependency on top policy-map list in order to
     solve bug with diff calculation with policy-map description and
     class maps.
    (CISCOIOS-424 / RT26043)

  - Fixed ip nat translation max-entries vs ip vrf order dependency problem.
    (CISCOIOS-422 / RT26036)

  - Removed tailf:cli-remove-before-change from track list, added
     when statements on track sub mode config and a mechanism to
     delete existing track when changing track type.
    (CISCOIOS-419 / RT26011)

  - Remove router ospf / default-information originate on device
     if deleted in NSO.
    (CISCOIOS-417 / RT26010)

  - Fixed interface service instance group and service-group dependency
     problem.
    (CISCOIOS-415 / RT259921)

  - Implemented new mechanism for supporting multiple default values hidden
     by device and still avoid a compare-config diff. Currently used on:
      interface * / wrr-queue cos-map *
      interface * / priority-queue cos-map *
    (CISCOIOS-411 / RT25973)

  - Fixed vrf definition vs router lisp / instance-id dependency problem.
    (CISCOIOS-410 / RT25965)

  - Added logging trap informational as default value.
    (CISCOIOS-409 / RT25964)

  - Changed exec command timeout time from connectTimeout to readTimeout.


cisco-ios v4.4.15 [December 02, 2016]
=====================================

Enhancements:

  - Added static warnings for crypto map * / [no] reverse-route static
     "this will remove all existing \\S+ on this map"
     "removing \\S+ will delete all routes and clear current ipsec"
    (CISCOIOS-413 / RT25977)

  - Additions to the YANG model:
     interface * / ipv6 policy route-map
     platform power redundancy-mode nplus1
     route-map * / match ipv6 route-source
    (CISCOIOS-412 / RT25974)

  - Add support for new PnP functionality in netsim, more specifically:
     Https 2-Way Authentication Support
     support Config Upgrade option for pushing Day0 to CPE
     Device SUDI Authentication service
     Certificate installation service
    (CISCOIOS-382)

Corrections:

  - API CHANGE:
     crypto map * ipsec-isakmp / set transform-set *
      changed from leaf to leaf-list
    (CISCOIOS-414 / RT25981)

  - Fixed crypto map delete problem for Catalyst devices by not including
     policy value in delete.
    (CISCOIOS-408 / RT25958)

  - Sort "ip nat translation max-entries vrf" entries prior to calculating hash
     checksum in order to avoid transaction id changing when device reboots and
     reorders them.
    (CISCOIOS-407 / RT25940)

  - Fixed router lisp / eid-table vs locator-set order dependency.
    (CISCOIOS-406 / RT25943)

  - Fixed interface * / standby version and standby list order dependency.
    Made "Warning: Current config does not permit HSRP version 1" an error.
    (CISCOIOS-405 / RT25934)

  - API CHANGE:
     snmp-server contact|location
      Removed tailf:cli-multi-value in order to not insert ""
    (RT25914)


cisco-ios v4.4.14 [November 24, 2016]
=====================================

Enhancements:

  - Added new optional ned-setting to change how NED writes config to memory:
     cisco-ios-write-memory-method
    Example:
     devices device <devname> ned-settings cisco-ios-write-memory-method
       "copy running-config startup-config"
    Note: default is "write memory"
    (CISCOIOS-401 / RT25863)

  - Additions to the YANG model:
     interface * / ospfv3 authentication
    (CISCOIOS-398 / RT25834)

  - Added 'cisco-ios-api new-ip-access-list' ned-setting for a new version of
     "ip access-list". A standard-extended combined list ordered by mandatory
     sequence numbers.
    (CISCOIOS-397 / RT25818)

  - CBR8 additions to the YANG model:
     cable downstream controller-profile *
     cable upstream controller-profile *
     cable profile downstream *
     cable profile mac-domain *
     cable profile service-group *
     cable profile wideband-interface *
     cable rpd *
     l2tp-class * / initial retransmit retries
     interface Wideband-Cable* / cable downstream attribute-mask
     ptp clock profile *
    (CISCOIOS-395)

  - Fixed problems with router bgp * / address-family * / redistribute:
     was not possible to delete redistribute entry in single transaction
    (CISCOIOS-396 / RT25799)

  - Additions to the YANG model:
     ip icmp redirect
     logging snmp-trap
    (CISCOIOS-379 / RT25639)

  - Added static warning:
     "configuring anyway"
    (CISCOIOS-379 / RT25639)

Corrections:

  - New method to solve interface / speed vs duplex ordering issue.
    (CISCOIOS-399 / RT25858, CISCOIOS-400 / RT25862, CISCOIOS-402)

  - Moved ip sla * / vrf dependency to avoid re-entering ip sla when
     it's deleted.
    (CISCOIOS-388 / RT25721)

  - Fixed interface * / ospfv3 vs ipv6 unicast-routing dependency problem.
    (CISCOIOS-398 / RT25834)

  - Fixed support for reading and writing alias config.
    API CHANGE:
     alias
      changed key from the line itself to "mode name"


cisco-ios v4.4.12 [November 18, 2016]
=====================================

Enhancements:

  - Added static warning:
     "%warning:"
    (CISCOIOS-393 / RT25781)

Corrections:

  - API CHANGE:
     crypto ipsec transform-set *
      Enforce cipher type exists in only transform (according to docs), i.e:
       transform1 = AH transform
       transform2 = ESP encryption + optional transform2-bits
       transform3 = ESP authentication
       transform4 = IP Compression
    (CISCOIOS-394 / RT25786)

  - Do not retry on "first remove .* from the above" device reply.
    (CISCOIOS-394 / RT25786)

  - Remove interface standby preempt on device if deleted in NSO.
    (CISCOIOS-390 / RT25738)

  - Support for configuring router id in route-map * / match source-protocol *
    (CISCOIOS-390 / RT25718)

  - Fixed ipv6 rip vrf-mode enable vs ipv6 router rip * / address-family ipv6 vrf
     dependency problem.
    (CISCOIOS-389 / RT25717)


cisco-ios v4.4.11 [November 15, 2016]
=====================================

Enhancements:

Corrections:

  - Changed cli-diff-dependency in order to avoid a serious NSO bug causing
     existing vlan's to be erronously deleted when a bridge-domain was added
     in an interface service instance.
    (CISCOIOS-387 / RT25727)


cisco-ios v4.4.10 [November 11, 2016]
=====================================

Enhancements:

  - IWAN 3.0 addditions to the YANG model:
     crypto gkm group *
     crypto map * * ckm
     crypto tls-tunnel *
     interface * / ipv6 address dhcp
     interface * / ipv6 nd autoconfig
     interface * / ipv6 unnumbered
     interface * / pm-path
     logging host ipv6 * vrf
     pm-agent
     router lisp / instance-id *
     router lisp / locator-set * / auto-discover-rlocs
     router lisp / service
     router lisp / syslog session changes
    (CISCOIOS-380 / RT25636)

  - Added static warning:
      "updated group cp to "
    (CISCOIOS-384 / RT25683)

Corrections:

  - Support for disabling logging console|monitor|buffered debugging.
    (CISCOIOS-379 / RT25639)

  - Restored error checking, broken after device reply split in v4.4.6.

  - Fixed router bgp neighbor activate vs inherit peer-session dependency.
    (CISCOIOS-383 / RT25660)

  - Fixed interface switchport vs ip dhcp snooping problem,
     correct order is:
     interface * /
      no ip dhcp snooping
      no switchport
    (CISCOIOS-381 / RT25632)

  - Added missing default 128 on:
     crypto ipsec transform-set * <transform1-bits>
    Note: Due to NSO bug with default values the when-statement had to be
          temporarily removed.
    (CISCOIOS-376 / RT25595)

  - Added missing default 30:
     interface * / ip pim query-interval
    (CISCOIOS-377 / RT25601)

  - Revisited interface speed vs duplex configuration order.
    (CISCOIOS-375 / RT25592)

  - Fixed interface * / switchport and ip source binding dependency problem,
     correct order is:
     ip source binding
     interface * / switchport
    (CISCOIOS-374 / RT25591)

  - Fixed useless check if last-config-change cisco-ios-transaction-id-method
    ned-setting gave a valid reply. If it does not, please change ned-setting
    to config-hash.


cisco-ios v4.4.9 [November 4, 2016]
===================================

Enhancements:

  - Optimized commit by rewriting special case transformation code.
    (CISCOIOS-372)

  - Additions to the YANG model:
     interface * / ipv6 wccp
    (CISCOIOS-369 / RT25537)

  - Additions to the YANG model:
     ip dhcp relay prefer known-good-server
    (CISCOIOS-368 / RT25541)

  - Additions to the YANG model:
     crypto map * * ipsec-isakmp / qos pre-classify
     enable secret level *
     ip ssh break-string
     ip ssh port *
     line * / autocommand
     menu *
    (CISCOIOS-365)

  - Additions to the YANG model:
     no service-routing capabilities-manager
     pnp profile * / transport * localcert|remotecert
     pnp profile * / transport * vrf
     voice-card * / no watchdog
    (CISCOIOS-344 / RT25338)

  - Additions to the YANG model:
     fabric buffer-reserve queue
     interface AppNav-Compress*
     interface AppNav-UnCompress*
     interface * / glbp * ip * secondary
     interface * / isdn termination multidrop
     ip igmp snooping last-member-query-interval
     ip msdp cache-sa-state
     ip nbar attribute-map * / attribute application-group
     mpls ldp password required
     resource policy / policy * global / system
     transport-map type persistent *

Corrections:

  - Fixed ethernet evc/cfm and interface * / service * ethernet / cfm
     dependency problems.
    (CISCOIOS-367 / RT25531)

  - Split multiple name-server entries into single entries upon sync-from.
    Added ordered-by user on ip name-server list.
    (CISCOIOS-371 / RT25557)

  - Removed tailf:cli-reset-siblings from police actions in cirflat mode.
    (CISCOIOS-337 / RT25289)

  - Fixed problem with abort rollback sending too many exit commands,
    deadlocking NED connection due to disconnect.


cisco-ios v4.4.8 [October 28, 2016]
===================================

Enhancements:

  - Additions to the YANG model:
     cable service type *
     route-map * / set default interface
     route-map * / set ip next-hop recursive
    (CISCOIOS-363)

  - Optimized sync-from device by optimizing config transformation after
     retrieving config from device.
    (CISCOIOS-359)

  - Added timing info log(s) in ncs-java-vm.log.
    Note: more detailed with cisco-ios-log-verbose ned-setting.
    (CISCOIOS-359)

  - Additions to the YANG model:
     key chain * / key * / accept-lifetime
     key chain * / key * / send-lifetime
    (CISCOIOS-360 / RT25491)

  - Additions to the YANG model:
     router eigrp * / neighbor *
    (CISCOIOS-361 / RT25492)

Corrections:

  - API CHANGE:
     route-map * / set ip next-hop
      Removed choice statement for sub leaves.
    (CISCOIOS-363)

  - API CHANGE:
     interface * / ip bandwidth-percent *
      Changed from container to list.
    (CISCOIOS-362 / RT25493)


cisco-ios v4.4.7 [October 26, 2016]
===================================

Enhancements:

  - Added /ip/prefix-list/prefixes-no-seq list to support prefix lists
     without seq numbers (available with no ip prefix-list sequence-number).
    (CISCOIOS-350 / RT25411)

Corrections:

  - Enable retry on "wait for the current operation to complete"
    (CISCOIOS-352 / RT25338)

  - Fixed problem with modifying policy-map * / class * / priority
    (CISCOIOS-358 / RT25455)

  - API CHANGE:
     policy-map * / class * / police [with tailfned police cirflat]
      Reason being cirmode only supports one action type.
    (CISCOIOS-337 / RT25289)

  - Fixed interface standby ip vs ip vrf forwarding dependency problem,
     correct order is:
     interface * /
      ip vrf forwarding
      standby * ip
    (CISCOIOS-353 / RT25447)

  - Fixed interface service instance and switchport dependency problem.
    (CISCOIOS-340 / RT25308)

  - Added cli-replace-all to 'ip host'.
    (CISCOIOS-357 / RT25452)

  - Fixed interface service instance and no ip address dependency problem,
     correct order is:
     interface * /
      no ip address
      service instance * ethernet
    (CISCOIOS-356 / RT25451)

  - Re-insert lacp rate after switchport change on interface since it
     clears the lacp rate config.
    (CISCOIOS-354 / RT25448)

  - Fixed interface switchport vs l2vpn xconnect context dependency problem,
     correct order is:
      no l2vpn xconnect context *
      interface * / ip address
    (CISCOIOS-355 / RT25449)

  - Re-insert duplex after media-type change on interface since media-type
     clears the duplex setting.
    (CISCOIOS-329 / RT25263)


cisco-ios v4.4.6 [October 21, 2016]
===================================

Enhancements:

  - Additions to the YANG model:
     class-map type appnav *
     domain * / vrf * / master hub / advanced / channel-unreachable-timer
     domain * / vrf * / master hub / load-balance advanced / path-preference [API CHANGE]
     domain * / vrf * / master hub / load-balance [API CHANGE]
     domain * / vrf * / master hub / class * / match application *
     domain * / vrf * / master hub / class * / path-last-resort
     flow record * / collect counter bytes long
     flow record * / collect counter flows
     flow record * / collect counter packets long
     flow record * / collect flow
     flow record * / collect routing vrf
     flow record * / collect timestamp absolute
     flow record * / match application name
     flow record * / match connection transaction-id
     interface * / service-insertion waas
     interface VirtualPortGroup *
     ip nbar attribute-map *
     ip nbar attribute-set *
     ip nbar custom * composite
     ip sla responder udp-echo
     performance monitor context * profile / traffic-monitor all
     policy-map type appnav *
     policy-map * / class * / distribute service-node-group *
     policy-map * / class * / monitor-load
     policy-map * / class * / pass-through
     route-map * / match ip route-source
     route-map * / set ip next-hop verify-availability
     route-map * / set ip next-hop verify-availability *
     router eigrp * / address-family ipv4 / af-interface * / stub-site wan-interface
     router eigrp * / address-family * / eigrp stub-site
     service-insertion
     virtual-service * / profile
     virtual-service * / vnic gateway VirtualPortGroup*
    (CISCOIOS-344 / RT25338)

  - Additions to the YANG model:
     platform punt-policer *
     platform punt-sbrl subscriber rate
     platform punt-sbrl wan punt-cause *
    (CISCOIOS-348)

  - Additions to the YANG model:
     ip sla * / http get
    (CISCOIOS-346 / RT25445)

  - Additions to the YANG model:
     ip sla responder twamp / timeout
     ip sla server twamp
    (CISCOIOS-343 / RT25324)

Corrections:

  - Re-insert service-policy after switchport change on interface since it
     clears the service-policy entries.
    (CISCOIOS-340 / RT25308)

  - Re-insert speed after media-type change on interface since media-type
     clears the speed setting.
    (CISCOIOS-329 / RT25263)

  - Sort "ip route vrf" entries prior to calculating hash checksum in order
     to avoid transaction id changing when device reloads and reorders them.
    (CISCOIOS-342 / RT25326)

  - API CHANGE:
     router bgp * / bgp listen range *
     Changed from container to list.
    (CISCOIOS-344 / RT25338)

  - API CHANGE:
     ip sla reaction-configuration * threshold-type xOfy
     Corrected to take 2 values.
    (CISCOIOS-346 / RT25445)

  - Replace interface / no speed with interface / speed auto on ME3600-X
    (CISCOIOS-329 / RT25263)

  - Split device reply on % and check each string for errors separately.
    (CISCOIOS-336 / RT25272)

  - Fixed interface switchport vs ip verify dependency problem,
     correct order is:
     interface * /
      [no] ip verify
      no switchport
    (CISCOIOS-335)


cisco-ios v4.4.5 [October 14, 2016]
===================================

Enhancements:

  - Additions to the YANG model:
     event manager applet * / action * regexp
     event manager applet * / action * syslog
     event manager applet * / action * wait
    (CISCOIOS-331)

  - Added static warning:
     "applying vlan changes may take few minutes"
    (CISCOIOS-330 / RT25264)

  - Additions to the YANG model:
     interface * / logging event spanning-tree status
    (CISCOIOS-327 / RT25238)

  - Additions to the YANG model:
     bfd-template single-hop * / interval
     class-map type traffic *
     logging queue-limit

Corrections:

  - Fixed problems with deleting call-home / profile
    (CISCOIOS-334 / RT25275)

  - Fixed interface switchport vs ip dhcp snooping problem,
     correct order is:
     interface * /
      switchport
      [no] ip dhcp snooping
    (CISCOIOS-333 / RT25270)

  - Fixed interface switchport vs udld dependency problem,
     correct order is:
     interface * /
      no udld
      switchport
    (CISCOIOS-332 / RT25272)


cisco-ios v4.4.4 [October 10, 2016]
===================================

Enhancements:

  - Ignore all device errors in abort phase in order to minimize risk of out
    of sync or config diff due to the reverse transaction failing too.

  - Additions to the YANG model:
     interface * / ospfv3 area|priority
     interface * / ipv6 rip * default-information
     ipv6 rip vrf-mode enable
     router lisp / ipv4|ipv6 route-import database
     router ospf * / capability vrf-lite
     router ospfv3 * / address-family * / capability
    (CISCOIOS-321 / RT25140)

  - Additions to the YANG model:
     crypto ikev2 profile * / match address local *
     crypto ikev2 profile * / virtual-template ? mode auto [API CHANGE]
    (CISCOIOS-319 / RT25108)

  - Additions to the YANG model:
     interface * / ip verify source
     ip source binding *
    (CISCOIOS-317 / RT25092)

  - Additions to the YANG model:
     interface * / ip nhrp server-only
     interface * / ipv6 nhrp server-only
    (CISCOIOS-326 / RT25183)

  - Additions to the YANG model:
     crypto isakmp profile * / description
    (CISCOIOS-316 / RT25021)

  - Additions to the YANG model:
     crypto map * * ipsec-isakmp / reverse-route
     crypto map * * gdoi / set reverse-route
    (CISCOIOS-315 / RT25020)

Corrections:

  - API CHANGE:
     class-map * / match access-group
    Changed from leaf to leaf-list due to device support for multiple entries.
    (CISCOIOS-325 / RT25182)

  - Fixed problem with deleting router LISP and interface LISP.
    (CISCOIOS-318 / RT25109)

  - Fixed interface service instance and cdp enable dependency problem,
     correct order is:
     interface * /
      no cdp enable
      service instance * ethernet
    (CISCOIOS-322 / RT25151)

  - Fixed another dependency problem with moving vlan name. Previous fix only
    worked if the existing vlan was deleted. This fix also works if only the
    previous name is deleted.
    (CISCOIOS-280 / RT24681)

  - Fixed bug with range list compact and mode config.


cisco-ios v4.4.3 [September 25, 2016]
=====================================

Enhancements:

  - Additions to the YANG model:
     spanning-tree mst configuration / revision
     spanning-tree mst forward-time
     spanning-tree portfast edge
     system jumbomtu
    (CISCOIOS-313 / RT24985)

  - Additions to the YANG model:
     ip sla * / profile packet / source-ip-addr
     ip sla * / service-performance * type ethernet / measurement-type / delay|jitter|receive
     ip sla * / service-performance * type ip
    (CISCOIOS-310)

  - Additions to the YANG model:
     interface HundredGigE *
    (CISCOIOS-309 / RT24966)

  - Additions to the YANG model:
     qos account layer2 encapsulation length
     qos aggregate-policer *
     no qos rewrite ip dscp
   (CISCOIOS-307 / RT24922)

  - Added static warning:
     "setting device to vtp .*"

Corrections:

  - API CHANGE:
     spanning-tree portfast
    Due to bad modeling.
    (CISCOIOS-313 / RT24985)

  - Split vlan lines into maximum 32 comma separated entries per line.
    (CISCOIOS-304 / RT24916)

  - Fixed interface * / service instance * ethernet / rewrite vs encapsulation
     dependency issue. Encapsulation must be set before rewrite.
    (CISCOIOS-311 / RT24981)

  - API CHANGE:
    Added presence true and cli-delete-when-empty on xconnect container:
     interface * / service instance * ethernet / xconnect
     interface ATM* / pvc * / xconnect
     interface CEM* / cem * / xconnect
     connect * / xconnect
    Reason being without it it was not possible to properly delete it. NSO
    erronously tried to enter xconnect without mandatory mode leaves.
    (CISCOIOS-311 / RT24981)

  - Removed bad trimming of device hw-module entries, stopping sync-from.
    (CISCOIOS-285 / RT24749)

  - API CHANGE:
     qos
    moved to leaf due to must be able to delete and create separately.
   (CISCOIOS-306 / RT24922)


cisco-ios v4.4.2 [September 21, 2016]
=====================================

Enhancements:

  - Additions to the YANG model:
     ethernet cfm enable
     interface * / no ethernet cfm enable
    (CISCOIOS-303 / RT24905)

  - Additions to the YANG model:
     crypto ikev2 profile * / match identity any
     crypto map * gdoi fail-close
     interface * / ipv6 tcp adjust-mss
     interface * / tunnel protection ipsec profile ? ikev2-profile
     ipv6 flowset
    (CISCOIOS-297 / RT24835)

  - Additions to the YANG model:
     mpls label range static
    (CISCOIOS-301)

  - Additions to the YANG model:
     l2vpn vfi context * / autodiscovery bgp signaling ldp / vpls-id
    (CISCOIOS-298)

  - Additions to the YANG model:
     interface pseudowire* / pseudowire type
    (CISCOIOS-296)

Corrections:

  - Added ordered-by user on ip dns view * / domain name-server vrf *
    (CISCOIOS-302 / RT24884)

  - Fixed interface pseudowire* / signaling protocol, label and pseudowire type
    dependencies, correct order is:
     interface pseudowire* /
      signaling protocol none
      label
      pseudowire type
    (CISCOIOS-301)

  - Fixed dependency between interface GigabitEthernet* / service instance
    and l2vpn evpn instance * point-to-point / vpws context * / member
    correct order is:
     no l2vpn evpn instance * point-to-point / vpws context * / member
     no interface GigabitEthernet0/0/2 / service instance
    (CISCOIOS-300)

  - API CHANGE:
     l2vpn vfi context * / autodiscovery bgp signaling bgp
    due to support for 'autodiscovery bgp signaling ldp'
    (CISCOIOS-298)

  - Fixed interface * / mtu vs channel-group dependency issue,
     interface * /
      mtu
      channel-group
    (CISCOIOS-299 / RT24842)

  - Improved interface Serial pattern for main and subif.
    (CISCOIOS-305)


cisco-ios v4.4.1 [September 16, 2016]
=====================================

Enhancements:

  - Add vtp mode, pruning, version and domain to show running-config (sync-from)
    (CISCOIOS-295 / RT24820)

  - Added static warning:
     "changing vtp domain name from"
    (CISCOIOS-295 / RT24820)

  - Added (optional) sequence number fo access-list *
     Note: Shown on device when "ip access-list persistent" is set.
          Must do a sync-from after changing ip access-list persistent.
    (CISCOIOS-287 / RT24757)

  - Additions to the YANG model:
     hw-module module * port-group *
    (CISCOIOS-285 / RT24749)

  - Additions to the YANG model:
     call-home / profile * / destination address
     call-home / source-interface
     ip http client proxy-server
     ip wccp source-interface
     ip wccp vrf *
     ipv6 wccp
     router bgp * / address-family * / redistribute lisp
     router lisp / ipv4 map-resolver map-request validate source
     router lisp / syslog verbose
    (CISCOIOS-288 / RT24761)

Corrections:

  - Fixed router bgp * / neighbor * inherit peer-session dependency issue,
    correct order is:
     router bgp * /
      neighbor * inherit peer-session
      neighbor * description
    (CISCOIOS-292 / RT24814)

  - Fixed interface * / switchport vs channel-group dependency issue,
    correct order is:
     interface * /
      no channel-group
      no switchport
    (CISCOIOS-291 / RT24791)

  - Fixed problem in setting:
     route-map * / match ipv6 address
    Entry must be deleted before being set.
    (CISCOIOS-293 / RT24818)

  - REVERTED API CHANGE in v4.4:
     interface speed and media-type changed back to enum leaves due to
     NSO bugs with default values and choice statements. This change
     also restored the XML backwards compatibility.
    (CISCOIOS-276 / RT24046)

  - API CHANGE:
     ip http client source-interface - changed from multi-value leaf to container
     interface * / ip multicast boundary - changed from list to container
     router lisp / ipv6 proxy-itr - allow v4 address before v6
    (CISCOIOS-288 / RT24761)

  - API CHANGE:
     no ip igmp snooping
     no ip igmp snooping vlan *
     ip igmp snooping l2-entry-limit
    Changed because it was not possible to send 'no ip igmp snooping'
    (CISCOIOS-286 / RT24751)

  - Fixed problem with # triggering prompt and aborting output of live-status
     exec any show commands.
    (CISCOIOS-289 / RT24711)


cisco-ios v4.4 [September 09, 2016]
===================================

Enhancements:

  - Optimized creation/deletion of range list config entries:
     vlan *
     spanning-tree mst *
     spanning-tree vlan *
    Individual entries compacted to range syntax upon commit.
    (CISCOIOS-269 / RT24210, CISCOIOS-283 / RT24728)

  - Additions to the YANG model:
     spanning-tree uplinkfast
     spanning-tree uplinkfast max-update-rate
    (CISCOIOS-282 / RT24726)

  - Additions to the YANG model:
     firewall vlan-group *
     interface * / cablelength
     interface * / tunnel mpls traffic-eng auto-bw
     mls ipv6 vrf
     network-clock input-source * ptp domain
     network-clock-participate T1 *
     route-map * / match ipv6 next-hop
     spanning-tree pathcost method
   (CISCOIOS-274)


Corrections:

  - API CHANGE:
    interface media-type and speed changed from enum leaf to container with
    leaves in choice statement in order to fix media-type rj45 vs mdix and
    speed dependency issues, correct order is:
     interface *
      [no] mdix auto
      [no] speed
      no media-type rj45
    (CISCOIOS-276 / RT24046)

  - Fixed interface * / media-type sfp vs speed and mdix auto dependency,
    correct order is:
     interface * /
      [no] speed
      [no] mdix auto
      media-type sfp
    (CISCOIOS-276 / RT24046)

  - Fixed interface * / switchport vs ip route-cache dependency,
    correct order is:
     interface * /
      no switchport
      [no] ip route-cache
     Note: By means of new meta-data interface diff move
    (CISCOIOS-277)

  - Fixed dependency problem with moving vlan name from one vlan to another.
    Must delete the vlan with the name before setting the name on the new vlan.
    (CISCOIOS-280 / RT24681)

  - Dependency fix in interface *
     Remove tunnel mpls traffic-eng before tunnel mode mpls traffic-eng
    (CISCOIOS-275)

  - Strip duplicate 'no switchport' in interface (inserted by NSO bug)

  - Fixed interface * / switchport vs service-policy input|output dependency,
    correct order is:
     interface * /
      no switchport
      service-policy input|output


cisco-ios v4.3.11 [September 02, 2016]
======================================

Enhancements:

  - Additions to the YANG model:
     crypto map * ipsec-isakmp / set security-association dfbit
     crypto map * ipsec-isakmp / set security-association ecn
     crypto map * ipsec-isakmp / set security-association idle-time
     crypto map * ipsec-isakmp / set security-association level per-host
    (CISCOIOS-268 / RT24616)

  - Additions to the YANG model:
     ip msdp description *
     ip msdp keepalive *
     ip msdp sa-filter in|out *
     ip pim spt-threshold
    (CISCOIOS-267 / RT24607)

  - Added optional 'regexp' leaf to cisco-ios-inject-config in order to be
    able to insert config at specified location(s). See README for more info
    and example.
    (CISCOIOS-266 / RT24606)

Corrections:

  - API CHANGE:
     ip pim rp-address changed from container to list.
    (CISCOIOS-267 / RT24607)

  - Fixed problems with router bgp * / address-family * / redistribute:
    sub leaf values were lost on device when changing entry
   (CISCOIOS-264 / RT24564, CISCOIOS-265 / RT24565)

  - API CHANGE:
     router bgp * / address-family * / redistribute ospf *
    changed due to bad vrf representation (only one needed).

  - Fixed problems with router bgp * / address-family * / redistribute:
     was not possible to delete redistribute entry in single transaction
    (CISCOIOS-259 / RT24516)

  - Re-fixed version string extraction for ios-xe devices (broken in 4.3.10).


cisco-ios v4.3.10 [August 26, 2016]
===================================

Enhancements:

  - Additions to the YANG model:
    performance monitor context * profile / exporter destination *
   (CISCOIOS-257 / RT24510)

  - Additions to the YANG model:
     event manager applet * / event ipsla
     event manager applet * / event snmp
     event manager applet * / trigger / correlate
     exception data-corruption buffer truncate
     firewall multiple-vlan-interfaces
     ip wccp web-cache
     mls mpls tunnel-recir
     no monitor session servicemodule

  - Expanded interface ucse *

  - Additions to the YANG model:
     power-supply dual
    (CISCOIOS-245 / RT24351)

  - Additions to the YANG model:
     interface * / mls qos trust cos pass-through dscp
     interface * / mls qos monitor
     mls qos cos policy-map
     rtr responder
    (CISCOIOS-255 / RT2440)

  - Added 2nd group leaf in aaa authentication login|enable|ppp config.
    (CISCOIOS-254 / RT24119)

  - Additions to the YANG model:
     l2vpn xconnect context * / no remote link failure notification
    (CISCOIOS-256)

  - Added static warning:
     "is not a legal lat node name"
    (CISCOIOS-242 / RT24315)

  - Added static warning:
     "conform burst size \\S+creased to"
    (CISCOIOS-213 / RT24105)

  - Additions to the YANG model:
     interface Service-Engine *
     platform qos match-statistics|performance-monitor|punt-path-matching
    (CISCOIOS-236 / RT24273)

  - Support for kbps in
     platform hardware throughput level
    (CISCOIOS-236 / RT24273)

Corrections:

  - Raised ip route * track range from 1-500 to 1-1000.
    (CISCOIO-261 / RT24531)

  - API CHANGE:
     router bgp * / neighbor * fall-over bfd
    changed PATH due to inconsistency vs device.
    (CISCOIOS-262 / RT24518)

  - Added workaround for old Maapi.attach() bug which could cause an error, now
     uses 2-arg version of attach() which avoids problem in NCS prior to 4.1.
    (CISCOIOS-260)

  - Fixed interface * / performance monitor vs performance monitor * / exporter
    destination dependency, correct order is:
     interface * / no performance monitor context
     performance monitor context * / no exporter destination
   (CISCOIOS-257 / RT24510)

  - Fixed IOS version string extraction for cat3550 and cat6500.

  - Fixed support for sending commands ending with ?

  - Fixed interface * / switchport vs standby and ip redirects dependency,
    correct order is:
     interface * /
      no standby *
      ip redirects
      switchport

  - Fixed interface * / switchport vs service instance * ethernet
    dependency issue, correct order is:
     interface *
      no service instance * ethernet
      no switchport
    (CISCOIOS-252 / RT24421)

  - Fixed interface * / speed vs media-type dependency issue, correct order is:
     interface *
      no media-type
      speed
     (CISCOIOS-251 / RT24402)

  - Fixed ip vrf * / rd vs router bgp * / address-family * vrf * / neighbor
    dependency issue, correct order is:
     router bgp * / address-family * vrf / no neighbor *
     ip vrf * / [no ]rd
    (CISCOIOS-211 / RT24079)


cisco-ios v4.3.9 [August 19, 2016]
==================================

Enhancements:

  - Mapped host ping from netsim.
    (CISCOIOS-250)

  - Additions to the YANG model:
     interface ucse*    (note: template only, no ucse specific config)
    (CISCOIOS-216 / RT24128)

  - Additions to the YANG model:
     service password-recovery
    (CISCOIOS-248 / RT24348)

  - Additions to the YANG model:
     stackmaker name
    (CISCOIOS-247 / RT24349)

  - Removed auto-inject of the following hard coded commands (before 'no crypto ikev2 keyring')
     do clear crypto session
     do clear crypto ikev2 sa fast
    Use new cisco-ios-inject-command ned-setting to register commands to inject:
     devices global-settings ned-settings cisco-ios-inject-command C1 config-line "no crypto ikev2 keyring \\S+" command "do clear crypto session" before-first
     devices global-settings ned-settings cisco-ios-inject-command C2 config-line "no crypto ikev2 keyring \\S+" command "do clear crypto ikev2 sa fast" before-first
    See README for more information.
    (CISCOIOS-204 / RT23918)

Corrections:

  - Added ordered-by user on router eigrp * / address-family * / af-interface
    (CISCOIOS-244 / RT24356)

  - Fixed interface * / service instance * ethernet / rewrite vs xconnect
    dependency issue, correct order is:
     interface *
      service instance * ethernet
       no xconnect *
       rewrite ingress tag pop 1 symmetric
    (CISCOIOS-224 / RT24150)

  - Fixed interface * / service instance * ethernet / rewrite vs encapsulation
    untagged dependency issue, correct order is:
     interface *
      service instance * ethernet
       no rewrite ingress tag pop 1 symmetric
       encapsulation untagged
    (CISCOIOS-224 / RT24150)


cisco-ios v4.3.8 [August 12, 2016]
==================================

Enhancements:

  - Additions to the YANG model:
     interface * / clock rate
    (CISCOIOS-229 / RT24190)

  - Additions to the YANG model:
     controller nid *
     l2vpn vfi context * / member *
     l2vpn vfi context * / autodiscovery bgp signaling ldp
     l2vpn evpn instance * point-to-point
     router isis * / segment-routing mpls
     segment-routing mpls
    (CISCOIOS-237)

  - Added static warning:
    "% ospf will not operate on this interface until ip is configured on it"

Corrections:

  - Fixed problems with checksum calculation.
     Strip left over whitespaces in show running-config after filtering.
     Strip 'No configuration change since last restart line' comment.
    (CISCOIOS-225 / RT24153)

  - API CHANGE: Moved cisco-ios-oper data used to cache encrypted secrets
     from devices/device to devices/device/ned-settings in order to optimize
     the CLI when used with a large number of devices.
    (CISCOIOS-239)

  - Added new package parameter "requires-transaction-states".
     This will instruct the NSO that this NED can not be used together
     with commit queues.
    (CISCOIOS-238)

  - Reverted bad API CHANGE in v4.3.7 in:
     aaa authentication login *
     aaa authorization exex *
     aaa authorization commands *
    Fixed problem with aaa local and local-case.
    Fixed if-authenticated and local order in aaa authorization.
    (CISCOIOS-214) (CISCOIOS-231)


cisco-ios v4.3.7 [August 05, 2016]
==================================

Enhancements:

  - Additions to the YANG model:
     interface * / ip multicast limit
     interface POS.* / aps
    (CISCOIOS-215)

  - Additions to the YANG model:
     ip access-list persistent
    (CISCOIOS-225)

  - Additions to the YANG model:
     logging console filtered
     logging monitor
    (CISCOIOS-217)

  - Additions to the YANG model:
     archive / log config / notify
     archive / log config / record
    (CISCOIOS-218)

  - Additions to the YANG model:
     interface * / glbp *
     ip dhcp pool * / netbios-node-type
    (CISCOIOS-232)

  - Added support for new tailf:meta-data "suppress-no-command" annotation. This annotation
    can be used to suppress the 'no *' command, that can not be removed on the device.
    Added above annotation in // aaa session-id
    (CISCOIOS-223 / RT24134)

Corrections:

  - NED sorts reverse order of 'no line vty .*' commands
    for example consider following two commands:
      - no line vty 12 15
      - no line vty 16 25
    when NED executes 'no line vty 12 15' device also deletes 'no line vty 16 25,
    and when NED sends 'no line vty 16 25' device throws '% Invalid input detected' error,
    so to handle this NED sort the above command in reverse order. delete bigger number first.
    (CISCOIOS-221 / RT24125)

  - Removed when expression in following leafs due to circular references,
     interface/Port-channel/port-channel/load-balance/link
     interface/Port-channel/port-channel/load-balance/flow-based
    (CISCOIOS-226)

  - API CHANGE: changed from individual leaves to single multi-value leaf in following lists
     aaa/authentication/login *
     aaa/authorization/exex *
     aaa/authorization/commands *
    (CISCOIOS-214)

  - API CHANGE: Changed "instance" leaf to list in "/ip/dhcp/pool */option *"
    and changed child leafs(ascii,hex,ip) as choice.
    (CISCOIOS-227)


cisco-ios v4.3.6 [July 29, 2016]
===============================

Enhancements:

  - Additions to the YANG model:
     service-instance mdns-sd
    (CISCOIOS-209)

Corrections:

  - Added default delete in /ip/ssh/client/algorithm/mac and /ip/ssh/client/algorithm/encryption
     correct command to remove above config is:
      - default ip ssh client algorithm mac
      - default ip ssh client algorithm encryption
    (CISCOIOS-205)

  - Changed(revert) following leafs type to boolean combined with 'tailf:cli-boolean-no'
      - /cdp/run
      - /interface * /cdp/enable
    (When above leafs have 'tailf:cli-show-no', it  displayed as 'no <name>' when it does not exist.
    This is an issue because not all interface supports 'cdp' command)
    (CISCOIOS-200)

  - Removed / interface * / ip mroute-cache distributed
    (Added ned-settings / cisco-ios-use-ip-mroute-cache-distributed)
    - set to true, if 'ip mroute-cache distributed' should be used instead of 'ip mroute-cache'
    - some devices/versions only allows 'ip mroute-cache distributed' and some devices/versions
      only allows 'ip mroute-cache'. but devices shows 'ip mroute-cache'
      when show running-config is executed. To avoid compare-config diff and
      rollback issue, we can use this ned-setting.
    (CISCOIOS-201)

  - Added tailf:cli-reset-container in /aaa authentication login *
    (CISCOIOS-214)

  - Added tailf:cli-diff-dependency (dependency to /interface *) in // logging source-interface *
    ("logging source-interface" is removed before "interface" when both deleted)
       no logging source-interface .*
       no interface .*
    (CISCOIOS-219)


cisco-ios v4.3.5 [July 15, 2016]
===============================

Enhancements:

  - Additions to the YANG model:
     interface * / ethernet cfm interface
     interface * / no ethernet cfm interface
    (CISCOIOS-198)

  - Additions to the YANG model:
     interface * / ip mroute-cache distributed
    (CISCOIOS-201)

Corrections:

  - Added 'name' and 'service-instance' as keys in /bridge-domain/member/service-instance
    Now it is possible to create multiple service-instances:
     bridge-domain <integer>
       member GigabitEthernet.* service-instance <integer>
       member GigabitEthernet.* service-instance <integer>
     !
    (CISCOIOS-199)

  - Changed following leafs type to empty and added 'tailf:cli-show-no'
      - /cdp/run
      - /interface * /cdp/enable
    (CISCOIOS-200)

  - Added presence statement in route-map * /continue
    (continue command can be configured without sequence-number)
    (CISCOIOS-203)


cisco-ios v4.3.4 [July 8, 2016]
===============================

Enhancements:

  - Additions to the YANG model:
     policy-map * / class * / shape average * / account
     interface * / ipv6 rip *
     router ospfv3 * / address-family [ipv4 | ipv6 | <vrf> ] summary-prefix *
     ip pim dm-fallback
     ipv6 router eigrp * / shutdown
    (CISCOIOS-193)

Corrections:

  - Added cli-replace-all to next-server, default-router, dns-server and
     netbios-name-server leaf-lists in ip dhcp pool.
    (CISCOIOS-191)

  - Use IOS version instead of XE version string.
    (RT22867)

  - Added dependency between /l2vpn/xconnect/context/member/service-instance and
    /interface/*/service/instance, correct order is:
     l2vpn xconnect context .*
        no member .* service-instance .*
     !
     interface .*
      no service instance .*
     exit
     no l2vpn xconnect context .*
    (CISCOIOS-194)

  - Added default delete in /ip/ssh/server/algorithm/mac and /ip/ssh/server/algorithm/encryption
     correct command to remove above config is:
      - default ip ssh server algorithm mac
      - default ip ssh server algorithm encryption
    (CISCOIOS-196)


cisco-ios v4.3.3 [June 30, 2016]
================================

Enhancements:

  - Additions to the YANG model:
     cable modulation-profile * tdma
     cable modulation-profile * mixed
    (CISCOIOS-190)

  - Additions to the YANG model:
     dot11 arp-cache optional
     dot11 ssid * / authentication open eap
     dot11 ssid * / authentication network-eap
     dot11 ssid * / mbssid
     interface Dot11Radio* / broadcast-key vlan *
     interface Dot11Radio* / mbssid
     interface Dot11Radio* / packet retries
     interface Dot11Radio* / power client local
     interface Dot11Radio* / rts
     interface Dot11Radio* / speed
     interface Dot11Radio* / world-mode dot11d
    (CISCOIOS-138)

  - Improved exec proxy login, enabling support for login without username.

  - Let '| prompts' answers override auto-prompts ned-setting in live-status
    exec and config exec commands.
    (RT23699)

Corrections:

  - Strip year prefix in version string extraction.
    (RT22867)

  - API CHANGE:
     Implemented support for interface * / cable source-verify
     which causes the source-verify leaves to be wrapper in hidden container.
    (CISCOIOS-183)

  - Performance improvement: removed cli-case-insensitive from ip route.
    (RT23692)

  - Return error in live-status exec and config exec when missing answer to prompt.
    (RT23686)

  - Fixed vrf vs ip nat translation vrf dependency issue, correct order is:
     no ip nat translation max-entries vrf *
     no vrf definition *
    (CISCOIOS-184)

  - Fixed problem with live-status exec ping and crypto key export commands.
    (RT23640)


cisco-ios v4.3.2 [June 24, 2016]
================================

Enhancements:

  - Additions to the YANG model:
     aaa accounting nested
     aaa accounting send stop-record authentication failure vrf *
     aaa authentication attempts login
     line * / login tacacs
     tacacs-server last-resort
    (RT23615)

  - Additions to the YANG model:
     aaa authentication login * local-case
     platform qos marker-statistics
     snmp mib persist v3mibs
    (RT23550)

Corrections:

  - Fixed problem with live-status exec any copy command.
    Added command ignore pattern on: [OK] and [Done]
    (RT23602)

  - API CHANGE: Added route-map set community choice for 'none' value.
    (RT23596)

  - Yet another interface speed auto vs duplex auto dependency fix.
    Correct order for FastEthernet on cat3550:
     interface *
      duplex auto
      speed auto
    Correct order for all else:
     interface *
      speed auto
      duplex auto
    (RT23163)

  - Fixed interface ip ospf vs switchport order dependency, correct order is:
     interface *
      no ip ospf
      switchport
    (RT23163)

  - Do not prompt for question on live-status exec show command.

  - API CHANGE
     snmp mib persist cbqos changed from enum leaf to leaf in container.
    (RT23550)

  - API CHANGE
     class-map * / match protocol *
    changed from individual leaves to multi-word-key list.
    (CISCOIOS-167)

  - API CHANGE:
     class-map * / match packet length *
    changed from container to multi-word-key list to support multiple entries.

  - Removed tailf:cli-delete-when-empty on interface * / standby * preempt
    (RT23553)

  - Fixed vlan vs interface switchport order dependency, correct order is:
     interface *
      no switchport access vlan 1
      no switchport
     vlan 101
    (CISCOIOS-169 / RT23538)

  - Added default statement '1' on interface * / switchport access vlan.


cisco-ios v4.3.1 [June 17, 2016]
================================

Enhancements:

  - Additions to the YANG model:
     interface * / ip authentication key-chain eigrp *
     interface * / ip authentication mode eigrp *
     interface * / ipv6 summary-address eigrp *
     interface * / no ip split-horizon
     interface * / ip split-horizon eigrp no-list *
     router * / distribute-list prefix-list
     router eigrp * / address-family ipv6 unicast vrf *
     router eigrp * / address-family ipv6
     router eigrp * / summary-metric *
    (CISCOIOS-164 / RT23457)

  - API CHANGE:
    ip community-list <number> has been changed to a single list, from two
    separate ones, in order to allow for easier handling of the new ned-setting
    cisco-ios-auto / ip-community-list-repopulate. This setting is used to
    restore the ip community-list on gateways which delete the entire list when
    one entry is deleted. NCS will then restore the list if the option is set.
    (CISCOIOS-160 / RT23405)

  - Additions to the YANG model:
     router eigrp * / metric
    (RT23438)

Corrections:

  - Fixed interface vrf and ip address dependency problem, correct order is:
     interface * /
      [ip] vrf
      ip address
    (CISCOIOS-166)

  - Fixed support for multiple address entries in
     ip dns view * / domain name-server vrf *
    (RT23464)

  - NCS/NSO workaround:
    Trim quotes erronously inserted by service node in live-status exec cmd.
    (RT23355)

  - API CHANGE:
     Added support for configuring vrf in:
      flow exporter * / destination
    (RT23438)

  - API CHANGE:
     interface Cable* / cable upstream * rate-limit
     interface Cable* / cable upstream * shutdown
    changed from show-no type empty to boolean-no type boolean.
    (CISCOIOS-161)

  - If answer (sent by prompts) is only one character, don't send NL (newline).


cisco-ios v4.3 [June 10, 2016]
==============================

Enhancements:

  - API CHANGE:
    All access-list's collected in a single list /access-list/access-list
    Fixed bug with editing entries. The device deleted the entire acl when a
    single rule was deleted but NSO did not. This has now been fixed by having
    NCS repopulate the ACL list after a rule delete.
    (RT23353)

  - Additions to the YANG model:
    interface * / ip ospf authentication key-chain
    key chain * / key * / cryptographic-algorithm
    router ospf * / area * authentication key-chain
   (RT23350)

  - Additions to the YANG model:
     ip dhcp pool * / host
     ip dhcp pool * / hardware-address
     ip dhcp pool * / client-identifier
    (CISCOIOS-156)

  - Additions to the YANG model:
     router ospfv3 * / address-family ipv6 unicast vrf *
    (RT23350)

  - Additions to the YANG model:
     interface * / service instance * ethernet / encapsulation untagged , dot1q
    (RT23328)

  - Rewrote the implementation for replacing encrypted device secrets with
    the cleartext cdb value in order to avoid compare-config diff and out
    of sync with service. The new design requires support for tailf:meta-data
    which means the minimum NCS/NSO versions had to be raised accordingly.
    All transformed secrets on the device should now be supported.
   (RT22669)

  - Added support for passing answers to prompts in live-status exec and
    config exec commands. Append '| prompts <answer1> [answer2]' to command.
    Note: For hitting enter only on a prompt, add the prompt key 'ENTER'
    Examples:
     admin@ncs(config-config)# exec "key config-key password-encrypt cisco123 | prompts cisco123"

    Also implemented a ned-setting - cisco-ios-auto-prompts - which can be
    used to register standard answers to standard questions. For example:
    devices global-settings ned-settings cisco-ios-auto-prompts Q1 question
      "System configuration has been modified" answer "no"
    See README file for more info.
   (RT22669)

Corrections:

  - API CHANGE:
     interface * / service instance * ethernet / encapsulation
    changed to a choice instead of separate entries.

  - Fixed service instance dependency problem, correct order is:
     interface * / service instance * ethernet /
      encapsulation
      bridge-domain
    (RT23328)

  - Improved suppport for setting type 6 passwords on device.

  - Fixed switchport vs ip address dependency using Java, correct order:
     interface *
      no switchport
      ip address
    (CISCOIOS-148 / RT23219)

  - Warning: Ignore info string "propagating wred configuration to" when
    modifying wrr-queue random-detect no-list
    (RT23001)

  - Default: Fixed default value problem with line * / login authentication
    (RT22801)


cisco-ios v4.2.3 [June 3, 2016]
===============================

Enhancements:

  - Additions to the YANG model:
    event manager applet * / event timer * name|ratelimit
   (RT23265)

  - Additions to the YANG model:
     ip msdp
   (RT22924)

  - Additions to the YANG model:
     policy-map * / class * / random-detect *-based aggregate
     policy-map * / class * / random-detect precedence values *
    (CISCOIOS-151 / RT23235)

  - Added new optional method to show running-config: cisco-ios-show-running-method
    Example:
    devices device <devname> ned-settings cisco-ios-show-running-method "show running-config full"

  - Added cisco-ios-inject-config ned-settings in order to inject config to
    top of running-config. Example:
     devices global-settings ned-settings cisco-ios-inject-config test config "version TEST"
    (RT23163)

  - Set ned-settings cisco-ios-cached-show-enable to default to true.


Corrections:

  - Fixed another bridge-domain dependency issue, correct order is:
     no interface BDI *
     no bridge-domain *
    (RT23020)

  - API CHANGE:
     route-map * / match route-type *
    In order to support multiple output syntax.
    (RT23150)

  - Added unique statement on vrf definition / rd
    If duplicate, device responds: '% "rd" 1:1 already in use by VRF X'
    (RT21072)

  - Added missing default values:
    ip ssh time-out 120
    ip ssh authentication-retries 3
   (RT22801)

  - Removed confusing ip prefix-list config without seq number.
    Note: always specify seq, because the device always shows it.
    (CISCOIOS-55)

  - BUGFIX: do not insert delete of policy-map / class / bandwidth|priority percent
    unless also adding numbers back.

  - API CHANGE:
     policy-map * / class * / random-detect *-based due to support for 'aggregate'
    (CISCOIOS-151 / RT23235)

  - API CHANGE:
    route-map * / set as-path prepend remodelled. Changed to same design as
    in cisco-nx, a multi-value string instead of 10 leaves.
    (RT22669)

  - Fixed the interface switchport order dependency;
     - interface *
        no ip address
        switchport
    (CISCOIOS-148 / RT23219)

  - Modified the last sethook (interface / switchport create) to set
   'no ip address' instead of delete '[no] ip address' if C650* device
    since those devices show:
     interface *
      switchport
      no ip address
    where most IOS devices only show:
     interface *
      switchport
    (CISCOIOS-148 / RT23219)

  - Removed two more sethooks due to not working well with FASTMAP:
     interface / switchport
      (delete: set 'no ip address' on interface)
     interface Port-channel.
      (delete: deleted channel-group on all Ethernet interfaces with same id)
    Add/delete dynamic config manually in transaction to avoid config diff.
    (CISCOIOS-148 / RT23219)

  - Fixed the following interface switchport order dependencies:
     - interface *
        no ip address X
        switchport
     - interface *
        speed X
        duplex full
     - interface *
        no switchport port-security
        no switchport mode access
     - interface *
        mode access
        no switchport trunk encapsulation dot1q
    WARNING: including description may breaks dependencies due to NCS/NSO bug
    (RT23163)


cisco-ios v4.2.2 [May 27, 2016]
===============================

Enhancements:

  - API CHANGE:
     Changed back aaa new-model to type empty or dependencies break.
    (RT22176)

  - Additions to the YANG model:
     route-map * / match route-type *
    (RT23150)

  - Additions to the YANG model:
     route-map * / set interface
    (RT23149)

  - Additions to the YANG model:
     radius-server host * timeout
     radius-server host * retransmit
    (CISCOIOS-138)

  - Additions to the YANG model:
     controller SONET * / ais-shut
     controller SONET * / overhead j0
     controller SONET * / au-4 * tug-3 * / tug-2 * channel-group * timeslots
     controller SONET * / au-4 * tug-3 * / tug-2 * framing no-crc4
     interface * / no flowcontrol bidirectional
     interface * / frame-relay interface-dlci / class
     interface * / ip header-compression
     interface * / ip tcp compression-connections
     interface * / multilink-group
     interface * / ppp multilink min-links
     interface * / no ppp multilink fragmentation
     map-class frame-relay *
    (CISCOIOS-134)

  - Additions to the YANG model:
     crypto isakmp profile * / qos-group
     crypto ipsec profile * / set security-association dfbit
     crypto ipsec profile * / set security-association dummy
     crypto ipsec profile * / set security-association ecn
     crypto ipsec profile * / set security-association idle-time
     crypto ipsec profile * / set security-association replay
    (RT23111)

Corrections:

  - Raised ip sla * / request-data-size range from 1400 to 1500.
    (CISCOIOS-146)

  - API CHANGE:
     route-map * / match
    (RT23150)

  - API CHANGE:
     access-list id changed from uint16 to string in:
      router bgp * / neighbor * filter-list *
      ip as-path access-list *
    (RT23078)


cisco-ios v4.2.1 [May 20, 2016]
===============================

Enhancements:

  - Support for NSO-4.2.

  - Additions to the YANG model:
     router bgp * / redistribute * include-connected
     ip igmp vrf *
     ip msdp vrf *
     ip multicast vrf * route-limit
     ip pim send-rp-announce
     ip pim send-rp-discovery scope
     ipv6 dhcp pool *
     ipv6 router eigrp * / eigrp router-id
     ipv6 router rip * / distribute-list prefix-list *
     ntp access-group ipv6
   (RT22924)

  - Additions to the YANG model:
     parameter-map type inspect * / icmp idle-time ? ageout-time
     parameter-map type inspect * / max-incomplete ? aggresive-aging
     parameter-map type inspect * / udp idle-time ? ageout-time
     parameter-map type inspect * / udp half-open
     parameter-map type inspect * / session total
    (CISCOIOS-130)

  - Additions to the YANG model:
     crypto gdoi group * / server local / sa ipsec * / tag cts sgt
     interface * / ip lisp source-locator
     router lisp / ipv4 map-cache site-registration
     router lisp / ipv4 route-export site-registration
    (RT22785)

  - Additions to the YANG model:
     interface * / ip verify source vlan dhcp-snooping
    (CISCOIOS-129)

  - Additional support for configuring passwords/keys in cleartext which get
    encrypted on the device, and still avoid a compare-config diff.
    The following secrets/keys/passwords were added:
     ip wccp * key
     mpls ldp neighbor *
     radius-server host * key
     tacacs-server host * key
     username * password
     username * secret
    (RT22801)

Corrections:

  - API CHANGE:
    Changed 'crypto ikev2 keyring * / peer * / pre-shared-key' YANG in order
    to support cleartext to encrypted device conversion without config diff.
    (RT22669)

  - API CHANGE:
     Type change uint16 to string: route-map * / match as-path *
     Removed string pattern on: route-map * / match ipv6 address
    (RT23078)

  - API CHANGE:
     aaa new-model - empty to boolean in order to support 'no aaa new-model'
     platform - generic list to detailed model in order to support no-commands
     logging rate-limit - in order to support 'no logging rate-limit'
    (CISCOIOS-135)

  - Fixed dependency problem with bridge-domain / member and interface.
    (RT23020)

  - Fixed problem with live-status exec commands not showing complete output.
    (RT23071)

  - Fixed switchport vs ip address dependency issue, correct order is:
     interface *
      no switchport
      ip address
    (CISCOIOS-137 / RT23067)

  - Improved password model to reset password type when only changing the
    secret.
    (CISCOIOS-113 / RT22801)

  - Fixed dependency problem with ipv6 neighbor and interface.
    (CISCOIOS-128 / RT22978)


cisco-ios v4.2.0 [May 13, 2016]
===============================

Enhancements:

  - Additions to the YANG model:
     cable service class * activity-timeout
     cable service class * admission-timeout
     cable service class * max-latency
     cable service class * min-packet-size
     cable service class * req-attr-mask
     cable service class * req-trans-policy
     cable service flow activity-timeout
     cable multicast mdf-disable WB-Incapable-CM
     cable trust *
     cable dsg chan-list * index * freq
     cable load-balance d20-ggrp-default
     cable metering ipdr ? session
     cable rf-change-dampen-time
     cable tag * / service-type-id
     controller Modular-Cable * / rf-channel * network-delay
     interface Modular-Cable* / no cable cm-status enable
      NOTE: Use: cable cm-status enable no-list
     interface Wideband-Cable* / cable ds-resiliency
    (RT22958)

  - Additions to the YANG model:
     interface * / ip flow monitor * input
     interface * / ip flow monitor * output
     interface * / no nhrp route-watch
     router eigrp * / address-family ipv4 / af-interface * / summary-address
     router eigrp * / address-family ipv4 / af-interface * / authentication
    (CISCOIOS-125)

  - Additions to the YANG model:
     l2tp-class * / authentication
     l2tp-class * / password
     pseudowire-class * / ip local interface
     pseudowire-class * / protocol
    (CISCOIOS-123)

  - Additions to the YANG model:
     interface * / bfd template
    (RT22871)

  - Added 'any' exec command under live-status to be able to execute any IOS
    command on the device. For example:
    admin@ncs# devices device ios1921-2 live-status exec any "show ppp summary"

  - Added 'verify' exec command under live-status.
    (CISCOIOS-116)

  - Support for configuring shared secrets in cleartext which get encrypted
    on the device. The encrypted secret from the device is replaced with the
    clear text from the CDB if the device config has not changed. If the
    secret is changed out-of-band the new value will be shown, i.e. cause
    a compare-config diff, which it should.
    The following secrets/keys/passwords are supported:
     enable password
     enable secret
     ip ftp password
     mpls ldp password fallback
     radius-server key
     tacacs-server key
    (RT22801)

  - Added info from output of 'show version in cached-show container in
    tailf-ned-cisco-ios module for fast access in service code. Info is
    updated upon login, i.e. may change if output of show version changes.
    Enable by setting: ned-settings cisco-ios-cached-show-enable version true
    Example output:
      cached-show version version 15.4(3)S4
      cached-show version model ME-3800X-24FS-M
      cached-show version license level MetroIPServices
      cached-show version license type Permanent
    Note: Even though this info is in config, obviously it is not config
          and can not be set on the device.
    (RT22867)

Corrections:

  - Changed most cli-diff-dependency statements to use relative paths instead
    of absolute paths to avoid a bug in ncs-3.4 branch.
    As a result, the following config has changed YANG path:
     ipv6 routing
    (CISCOIOS-127)

  - API CHANGE:
    Changed 'ip forward protocol udp' API in order to allow support for
    no-entries and entries. Remodelled as a list using optional disabled
    keyword. For example: set 'ip forward-protocol udp tftp disabled" for
    'no ip forward-protocol udp tftp'.
    (RT22958)

  - API CHANGE (internal leaves renamed) due to support for hidden secrets:
     aaa group server tacacs+ * / server-private * key
     interface * / standby * authentication md5 key-string
     interface * / vrrp * authentication md5 key-string
     ip ftp password
     key chain * / key * / key-string
     radius server * / key
     router lisp / ipv4 etr map-server * key
     tacacs-server host * key
     tacacs-server key
     username * password

  - Removed sethook on crypto pki trustpoint. Add dynamic config manually.
    (CISCOIOS-115)

  - Redesigned confirmation handling when applying config. Always begin by
    sending a 'y'. If no prompt after 1 second, send 'es\n' in order to
    complete a 'yes\n' to avoid getting stuck at questions. Reason for change
    is that now almost all confirmation prompts will be supported.
    (CISCOIOS-115)


cisco-ios v4.1.6 [May 4, 2016]
==============================

Enhancements:

  - Additions to the YANG model:
     router lisp / locator-table
     router lisp / loc-reach-algorithm rloc-probing
     router ospf * / redistribute lisp subnets
    (RT22785)

  - Additions to the YANG model:
     mpls ldp password fallback
    (RT22800)

  - Additions to the YANG model:
     interface * / ethernet oam link-monitor receive-crc window
     interface * / isis bfd disable
     interface * / isis hello-multiplier
     router ospf * / prefix-priority high route-map
    (RT22802)

  - Additions to the YANG model:
     call-home / vrf
     interface * / tunnel mode ipv6
     ip dhcp use vrf remote
     ip dns name-list *
     ip dns view vrf * *
     ip dns view * / domain name-server vrf *
     ip dns view-list * / view vrf * *
     ip dns view-list * / view vrf * / restrict
     parameter-map type inspect-vrf *
     parameter-map type inspect * / tcp syn-flood limit
     parameter-map type inspect * / max-incomplete ?
     parameter-map type inspect * / threat-detection rate fw-drop
     parameter-map type inspect * / threat-detection rate inspect-drop
     parameter-map type inspect * / vrf * inspect
     virtual-service / ip shared host-interface
     virtual-service / activate
    (CISCOIOS-90)

Corrections:

  - Fixed compare-config diff when setting line * / exec, hidden default.

  - Removed ordered-by "user" on snmp-server enable traps list.
    (RT22801)

  - Corrected spelling in 'mpls ldp neighbor * labels accept', was lables
    (RT22803)

  - Special reorder fix for NCS bug manifesting in router ospf / max-metric
    (Fixed in NCS 3.4.5 or newer)

  - Fixed ordering isssues in router * / distribute-list when adding and
    deleting entries.

  - Reviewed and added several vrf dependency cli annotations.

  - Added ipv6 unicast-routing dependency on router bgp * / address-family ipv6.


cisco-ios v4.1.5 [April 28, 2016]
=================================

Enhancements:

  - Expanded the YANG model in:
     interface * / dialer map ip *
    (RT22769)

  - sdm prefer expansions to the YANG model.
    (RT22734)

  - Improved and bugfixed login code.
    (RT22712)

  - Additions to the YANG model:
     router odr
     router ospfv3 * / shutdown

  - Improved drned test system. Removed old lux tests.
    (CISCOIOS-87)

Corrections:

  - Fixed switchport dependency issue, correct order is:
     interface *
      no switchport nonegotiate
      no switchport mode access
    (RT22766)

  - Fixed interface * / mls qos vlan-based vs interface * / service-policy
    dependency problem.
    (RT22748)

  - Added cli-replace-all on 'line * / transport input|output'.
    (RT22717)

  - Removed sethook on 'ip vrf' which used to automatically clear
    'ip vrf forwarding' on all interfaces as well as delete all routes
    using the same vrf. Reason being dynamic sethooks have proven to
    cause problems with FASTMAP as well as cauing unwanted config changes.
    If you want to mimic the device behaviour of cleaning vrf config when
    a vrf is deleted, you have to add those deletes to your transaction
    as well in order to avoid config diff.
    (RT22686)

  - Removed sethook on 'ip routing'. See reason above.

  - Fixed problem with banner in newer NCS/NSO versions.

  - Fixed support for live-status exec reload without giving an error.


cisco-ios v4.1.4 [April 20, 2016]
=================================

Enhancements:

  - Made the automatic ip address reconfiguration upon vrf forwarding change
    on interfaces configurable. Default is off. To enable:
     devices device <devname> ned-settings cisco-ios-auto vrf-forwarding-restore true
    (RT20304)

  - Answer yes on 'ACCEPT? (yes/[no])' pattern.

Corrections:

  - Fixed interface * /crypto map vs crypto map dependency.
    (RT22592)

  - Answer 'yes' instead of 'y' if prompt regarding destroying certificates.

  - Ignore retry on 'please remove .* from .* first' pattern.

  - Do not accept cisco-ios-inject-interface-config without interface config.


cisco-ios v4.1.3 [April 11, 2016]
=================================

Enhancements:

  - Additions to the YANG model:
     Add output of BOOT path-list from 'show boot' to boot system config
     in order to avoid a diff. Note: For devices which only support one
     boot system entry you have to delete the old entry explicitly since
     cisco-ios supports multiple entries.
    (RT22491)

  - Additions to the YANG model:
     interface * / lacp min-bundle
     interface * / vrrp * vrrs leader
     interface * / vrrp * address
     interface * / vrrs pathway *
     ipv6 neighbor *
    (CISCOIOS-90)

  - Additions to the YANG model:
     boot config
     ip dhcp pool * / bootfile
     interface * / ipv6 dhcp client information refresh minimum
     interface * / ipv6 verify unicast source reachable-via
     interface * / isdn termination multidrop
     interface * / l2protocol peer|forward
     interface * / soak link notification
     interface * / wrr-queue min-reserve
     ip ssh pubkey-chain / username * / key-hash *
     ip ssh rsa keypair-name
     ipv6 spd queue
     line * / logging synchronous level
     mac-address-table move update
     mls qos min-reserve *
     radius-server attribute * mandatory
     route-map * / set ip qos-group
    (CISCOIOS-87)

  - Added a new optional method for NETSIM to calculate transaction id:
     cisco-ios-transaction-id-method confd-state-trans-id
    which uses 'show confd-state internal cdb datastore running transaction-id'

  - Improved login code to be more robust in order to improve chance of
    logging in on devices with banner(s) confusing the login process.

  - Disable additional show commands for show() if non existant to save
    bandwidth.


Corrections:

  - Fixed interface * / channel-group vs interface * / mls dependency.
    (RT22459)

  - Reverted the bad fix in 4.0.5.1 where Port-channel interfaces did not
    show "no shutdown" when shutdown. The reason being, without it it was
    not possible to bring down a newly created Port-channel interface since
    nothing was sent and the interface default to 'shutdown' when created.
    For those who want to create a shutdown Port-channel interface they
    have to set shutdown explicitly when creating the interface to match
    the device which usually brings up Port-channel as shutdown.
    (RT22456)

  - Removed all use of cli-trigger-on-delete since it turns out it has
    no other effect than negating cli-trigger-on-set, making it into a
    normal cli-diff-dependency.
    (TRAC 14332)

  - API CHANGE: mgcp behavior changed from hardcoded config to a list.

  - Ignore set/delete of unsupported injected interface config.

  - Improved snmp-server user config creation from 'show snmp user' and
    added support for access-list info.

  - Set verbose logging to default false. Enable with cisco-ios-log-verbose
    ned-settings option.


cisco-ios v4.1.2 [April 06, 2016]
=================================

Enhancements:

  - Additions to the YANG model:
     interface * / isis password
    (RT22402)

  - Additions to the YANG model:
     router bgp * / address-family * / bgp nexthop route-map
     clock summer-time date
     cns config partial
     cns id string
     controller dwdm *
     hw-module slot * process-max-time
     interface * / arp frame-relay
     interface * / clns mtu
     interface * / clns router isis
     interface * / ip accounting precedence
     interface * / ip ospf mtu-ignore
     interface * / mpls propagate-cos
     ip admission max-login-attempts
     ip auth-proxy max-login-attempts
     ip radius source-interface ? vrf *
     ip sla key-chain
     ip telnet tos
     line * / rotary
     l2 vfi * / rd
     l2 vfi * / route-target
     memory free low-watermark processor
     mls aging long
     port-channel per-module load-balance
     process cpu statistics limit
     process-max-time
     route-map * / match source-protocol *
     route-map * / set ip precedence
     scheduler runtime netinput
     tacacs-server administration
     track * / ip vrf

  - Additions to the YANG model:
    ip cef load-sharing algorithm universal
    no ip routing protocol purge interface
    ip sla * / udp-jitter * num-packets

  - Additions to the YANG model:
     aqm-register-fnf
     no cdp tlv location|app
     controller ISA *
     interface * / ipv6 address * link-local
     interface * / backup interface
     interface * / ip summary-address eigrp *
     interface * / ip ospf database-filter all out
     interface * / ip ospf lls
     interface * / mls qos cos
     interface * / srr-queue bandwidth limit
     ip auth-proxy|admission max-nodata-conns
     ip dhcp bootp ignore
     ip nat translation tcp-timeout
     ip sla reaction-trigger *
     isdn leased-line
     logging message-counter
     mac-address-table synchronize
     no mac-address-table aging-type routed-mac
     resource policy
     router bgp * / address-family * / aggregate-address <ipv6>
     router bgp * / neighbor * path-mtu-discovery
     router eigrp * / default-metric
     router eigrp * / distance
     router eigrp * / network *
     no setup express
     no shell processing
     switch * provision
     track * rtr
     udld enable
     no vstack
    (RT22339)


Corrections:

  - Bugfixed support for special characters \ ? and " in 'crypto isakmp key'.
    (RT22356)

  - Fixed support for ? character in description strings.

  - API CHANGE:
     Removed config mode from ip sla * / udp-jitter|icmp-echo|udp-echo etc.
     in order to support reconfiguration of contents. Reason was IOS jumped
     to submode if ip sla was re-entered.
    (RT22339)

  - API CHANGES:
     route-map * / match tag changed from leaf to leaf-list
     monitor session * entries changed to multi-word-key lists
    (RT22339)

  - Fixed several bugs in: 'router ospf * / mpls traffic-eng'


cisco-ios v4.1.1 [March 23, 2016]
=================================

Enhancements:

  - The method to use by the NED for calculating transaction ID is now a
    configurable option.
    The config option cisco-ios-transaction-id-method is available under:
     /ncs:devices/global-settings/ned-settings/
     /ncs:devices/ncs:profiles/profile:cisco-ios/ned-settings/
     /ncs:/device/devices/device:<dev name>/ned-settings/
    Four values are supported:
     config-hash - Calculate MD5 on a snapshot of the entire running
                   config for calculation. (Default)
     last-config-change - Use the 'Last configuration change' timestamp
                          in running config only (WARNING: changed at reboot)
     config-id - Use the 'show configuration id' command to determine
                 if config changed. (WARNING: changed at reboot)
     config-history - Use the 'show configuration history' command to
                      determine if config changed. (WARNING: changed at reboot)
    (RT20096)

  - CBR additions to the YANG model:
     cable downstream qam-profile *
     cable load-balance d30-ggrp-default policy
     cable load-balance d30-ggrp-default threshold
     cable video
     controller Upstream-Cable *
     interface Cable* / upstream *
     interface Cable* / no cable upstream * equalization-error-recovery
     interface Integrated-Cable *
     interface  Wideband-Cable* / cable rf-channels channel-list *
     interface  Wideband-Cable* / cable rf-channels controller *
     packetcable authorize vanilla-docsis-mta
    (CISCOIOS-76)

  - Expanded the YANG model in:
     card *
     controller Integrated-Cable *
     interface Cable* / cable
    (CISCOIOS-76)

  - Additions to the YANG model:
     interface * / ipv6 dhcp relay source-interface
     interface * / ipv6 nd reachable-time
     interface Video *
     logging linecard

  - Additions to the YANG model:
     interface * / dot1x timeout quiet-period
     interface * / dot1x timeout ratelimit-period
     interface * / dot1x timeout held-period
    (CISCOIOS-75)

  - Additions to the YANG model:
     tacacs server * / single-connection
     tacacs server * / send-nat-address

  - Additions to the YANG model:
     tacacs server * / port
    (RT22321)

Corrections:

  - API CHANGE: cable fiber-node * / downstream *
    API CHANGE: interface Cable* / downstream *
    Made cable fiber-node * / upstream * into a list.
    (CISCOIOS-76)

  - Dequote key string when configuring 'crypto isakmp key' in order to
     support key characters like quotation marks.
    (CISCOIOS-77)

  - Improved java code to retrieve IOS version, solving issue with XE.
    (RT22208)

  - Moved crypto exec commands into 'exec' action to avoid being triggered
     by malformed or missing crypto config. Hence, to run exec commands you
     now must start with 'exec <command> [options]'. For example:
     > admin@ncs(config-config)# exec crypto key generate rsa general-keys
    (RT21754)

  - Fixed problem with logging discriminator * drops|includes strings.
    (RT22321)

  - API CHANGE: 'table-map * / map from' due to bugfixing and support for
     multiple 'from' entries per 'to'.
    (RT22321)


cisco-ios v4.1 [March 17, 2016]
===============================

Enhancements:

  - Added 'show verbose' toggle to live-status exec for increased logging.

  - Additions to the YANG model:
     interface * / ip bandwidth-percent
     interface * / ip next-hop-self eigrp no-list *
      (note: implements: interface * / no ip next-hop-self eigrp *)
     interface * / vrrp * address-family ipv4
     interface * / vrrp * address-family ipv6
     ip cef load-sharing algorithm original
     ip dhcp pool * / class *
     ip route static install-routes-recurse-via-nexthop
     ipv6 cef load-sharing algorithm original
     ipv6 icmp error-interval
     fhrp version vrrp
     flow exporter * / option interface-table
     flow exporter * / option exporter-stats
     flow exporter * / option sampler-table
     parameter-map type inspect-zone *
     zone security * / protection
    Expanded the YANG model in:
     flow record * /
     flow monitor * /
    (RT22111)

  - Additions to the YANG model:
     class-map * / match protocol attribute traffic-class *
     class-map * / match protocol attribute business-relevance *
     domain * / vrf * / master hub / monitor-interval *
     domain * / vrf * / border / + / password
    (RT22068)

  - Moved vrrp config from interface Vlan to all Ethernet interface.
     API CHANGE: ip icmp rate-limit unreachable
    (RT22111)

  - Additions to the YANG model:
     class-map * / match protocol attribute traffic-class *
     class-map * / match protocol attribute business-relevance *
     domain * / vrf * / master hub / monitor-interval *
     domain * / vrf * / border / + / password
    (RT22068)

  - no macro auto monitor
    (CISCOIOS-62)

  - Added support for 'encrypted' option in 'snmp-server user <user> <group> v3'
    (RT21908)

  - Added new global, profile and device pre-configuration to inject config
     in interface when syncing from device (parsing running-config).
     See README file for details on 'cisco-ios-inject-interface-config'.

  - Additions to the YANG model:
     class-map type control subscriber *
     device-sensor accounting
     device-sensor notify
     interface * / dot1x timeout supp-period
     interface * / snmp trap mac-notification change
     interface * / source template
     ip device tracking probe auto-source
     template
    (CISCOIOS-62)

  - Additions to the YANG model:
     account config in policy-map * / class * / bandwidth remaining
    (RT21973)


Corrections:

  - API CHANGE: ip forward-protocol udp changed from leaf to leaf-list.
    (RT22007)

  - Reworked switchport API:
     IMPORTANT NOTE: Devices which do not show 'switchport' when enabled
      must configure cisco-ios-inject-interface-config to inject it.
      See the README file.
     'no switchport' is shown on all Ethernet and Port-channel interfaces
      by default (with help of a static global config inject)
     API CHANGES:
      Removed interface * / no-switchport and yes-switchport
       (always use interface * / switchport container)
      Removed tailfned switchport config (show-no and show-yes)
      Removed cisco-ios-switchport-format ned-setting.
      Removed switchport on wlan-ap, BDI, Vlan and Virtual-Template if.
    (CISCOIOS-33)

  - API CHANGE: ipv6 cef

  - Added 'presence true' in 'domain * / vrf * / border' container.
    (RT22068)

  - Added non-strict leafref on 'interface * / domain'

  - Added vrf definition dependencies on 'crypto ? profile * / match fvrf'

  - API CHANGE: interface * / service-policy type performance-monitor

  - Added 'presence true' in 'domain * / vrf * / border' container.
    (RT22068)

  - Raised interface * / ip mtu from 9198 to 9216.
    (RT22083)

  - Changed vrf forwarding ip address code to show in transaction prepareDry.
    (RT20304)

  - Fixed multiple VRF support in ip nat inside|outside source static.
    (RT22104)

  - Fixed diff problem with interface / logging event link-status using
     new cisco-ios-inject-interface-config to insert defaults.
    (RT21612) (RT22102)

  - Completely reworked switchport dependencies due to yet another problem.
    (RT22066)

  - Removed sethooks on interface * / vrf forwarding | ip vrf forwarding
     which deleted all IP addresses on interface, introduced in 3.11.0.9.
     One reason was that it stopped reconfiguration of IP address in fastmap.
     Introduced new Java code which automatically writes back the primary IP
     from the CDB to the device if vrf forwarding is modified.
     This will preserve the interface IP address configuration in CDB.
     Note: Code needs to be expanded to also write back secondary addresses.
    (RT20304) (CISCOIOS-61)

  - Added subinterface dependencies to base interface to fix delete problem.

  - API CHANGE: removed support for 'random-detect' without argument.

  - Always use 'random-detect precedence-based' to enable precedence.
     On a real device this config is hidden, showing only 'random-detect',
     but the NED auto-converts that to 'random-detect precedence-based.
    (RT21996)

  - Fixed more mls qos clean up bugs.
    (RT21705)

  - Fixed policy-map * / class * / shape vs queue-limit dependency.
    (RT21989)

  - Joined switchport enable switch and config for show-no devices in order
     to be able to delete separate config when disabling switchport.
    (RT21933)


cisco-ios v4.0.6 [February 26, 2016]
====================================

Enhancements:

  - Additions to the YANG model:
     crypto isakmp fragmentation
     crypto isakmp identity
     crypto isakmp invalid-spi-recovery
     crypto isakmp profile * / match identity host
     diagnostic monitor syslog
     gw-accounting
     interface * / ip mask-reply
     no ip spd enable & no spd enable
     ip ssh dh min size
     ip ssh server algorithm encryption
     no ip ssh server authenticate user publickey
     no ip ssh server authenticate user keyboard
     no ip ssh server authenticate user password
     line * / timeout login response
     logging rate-limit * except warnings
     no parser cache
     no snmp-server trap authentication unknown-context
     no snmp-server sparse-tables
     vstack director|basic
    (RT21939) (CISCOIOS-55)

  - Added three new static warnings in mls qos area.

  - Support for SSH/TELNET proxy login configuration.
    Configure cisco-ios-proxy-settings under ned-settings to enable.
    See README file for example.

  - Additions to the YANG model:
    crypto map * ipsec-isakmp / set security-association dummy

  - Additions to the YANG model:
     interface * / access-session closed
     interface * / access-session port-control
     interface * / service-policy type control subscriber
     policy-map type control subscriber
    (CISCOIOS-21)

  - Support for exec proxy login configuration.
    Configure cisco-ios-proxy-settings under ned-settings to enable.
    See README file for example.
    (CISCOIOS-21)

  - Additions to the YANG model:
     bridge *
     bridge irb
     cns dhcp
     crypto call admission limit ike
     dot11 ssid *
     interface BVI *
     interface Dot11Radio1 *
     interface * / bridge-group
     ip http help-path
     line * / flowcontrol
     line * / speed
     service-module wlan-ap 0 bootimage

  - New ned-setting to configure switchport mode (see README file)
    devices device <devname> ned-settings cisco-ios-switchport-format
    (RT21751)

  - Additions to the YANG model:
     monitor session * type local / source interface
     policy-map * / class * / queue-limit percent
   (RT21543)

  - API CHANGE: 'license feature' now implemented as a list.
    (RT21543)

  - Additions to the YANG model:
     arp *
     arp vrf *
    (CISCOIOS-32)

  - Additions to the YANG model:
     interface * / ip igmp max-groups
     interface * / ip igmp filter
     interface * / service instance * ethernet / storm-control
     interface * / vlan-range * / service-policy
    (RT21617)

Corrections:

  - API CHANGE: crypto ipsec transform-set * / mode, added mode transport require
   (CISCOIOS-55)

  - Additions to the YANG model:
     event manager applet * / action * cli
    (CISCOIOS-53)

  - Rewrote mls qos srr-queue code to fix several problems with it.
    (RT21705)

  - Added missing connectTimeout in NED setup.

  - Bugfix in router distribute-list lists. Removed bad choice statement.

  - Ignore warning for delete of non-existant vrf definition in order to
    not break retry loop for 'in progress' message.

  - Fixed dependency problem with crypto keyring and crypto isakmp profile.

  - API CHANGE: Changed mls qos srr-queue config. Fixed bugs.
    (RT21705)

  - Added hard coded error checks for 'Error Message '
    and 'HARDWARE_NOT_SUPPORTED'.
    (RT21706)

  - New dependency rule on:
     interface / mls qos trust dscp|cos|ip-precedence
    (RT21723)

  - API CHANGE: interface BRI key changed from uint16 to string.
    (RT21480)

  - interface / logging event link-status type change for C3550 devices.
    (RT21612)

  - Discard duplicate 'no mls qos srr-queue ..' lines
    (RT21705)

  - New dependency rule on:
     interface * / xconnect vfi
    (RT20880)

  - More interface * / switchport depdendency fixes
     (interface / ip igmp | ip dhcp | service-policy)
    (RT21617)

  - Added cli-recursive-delete to Port-channel interface
    (RT21617)

  - Revisited dependency rule on:
     interface * / service instance * ethernet / bridge-domain
    (RT21477)

  - Removed (debug) show strip of 'switchport mode dynamic desirable'.
    (RT21554)

  - New bridge-domain dependency rule on:
     l2 vfi *
    (RT20880)

  - Do not show (send) 'no shutdown' on Port-channel interfaces.

  - Removed sethook for add/del ip route-cache with switchport & me340x.

  - API CHANGE: interface * / switchport port-security maximum

  - New dependency rule on:
     interface * / switchport port-security
    (RT21617)


cisco-ios v4.0.5 [January 29, 2016]
===================================

Enhancements:

Corrections:

  - New dependency rule on:
     router ospf * / disable passive-interface *
    (RT21266)

  - New dependency rule on:
     ip route * interface
    (CISCOIOS-34)

  - New dependeny rule on:
     interface * / service instance * ethernet / bridge-domain
    (RT21477)

  - New dependency rule on:
     interface * / switchport nonegotiate
    (RT21542)

  - New dependency rules on:
     interface Vlan*
     bridge-domain *
    (RT20880)

  - New dependency rules on:
     interface * / switchport trunk allowed vlan
     interface * / switchport mode

  - Reworked switchport re-ordering routines in NED.
    (RT21470)

  - Changed type used for the ipv4 addresses to one standard type.
    (CISCOIOS-38)


cisco-ios v4.0.4 [January 22, 2016]
===================================

Enhancements:

  - The NED is now configurable regarding how an applied configuration is
     saved to persistent memory on the device. See README for further details.
    (RT21318)

  - Additions to the YANG model:
     interface Port-channel * / port-channel load-balance
     interface Port-channel * / port-channel min-links
    (RT21365)

Corrections:

  - Fixed an parser issue in the NED that could happen when
     reading vlan config from the device.
    (RT21352)

  - Added dependency rule on:
     router ospf * / passive-interface
    (RT21266)

  - Fixed a dependency rule on:
     interface * / switchport / mode
    (RT21116)

  - Changes done to correct behavior of the interface * switchport command.
    (RT21415)


cisco-ios v4.0.3 [January 6, 2016]
===================================

Enhancements:

  - Increased <ncs-min-version> to 3.4.2 (tr13900)

  - Changed <ncs-min-version> support to 3.4.2, 4.0 and 4.1

  - The NED can now be manually configured regarding which
    format to use when setting police configurations on the
    device. See README for further info.

  - Additions to the YANG model:
     interface Virtual-Template * / ppp ipcp address
     vpdn-group *
    (CISCOIOS-14)

  - Additions to the YANG model:
     router bgp * / template peer-policy * / send-label
     router bgp * / template peer-policy * / inherit
     router bgp * / template peer-session * / inherit
     router ospf * / nsr
     router ospf * / fast-reroute
     interface * / ipv6 nd ra lifetime
     controller SONET * / alarm-report
     controller SONET * / description
     controller SONET * / shutdown
     crypto pki trustpoint * / enrollment pkcs12
     file verify
     license feature port
     service prompt
    (RT21182)

  - Additions to the YANG model:
     interface * / medium
     interface * / crypto engine
     crypto map * ipsec-isakmp / set security-association replay
     crypto map * ipsec-isakmp / reverse-route
    (CISCOIOS-1)

Corrections:

   - API CHANGE:
     Configurations using ipv6 addresses. The data type for ipv6 addresses
     has been changed in the YANG model (see corrections info below).

  - Changed type used for the ipv6 addresses to deal with expanded
     address formats as well as upper/lower case letter formats.
    (RT21194)

  - New dependency rule on:
     router bgp * / neighbor * inherit peer-session

  - Changed ranges in:
     interface * / bfd interval
     controller SONET * / au-4 *
    (RT21182)

  - Added tailf:cli-display-separated on:
     router bgp * / address-family * / redistribute connected
     router bgp * / address-family * / redistribute static
     router bgp * / address-family * / redistribute mobile
     router bgp * / address-family * / redistribute odr
    (RT21216)

  - Moved to interface-ethernet-post-grouping:
     interface * / channel-group

  - Added dependency rules on:
     interface * / isis ipv6
     interface * / isis three-way-handshake
     interface * / channel-group
    (RT21074)

  - Fixed interface negotiation auto and speed dependency problem.
    (RT20969)

  - Removed compact-syntax from policy-map/class/police rate percent.
    (RT20968)

  - crypto map * ipsec-isakmp / set peer changed from leaf to leaf-list.
    (RT20964)

  - Removed all 'mandatory true' keywords.

  - Removed reset-container workaround in pir-be due to NCS bug. Now
     requires minimum NCS-3.4.4 to work.
    (RT20959)


cisco-ios v4.0.2 [November 18, 2015]
====================================

Enhancements:

  - Additions to the YANG model:
     interface * / mac access-group * in
     interface * / no ip mroute-cache
    Expanded:
     interface * / ip arp inspection limit

  - Additions to the YANG model:
     interface * / speed 10000

  - Additions to the YANG model:
     file prompt
      (RT20831)
     interface * / ip nbar protocol-discovery

  - Reworked config exec command 'crypto' to support all exec sub-commands.

  - Additions to the YANG model:
     aaa group server radius * / backoff exponential
     aaa group server tacacs+ * / server name *
     cns
     crypto map * ipsec-isakmp / set security-association lifetime
     crypto pki trustpoint * / auto-enroll
     crypto pki trustpoint * / fqdn
     crypto pki trustpoint * / password
     crypto pki trustpoint * / source interface
     interface * / authentication
     interface * / dot1x
     interface * / ip hello-interval eigrp *
     interface * / ip hold-time eigrp *
     interface * / ip inspect * in|out
     interface * / ip nhrp map *
     interface * / ipv6 eigrp *
     interface Wlan-GigabitEthernet *
     ip dhcp pool * / next-server *
     ip http authentication aaa exec-authorization
     ip inspect name *
     ip local policy route-map
     no ip rcmd domain-lookup
     ipv6 router eigrp *
     login block-for
     radius-server dead-criteria
     radius-server deadtime
     security authentication failure rate
     tacacs server *

  - Support for 'no spanning-tree vlan' using no-list construct:
     spanning-tree vlan no-list 1234
     (and to remove no-entry) no spanning-tree vlan no-list 1234

  - Added static warning '%Pool nat-pool mask .* too small'
    (RT20728)

  - Additions to the YANG model:
     interface * / link state group
     link state track *
    (RT20522)

  - route bgp clean up. Minor API changes.


Corrections:

  - Fixed dependency: service-policy vs interface service instance
     and interface switchport.
    (RT20833,20834)

  - Fixed dependency: bridge-domain vs interface service instance.
    (RT20880)

  - Revisited. Order dependency between interface / duplex & speed.
    (RT20803)

  - Changed logging host ipv6 type to inet:host to avoid case diff.
    (RT20735)

  - Removed 'license udi' from yang since not config.
    (RT20819)

  - API change: cns exec

  - Reenabled write memory in revert & commit. Was accidently disabled
    in earlier patch releases.

  - Fixed order dependency when setting interface duplex (vs speed).
    (RT20803)

  - Added 1..4094 range on switchport vlan config.
    (RT20824)

  - Retry oper command when get 'Device or resource busy'.
    (RT20818)

  - Renamed crypto isakmp policy * / encryption to encr to match show.
    (RT20831)

  - Added cli-case-insensitive to logging host ipv6.

  - Fixed order dependency when modifying ip route vrf interface.
    (RT20746)

  - API change: crypto isakmp keepalive

  - API change: crypto pki trustpoint * / rsakeypair

  - API change: event manager applet * / event timer

  - API change: ip dhcp pool * / netbios-name-server *

  - API change: spanning-tree vlan

  - Removed three unused hooks.

  - Show 'router bgp * / no bgp log-neighbor-changes'.
    (RT20549)

  - Add all and replace-as to outer bgp * / neighbor * remove-private-as.
    (RT20435)

This NED does require NCS version 3.2.3 or later.


cisco-ios v4.0.1 [October 15, 2015]
===================================

Enhancements:

  - Additions to the YANG model:
     router eigrp * / offset-list
     interface * / traffic-shape
    (RT20383)

  - Additions and expansions to the YANG model:
     arp access-list *
     mac-address-table aging-time
     interface * / ipv6 nd raguard
     ip arp inspection filter *
     ip rcmd
     vlan access-map *
     wsma agent *
     wsma profile *
    (RT20274)

  - Added support for add&remove when modifying allowed vlan list in.
     interface * / switchport trunk allowed vlan.
    Note: Vlan's still in single 'vlans' list. add/remove keywords are
    inserted automatically by the NED when modifying list.
   (RT17385,RT20282)

  - Additions to the YANG model:
     router lisp / site * / eid-prefix * route-tag|accept-more-specifics
     router lisp / site * / allowed-locator *

Corrections:

  - Fixed router mpls * bug.
    (RT20508)

  - Removed distance from key in 'router rip|bgp / distance *'. API change.

  - Fixed issue with changing a 'crypto isakmp key' entry.
    (RT20466)

  - Fixed bug with 'mpls ip'.

  - Removed initial NETSIM router and ipv6 config.

  - Added default value to set security-association lifetime seconds.
    (RT20359)

  - Fixed order dependency when modifying ip access-list object-group rule.
    (RT18823)

  - Fix for class-map modifications (removed obu-diffs).

  - Fixed problem with confirmation code when deleting user.
    (RT20354)

  - Added 3 second sleep after 'do clear crypto ikev2 sa fast'.
    (RT20042)

  - Dependency fix: Remove crypto isakmp profile before crypto keyring.
    (RT20321)

  - Restored vrf support in 'ip nat inside source route-map'.
    (RT20380)

  - Fixed dependency problem with interface ppp and dialer.
    (RT20386)

This NED does require NCS version 3.2.3 or later.


cisco-ios v4.0 [Sept 23, 2015]
==============================

Enhancements:

  - Support for NSO-4.1.

  - Additions to the YANG model:
     boot system *
     ip nat translation max-entries all-host
     ip nat translation max-entries list

  - Added static warning:
    'for vrf .* scheduled for deletion'

  - EPN 4.0 support for MEF and TI (Transport infrastructure).
    Additions to the YANG model:
     ethernet lmi
     interface * / ethernet cfm mip level *
     interface * / mpls igp ldp sync
     ip sla enable timestamp
     ip sla ethernet-monitor
     ip sla * / ethernet jitter * mpid * domain *
     route-map * / set mpls-label
     router isis * / fast-reroute per-prefix
    Expanded the YANG model in:
     ethernet cfm
     interface * / ethernet oam link-monitor. API change.
    (RT18226, RT18277)

  - Added support for:
     snmp-server user *
    Note: v3 auth&priv passwords are retrieved from CDB since hidden on device
    (RT19569)

  - Additions to the YANG model:
     interface * / clock source
     interface * / crc
     interface * / dsu mode
     interface * / mls netflow sampling
     interface * / ip ospf authentication-key
     interface * / ip ospf dead-interval
     interface * / ip pim sparse-dense-mode
     interface * / scramble
     interface * / serial restart-delay
     interface POS*
    (19814)

  - Additions to the YANG model:
     card type e3 *
     interface POS* / pos
     ip nat translation icmp-timeout
     ip nat service list * ftp tcp port *
     ip rcmd source-interface
     ipv6 route static bfd *
     macro name (as a quoted string with \\r\\n for newline and no @)
     snmp-server tftp-server-list
    (RT19823)

  - Implemented support for terminal server telnet login without user, pw.
    (RT19766)

  - Implemented basic IWAN support.
    Additions to the YANG model:
     domain
     flow
     interface * / domain
     ip default-network *
    (RT19342)

  - Additions to the YANG model:
     aaa accounting auth-proxy *
     aaa accounting dot1x *
     aaa authentication username-prompt
     aaa authorization auth-proxy *
     aaa server radius dynamic-author / client *
     access-list dynamic-extended
     crypto isakmp profile * / self-identify
     interface * / down-when-looped
     interface * / isis three-way-handshake
     ip nat translation max-entries vrf *
     ip nat translation max-entries all-host
     ip nat translation max-entries list
     ip wccp *
     mac address-table notification
     radius-server attribute *
    (RT19411)

  - Additions to the YANG model:
     crypto ipsec profile * / set pfs|isakmp-profile|mixed-mode|identity
     crypto isakmp profile * / keepalive|local-address
     crypto ikev2 profile * / lifetime
     interface * / platform ring
     object-group service * / icmp *
     track * ip sla
    (RT19672, RT19673, RT19675, RT19727, RT18823, RT20035)

  - Expanded 'ip route vrf *' configuration with vasi interfaces.
    Expanded the YANG model in:
     interface vasileft *
     interface vasiright *
    (RT19411)

  - Additions to the YANG model:
     interface Vlan * / vrrp *
    (RT19569)

  - Additions to the YANG model:
    Expanded object-group service * / tcp * config.
    Expanded object-group service * / udp * config.
    object-group service * / tcp-udp *

  - Expanded interface * / ip nhrp config.
    Other addditions to the YANG model:
     interface * / ip pim nbma-mode
     interface * / tunnel key|vrf
     policy-map * / class * / set dscp tunnel

  - Additions to the YANG model:
     interface ATM * / atm route-bridged
     interface ATM * / pvc * / class-vc
     interface ATM * / pvc * / pppoe-client / dial-pool-number *
     interface * / mls qos bridged
     interface * / no wrr-queue random-detect *  (note: use no-list ..)
     interface * / tunnel mode gre
     ip pim register-source
     router isis * / nsf ietf
     router eigrp * (Virtual-Instance config)
     no ip pim autorp
     vc-class atm * / vbr-nrt
    (RT19074)

Corrections:

  - Fixed problem with changing police values in policy-map / class
    (RT19880)

  - Added 'do clear crypto ikev2 sa fast' before 'no crypto ikev2 keyring'
     config
    (RT20042)

  - Added cli-recursive-delete to Virtual-Template interface.
    (RT20042)

  - Fail commit (write memory) and throw exception if flash is full.
    (RT16197)

  - Reworked 'mac access-list extended' list. API change.
     Note: old implementation only allowed one entry and no options.

  - Reworked 'interface * / ip helper-address' from a container to a list.

  - Bugfixing in 'ip nat outside source'. Bad vrf support. API change.

  - Removed ordered-by user to ip access-list extended.
    (RT18927)

  - Reworked 'logging source-interface' to support multiple entries.

  - Fail commit (write memory) and throw exception if flash is full.
    (RT16197)

  - Reworked 'mac access-list extended' list. API change.
    Note: old implementation only allowed one entry and no options.

  - Reworked 'interface * / ip helper-address' from a container to a list.

  - Reworked 'logging source-interface' to support multiple entries.

  - Fixed tailfned police cirflat mode, actions were not on same line.

  - Bug fixed ip nat inside source list, vrf did not work. API change.

  - Bugfixing in 'ip nat outside source'. Bad vrf support. API change.

  - Eased up ip nat inside source config by removing sequence on options.
    (RT20020)

  - Added static warning 'removed due to vrf change'.

  - Quote and dequote description strings.

  - Removed compact-syntax from police rate submode actions.

  - Removed ordered-by user from class-map list.

  - Added 'policy-map * / class class-default' to fix diff. API change.

  - Bugfixed object-group service * / icmp|udp|tcp
    (RT18823)

  - Broke out 'interface * / xconnect' in order to fix dependency problem.

  - Broke out 'cable l2vpn * / service instance * ethernet / xconnect' in
     order to change 'backup peer *' to a list with maximum 3 entries.

  - Raised config command retry from 60 seconds to 66 seconds.

  - Fixed interface * / ip virtual-reassembly in|out config.

  - Support for ipv6 in snmp-server group access. API change.

  - Added subslot key to 'card type t3 *' config. API change.

  - Changed ipv6 source-route to boolean. API change.

  - Fixed ip dhcp snooping problem. API change.

  - Made 'router bgp * / aggregate-address' into a list instead of container.

  - Fixed problem in crypto isakmp key for IPv4 address.

  - Retry delete of crypto isakmp profile for one minute when getting
     getting 'is still in use and cannot be removed'.
    (RT19977)

  - Remove IP address(es) when vrf forwarding set on interface.

  - Do not wait for echo with 'copy system:running-config tftp'
    (RT19968)

  - Fixed serious bug in getTrandId causing bad checksum due to NVRAM
     comment not being stripped.
    (RT19521)

  - Fixed bug in isakmp key. Added support for ipv6.

  - Fixed bug in isakmp key by splitting list. NOTE: API change.

  - Added cli-no-value-on-delete extension to banners.

  - Changed crypto isakmp profile * / match identity address * to a list.

  - Redesigned policy-map / class / police rate config to work on asr1000.
     NOTE: API change.

  - Delete all contents in interface Tunnel before deleting it.
    (RT19667)

  - Bug fixed 'interface * / no wrr-queue random-detect *'
    (RT19239)

  - Fixed problem with deleting crypto ikev2 proposal.
    (RT19674)

  - Treat '% interface .* hsrp [a-f0-9:]* removed due to vrf change'
     as warning, i.e. ignore.
    (RT19520)

  - Treat '%no specific protocol configured in class (.*) for inspection'
     as warning, i.e. ignore.
    (RT19478)

  - Fix for copy exec command prompting for remote host and filename.

  - Bugfixed router isis * / hello padding

  - Remove zone-pair / service-policy before adding it back.

  - Do not log enable password in trace.
    (RT20243)

This NED does require NCS version 3.2.3 or later.


cisco-ios v3.10.0 [June 24, 2015]
=================================

Enhancements:

  - Support for NSO-4.0 (note: NSO is new name for NCS)

  - Added README file.

  - The NED is now compliant with NSO 4.0 regarding registering
    platform information. Run 'show devices device platform'

  - Additions to the YANG model:
     interface * / isis hello-interval
     ip pim autorp listener
     no mls flow ipv6

  - Additions to the YANG model:
     clns routing
     crypto keyring * / pre-shared-key address *
     interface Dialer *
     interface * / auto qos voip cisco-phone
     interface * / ip dns view-group
     ip dns server view-group
     ip dns spoofing
     ip dns view *
     ip dns view-list *
     ip nat outside source static
     mls rate-limit all mtu-failure
     mls rate-limit multicast ipv4 *
     snmp ifmib trap throttle
     snmp-server file-transfer access-group *
   (RT19153, RT19068)

  - Additions to the YANG model:
     ip sla * /  service-performance type ethernet
     policy-map * / class * / dbl
     vlan configuration *
    (RT19088, RT19124)

  - Additions to the YANG model:
     cable load-balance group *
     controller Integrated-Cable *
     interface * / cable upstream bonding-group * / description
     interface * / tx-queue
     interface * / qos trust
     qos
     qos dbl
     qos map dscp *
     qos map cos *
    (RT19085, RT19044)

  - Additions to the YANG model:
     cable downstream
     cable linecard *
     interface Asi *
     interface Qam-red * / cable
     interface * / lacp rate
     line * / location
     logging event trunk-status global
     logging origin-id
     redundancy / linecard-group * / class
     service heartbeat, three new timer config
    (RT18815, RT19044)

  - Expanded router bgp * / template config.
    (RT19011)

  - New error detection method. Any text output when a config is set is
     now treated as an error. Except for warning texts, static (hard coded)
     or dynamic (added by user). Additional warnings (regexp expressions)
     can be configured in ned-settings/cisco-ios-config-warning.
     See README file for more information.
     (#12532)

  - Added exec command to see warning message since last commit:
     # devices device <devname> live-status exec show warnings

  - Expanded interface * / service instance * ethernet / l2protocol tunnel

  - Other additions to the YANG model:
     aaa group server tacacs+ * / ip vrf forwarding
     ip access-list logging hash-generation|interval
     object-group network *
     object-group service * / tcp-udp *
     object-group security *
     parameter-map type inspect-global
     policy-map * / class * / police / conform-color
     template
     (RT18823)

  - Added basic support for BGP flowspec.
    (RT18776)

  - Expanded crypto ikev2 config.
    (RT18823)

  - Expanded event config.

  - Additions to the YANG model:
     crypto isakmp client configuration group *
     exception crashinfo file
     flowspec
     interface Qam-red *
     interface * / cable carrier-id
     ip options drop
     router bgp * / address-family * flowspec
    (RT18823)

  - Additions to the YANG model:
     interface * / ip ospf ttl-security
     router bgp * / redistribute * subnets
    (RT18750)

  - Additions to the YANG model:
     no cdp advertise-v2
     class-map * / match cos inner
     class-map * / match service instance ethernet
     no crypto isakmp default policy
     ethernet evc * / oam protocol ldp
     pseudowire-class * / interworking
    (RT18671)

  - Additions to the YANG model:
     crypto map * ipsec-isakmp / description
     crypto map * ipsec-isakmp / set pfs
     spanning-tree portfast edge trunk
   (RT18529)

  - Append a 'default crypto ikev2 authorization policy' cfg-exec after
     a 'no crypto ikev2 authorization policy default' config command.

  - Added NCS version and NETSIM confd&NED version in ned trace.

  - Insert vlan's shown with 'show vlan-switch' into config.
    (RT18450 / #12833)

  - Additions to the YANG model:
     aaa group server tacacs+ * / server *
     mpls traffic-eng auto-tunnel primary
     interface / autonomic *
     autonomic, autonomic registrar / whitelist|shut
     (#12834)

  - Additions to the YANG model:
     aaa accounting update periodic
     boot network
     interface * / auto qos voip
     interface * / ethernet uni id
     interface * / ip arp inspection limit
     interface * / vlan-id dot1q *
     interface * / xconnect vfi
     ip pim snooping
     line * / accounting
     line * / authorization commands *
     login on-failure|on-success
     mac-address-table limit vlan *
     mls flow ipv6
     mls rate-limit layer2 mac-security
     password encryption aes
     vlan filter *
    (RT18285, RT18297, #12755, #12756)

  - Expanded and improved controller SONET config.

  - Additions to the YANG model:
     call-home
     class cem *
     controller SONET *
     controller * / cem-group *
     controller * / ima-group
     crypto ikev2 client flexvpn *
     crypto ikev2 redirect client
     crypto pki trustpoint * / usage
     exception crashinfo buffersize
     hw-module ism *
     interface ATM *
     interface CEM *
     ip tftp blocksize
     ip sla * / ethernet y1731 delay * / aggregate interval
     line * / authorization exec
     network-clock
     pseudowire-class * / sequencing
   (RT18249 / #12721)

  - Expanded config in the YANG model:
     interface * / isis
     interface * / peer
     interface * / ppp
     interface * / tunnel mpls
     monitor session *
     ptp clock ordinary domain *
     router bgp *
     router isis *

  - Build pnp erlang code in netsim if erlc is in path.


Corrections:

  - Exit interface mode before each new interface config to avoid
    adding config from unknown interface type to previous one.
    (RT19240)

  - route-map * / set as-path prepend * should be able to take identical number

  - interface * / mls qos trust API change.

  - Changed Modular-Cable rf-channel leaf interleave-depth to interleave.

  - Added ordered-by user to policy-map / class and class-map lists.

  - Made cable fiber-node * / downstream * into a list.
    Made Interface Cable * / downstream * into a list.
    (RT19044)

  - Changed interface Async key to string.

  - Changed line * * from uint16 to string.

  - Fixed order dependency in ip access-list with object-group's.
    (RT18823)

  - Fixed router bgp neighbor delete dependencies. Minor api change.
    (RT18670)

  - Fixed dependency problem with pw-class and pseudowire-class list.
    (RT18924, RT19093)

  - Add 'do clear crypto session' before 'no crypto ikev2 keyring' config.
    (RT18897)

  - Fixed problem with deleting mac-address-table limit vlan *

  - Fixed bug in interface-name-grouping. Mixup between ToP and Tunnel.
    (RT18880)

  - Fixed dependency problem with policy-map / class and class-map.
    (RT18808)

  - Corrected interface * / no bfd echo (type empty -> type boolean).
    Corrected interface * / no standby bfd (type empty -> type boolean).
    Corrected interface * / no ip redirects (type empty -> type boolean).
    (RT18823)

  - Remove service-policy before changing it.
    (RT18804)

  - Fixed bug with error text causing exit from config mode upon commit.

  - Fixed dependency problem with l2 vfi and interface xconnect vfi.
    (RT18759)

  - Switched aaa accounting stragegy to suppressed mode due to lack of mode
    support on some devices. Convert mode-syntax upon read from device.
    (RT18576)

  - Fixed ordering issue in switchport (mode access vs trunk encap.)

  - Once again reworked switchport yang model. This time as a choice
     statement with support for 'show-yes' devices (e.g. 7600) that
     does now show 'no switchport' but shows 'switchport'. The other
     devices ('show-no' type) got an extra boolean leaf variable to
     fix a bug where 'no switchport' was not sent to device.

  - Fixed bug with 'switchport trunk allowed vlan add' triggering
    conversion of description text.

  - Added 'tailf:cli-preformatted' to all description leaves to suppress
    quoting of non-config elements when displaying them.

  - Ignore insertion of vlan's into config for VTP Clients.

  - Changed interface * / autonomic connect to type empty.

  - Use 'show vlan' first, fallback to 'show vlan-switch'.

  - Also ignore 'no switchport' incomplete command error.

  - Cleaned netsim/ios.xml start config.

  - Ignore 'switchport' incomplete command error due to the confusing design
    where some ios devices need it and some don't.

  - Fixed order dependency in interface / service instance / encapsulation.

  - Added explicit exit in router lisp / eid-table * mode.
    (#12725)

  - Bugfixed router isis.

  - Reworked router isis / distance.

  - Reworked logging host to support vrf and ipv6 properly.

  - Bugfixed router bgp * / address-family * / redistribute ospf.

  - Bugfixed router lisp / ipv4|ipv6 etr|itr enable.

  - Treat 'already in use by' as an error.

  - Treat 'VLAN .* does not exist, creating vlan' as warning, i.e. ignore.


This NED does require NCS version 3.2.3 or later.


cisco-ios v3.8.0 [March 16, 2015]
=================================

Enhancements:

  - Support for NCS-3.4.

  - Additions to the YANG model:
     csdb tcp|udp|icmp|session
     snmp-server queue-limit notification-host
    (#12674 / RT18132)

  - Additions to the YANG model:
     router bgp * / bgp additional-paths
     interface pseudowire * / service-policy
    (#12675 / RT17994)

  - Additions to the YANG model:
     interface * / xconnect encapsulation / mtu
    (#12676 / RT18079)

  - Additions to the YANG model:
     interface * / switchport private-vlan
    (#12679 / RT18009)

  - Additions to the YANG model:
     interface * / wrr-queue dscp-map *
     interface * / priority-queue dscp-map *
     interface * / priority-queue queue-limit
    (#12681 / RT18170)

  - Additions to the YANG model:
     interface * / l2protocol-tunnel lldp
     interface * / l2protocol point-to-point
    (RT18073)

  - Added interface BRI and Async basic configuration.

  - Populated router bgp * / peer-policy & peer-session lists.

  - Expanded policy-map class random-detect config.

  - Merged cloudvpn-develop (commit: 13d3dc9 from 2015-03-05):
    Enhancements:
     Added license command under live-status, e.g.:
      devices device csr1000v live-status exec license smart deregister
     Implemented support for 'default' exec commands in config mode, e.g.:
      devname(config) default crypto ikev2 authorization policy
     Additions to the YANG model:
      aaa attribute list *
      class-map * / match protocal attribute category *
      crypto dynamic-map *
      crypto ikev2 profile * / aaa authorization group
      crypto ikev2 authorization policy *
      crypto ikev2 keyring * / peer * / identity
      crypto ikev2 profile * / nat keepalive
      crypto ikev2 profile * / match fvrf
      crypto isakmp nat
      crypto isakmp profile *
      crypto keyring *
      crypto map * ipsec-isakmp|ipsec-manual
      crypto logging ikev2
      ip dhcp pool * / vrf
      ip nat inside source route-map *
      ip sla * / * / vrf
      ip ssh stricthostkeycheck
      logging source-interface * vrf
      pnp startup-vlan
      pnp tag
      .. and more minor additions ..
    Corrections:
     Remove ip sla entry before modifying it upon error from device.
     Modifications in the YANG model:
      aaa accounting exec *
      crypto ikev2 profile * / authentication
      crypto isakmp policy * / encryption
      crypto map *
      interface * / ipv6 nd ra suppress
      interface * / ntp multicast client
      ip nat outside source
      pnp profile * / transport
      .. and more minor various bugfixes ..

Corrections:

  - Numerous router rip,ospf,ospfv3,isis and bgp bug fixes.

  - Fixed dependency issues:
    On Catalyst devices there is a dependency between:
     interface * / switchport and interface * / l2protocol-tunnel.
    The config under l2protocol-tunnel must always be deleted before
     switchport is deleted.
    The same dependency also applies to
     interface * / switchport and interface * / service.
    (RT18073)

  - Removed all specific snmp-server enable traps. Replaced by generic list.

  - Changed router * / passive-interface from a leaf to a list.
    Fixed deletion of 'no passive-interface' on router config.
    (#12683)

  - Fixed order dependancy between service instance and ethernet evc.
    (#12686 / RT18200)

This NED does require NCS version 3.2.2 or later.


cisco-ios v3.6.0 [February 13, 2015]
====================================

Enhancements:

  - Implemented cable config support for 10000 Software.
    (#12353 / RT17563)

  - Added support for sub-interface Port-channel #.#

  - Automatically delete channel-group and shutdown dependant ethernet
    interfaces when Port-channel interface is deleted.
    (#12304 / RT17422)

  - General config expansion for improved MPLS and L2VPN support.

  - Expanded router lisp config.
    (#12432 / RT17707)

  - Improved expect of sent commands at login, solving login problems
    on XE devices.
    (#12482 / RT17798)

  - Expanded ethernet cfm config.

  - Added aaa accounting update periodic config.
    (#12484 / RT17825)

  - Added support for aaa accounting connection.
    (RT17164)

  - Added interface ip ospf authentication config.
    (#12483 / R17835)

  - Added numerous ASR903/ASR920 L2 config.
    (#12481 / RT17687)

  - Added ip tcp mss config.
    (#12480 / RT17774)

  - Added copy, reload and traceroute commands under live-status.
    (#12479 / RT17836)

  - Support for vlan ranges in service instance encapsulation dot1q config.
    (#12430 / RT17629)

  - Added generic snmp-server enable traps list accepting all entries.

  - Added basic support for cable l2vpn and cable  dot1q-vc-map

Corrections:

  - Reworked aaa authentication and authorization lists method options.
    Non-backwards compatible 'aaa authorization network' model.
    (#12353 / RT17164)

  - Fixed bug in router ospf * / redistribute bgp|eigrp config.

  - Fixed bug with setting banners. Removed delimiter config to avoid
    compare config diff.
    (#12300 / RT17300)

  - Fixed ordering problem when deleting switchport mode trunk and
    switchport trunk encapsulation dot1q.
    (#12312 / RT17421)

  - Do not delete switchport container when member leaves are deleted.

  - Made index value mandatory in ip explicit-path in order to modify and
    delete entries.
    (#12341 / RT17511)

  - Restored router bgp / address family ipv6 configuration mode.

  - Remodelled ethernet cfm service evc to list.

  - Removed auto-create of vlan when configuring bridge-domain.
    Fixed order dependancy problem with service-policy.
    (#12506 / RT17782)

  - Do not delete config in set-hook's if talking Netconf.
    (#12525)

This NED does require NCS version 3.2.2 or later.


cisco-ios v3.4.0 [December 1, 2014]
===================================

Enhancements:

  - Support for NCS-3.3.

  - Added ping and clear exec commands under live-status, e.g.:
     devices device c7200 live-status exec ping 192.168.1.1
     devices device c7200 live-status exec clear ppp queues

  - YANG data model updates
     mls qos map ip-prec-dscp

    (RT17250)

  - Added basic support for new L2VPN config, YANG data model updates
     interface pseudowire
     l2vpn xconnect context

    (#12180 / RT17250)

  - Added route-map / continue configuration.

  - Updated for new NCS features 'trim-defaults' which hides config set
    to default values.

    (#12181)

Corrections:

  - Fixed problem with route-map * / set community reordering values.
    (#12140)

  - Remove ip prefix sequence entry before modifying it.

  - Fixed problem with deleting ip vrf *, bad prefix used.
    (#12150)

  - Ignore incomplete crypto profile warning.

  - Moved policy-map description to before class.


This NED does require NCS version 3.2.2 or later.


cisco-ios v3.2.0 [November 5, 2014]
===================================

Enhancements:

  - Added minimal router eigrp support.

  - Populated ip dhcp pool config.

  - Reworked card config.

  - Added version string in NED trace. Grep for 'NED VERSION:'

  - Added support for show commands under live-status exec show, e.g.
     devices device <devname> live-status exec show running-config.

  - Added ncs java log and ned trace print of TransactionId.

  - Added 'tailfned police' options: cirmode|cirflat|bpsflat|numflat
     in order to support various versions of policy-map / class /
     police config styles. This config disables non used versions
     of police config.

  - Added support for c3750 policy-map / class / police config syntax.

  - Added support for match config in router bgp redistribute ospf list.
    (Issue tracker: #15339)

  - Added support for PPP/Ethernet/Frame Relay Bundled Service and
     Ethernet MPLS L2VPN.
     (Issue tracker: #15601)

  - Implemented basic crypto ikev2 config support.

  - Implemented support for 'crypto pki server <cs-lbl> [rollover [cancel]]
     exec commands in config mode.

  - Implemented full support for NTP configuration, modified/expanded:
     ntp and interface / ntp commands.

  - Implemented full support for SNTP configuration, implemented:
     sntp command.

  - Implemented full support for DNS configuration, modified/expanded:
     ip host, ip name-server and ip domain commands.

  -  Added crypto ssl support and crypto pki enroll <name> command.
     (Issue tracker: #16986)

  -  Added support for policy-map / class / random-detect dscp[-based].
     (Issue tracker: #16914)

  -  Added 'backup peer' and 'synchronous mode' interface config.
     Reworked xconnect in interface and interface service instance.
     Added bridge-domain hook for auto creating vlans with same <id>.
     Added connect config.p
     (Issue tracker: #16792)

  -  Implemented support for 'crypto key' exec commands in config mode.
     Added basic crypto isakmp and crypto ipsec support.
     Expanded mobile ip and dialer config.
     (Issue tracker: #16556)

  -  Added crypto pki trustpoint create&remove hook for automatic creation
     and removal of corresponding pki certificate chain.

  -  Added remove hooks on /ip/vrf and /vrf/definition lists to automatically
     delete routes on same vrf.

  -  Implemented basic VoIP config support.

  -  YANG data model updates
      ip mobile router
      controller Cellular
     (Issue tracker: #16556)

  -  YANG data model updates
      mls qos srr-queue

  -  YANG data model updates
      ip dhcp pool

  -  Added interface-ethernet-grouping to Port-channel interface.

  -  Added storm-control action shutdown|trap config items.
     (Issue tracker: #16046)

  -  Added support for 'sequence' keyword to ipv6 access-list
     (Issue tracker: #16156)

  -  YANG data model updates
     ip pim vrf
     (Issue tracker: #15682)

  -  YANG data interface model updates
     interface LISP
     interface Tunnel
     interface Ethernet (for XE)
     interface MFR - Multilink Frame Relay bundle
     interface * / switchport

  -  YANG data routing model updates
     router rip
     router eigrp
     router ospf vrf
     router * / distribute-list

  -  New YANG data model top containers
      track
      pnp profile
      pseudowire-class

  -  YANG data model updates
     event manager applet / action

  -  YANG data model updates
     l3vpn encapsulation

  -  YANG data model updates
      card

  -  YANG data model updates
      ip explicit-path
      ip flow-aggregation cache

  - Expanded mobile ip and dialer config.

  - Expanded LISP config.

  - Expanded crypto config.


Corrections:

  - Fixed order dependency issues in policy-map/class/police when switching
     between an aggregate and policer.
     NOTE: Requires support for 'tailf:cli-trigger-on-set' option.

  - Fixed issue with remove before change on the following leafs:
     interface * / service-policy / input
     interface * / service-policy / output

  - Added cli-show-no to shutdown leaf in in order to be able to bring
     down (send 'no shutdown') interface after creation.

  - Moved policy-map/class/police aggregate out of police choice.

  - Change argument to setTimeout() from seconds to millisecs in
     NED Java code due to NCS NED API change.

  -  Moved table-map,mls,policer,class-map and policy-map to before interface.
     (Issue tracker: #16091, #15854)

  -  Added error checks:
     'please remove the service-policy on the zone-pair'
     'in use, cannot'
     (Issue tracker: #16093)

  -  Remodelled interface/ip access-group to support both in and out entries.
     (Issue tracker: #16045)

  -  Display 'switchport port-security' on a separate line.

  -  Fixed order dependency issues in policy-map/class/police when
     switching between an aggregate and policer.
     (Issue tracker: #16574)

  -  Fixed dependency between interface switchport and service instance
     delete.
     (Issue tracker: #16792)

  -  Fixed problem with 'no media-type' clearing duplex on interface.
     (Issue tracker: #16792)

  -  Fixed policy-map / class / bandwidth percent modification order
     dependency problem.

  -  Fixed order dependency problems in policy-map / class / random-detect
     dscp, discard and precedence.
     (Issue tracker: #16914)

  -  Repaired support for multiple certificates.

  -  Fixed typo in access-list.
     (Issue tracker: #15626)

  -  YANG data routing model bugfixes
      ip
      ipv6

  -  YANG data routing model bugfixes
      service

  -  YANG data routing model bugfixes
      snmp-server host vrf

  -  YANG data routing model bugfixes
      vrf definition

  -  YANG data routing model bugfixes
      aaa

  -  YANG data routing model bugfixes
      interface * / service-policy

  -  YANG data routing model bugfixes
      access-list

  -  YANG data routing model bugfixes
      ip domain-name
      ip domain-list
      ip domain-lookup


cisco-ios v3.0.2 [April 30, 2014]
=================================

Enhancements:

  -  Implemented support for interface service instances
     (Issue tracker: #15246)

  -  Added check in java for commit error "not configured"

  -  Updated YANG data model


cisco-ios v3.0.1 [April 7, 2014]
================================

Enhancements:

  -  Added basic support for Locator/ID Separation Protocol (LISP)

  -  Added support for config overwrite after OS upgrade.
     (Issue tracker: #14618)


cisco-ios v3.0 [March 4, 2014]
==============================

This release is function wise equivalent to the previous release, which was 1.8.
The only difference is that this NED now is maintained using
a new product life-cycle which is independent of the NCS release life-cycle.

The version number 3.0 has been chosen to indicate a common baseline for
all NEDs, which is higher than any previous NED version.

For this NED no upgrades are necessary.

The Tail-f package: cisco-ios NED was initially imported 2014-02-25

=====================================================================
